# P23.13 final design specification — Architectural Drawing + Local Instrument

**Owner-ratified design specification · 2026-09-16 · not an implementation plan.**

This specification is the final P23.13 design authority. The
[owner-supplied synthesis](./P23.13-synthesized-direction.md) records the rationale,
the [brief](./P23.13-designer-brief.md) preserves locked constraints, and current code
records implementation reality. The earlier independent proposal is superseded. All screen
thresholds remain tuning values.

Companion: [interactive specimen atlas](./P23.13-ink-and-instrument-atlas-Designer-D.html).
The atlas is a design study using illustrative geometry, not compiler output or a functional
editor. **This specification remains authoritative.** The atlas has a narrower visual-QA role:
its optical specimens may be used as a human reference for visual hierarchy, state composition,
symbol/icon appearance, density and theme framing where they illustrate this specification.
Atlas geometry, simulated interaction and numeric behavior are never domain or behavior
authority, and any conflict resolves to this specification. Numeric thresholds below are
proposed starting values, not measured usability results.

Inputs: [brief](./P23.13-designer-brief.md), [design context](./P23-design-context.md),
[P23.12 contract](./P23.12-final-design-contract.md), and
[precedent research](../Deep-research/P23-Staging-Research/P23.13-architectural-plan-drafting-precedent-research.md),
especially §§1, 3.14–3.15 and its 34-question register. Consume the brief's post-baseline
P23.12 and anchor-release corrections. The supplied four-proposal synthesis controls the target;
the independent proposals are historical inputs, not competing authorities.

## 1. Position

A museum creator should see rooms and openings before seeing controls. Use a continuous,
graphite-filled architectural band, ample uncolored room space, and restrained labels.
When someone edits, disclose a small drafting instrument around the affected geometry:
one active handle, one winning snap, and the dimensions relevant to that operation.

The distinctive choice is **a dark cut silhouette that survives selection**. Blue lives
outside the mass, so even an active plan remains legible architecture. Normal and near walls
use the same graphite mass; zoom never washes walls into a different apparent material.
Non-bounding walls remain solid physical walls, with a lighter body and the same credible physical contour. There is no permanent role glyph.
Hover/selection context explains room behavior without an invented drafting convention.

The synthesis selects one light, slightly cool paper system across all shell themes. Strong
cut hierarchy comes from optical weight and negative space; adding marks to imitate CAD
is not a design objective. The six owner choices are ratified in §15.

Adopt all three research challenges: separate physical band from drafting ink; automatic
semantic zoom; optional exact entry during drafting. Exact entry is a ratified interaction
extension that still requires implementation planning; it is not a claim that the current app supports it.

## 2. Visual contract

All sizes are CSS screen pixels unless explicitly in meters. No screenshot scale is a
physical measurement. Use existing `--editor-plan-*` token ownership; names below describe
semantic roles, not new persisted styles. Fonts use local fallbacks; no font download is required.

| Role | Proposal | Usage |
|---|---|---|
| Paper | `#F5F7F8` | Landed cool Plan Paper under PLATE Light and legacy compatibility surrounds |
| Ink / cut mass | `#343C43` | Bounding wall body, main labels |
| Secondary ink | `#596570` | Dimensions, references, areas, frame lines |
| Partition body | `#ADB6BD` | Non-bounding physical band, dark perimeter |
| Selection | `#2F8CFF` | Invariant blue overlay; reinforce edges with `#145DA8` |
| Snap | `#146D68` | Winning marker plus relation shape/text |
| Invalid | `#9B3149` | Broken proposal, stop marker, reason |
| Focus | `#202830` on paper knockout | Double outline, never hue alone |
| Grid minor / major | `#E4E9ED` / `#CED6DD` | Nonessential context only |

Typography: room name uses **Avenir Next Demi Bold**, falling back to the existing sans
and system sans, 13/16 at weight 600. Ordinary UI text inherits the application font.
References and dimensions use **SFMono-Regular / Consolas / monospace**, 11/14, regular;
room area uses the ordinary sans, 11/14. Drawing typography never uses all-cap titles,
reference pills, condensed fitting, or artificial letter spacing. The specimen page's large
title is editorial presentation, not proposed editor chrome.

Critical small text uses Ink or Secondary ink on Paper. The lighter blue is not small text:
use its dark companion for numeric focus/selection labels. Low-contrast grids and passive
Scene are supplementary; interactive boundaries need a distinct dark edge or shape.

| Mark | Screen treatment |
|---|---|
| Physical wall | Exact projected band, graphite fill, 1 px exterior profile |
| Free cap / jamb | 1.25 px ink; square physical termination |
| Layout object | 1 px secondary outline; 5% ink fill |
| Window frame / Door type cue | 1 px secondary ink; Door cue is dashed and perpendicular to the host, with at least three clearly separated dash segments at Normal/Near; cue is symmetric screen-space ink and may extend beyond the Wall band |
| Dimension / witness | 0.75 px secondary; 4 px oblique end ticks |
| Hover | 1 px neutral contour, 3 px outside body; no handles |
| Selected | 1.5 px blue outer contour, 2 px paper separation |
| Focus | 2 px dark ring, 2 px paper moat; encloses current target |
| Snap guide / baseline | 1 px; source/guide distinct from actual geometry |

No blanket blue wall fill. No room fill at rest. No decorative hatch.

## 3. Architecture, joints and openings

### Walls and role

Band edges must come from compiled geometry. Drafting ink can have a 1 px floor; physical
width cannot. At projected thickness below 2 px, retain exact band and add a centered 1 px
silhouette ink line. This is a readability mark, excluded from measurements, snapping and
hit truth. Two separate walls never merge semantically because their ink touches.

At valid L/T/X junctions, show compiled connected mass without redundant internal profiles
where the compiler supplies an unambiguous continuous outline. Never erase a discrepancy by
inventing a union, bevel, bridge or extension in SVG. If today's compiler exposes a seam,
retain it and record it for P23.15. The atlas's clean schematic joints are the desired visual
reading, not evidence that difficult mixed-thickness or tangent cases are solved.

Mixed thickness keeps actual shoulders; mixed height adds no invented overhead or cut-plane
classification. Free ends are closed square caps. Curves use exactly the same band/profile
and the shared evaluator, with no local curve approximation policy. Shared walls draw once.

Non-bounding walls have the same continuous dark outline and a lighter solid body. No permanent
interior role mark is included. Hover/focus/selection context says `Does not define room boundary`.
At a subpixel band the luminance difference may cease to communicate; keep the wall visible
and recover role through context, rather than inventing another overview symbol. Never label
these “interior,” “nonstructural,” “temporary,” or hidden. A1 in §15 ratifies this treatment.

### Door and Window geometry

Let `w` be the hosted width and `t` the physical wall thickness. The void always occupies
exactly that authored interval, with jambs on its ends. Frame strokes follow the compiler's
opening presentation frame; curved hosts do not get straightened by the SVG consumer.

| Regime | Door | Window |
|---|---|---|
| Normal / near | Void + cut termination edges + one **dashed symbolic type cue perpendicular to the host**, centered in the opening, showing **at least three clearly separated visible dash segments** (optical starting point: 1 px secondary ink, approximately 2 px dash / 2 px gap, approximately 10–12 px total screen-space length, symmetric about the Wall centerline; the cue may extend beyond the projected Wall band when needed for recognition) | Void + cut termination edges + up to two symbolic strokes, separated by `min(4px, 0.36t)` in screen space |
| Far, width ≥8 px | Same three-dash perpendicular Door type cue where projected space keeps it legible; jambs simplified to 1 px | One continuous center frame stroke across the true interval |
| Width <8 px / extreme overview | Keep actual gap; use the existing displaced symbolic cue rather than compressing the three-dash cue into unreadable noise | Keep actual gap; 4 px double-line cue displaced 5 px outside host |

The Door type cue is a dashed line centered on the authored opening and perpendicular
to the host Wall. At Normal and Near presentation it must show **at least three clearly
separated visible dash segments**. The cue is screen-space drafting ink and may extend
beyond the projected Wall band when necessary for recognition. Any extension must be
**symmetric about the Wall centerline**, so the cue never implies hinge side, leaf
orientation, swing direction, opening side, sill, threshold construction, or animation state.

The visual cue may exceed physical Wall thickness. That is intentional. Its extent is
presentation only and must never modify authored opening width, Wall thickness,
geometry/compiler output, hit geometry, snapping, measurements, validation, or Door
mechanics. Do not force the cue to fit inside the physical Wall band. Cue length is
governed primarily by screen-space legibility, not by physical Wall thickness. Optical
starting values (not world-space dimensions or persistent semantics): 1 px secondary-ink
stroke, approximately 2 px dash / 2 px gap, minimum total screen-space cue length around
10–12 px, enough length to produce at least three visible painted dash segments.

Rotate the cue with the host so it remains perpendicular to the local host tangent. Never
offset the cue toward one Wall face; any protrusion remains symmetric around the host Wall
centerline. Semantic zoom keeps the hierarchy: Normal / Near shows at least three clearly
visible dash segments; Far preserves the three-dash cue where projected space still supports
it legibly; extreme tiny openings and overview use the existing small-opening fallback rather
than compressing the three-dash cue into unreadable noise. Subpixel-opening cues must never
widen the physical cut. Space displaced cues along the same stable host side; if they collide,
suppress lower-priority cues, retain the actual cut and reveal the selected kind in target
context. At extreme overview, individual adjacent openings cannot honestly all remain
distinguishable; selection and zoom recover detail.

Two frame lines are the limit: no mullions, panes, sash, handles or glazing layers. Door has
no hinge, leaf, handing, swing arc, opening-side, sill, threshold, construction-thickness, or
side inference. The dashed perpendicular type cue is a neutral symbolic-only mark, not a
physical sill, threshold, leaf, construction specification, or animation state. Window remains
the parallel-line symbol. Two Window strokes
require ≥3 px projected separation and ≥1 px margin to each cut edge; collapse to one stroke
before either condition fails. At less than 8 px projected width, termination detail drops
before the displaced cue. No world-unit frame spacing is introduced. Width transitions enter
at 9 px and leave at 7 px; second-line visibility enters at 3.5 px separation and leaves at
2.5 px, subject to the hard edge margin. A2 in §15 ratifies this Door cue (three-dash,
symmetric screen-space extension; A2 remains ratified, not reopened). Creator recognition
should still be checked during visual QA/usability review; that evidence tunes presentation,
not the underlying product decision. The resting 2D cue stays independent from any future
authored hinge/swing/animation semantics.

Rectangular, rounded and pointed profiles have the same resting Plan symbol because their
elevation silhouette cannot be inferred from the top view. Selected context says `Rounded
arch` or `Pointed arch` when authored. No invented arc on the floor. Min-width hosts and
near-end/near-junction/near-neighbor openings use true cuts; fit feedback is discussed in §7.

## 4. Rooms, labels and density

Normal full stack: name / reference / derived area. Example: `Gallery` / `R-7K3M` / `24.5 m²`.
Use the existing identity resolver and area source, not renderer-created names or area math.
Display area to one decimal; positive values rounding to zero read `<0.1 m²`. Display rounding
never changes precision or dimensional input. Name equal to reference is shown only once.

Names can occupy two lines, maximum 160 px. Area and references are never wrapped or shrunk.
Full identity means complete authored name and complete reference; area is supplementary.
Long selected names wrap in the selection readout, with bounded scroll for pathological
length, rather than forcing every room label to expand.

Room text is a free-space layout problem, not a point-anchor problem. Begin with the canonical
Room polygon projected to screen. Subtract protected wall/opening edges, authored-object
footprints and active annotation/control bounds from the *label eligibility mask only*.
This does not modify geometry, Room boundaries or topology. Derive up to eight large free-space
candidates, ranked by clearance then proximity to the Room's semantic center, with canonical
ID as the deterministic tie-break. Test the complete text rectangle, including concave edges,
not merely its center. Reserve 8 px to core geometry, 12 px to acquisition/handle zones and
24 px to active text. Invisible object hit regions are not blanket text obstacles; actual
footprints are. Very light passive Scene may yield to a Room label.

Choose a candidate that fits the full stack first. Prefer tier reduction at the existing
candidate over relocating to a distant pocket. Only after suppressing area and then reference
(except duplicate pairs) may a label change free-space component. A selected Room uses this
same process, never a centroid override. The bounded search can fail; the fallback is honest
suppression or the selected readout, never forced overlap.

Preserve the accepted candidate during a gesture and small pan/zoom. Reconsider only on
geometry changes, an actual collision, or LOD transition. No optimization every pointermove.
A label may shift at most 12 px of layout displacement from its accepted candidate before suppressing a tier (ordinary world-to-screen pan movement is excluded). Never
animate labels across rooms. Reappearance requires 8 px extra clearance and 150 ms after
zoom/gesture settles. Accessibility reduced-motion requires no animated placement at all.

Drop order: area → reference → whole resting label. For duplicate names, protect name +
reference as a pair: drop area, then suppress the entire pair if it cannot fit. Never leave
two ambiguous identical names with hidden references. No warning or forced renaming.

| Face condition | Rest | Selected / active |
|---|---|---|
| Large / medium | Full stack if safe | Same stack; faint fill plus boundary overlay |
| Small | Name, or duplicate name+reference pair | Full identity in local free space if possible |
| Tiny / no safe interior | No resting label | Full identity in fixed canvas selection readout |
| Dense adjacent rooms | Stable drop order; no leaders | Selected room wins; neighbors suppress |

Selected Room uses 5% blue interior tint plus a 1.5 px Room-side boundary overlay on the interior side of the canonical Room boundary, separated from wall ink.
No wall handles appear solely because a Room is selected. Hovering a participating wall adds
its neutral contour and context `Bounds Gallery · …`; it does not select another entity.
The fixed canvas readout is a non-mutating fallback, inset 12 px from the viewport safe corner,
maximum 280 px or available viewport width. It is not a second Inspector or rename field.
It may overlap passive context, never active handles or core geometry; reserve a safe inset
for it if the viewport has no free drawing space. Narrow views reflow it above the drawing.

Collision priority: active control/selected identity → invalid marker + reason → winner snap
and precision → Room identity → Layout object context → passive Scene. If first two compete,
keep the active control at geometry and move the reason to the fixed readout. Dimensions may
move outward or enter the readout; no unresolvable pile of text. Compiler diagnostics keep
a 6 px marker at source; colliding reasons consolidate in one readout with count and existing
Inspector destination. Never hide invalid geometry itself.

## 5. Semantic zoom

Global scale `s` is CSS pixels per world meter, independent of device pixel ratio. Initial
classification: far `<24`, normal `24–79.99`, near `≥80`. Hysteresis: far→normal at 28,
normal→far at 20; normal→near at 88, near→normal at 72. On jumps, resolve directly to the
appropriate regime. During an edit, hold the active target's control set until completion.
Entity-specific projected-size rules still govern tiny features at any global scale.

| Entity | Far | Normal | Near |
|---|---|---|---|
| Wall | Exact band + silhouette ink floor | Band + profile; lighter non-bounding body | Same; active baseline only |
| Door / Window | Simplified cue (§3; three-dash Door cue preserved far where legible, fallback at extreme overview) | Full neutral symbols (Door ≥3 dash segments) | Same; selected controls |
| Room | Name only where safe; duplicate pair or none | Full stack with tier suppression | Same fixed text size |
| Junction | No passive nodes | Selected wall endpoints; active tool winner | Same contextual rule; no node cloud |
| Curve points | Selected only; cull passive controls below 6 px/m | Selected bend points; polygon only on focus/drag | Same; extra space is not permission for clutter |
| Dimensions | Active only or fixed readout | Selected/active set | Same; no permanent chains |
| Layout objects | Simplified footprint, 1 px minimum ink | Full geometric footprint | Selected allowed handles |
| Scene context | 15% secondary ink | 30% secondary ink | 30%, no extra detail |
| Grid | Major only if spacing ≥16 px | Major plus minor when minor spacing ≥12 px | 0.25 m minor when spacing ≥12 px |

Below 6 px/m no resting Room labels or Junction/curve control cloud. A selected/focused
control remains available through the fixed readout and keyboard navigation; focusing it
reveals its screen-size marker even at overview. Geometry never disappears because its
label or handle does. Grid visibility and snap step are independent; never silently change
0.25 m snapping when grid detail drops. Ruler intervals use readable 1/2/5 progressions with
labels at least 48 px apart; scale bar stays segmented, axes remain X/Z and meters.

## 6. State and control grammar

| State | Wall / Opening | Room / Layout object | Handles / feedback |
|---|---|---|---|
| Rest | Ink geometry, neutral symbol | Unfilled Room; light object footprint | None |
| Hover | Offset neutral hairline, no blue | Neutral contour; no fill | No new handles; target context on dwell/focus |
| Focus | Dark double ring with paper moat | Same ring around boundary or target | Focused handle ring survives every other state |
| Selected | Separated blue perimeter; retain ink | Room faint fill+boundary; object blue outline | Only owner-allowed controls |
| Direct edit | Proposed real band + ghost baseline | Proposed footprint/boundary | Active handle filled; local dimensions |
| Snap | Single shape at winner; short source accent | Same rule | Optional one guide + relation label |
| Pending proposal/draft | Real band at 70% opacity; solid profile | True candidate footprint | Blue endpoint and relevant dimensions |
| Known-invalid proposal | Real proposal at 35%; broken 4/3 perimeter | Same broken contour | Octagonal stop mark with × + local reason |

Non-color identity: hover = single neutral contour; selection = separated double silhouette
with actionable handles; focus = enclosing double ring; pending proposal = continuous candidate (not accepted)
with active endpoint; invalid = broken contour + ×; snap = relation-specific glyph + word.
Direct edit includes a faint baseline and dimension witnesses, distinguishing it from rest.
Only an **accepted commit** installs geometry: return it to normal committed ink and remove
proposal/refusal graphics. A pending proposal may become known-invalid or be refused at
commit; a continuous contour never asserts that validation has already succeeded.

Precedence is **compositional**, not “top state replaces all”: geometry → role/symbol →
selection → proposal → source/guide → handles → winner/invalid marker → focus ring → text.
Invalid suppresses a success-colored snap assertion but can retain an explanatory source.
A selected wall under snap stays selected; only the winning marker claims the relation.

Hit authority follows **captured gesture → focused control of the selected owner → other
eligible selected-owner controls → current-tool eligible targets → existing entity fallback**.
A curve point owned by the selected Wall therefore beats an unrelated co-located Opening or
Junction; an active Opening width square beats its host Wall. There is no global entity-class
ranking above owner intent. Within a tier, use nearest screen distance, then canonical identity.
At a true overlap, keyboard control traversal reaches the alternative without moving geometry.
Captured ownership never changes mid-gesture. An ordinary hover reveals no new controls.

The existing fallback may continue to prefer endpoints/openings over bodies; that is a fallback,
not authority over active owner controls. Adapt the canonical hit resolver; do not install an
SVG-side competing resolver. Arrange uses its existing owner pipeline. Passive Scene is never
a Layout target. Mode changes cancel capture and clear the prior owner's instrument.

| Control | Visible mark | Reveal / target |
|---|---|---|
| Junction / Wall endpoint | Filled 7 px diamond | Selected wall endpoints, selected Junction, or Wall-tool winner |
| Curve bend point | Hollow 8 px circle | Selected curve; active point fills blue |
| Opening width edge | 7 px square straddling jamb | Selected or editing Opening |
| Opening slide | Short paired grip across symbol center | Selected Opening; body drag remains same operation |
| Object resize | 7 px corner square | Only where owner already supports resize |
| Object rotation | 14 px ring with short radial tick | Only where owner supports rotation |

Pointer hit targets at least 24×24 px; coarse-pointer target 44×44 where space allows.
Overlap resolves by selected owner and focus, not increasing visible mark sizes indefinitely.
Selected curve shows bend circles only. Focus/drag adds a 1 px broken control polygon and
its true reference centerline. Baseline geometry appears only during edit at 20% ink; original
Opening is ghosted during slide to show displacement. No free Bézier tangent handles invented.

Bend discoverability: when a selected wall is hovered, show **the current platform binding**
for the canonical `layout.wall.bend` command in target context, alongside the existing
`Add bend point here` menu path. Do not hard-code a second shortcut map in presentation.
Current defaults are Meta (`⌘`) on macOS and Alt elsewhere, verified in
[`EDITOR_COMMAND_BINDING_DEFAULTS`](../../apps/editor/src/lib/editor/editor-command-intent.ts:52);
[`resolveEditorCommandIntent`](../../apps/editor/src/lib/editor/editor-command-intent.ts:74)
resolves those bindings. These are current code evidence, not an immutable design binding.
Do not add a floating mutation toolbar or a second straight/curved conversion button.

Keyboard requirement: canvas is one tab stop. Enter on a selected entity enters its control
group; arrow keys traverse controls in canonical endpoint/arc order; Enter activates a
numeric edit; Escape exits edit, then group. Tab leaves to the next existing surface.
A focused Junction opens X/Z, curve point X/Z, opening edge width, slide grip offset,
object corner its already-supported size and rotation its already-supported yaw. Use the
same canonical commands as Inspector/pointer. Merely moving focus never changes selection
or history. Readout announces name/reference, control role, current value and units.
P23.14 must reconcile shell focus handoff; this is a proposed Plan-local interaction contract.

## 7. Precision, previews and refusals

Dimensions are **working information**, never authored annotations. Use 0.75 px witnesses,
4 px slanted ticks and horizontal 11 px mono text on a small paper knockout. Offset from the
band by 18 px; reserve a second lane at 34 px when necessary. Choose the side with more
space once at gesture start; freeze it until end. Short spans push text outward, then to
readout; never scale text smaller. Display meters and degrees explicitly; actual precision
comes from the canonical value, not its formatted string.

| Context | Automatic while editing | Selected, idle |
|---|---|---|
| Straight Wall draw / endpoint reshape | Length, angle; adjacent affected span if essential | Length only; angle through focused precision |
| Curved Wall bend | Arc length + active point X/Z on focus | Arc length (label explicitly `arc`) |
| Rigid Wall move | ΔX / ΔZ; baseline | Length; no global coordinate field |
| Junction move | ΔX / ΔZ; X/Z when numeric editing | Coordinates on focused handle |
| Opening insert / slide | Width + canonical-start offset; nearest end/neighbor clearance | Width; offset on focus |
| Opening resize | Width + changing clearance | Width |
| Rect Room | Width + depth; candidate enclosure | Room area, no permanent dimension chain |
| Poly Room | Current segment length/angle; closure cue | Room stack |
| Layout object transform | Supported size/delta/yaw only | No automatic complete bounding box |

**Adopt optional type-to-enter** for straight Wall draw length/angle, rectangle width/depth,
Opening insertion width/offset and Opening slide/resize offset/width. Focused direct-edit
controls may edit existing numeric fields through the same canonical planner. No new curve
radius/tangent constraints. Passive measurements such as clearance and area are never inputs.

During active creation, a digit or decimal separator starts entry in the primary field.
The `-` trigger is permitted only for a field whose canonical domain allows negative values
(e.g. signed angle, movement delta or world coordinate). It does not start Length, Width,
or canonical-start Opening offset entry: those fields are nonnegative (Length/Width must
be positive). Field constraints drive the trigger; display labels do not. A minus inside an
already focused native input is handled as editable text and validated normally, never
silently coerced. Composition events and keys in other text fields never intercept.
Tab cycles forward through the finite applicable field set; Shift+Tab cycles backward, both
wrapping within that active numeric editor; click on its underlined value is the pointer
alternative; keyboard-focused control + Enter is the discoverable alternative. Idle dimensions
remain passive unless explicitly focused. No global shortcut hijack while simply viewing.

Entry replaces the displayed value with a small input at the same location. Lengths default
to meters; `m`/`cm` suffixes accepted, angles degrees; no expressions or command language.
Explicit values outrank a conflicting snap: remove its winner marker and report `Exact value`.
In an active pointer drag, typing freezes the proposal, transfers to numeric editing, and
consumes the eventual pointer-up without committing; Enter submits once. For click-based
drawing Enter submits the current segment/insertion once; Wall tool continues from that end.
Escape in a field restores the pre-entry candidate; second Escape cancels the gesture. Tool
change cancels the whole proposal. Blur never silently commits. Invalid values stay editable
with a reason; no history or allocation. Numeric entry must pass the same release validation.

| Gesture | Preview composition / completion |
|---|---|
| Wall draw | Real band, compiled cap/join if available, active endpoint, length/angle, one snap. Candidate closure gives faint Room fill only when canonical candidate actually yields a face; otherwise say `Close at junction`, never promise a Room. |
| Rectangle / polygon | All candidate physical walls, last segment precision, derived face cue when valid. No invented fixed example dimensions. |
| Opening insert | Neutral host contour, true ghost cut, neutral Door/Window symbol, width/offset; candidate rejection where fit is already known. Host remains architecturally visible outside the cut. |
| Opening slide / resize | Original faint symbol; moving actual cut and jambs; width/offset/clearance witnesses; source highlight on blocked neighbor/end. |
| Junction / endpoint drag | All affected wall bands and openings; active diamond; deltas, optionally coordinates; no Room-owned vertices. |
| Rigid Wall move | Candidate band plus connected changes as canonically derived, original faint baseline, ΔX/ΔZ. |
| Subdivide / bend insert | Existing resolved click position gets one node preview. No visual reallocation of references; selection follows the existing command result. |
| Bend drag / removal / Curved toggle | Final candidate band primary; selected bend circles secondary; guides during manipulation. Inspector remains the toggle/remove command surface. |
| Object presets | Column-like or slab-like creation ghost may explain the preset. On commit it becomes the ordinary geometry footprint. |

Known invalid preconditions may be shown before commit; do not claim full validation before
the existing release validator runs. On release re-derive, then show the actual accepted or
refused candidate. A changed winner is updated, never left reporting stale pointermove truth.
A refused proposal remains broken with a reason for 1.2 s or until next deliberate action;
original geometry remains at its committed position, ink and opacity. A rejected candidate never recolors, displaces or fades that original. Escape clears it immediately. Announce the refusal once
through the existing status channel. Cancel and successful commit clear all transient draft
scaffolding immediately; normal selected controls may remain after commit.

| Refusal class | Local language / source emphasis |
|---|---|
| Opening does not fit / overlap | `Opening does not fit here` / `Overlaps another opening`; mark nearest limiting jamb or host end |
| Unsupported topology / degree | `This junction cannot accept this connection`; mark canonical affected junction |
| Curve not render-safe | `This bend cannot be built`; mark refused curve/control; never claim a guessed radius limit |
| Degenerate geometry / invalid length | `Wall is too short` only if validator gives that reason; otherwise use its precise supplied reason |
| Unresolved host / click distance | `Choose a position on a wall`; omit unavailable command; no fictional candidate |
| Release differs from preview | Accepted: show release truth; refused: `Position cannot be used` plus validator's reason |
| Existing compiler diagnostic | Persistent 6 px diagnostic marker and Inspector issue; never style the accepted baseline as a pending or known-invalid proposal |

Copy is illustrative: use canonical reason data, and never turn a generic rejection into an
invented diagnosis. No automatic repair or detached substitute geometry.

Snap glyphs: endpoint = square; midpoint = triangle; intersection = × inside circle;
perpendicular/orthogonal = right angle; Opening edge = bracket; grid = small cross.
Each gets a short relation label, a 2 px source accent if useful, and at most one guide.
These describe eligible canonical snap winners only. A missing snap family is not added by
this proposal; omit its glyph until the owner can actually honor that relation. Marker shape
may resemble a handle, but the handle is stationary and actionable while the winner has a
relation label and no independent hit target.

## 8. Objects, chrome and empty states

Generic cylinder and sphere show their compiled circular footprint; box shows its compiled
outline. Do not add a column cross, concrete hatch, level arrow, plinth name or structural cut
fill. Selected allowed handles use §6. No room ownership is inferred from spatial overlap.

Passive Scene uses no fill, thin outline, and 30% secondary ink at normal/near; 15% far;
10% inside the local instrument zone during Wall/Opening authoring. Never hide architecture beneath it. No resting labels in
Layout. Cross-owner Navigator emphasis may temporarily raise its outline to 60% and show
one name in passive context; it remains non-hittable for Layout. Arrange uses its own owner
selection vocabulary and commands; this proposal supplies only footprint hierarchy there.

Keep existing rulers, scale, axis key and Grid toggle positions. Empty drawing reads:
**Start your plan**
“Draw connected walls with **Wall**, or start with **Rect Room** or **Poly Room**.”
Below: “Scroll to zoom · Middle-drag to pan.” Use exact toolbar labels. A small neutral
open-corner sketch is illustrative, not selectable; no fake `10.0 m × 8.0 m` promise.
One committed wall removes the first-run message. A sparse plan does not nag the creator to
close a Room. Broken imported geometry uses diagnostics, never the empty-state illustration.

## 9. Icons and accessibility

The landed 2026-09-17 owner ruling retains the existing toolbar icons:

| Action | Retained icon |
|---|---|
| Wall | Lucide `BrickWall` |
| Rect Room | Lucide `Square` |
| Poly Room | Lucide `Pentagon` |
| Door | Lucide `DoorOpen` |
| Window | Lucide `Grid2x2` |

These five icons retain their original 24-unit viewBoxes and existing toolbar rendering
(14 CSS px). Do not substitute the earlier proposed miniature drafting-symbol silhouettes.
**The Plan Door drawing cue remains the ratified perpendicular three-dash cue in §3;
the toolbar Door icon is separate and does not need to reproduce it.** The Plan Window
drawing symbol likewise remains independent of the retained `Grid2x2` toolbar icon.

The six integrated custom marks—Select, Column preset, Platform preset, Plinth preset,
Snap and Grid—retain their settled native 20×20 paths, 1.5 px optical strokes, square
architectural corners and butt line ends. Visible labels remain authoritative.
[Production toolbar SVG symbols](./P23.13-drafting-icons-Designer-D.svg) contain only
these eleven landed toolbar marks. [Additional icon studies](./P23.13-icon-studies-Designer-D.svg)
contain Bend, Junction, Add junction, Box, Cylinder and Sphere as non-production study
marks; their presence is not authority to add or replace toolbar actions.

Selection/tool arming changes surrounding UI treatment, never invents a second icon
perspective. Preset icons remain flat: Column = cut circle, Platform = wide shallow
rectangular footprint, Plinth = compact square footprint with a centered inset cue.
No isometric facets. These optical archetypes may explain creation, but are never reused
to classify committed generic objects. Bend remains with its existing command, not a
new toolbar tool.

The atlas provides composable state checkboxes, a projected-feature gate slider, native-width
Door/Window recognition strips, local attention and free-space specimens, a simulated numeric-entry
lifecycle lab and a grayscale toggle. **PLATE Light is the default surrounding presentation.**
The seven legacy surrounds—navy-blue, salon-espresso, electric-plum, acid-moss,
porcelain-atelier, synth-sunset and velvet-kodachrome—are optional compatibility tests,
not co-equal product directions. Landed Plan Paper `#F5F7F8` and its semantic palette
stay constant. Surround swatches remain illustrative; production compatibility checks
use actual shell tokens without changing Plan semantics.

Focus remains a dark ring separated by paper on poché, tint, selected edge and handles.
Invalid retains broken geometry, stop shape, × and language under grayscale or red/green
loss. Snap uses shape/text rather than green. Do not rely on opacity alone for interactive
states. Hover is supplementary; all context is available through selection/focus. Screen
reader summaries include control role, identity, value, snap relation and refusal once per
meaningful change, not each pointermove. Preserve native input editing keys and IME.

Validation targets for child plan: small actionable text 4.5:1, essential control outlines
3:1 against adjacent surfaces, keyboard path to every displayed actionable handle and
editable dimension, 200% text zoom, and coarse-pointer targeting. These are design acceptance
targets, not an accessibility certification. Decorative grid/passive context are exceptions
only because the task remains fully operable without them. P23.14 must raise status-hint
legibility and reconcile shell focus return; neither issue is solved by this artifact.

## 10. Boundary and risk log

| Boundary / risk | Decision |
|---|---|
| P23.12 identity | Consume resolver, exact references and inheritance; no schema changes, minting or rename path |
| P23.14 shell | Require naming agreement, readable status, focus handoff. **P23.13 itself performs no ribbon/tree/Inspector restructuring** — that work belongs to P23.14, which was re-scoped by owner review (2026-09-17) to *Editor Shell & Visual System Foundation* and may substantially revise shell presentation, hierarchy and panel composition while preserving architecture/ownership contracts (see `docs/design/P23.14-shell-design-context.md` §9–§10 and the roadmap §P23.14) |
| P23.15 joins | Target continuous mass; expose compiler truth until correct shared geometry exists |
| Door mechanics / swing | Deferred from P23.13. Future authored hinge/swing/open-state semantics may drive 3D Door mechanics and animation, and may be exposed contextually in Plan when selected/editing. The resting P23.13 dashed perpendicular three-dash cue is symbolic only and does not encode hinge side, leaf orientation, handing, swing direction, opening side, sill, threshold construction, or animation state. |
| Durable Column/Platform roles | Potential future value for retrieval, only with demonstrated workflow need; no role inference now |
| Numeric entry | Ratified as one transient interface to existing commands; interaction/state cost must be included in child plan |
| Readout / label placement | Derived bounded screen layout only; no persistent tag/annotation system |
| New snap family | Only display what canonical snap owner can honor; no inference engine expansion |
| Curves | Same evaluator; no tangent solver, radius field, arbitrary Bézier editor or auto-repair |
| Visitor / Camera | Editor-only presentation and controls; Camera Plan receives passive architecture only |
| Legacy / relic | Smoke rendering only; frozen visitor relic unchanged |
| Atlas QA role | Non-authoritative optical reference only. May support human visual QA for hierarchy, state composition, symbols/icons, density and theme framing; never topology/domain validation, canonical behavior, theme-token truth or numeric acceptance. This specification wins on conflict. |

## 11. Open-question register — all 34 research questions

**R** = ratified/specification decision; **T** = implementation tuning inside stated constraints;
**F** = explicitly future scope. None is silently inherited.

| # | Question | Position / anchor |
|---|---|---|
| 1 | Physical thickness vs ink | R: exact band + screen profile, §3 |
| 2 | Resting body | R: graphite mass at normal/near; same silhouette far, §1 |
| 3 | Non-bounding grayscale | R: lighter solid body only + contextual role, §3; ratified A1 |
| 4 | Band simplification | R/T: <2 px projected thickness; global 24/80 px/m with hysteresis, §5 |
| 5 | Mixed joined mass | R: compiler shoulders; no visual topology repair, §3 |
| 6 | Handle shapes | R: diamond / circle / square / corner square, §6 |
| 7 | Junction reveal | R: selected wall/Junction or active tool winner, §6 |
| 8 | Baseline visibility | R: editing only, faint original reference, §6 |
| 9 | Curve scaffolding | R: points selected; polygon on focus/drag, §6 |
| 10 | Wall type-to-enter | R: adopt length/angle, §7; ratified A5 |
| 11 | Opening type-to-enter | R: adopt width/offset, §7; ratified A5 |
| 12 | Automatic dimensions | R: context table, §7; no permanent chains |
| 13 | Dimension editability | R: typing while drafting; explicit click/focus otherwise, §7; ratified A5 |
| 14 | Neutral Door | R/T: true cut + dashed perpendicular three-dash Door type cue (≥3 segments, symmetric screen-space extension); owner-ratified A2, recognition remains visual-QA/usability evidence, §3 |
| 15 | Window construction limit | R: two symbolic frame strokes only, §3 |
| 16 | Far opening distinction | R/T: three-dash perpendicular Door cue vs parallel-line Window cue where legible; small-opening density fallback, §3 |
| 17 | Future swing frame | F: no schema in P23.13; future authored Door mechanics may add truthful hinge/swing semantics, §10 |
| 18 | Room typography | R: 13 px name / 11 px reference / 11 px area, §2 |
| 19 | Drop order | R: area → reference → label; duplicate pair exception, §4 |
| 20 | Selected full identity | R: local when safe, fixed readout otherwise, §4; ratified A4 |
| 21 | Label travel | R/T: 12 px cap, frozen candidate, 8 px reentry margin, §4 |
| 22 | Room selection | R: 5% fill + Room-side boundary overlay, §4 |
| 23 | Generic geometry | R: footprints only, §8 |
| 24 | Durable roles worth adding | F: not justified for this slice; future workflow evidence, §10 |
| 25 | Passive Scene quietness | R/T: 30% normal, 15% far, 10% locally during authoring, §8; ratified A3 |
| 26 | Passive labels | R: none resting; one cross-owner context on emphasis, §8 |
| 27 | Paper vs dark | R: fixed cool paper in every shell, §1/9; ratified A6 |
| 28 | Grayscale states | R: explicit contour/shape/line grammar, §6 |
| 29 | Focus | R: dark double ring + paper moat, §6 |
| 30 | Color-deficient invalid | R: broken contour + stop/× + local reason, §7/9 |
| 31 | Resting drawing | R: yes, controls disclosed by intent, §1 |
| 32 | Calm vs precision | R: calm at rest; numeric precision where touched, §1/7 |
| 33 | Credibility vs baggage | R: cut hierarchy, voids, working witnesses; reject layer/style/tag systems, §3/7 |
| 34 | Presentation vs semantics | R: all graphics derived; future swing/roles separate; typing transient, §10 |

## 12. Review and acceptance map

The atlas supplies the four required compositions (small rectilinear, angled/shared-wall,
curved, dense) at far/normal/near, plus active-state, opening, icon, empty and theme studies.
Its controls compose layers, exercise projected-feature gates and illustrate numeric-entry
behavior, alongside grayscale/theme framing. The isolated numeric lab supports a simulated
Opening slide and explicit input; it does not claim production selection or validator behavior.
For **visual QA only**, these optical specimens may be used as a human reference for the
appearance they intentionally demonstrate: hierarchy, state composition, Door/Window cue
appearance, icon silhouette, density and theme framing. They are not acceptance evidence for
topology, geometry correctness, hit/snap behavior, history, identity, release validation or
exact numeric semantics; the specification and production tests govern those concerns.

| Brief §6 case | Explicit treatment | Later acceptance scenario |
|---|---|---|
| 1 Zoom | §5 matrix/hysteresis | Wheel across both thresholds repeatedly; tiny accepted walls remain visible |
| 2 State collision | §6 composition/hit ordering | Focus selected Opening edge at Junction; keyboard reaches both eligible controls |
| 3 Direct edit set | §6–7 controls/preview table | Move, reshape, subdivide, bend add/drag/remove, toggle and Opening edit: valid/refused/cancel |
| 4 Snap/refusal | §7 glyphs and reason classes | Verify every supported winner and release re-derivation; one source/guide |
| 5 Openings | §3/7 | Three profiles × Door/Window × three zooms; verify three-dash perpendicular Door cue (≥3 visible segments, symmetric screen-space extension that may exceed the Wall band) at 8/20/100 px and Window parallel-line collapse rules; end/junction/neighbor fit limits; confirm cue extent never modifies opening/Wall geometry, hit, snap, measurement, or validation |
| 6 Curves | §3/5/6 | Dense bend chain, sub-6 px/m focus, tangent join truth and render refusal |
| 7 Rooms | §4 | Four face sizes, two duplicate names, long name, suppressed selected label |
| 8 Density | §4/5 | Small pans cannot rearrange label candidates; active target reserves room |
| 9 Non-bounding | §3 | Compare solid walls in color/grayscale, including far and narrow bands |
| 10 Objects/Scene | §8 | Cylinder/box/sphere, preset commit, cross-owner overlap, active authoring |
| 11 Empty/sparse/broken | §8/7 | Empty → one Wall; invalid imported geometry; legacy internal smoke |
| 12 Theme/accessibility | §9 | Seven actual themes, grayscale, keyboard-only, 200% text zoom |
| 13 Research judgments | §11 | All 34 positions reviewed, including explicit future deferrals |

Implementation/visual QA should validate first: (1) whether the dark mass is calm enough in
dense plans, (2) whether the ratified three-dash perpendicular Door cue is recognized reliably
without instruction (Normal/Near atlas examples visibly contain at least 3 dashes; symmetric
extension beyond the Wall band is intentional and presentation-only), and (3) whether exact
entry remains worth its interaction cost in use.
Tune thresholds using the same fixtures; do not reopen identity or ownership to fix presentation
problems. **Design ratification is complete.** The next artifact is the implementation-ready
child plan with canonical API mapping and focused acceptance tests. This specification does
not itself advance tracker status or authorize unrelated scope.

## 13. Salience, ink and local attention — governing refinements

Global regimes select policy; projected features decide visibility. Do not implement three
visual themes. A 40 mm wall may need silhouette help at near zoom while a thick wall still
shows its band at overview. Window separation, opening width, text fit, grid spacing and
control eligibility are independent gates. Use hysteresis per feature as well as per regime.

Dense ink budget: if projected parallel profile edges have <1.5 px separation, draw one
neutral silhouette aid instead of two competing exterior ink strokes. Preserve the exact
band underneath. Never fill a gap between disconnected walls, close an opening, remove a
canonical piece of geometry, or imply a new join. When several unrelated silhouette aids
collide, retain exact bands and prioritize selected-owner supplemental ink; reduce redundant
non-selected profiles before adding thickness. Baseline hit/snap/measurement geometry never
uses these aids. Symbolic small-opening cues stay displaced from the true cut.

The local instrument zone is the screen-space bounds of the active handle and its working
annotation, padded 64 px (maximum attention extent 180 px from the active work locus). A long
Wall move does not dim the entire Wall's bounding rectangle. Inside the zone: suppress
unrelated Room area first, then reference subject to duplicate-name pairing; reduce passive
Scene to 10%; remove nonessential object labels. Actual architecture and authored LayoutObject
outlines retain full rest legibility. Outside the zone: absolutely no dimming or annotation
changes solely because a gesture is active. Selected identity, refusal, working values and
the winner own the budget. The zone has no visible border or spotlight in the product.

Freeze the acquisition/control vocabulary, chosen dimension lane, selected label candidate
and entry focus during a gesture. Allow lower-priority annotation to suppress when it actually
collides. Restore displaced/suppressed metadata after gesture end with the same 150 ms
settling rule. Numeric-entry focus is not lost because zoom crossed a gate.

Pending proposal is not validated success: use a neutral/blue continuous candidate and an
active endpoint without a success check or green fill. Cheap known-invalid preview can show
refusal; final acceptance belongs to the canonical commit validator. For a valid moving
candidate the old baseline may be faint; for a known-invalid or rejected candidate the
original must be fully restored visually immediately. Committed diagnostics are separate:
small persistent source marker + existing issue description, never a broken live proposal.

## 14. Code-reality audit and conflict classification

Reviewed against the current working tree on 2026-09-16. These are capability mappings and
mismatches, **not task slices, an implementation sequence or a file-by-file change plan**.
Code evidence links use named functions as well as line anchors to make findings inspectable.

Classes: **M** straightforward presentation mapping; **W** implementation work within P23.13;
**A** locked architectural constraint requiring adaptation; **S** new semantics, deferred.
Only A/S constrain the synthesized ceiling. Existing code habits classified W do not.

| System | Current evidence / gap | Class | Design survives through |
|---|---|---|---|
| One render path | `buildPlanRenderModel` consumes compiled physical walls, rooms, objects and opening polylines | M | Retain `CompiledLayoutGeometry → PlanRenderModel → SVG`; richer derived primitives remain in that path |
| Physical band vs ink | `architecturalStrokeStyle` clamps physical-looking width to `max(7, thickness × scale)` | W | Exact band plus separate screen ink; remove the conceptual coupling rather than restyling the clamp |
| Band geometry / joins | `CompiledPhysicalWall` exposes samples, thickness, sections, solid spans and centerline polylines, **not a finished joined Plan-band polygon contract** | W + A | Derive richer band presentation at the canonical geometry boundary using shared samples; no SVG repair/resampling. Junction-correct geometry remains P23.15; seams cannot be cosmetically hidden |
| Openings | `openingSymbol` currently derives jambs from width/normal and uses **three** Window strokes; Door uses full span | W | At most two Window parallel-line strokes plus the ratified three-dash perpendicular Door cue (≥3 segments, symmetric screen-space extension, presentation-only), with projected-size gates. Curved frames/cues must use canonical host evaluation, not constant-normal local offsets |
| Names / references | `roomIdentity`, `identitySecondaryReference`, `identityLabelPair` exist | M | Reuse the shared resolver; no renderer raw-ID formatting or new identity fields |
| Room placement | `interiorLabelPoint` guarantees an interior point; overlay uses area ≥1 m², scale ≥6 px/m, 24 px point-radius suppression and document-order wins | W | Full text-box free-space search, canonical-ID tie-break, sticky candidate, tier suppression; an inside point alone is insufficient |
| Room area | Compiled Room floor polygon and existing selected metrics provide area context; overlay emits only name | W | Expose existing derived metrics in the label presentation; no annotation objects |
| Zoom | Isolated thresholds exist (e.g. handles at 6 px/m), no unified salience/hysteresis contract in inspected Plan path | W | Add presentation-state hysteresis, projected gates and active-control freeze without durable view semantics |
| Local attention | Overlay blockers suppress names; no spatial instrument-zone policy | W | Local annotation/context budgeting; leave rest of drawing unchanged |
| Selection / focus | Style variants and separate layers exist; selected casing/body can recolor architecture; no specified moat/control focus composition | W | Preserve original profiles; independent moat/selection/focus layers |
| Handles | Curve/Junction controls are circle primitives; lone hover may expose a Junction | W | Shape-coded owned controls and invisible acquisition targets; eliminate ordinary-hover handle disclosure |
| Hit authority | `resolvePlanHit` explicitly puts endpoint/Opening before curve control; selected curve candidates are supplied | W | Owner/tool eligibility preceding fallback; keep canonical resolver and single selection |
| Snaps | `resolveLayoutSnap` provides winner/source owners and guide kinds; exact enum includes junction, midpoint, intersection, edge, span, object bounds, grid, orthogonal and extension | M + W | Reuse winner; map its actual relation to glyph/text and reduce visual guide count to one. Do not relabel orthogonal as arbitrary perpendicular |
| Direct-edit proposals | `previewArchitectureEdit` uses immutable baseline and `transientArchitectureEdit`; canonical planner runs at release | M + W | Reuse that truth separation; enrich preview band/opening graphics and original/refusal composition |
| Pending vs accepted | `architectureEditIntentFor` distinguishes pending from cheap known-invalid; pending is not acceptance | A | Closure wash needs canonical candidate face evidence. Otherwise show proposed closure target without promising a Room |
| Release truth | `commitArchitectureEditGesture` re-derives actual release position and commits once or cancels | M | Preserve release truth; numeric mode explicitly transfers ownership so late pointer-up cannot commit |
| Refusal persistence | `finishArchitectureEditGesture` clears transient intent immediately; Inspector status carries failure | W | Retain a render-only refusal annotation briefly, with unchanged original; no persistent candidate or history |
| Working dimensions | Existing text primitives and edit overlays exist, but no full screen-lane/short-span contract | W | Derived frozen lanes and measured text extents, not annotation state |
| Curved length | `CompiledPhysicalWall.length` and `compiledPhysicalWallLength` available | M | Use canonical arc length; never replace with endpoint chord length |
| Exact entry | Viewport `onKeyDown` handles Escape/Delete/Backspace; no dimension-attached numeric-entry lifecycle in inspected path | W | One transient numeric editor using existing canonical operation paths, not a separate model or solver |
| Opening edits | `layout-interaction` maintains baseline offset/width and raw candidate; `createDefaultWallFirstOpeningIntent` supplies creation input and viewport calls `updateWallFirstOpening` for accepted drag edits | M + W | Width/offset input and controls share this operation ownership; show refusal rather than silently clamping explicit values |
| Selected identity readout | Existing viewport selection feedback resolves shared identity; no bounded free-space-failure readout as specified | W | One temporary read-only fallback; ratified A4, no Inspector duplication |
| Layout / Scene | Render-model and CSS have distinct footprint styles; Scene is pointer-events none; current passive style is dashed with fill | M + W | Ratified A3: continuous light no-fill Scene; Layout objects stay authored and selectable by owner |
| Theme | `--editor-plan-*` semantics isolated from theme overrides; current paper is warmer | M | Ratified A6: one cool paper palette, all shell surrounds unchanged |
| Icons | Landed toolbar retains BrickWall / Square / Pentagon / DoorOpen / Grid2x2 and integrates six settled custom drafting marks | R | Preserve the five original toolbar icons and six custom marks (§9); the three-dash Door cue belongs to the Plan drawing, not the toolbar icon; no extra toolbar architecture |
| Keyboard | Viewport is focusable, but complete control-group traversal and dimension entry are not present in inspected handler | W | Ratified A5 Plan-local group behavior; shell handoff requirement to P23.14; not silently dropped |
| Extra construction / roles | Opening type has offset/width/height/sill/profile, no hinge or frame construction; ordinary object types do not retain preset roles | S | No swing, construction dimensions, BIM class, hatch, overhead or durable preset classification in P23.13 |

### Evidence index

- [Plan model metadata and wall projection](../../apps/editor/src/lib/layout/plan-render-model.ts:165),
  especially physical-Wall projection around line 645 and the layers around 680.
- [SVG width clamp and opening symbol](../../apps/editor/src/lib/editor/layout/PlanSvg.svelte:95),
  with architectural/Scene styles around line 254.
- [Compiled physical-Wall contract](../../packages/layout-core/src/layout-geometry-types.ts:239).
- [Room anchor](../../apps/editor/src/lib/editor/layout/plan-overlays.ts:68),
  [handle reveal](../../apps/editor/src/lib/editor/layout/plan-overlays.ts:1042),
  [current name suppression](../../apps/editor/src/lib/editor/layout/plan-overlays.ts:1130).
- [Canonical hit resolver](../../apps/editor/src/lib/editor/layout/plan-hit.ts:325).
- [Snap kinds](../../packages/layout-core/src/layout-snap.ts:40) and `resolveLayoutSnap`.
- [Transient preview and release](../../apps/editor/src/lib/editor/layout/LayoutPlanViewport.svelte:708),
  [intent classification](../../apps/editor/src/lib/editor/layout/plan-overlays.ts:547),
  [keyboard handler](../../apps/editor/src/lib/editor/layout/LayoutPlanViewport.svelte:3371).
- [Identity helpers](../../apps/editor/src/lib/editor/identity/layout-identity-view.ts:61).
- [Opening gesture candidates](../../apps/editor/src/lib/editor/layout/layout-interaction.ts:207),
  [opening creation-intent helper](../../apps/editor/src/lib/editor/layout/layout-opening-editing.ts:76).
- [Authored Wall/Opening data](../../packages/layout-core/src/layout-wall-first-types.ts:149).

## 15. Six owner decisions — ratified 2026-09-16

| ID | Ratified choice | Code/product implication and tradeoff | Status |
|---|---|---|---|
| A1 Non-bounding | Lighter solid body only; role in focus/selection context | Existing `role` supports this directly. Fewer invented marks; role is less obvious when the band becomes subpixel | **Approved** |
| A2 Door cue | Dashed symbolic cue **perpendicular to the host**, centered in the true opening, showing **at least three clearly separated visible dash segments at Normal/Near** (optical starting point ≈ 2 px dash / 2 px gap, ≈ 10–12 px total; symmetric screen-space extension that may exceed the Wall band; below 8 px use the displaced symbolic fallback rather than compressing the cue) | No schema change. Stronger Door/Window discrimination without implying hinge, leaf, handing, swing, opening side, sill, threshold construction, or animation state. Cue extent is presentation-only and never modifies opening/Wall geometry, hit, snap, measurement, or validation. Future authored Door mechanics remain independent | **Approved — remains ratified, not reopened** |
| A3 Passive Scene | Very-light continuous outline, no fill/label at rest | CSS presentation change from dashed footprint. Semantically cleaner; must keep lower value than authored LayoutObjects | **Approved** |
| A4 Selected Room fallback | One bounded temporary read-only canvas readout when no safe label rectangle fits | Existing identity helpers support content; new transient presentation surface needed. Solves tiny/concave Rooms without overlap, but consumes viewport space | **Approved** |
| A5 Exact-entry reach | During gestures **and** explicitly focused selected primary dimensions | Existing canonical commands remain authority. New focus/lifecycle work; more precision with less Inspector travel, but selected measurements now carry an editable affordance | **Approved** |
| A6 Paper temperature | Cool off-white `#F5F7F8` | Token change from warmer current paper, no per-theme variants. Clear technical hierarchy; validate comfort in dense plans during visual QA | **Approved** |

All six owner choices are now part of the final P23.13 design authority. Recognition, density,
accessibility and threshold studies remain QA/tuning evidence; they no longer represent open
product decisions.

## 16. Concise ratification/implementation acceptance checklist

- [ ] Exact physical widths and accepted geometry survive every scale, including dense joins;
      no renderer patches, hidden topology changes or invented height lines.
- [ ] Door/Window discrimination evaluated at 8/20/100 px; the Door uses the three-dash
      perpendicular cue (at least three clearly separated visible segments at Normal/Near,
      symmetric screen-space extension that may exceed the Wall band without modifying
      opening/Wall geometry, hit, snap, measurement, or validation) and Window uses parallel-line
      frame strokes; two Window strokes collapse before merging;
      no hinge, handing, swing, opening-side, sill, threshold construction, or elevation arc appears.
- [ ] Concave/tiny/dense Rooms use complete-box free space; duplicate name/reference stays paired;
      selected identity never jumps to an unsafe centroid; fallback is bounded and temporary.
- [ ] Global and per-feature hysteresis prevent flicker; active control/focus/lane stay frozen;
      authored objects never vanish just because global regime is far.
- [ ] Hover, selected, focus, proposal, winner and validity compose on the same target;
      selected-owner controls win overlapping unrelated geometry through canonical hit authority.
- [ ] Local attention changes only the work neighborhood, preserving distant labels and geometry.
- [ ] Numeric entry from gesture/focused primary dimension: Tab and Shift+Tab (forward/reverse wrapping), Enter, two-stage Escape,
      pointer-up consumption, blur, IME, field-aware signed triggers and invalid retry behave as specified; one operation or none.
- [ ] Rejected proposal leaves committed geometry at original position, color and opacity;
      diagnostics stay distinguishable from transient refusal; release snap truth is current.
- [ ] Curve measurement says arc and uses canonical length; creation closure waits for canonical
      candidate face evidence; all cancellation paths remove transient scaffolding.
- [ ] The five retained toolbar icons match their existing Lucide marks; the six custom drafting
      marks retain native 20×20 paths. Plan Door remains three-dash perpendicular ink and Plan
      Window remains parallel-line ink, independently of toolbar silhouettes. Preset archetypes
      never classify committed objects. PLATE Light is the surround baseline; legacy compatibility
      tests preserve landed Plan Paper `#F5F7F8` and one semantic palette.
- [ ] Keyboard reaches each actionable control/dimension, focus survives grayscale and selection,
      status text remains readable; no new visitor imports, identities or document semantics.

The updated atlas is a standalone interaction/optical study, not a running compiler-backed
editor. It is **non-authoritative**, but its optical specimens may be used as a narrow human
visual-QA reference for visual hierarchy, state composition, Door/Window cue appearance,
icon silhouette, density and theme framing. It must not be used to validate topology,
geometry correctness, hit/snap behavior, history, canonical identity, release validation or
exact numeric semantics. Where atlas and specification differ, **this specification controls**.
Its numeric-entry lab models the lifecycle and shows explicit simulated rejection; it does not
establish domain validity. Production behavior and creator-recognition evidence remain QA/usability
work, not unresolved owner decisions.

### Artifact verification — synthesized revision

- Browser inspected at 1440×1000 and 390×844; no horizontal page overflow. Native recognition
  strips remain 300 CSS px wide, preserving their 8/20/100 px opening widths at browser 100%.
- Verified composed selection/focus/proposal/refusal, grayscale, twelve fixture plates and
  seventeen native 20×20 SVG icons. Broad-regime hysteresis produced overview/overview/
  reading/reading/edit/edit/reading at 19/25/28/80/88/79/71 px/m. Window second-line hysteresis
  stayed visible at 50→40 and disappeared at 34 px/m.
- Numeric lab: pointer-up while typing left commit count zero; Enter applied once; invalid
  9 m width refused without another commit; first Escape restored the candidate, second
  cancelled; typing from slide entered offset and Tab focused width. Inspected desktop
  dimension-attached input and mobile fallback layout.
- Standalone JavaScript syntax, local specification links and repository whitespace checks
  passed. These are artifact checks from the pre-ratification atlas revision; the A2 cue
  orientation/dash change requires the normal visual-QA recheck, not domain revalidation.
  The three-dash enlargement recheck must confirm Normal/Near atlas Door examples visibly
  contain at least three dashes, Plan Window stays parallel-line based and distinct, and no
  old solid or host-parallel Plan Door cue remains. Toolbar icon QA separately checks the
  retained `DoorOpen` / `Grid2x2` marks under the landed owner ruling in §9.

### Consistency review resolved — 2026-09-16

Applied all seven review items. The lifecycle vocabulary is pending proposal/draft →
known-invalid proposal or accepted commit; Door marks are consistently called a Door type cue;
the Wall LOD row contains no permanent role glyph. Static atlas plates now apply 15%/30%
passive-Scene opacity and projected grid spacing, including 0.25 m minor lines at 96 px/m.
Shortcut copy resolves the current canonical binding, with verified Meta/Alt defaults cited.
Numeric triggers respect field signedness; Tab/Shift+Tab explicitly cycle forward/backward.

Verification: browser confirmed Far/Normal/Near Scene opacities 0.15/0.30/0.30, minor-grid
absence at 18/44 and presence at 96 px/m. Minus left unsigned offset drafting in gesture mode;
a signed-field trigger accepts it. Forward/reverse Tab traversed the numeric fields with zero
commits. JavaScript syntax, terminology, local links and whitespace checks passed.

Two qualifications: the reviewed shortcut defaults were already correct in canonical code;
and in a two-field cycle forward/reverse navigation reaches the same other field, although
explicit direction remains required for the general contract.

### Owner ratification applied — 2026-09-16

A1–A6 are approved. A2 replaces the prior host-parallel solid Door cue with the restrained
symbolic treatment rotated perpendicular to the host and rendered dashed, now enlarged for
legibility: at least three clearly separated visible dash segments at Normal/Near, symmetric
screen-space extension that may exceed the Wall band without modifying authored geometry,
hit, snap, measurement, validation, or Door mechanics. A2 remains ratified, not reopened.
This does not add Door mechanics, hinge, handing, swing, opening-side, sill, threshold, or
2D swing semantics. Future authored hinge/swing/open-state data may support
3D Door animation and contextual Plan editing in a later slice. P23.13 remains limited to a
truthful resting identity cue. The next artifact may proceed to implementation-ready planning.
