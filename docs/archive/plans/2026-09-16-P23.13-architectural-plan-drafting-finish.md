# P23.13 — Architectural Plan drafting finish · implementation-ready plan

**Status:** implementation-ready child plan (no code in this task).
**Date:** 2026-09-16 · **Baseline:** `main` @ `c2a2404` (P23.11 curves + P23.12 identity + PR #54/#56 fixes).
**Authority order:** final design specification → synthesized direction → designer brief → remaining-roadmap reconciliation → current code.
**Design authority:** `docs/design/P23.13-final-design-specification-Designer-D.md` (authoritative). Atlas (`P23.13-ink-and-instrument-atlas-Designer-D.html`) is narrow visual-QA reference only — hierarchy, state composition, Door/Window appearance, icon silhouette, density, theme framing; never geometry/topology/hit/snap/validation/identity/history/numeric authority. Icons: `P23.13-drafting-icons-Designer-D.svg`.
**A1–A6 are owner-ratified. This plan reopens none of them.** A2 Door rule (do not redesign): true authored cut; centered dashed line perpendicular to host; Normal/Near ≥3 clearly separated visible dash segments; symmetric screen-space extension that may exceed both Wall faces; never changes Wall/opening geometry, hit/snap/measurement/validation; never implies hinge/swing/leaf/handing/direction/animation; future Door mechanics are separate future semantics with no P23.13 schema.

---

## 1. Current-state reconciliation

Notation per system: **current → gap → target → canonical owner → exact seam → tests/evidence**.

### 1.1 Presentation pipeline (`CompiledLayoutGeometry → PlanRenderModel → SVG`)

- **Current.** `buildPlanRenderModel` (`apps/editor/src/lib/layout/plan-render-model.ts:545`) always emits 13 layers from pre-flattened `solidCenterlinePolylines` / opening `centerPolyline`. Canonical walls render once with `role` (`:642-676`); legacy room walls render without `role` (`:579-589`). No `room-name`/`dimension-label` emitted here — labels live in `plan-overlays.ts:1136` layer 13. Style vocabulary is `PlanStyleToken` (`:11-110`).
- **Gap.** The model passes *centerlines + thickness meters* and leaves all band-vs-ink decisions to SVG, which then couples them: `architecturalStrokeStyle` clamps to `max(7, t·ppm)` (`PlanSvg.svelte:95-101`), wall body and casing both scale with `--architecture-width`, and the opening void punches with `room-bg` (`:278-292`), which fails outside rooms. Legacy partitions get no `role`, so they are indistinguishable. No offset-band polygon or curve-parameter metadata survives (`layout-geometry-types.ts:258` carries samples only).
- **Target.** One derived Plan presentation model: the canonical physical-width projection
  from compiled centerline/section truth (exact authored thickness and canonical opening
  cuts preserved) + independent screen ink (1px floors, silhouette aid, selection/moat
  overlays) + symbolic opening cues. No second geometry truth; no SVG topology repair.
  Where canonical geometry cannot supply a junction-correct band, expose the seam for
  P23.15; never fake it in presentation.
- **Owner/seam.** `layout-core` owns compiled samples/sections/spans; `plan-render-model.ts`
  owns stable renderer-neutral source facts derived from canonical geometry (opening kind,
  center, tangent/normal, authored width, host thickness, wall role — never CSS px, zoom
  regime, or hysteresis); transient `plan-salience` owns all projected-size gates, hysteresis
  and gesture freezing; the resolved screen-presentation layer (salience output consumed as
  overlay/presentation input) determines Door normal-vs-small fallback, Window 1-vs-2 lines,
  silhouette aids and feature visibility; `PlanSvg.svelte` owns paint only.
- **Evidence.** `apps/editor/tests/lib/layout/plan-render-model.test.ts`, `plan-render-boundary.test.ts`, `layout-geometry-golden/parity/boundary.test.ts`.

### 1.2 Wall band, ink, silhouette, partitions, caps, curves

- **Current.** Walls paint as two centerline strokes: `.wall-casing` (`--architecture-width + 2px`, `+4px` when selected) + `.wall-line` body (`PlanSvg.svelte:181-191, 265-277`). Partition distinction exists only as ad-hoc `color-mix` when `role==='partition'` (`wallPartitionClass`, `:116-120`). Curves arrive flattened (`sampleSegment` flatness 0.01m / span 0.25m, `layout-geometry-curve.ts:384-450`); SVG never sees cubics. Joins are square-mitered touching masses; junction-correct seams are explicitly P23.15.
- **Gap.** (a) `max(7,…)` clamp lies about thin/far walls instead of keeping the canonical-width projection + separate 1px silhouette aid. (b) Selection halo scales with physical width instead of a zoom-independent overlay. (c) Void punch color assumes room background. (d) No dense-ink simplification policy. (e) Partition body has no dedicated token.
- **Target (§3, §13 of spec).** Always render the canonical physical-width projection from
  compiled centerline/section truth, preserving exact authored thickness and canonical
  opening cuts (P23.13 does not solve joined-band topology — that contract belongs to
  P23.15; seams from insufficient canonical geometry stay exposed, never repaired in
  presentation). Below 2px projected thickness add centered 1px silhouette ink (excluded
  from measure/snap/hit); dense parallel edges <1.5px separation collapse to one neutral
  silhouette aid preserving the band underneath; non-bounding walls keep the same dark
  contour with lighter solid body + contextual role text (A1); free ends get square caps;
  curves use the canonical evaluator path with no local approximation policy.
- **Owner/seam.** Band projection from the canonical geometry boundary (`layout-geometry.ts:348-375` canonical compile; `wall-centerline.ts:224-256` single adapter); source facts in `plan-render-model.ts` (thickness, role, opening center/tangent — never resolved screen decisions); gates + aids resolved in transient `plan-salience`; paint + tokens in `PlanSvg.svelte` + `plan.css`/`tokens.css` (`--editor-plan-*` ownership stays; add band/silhouette/partition tokens, paper `#F5F7F8` in S2).
- **Evidence.** Thin-wall band, dense-join, curved-wall render-model unit tests + visual-QA plates (small/angled/curved/dense at far/normal/near).

### 1.3 Door / Window grammar (A2)

- **Current.** `openingSymbol` (`PlanSvg.svelte:137-167`) derives jambs from endpoints ± `normal·halfWall` (correct, physical). Windows draw **three** frames (`-0.28/0/+0.28·t`); Doors draw gap + jambs + `.door-threshold` **along the span** (`:192-211`) — i.e. the old host-parallel solid cue the spec explicitly replaced. Dash/ink: `.door-threshold` 1px, `.window-frame` 1.35px, `vector-effect:non-scaling-stroke`.
- **Gap.** Three defects vs ratified A2: (1) Door cue is host-parallel, not perpendicular; (2) Door cue is 1–2 dash-scale, not ≥3 segments at Normal/Near; (3) Windows have 3 strokes, spec allows max 2 with projected-size collapse. No shared cue helper — production has one `openingSymbol` (good) but previews/selection/zoom must reuse it, not reimplement.
- **Target (spec §3).** Door: exact authored void + jamb termination + **one shared perpendicular dashed cue primitive**: centered on opening, rotated with host tangent (canonical `CompiledOpening.center.tangent/normal/yaw`, `layout-geometry-types.ts:82-108`; curved hosts use canonical evaluation, never constant-normal offsets), 1px secondary ink, `2/2` dash, ~10–12px total screen length yielding ≥3 painted dashes at Normal/Near, symmetric about centerline, allowed to exceed the Wall band; presentation-only (never hit/snap/measure/validate/mechanics). Far ≥8px preserves the 3-dash cue where legible; `<8px`/extreme overview uses the existing displaced symbolic fallback, never a compressed cue. Window: true cut + max two parallel strokes separated by `min(4px, 0.36t)` screen, ≥3px separation and ≥1px edge margins or collapse to one; `<8px` drops termination detail before the displaced cue; hysteresis enter 9/leave 7px width, second-line enter 3.5/leave 2.5px.
- **Owner/seam.** `plan-render-model.ts` exposes stable renderer-neutral source facts only
  (opening kind, center, tangent/normal/yaw, authored width, host thickness — never CSS px,
  zoom regime, or hysteresis). Transient `plan-salience` owns the projected-size gates,
  hysteresis and gesture freezing that resolve Door normal-vs-small fallback and Window
  1-vs-2 lines; the resolved screen-presentation layer carries those decisions as
  overlay/presentation input. `PlanSvg.svelte` paints the cue in **screen space** (`screenAt` + fixed-px perpendicular segment, `stroke-dasharray="2 2"`, symmetric `y±cue/2` pattern like the atlas `opening()` helper). jamb math stays world-based. All consumers (rest, preview, selected/edit, zoom) call the one helper.
- **Evidence.** Source-fact render-model tests asserting kind/center/tangent/width/thickness passthrough is invariant across 8/20/100px presentation scales; S2 tests Door/Window screen-space gate resolution at 8/20/100px; 3-dash cue paint test with thin wall proving symmetric protrusion + unchanged cut/jamb coordinates; no-hinge/swing assertion (no leaf/arc elements).

### 1.4 Room labels

- **Current.** `interiorLabelPoint` (`plan-overlays.ts:74-144`) guarantees an interior point (centroid or largest-ear fallback). Emission (`:1130-1176`) gates on face ≥1m², scale ≥6px/m, 24px suppression radius vs diagnostics/other labels/handle centers, document-order tie-break. No area on Plan (Inspector only). Selected-room text is the status/meta line (`LayoutPlanViewport.svelte:3670-3691`), not free-space placement.
- **Gap.** Point anchor ≠ text-box fit; no Name/Ref/Area tier system; no duplicate Name+Ref pairing; no protected-region mask (walls/openings/objects/active controls); no sticky candidate; no displacement cap / settle / hysteresis; no bounded fallback readout for tiny/dense rooms.
- **Target (spec §4).** Full free-space system: canonical floor polygon → subtract wall/opening edges, object footprints, active handle/annotation bounds (label mask only, never geometry) → up to 8 candidates ranked by clearance then semantic-center proximity, canonical-ID tie-break → test full text rect (concave-aware) → sticky accepted candidate across gesture/small pan-zoom (≤12px layout drift before tier change; pan movement excluded; 8px + 150ms reentry; reduced-motion = no animation) → tier drop area→reference→label (duplicates protect name+reference as a pair) → fixed bounded temporary canvas readout (≤280px, 12px safe inset, never over handles/core geometry) when nothing fits. Selected rooms use the same placer, never a centroid override.
- **Owner/seam.** Derived transient overlay state in `plan-overlays.ts` (+ small placement helper module); identity content from `layout-identity-view.ts` (`roomIdentity`, `identityLabelPair`); readout as viewport-local Svelte state (never document/history). Area comes from existing derived metrics (compiler floor polygon / selected metrics), never renderer math.
- **Evidence.** Concave/tiny/dense/duplicate/long-name placement tests; sticky-candidate + suppression-order tests; fallback-readout component tests.

### 1.5 Semantic zoom / salience

- **Current.** Isolated thresholds only (handles @6px/m, label floors above); no unified regime/hysteresis in the Plan path. Grid minor culled <6px (`layout-plan-transform.ts:213-250`); ruler `niceStep` target 80px; scale bar 2-segment target 100px; zoom clamp [2,2000] (`:93-182`).
- **Gap.** No broad regime + per-feature gates + hysteresis; no gesture freeze; Scene/grid/label/control LOD uncoordinated.
- **Target (spec §5, §13).** Global `s` (CSS px per meter): far <24, normal 24–79.99, near ≥80; hysteresis far→normal 28 / normal→far 20 / normal→near 88 / near→normal 72; jumps resolve directly. Per-feature projected gates (Wall silhouette <2px; Window separation/edge margins; opening width 9/7; second-line 3.5/2.5; Room tiers; Junction/curve controls; LayoutObject simplification with 1px ink floor; Scene 15%/30%; grid major ≥16px, minor ≥12px incl. 0.25m minor at near; dimensions selected/active only at far). Freeze active control set/lane/candidate/focus for the gesture duration. All thresholds are tuning values, never document semantics.
- **Owner/seam.** New `plan-salience.svelte.ts` (or overlay-local) derived presentation state: regime + per-feature visibility + hysteresis memories; consumed by the resolved screen-presentation layer (overlay/presentation input), overlays and chrome — never written back into `PlanRenderModel`. Gesture freeze keyed off the existing architecture-edit snapshot/baseline.
- **Evidence.** Hysteresis traversal tests (19/25/28/80/88/79/71 px/m expectations), gate unit tests, gesture-freeze interaction tests.

### 1.6 State composition

- **Current.** `TOKEN_CLASSES` single-blue selection language (`PlanSvg.svelte:19-72, 250-388`); hover `#55a1ff` thinner/softer; preview intents via `architectureEditIntentFor/withArchitectureEditIntent` (`plan-overlays.ts:547-668`) pushed to `drafts` only; invalid = `architecture-edit-intent-invalid` + Inspector `role="status"` + diagnostics accordion.
- **Gap.** Selection can recolor architecture (casing/body) instead of surrounding it with paper separation; hover/selected distinction relies partly on hue; focus has no dedicated Plan ring distinct from selection; pending vs accepted vs known-invalid vocabulary needs the compositional stack enforced in paint order.
- **Target (spec §6).** Compositional paint order: geometry → role/symbol → selection → proposal → source/guide → handles → winner/invalid marker → focus ring → text. Rest = ink geometry + neutral symbol. Hover = offset neutral hairline, no blue, no handles. Selected = separated blue perimeter + paper moat, graphite survives. Focus = dark double ring + paper moat over everything. Pending = neutral/blue continuous candidate + active endpoint (never success green/check). Known-invalid = proposal at 35% + broken `4/3` perimeter + octagonal stop + × + local reason; original at full ink/opacity/position. Accepted commit = normal ink, all transient scaffolding cleared immediately.
- **Owner/seam.** Token + class changes in `PlanSvg.svelte`/CSS; intent pipeline reuse (`layout-transient-edit.ts:130-178`); refusal persistence as render-only annotation (~1.2s or until next action, Escape clears).
- **Evidence.** State-composition component tests (selection+focus+snap+invalid on one target), grayscale/non-color-grammar checks.

### 1.7 Hit authority / controls

- **Current.** `resolvePlanHit` (`plan-hit.ts:325-372`) fixed order: vertex → canonical endpoint → interior-anchor → legacy opening → canonical opening → curve control → object → legacy wall → canonical wall → room. Junction outranks curve control by comment (`plan-overlays.ts:1051-1054`). Handles: curve circles + junction diamonds via overlays; opening edge squares + slide grip via `wallOpeningEdgeWorldPoints/pushWallOpeningAffordances` (`:261-304` — the single render+hit source, good). Acquisition targets are visible-mark sized.
- **Gap.** No captured-gesture → focused-selected-owner-control → other-selected-owner-controls → tool-eligible → fallback authority; no nearest-screen-distance + canonical-ID tie-break within tier; no invisible 24px (44px coarse) acquisition targets; keyboard traversal group absent in viewport (`onKeyDown:3371-3585` handles Escape/Delete but no alphanumeric entry branch, no control-group Enter/arrows).
- **Target (spec §6).** Owner-aware resolver adaptation: keep `resolvePlanHit` as the single canonical fallback, add an owner/tool eligibility pre-pass in the viewport (captured gesture wins; focused control of selected owner beats co-located opening/junction; active opening width square beats host wall). Shape code: diamond = topology endpoint/junction; hollow circle = curve bend (fills when active); square = opening edge straddling jamb; paired grip = slide; corner square / ring+tick only where the owner already supports resize/rotation. Focus/drag adds 1px broken control polygon + true centerline; baselines only during edit at 20% ink.
- **Owner/seam.** Adapt `plan-hit.ts` + viewport click/keyboard routing; reuse `wallOpeningEdgeWorldPoints` as the single affordance source; never an SVG-side resolver.
- **Evidence.** Co-located control tests (opening edge vs junction vs curve control), owner-priority tests, acquisition-target size tests.

### 1.8 Snap / guide presentation

- **Current.** Canonical `resolveLayoutSnap` (`layout-snap.ts:527-650`) with `SEMANTIC_RANK` (`:52-63`) and stable `rank→distance→key` winner (`:216-242`); opening-drag has its own anchored pipeline (`:663-1024`). Presentation via `withLayoutSnapFeedback` (`plan-overlays.ts:677-697`). Glyph vocabulary partially present; marker radii 5/4/3px (`:147-157`).
- **Gap.** Presentation allows more than one guide conceptually; some paths bypass the winner (`roomUnit` uses `snapToGrid`, object rotate bypasses snap, wall-chain hover snaps live geometry without moving-owner exclusion — see interaction recon). Explicit numeric value does not yet suppress snap-success presentation.
- **Target (spec §7).** Reuse winner only: one marker (endpoint square / midpoint triangle / intersection ×-in-circle / orthogonal right-angle / opening-edge bracket / grid cross) + short relation text + ≤1 guide + optional 2px source accent. No candidate cloud, no new families. Explicit value outranks conflicting snap (winner marker removed, `Exact value` reported).
- **Owner/seam.** Presentation-only changes in overlays/viewport; snap math untouched; route stray paths through `resolveLayoutSnap` or document why not.
- **Evidence.** Winner-only tests incl. release re-derivation (PR #56 anchor rule), glyph mapping tests, explicit-value-suppresses-snap test.

### 1.9 Working dimensions

- **Current.** `dimension-label` edge lengths from canonical `wall.length` when available else chord (`plan-overlays.ts:874-889,977-988`); offset 5px (`:37`); no lane/freezing/overflow contract.
- **Gap.** No transient dimension system: no frozen lane per gesture, no short-span outward push → readout fallback, no per-context table (wall length/angle, arc length + `arc` tag, ΔX/ΔZ, opening width/offset/clearance, rect W×D, supported object transforms).
- **Target (spec §7).** Derived 0.75px witnesses + 4px oblique ticks + 11px mono on paper knockout, 18px lane (34px second lane), side chosen once at gesture start and frozen; short spans push outward then to readout, never shrink text; meters/degrees explicit; display rounding never changes precision.
- **Owner/seam.** Overlay-derived geometry + viewport readout fallback; canonical lengths (`CompiledPhysicalWall.length`, `compiledPhysicalWallLength`) for curves — never chord.
- **Evidence.** Lane-freeze, overflow-to-readout, arc-label tests.

### 1.10 Type-to-enter (A5, ratified)

- **Current.** None in viewport. Exact numbers live in Inspector (`layout-exact-input.ts:14-35` format/parse; `planExact*` planners). `resolveWallChainEndpointAtLength` (`layout-interaction.ts:1138-1150`) has zero callers — dead direction memory.
- **Gap.** Entire lifecycle missing: gesture/focused-dimension triggers, signedness, units, Tab cycling, one-shot Enter, two-stage Escape, blur-never-commits, IME safety, pointer-freeze + pointer-up consumption, invalid-retry, single-history-operation guarantee.
- **Target (spec §7).** One transient numeric editor on the working dimension: digit/decimal starts entry in primary field during gesture (`-` only where the domain allows negatives — angle/delta/coords, never Length/Width/nonnegative offset); Tab/Shift+Tab wrap within the finite field set; click on underlined value = pointer alternative (**retired — D1, 2026-09-17: see S7/S8; the keyboard door and gesture typing are the reachable alternatives, and exact values otherwise live in the Inspector**); focused control + Enter = keyboard alternative; typing freezes pointer proposal and consumes the eventual pointer-up; Enter submits once through the same canonical planner/validator; Escape restores pre-entry candidate, second Escape cancels; tool change cancels; blur never commits; invalid stays editable with reason, no history/allocation; explicit value outranks snap.
- **Owner/seam.** New viewport-attached transient editor reusing `layout-exact-input` parse/format + `planExact*`/canonical operation paths (`planCreate/UpdateWallFirstOpening`, wall/junction planners); ownership transfer flag consumed by `commitArchitectureEditGesture`/`pointerup`.
- **Evidence.** Lifecycle tests: pointer-up-consumed, Enter-once, invalid-no-commit, Esc-Esc, Tab wrap, signedness gate, IME passthrough, one-or-none history assertion.

### 1.11 Preview / refusal lifecycle

- **Current.** Transient architecture edits (`begin→preview→plan-once-at-release→commit/cancel`, `LayoutPlanViewport.svelte:591-800`, `layout-transient-edit.ts:130-259`) with release re-derive for architecture/room-unit/interior-anchor — but **not** for canonical opening drag (release commits remembered candidate `:2763-2776`) or legacy opening drag (per-move writes + blind commit). Opening creation intent clamps only at creation (`layout-opening-editing.ts:55-66`); drag/edit must reject.
- **Gap.** Opening slide/resize/insert need release-truth (PR #56 class) + known-invalid preview + brief refusal persistence with original untouched; per-tool preview composition (true band, ghost cut, neutral symbol, closure cue only on canonical face evidence) unspecified in code.
- **Target (spec §7).** Per-tool preview table; closure wash only when canonical candidate yields a face (`Close at junction` otherwise); refused proposal persists broken ~1.2s/until next action/Escape-clears with original at committed position/ink/opacity; cancel clears all scaffolding; release re-derives then shows accepted or refused truth.
- **Owner/seam.** Extend `releaseArchitectureEdit` pattern to opening drag; viewport + `layout-interaction` candidate computation; refusal as render-only annotation (`plan-refusal.ts`, S8).
- **Evidence.** Release-differs-from-preview tests, refusal-persistence + original-preserved tests, per-tool preview tests. **Landed (S8, 2026-09-17):** `p23-13-preview-closure` + `p23-13-refusal` (see the slice's Done blocks).

### 1.12 Local attention, LayoutObjects, Scene, grid/ruler/paper, icons

- **Current.** Overlay blockers suppress names (`:1130-1176`); no instrument-zone policy. **Corrected 2026-09-17 (the section predates S2/S4 landing):** the paper is already the ratified cool `#F5F7F8` (`tokens.css:165`, S2 step 3 resolves through that token), so the "warm paper token" the Replaced list names was **S2's** work, not S9's, and the Wall-mass palette migration it was paired with is **S4's** (see S2's Deferred note). LayoutObjects already render exactly one authored footprint per object (`compiled.objects[].planFootprint`) in screen-constant ink; the Scene footprints were dashed + filled on the Layout canvas, which is what step 1 changes. Toolbar uses lucide icons (`LayoutDraftToolbar.svelte:2` — BrickWall/DoorOpen/…), no repo SVG family; Door icon would need the 3-dash cue.
- **Target.** Instrument zone = active handle + annotation bounds + 64px pad (≤180px from locus), no visible border: suppress area→reference (pair-safe), Scene→10%, nonessential object labels; architecture + object outlines stay full; nothing outside changes; restore on settle (150ms). Objects: authored footprints, simplified only by projected size, 1px ink floor. Scene: continuous, no fill/labels, 15% far / 30% normal-near / 10% in zone, non-hittable. Grid major ≥16px, minor ≥12px (0.25m minor at near); ruler 1/2/5 ≥48px labels; scale segmented X/Z meters; snap step independent of grid detail. Paper `#F5F7F8` one palette across shells (token change only). Icons: integrate the repo SVG family for every toolbar icon **except five kept originals (owner ruling, 2026-09-17): Wall, Rect Room, Poly Room, Door, Window** — those stay as they are, marked owner-ratified, so the Door 3-dash / Window-parallel redesigns are not adopted; Bend stays with existing command (no new tool), presets never classify committed objects.
- **Evidence.** Zone-suppression/restoration tests, Scene opacity/hit tests, token tests, icon silhouette tests (kept originals pinned as-is for Wall/Rect Room/Poly Room/Door/Window, new silhouettes for the rest). **Landed (S8, 2026-09-17):** `plan-attention.ts` (zone + 150 ms settle) + the S3 placer's `tierDropZone` (pair-safe `full` → `name-reference`, region-scoped on the accepted anchor), pinned by `p23-13-attention`. **Wired:** the Scene row is `withPlanAttentionSceneInk` — a per-footprint `sceneInkFor` override that answers §1.12's 10% inside the zone and S2's regime value outside it, `min`-guarded so the zone can only ever quiet ink and never brighten it (a regime dimmer than the zone keeps its own value), pinned by `p23-13-attention`. **Open by design:** the object-label half ("nonessential object labels") needs Plan object labels, which do not exist until S9 draws them, and a *global* label mute while the instrument is live would change surfaces outside the zone, which this row forbids. The zone is that pass's input; the seam is S9's object-label work.

---

## 2. Implementation architecture

```
LayoutDocument (authored truth)
  → compileLayoutGeometry() (one compiler; curve/single-evaluator; sections/spans/openings)
  → PlanRenderModel (stable renderer-neutral source facts from canonical geometry:
     wall thickness + role, opening kind/center/tangent/normal, authored width,
     host thickness, floor polygons — never CSS px, zoom regime,
     or hysteresis)
  → plan-salience (transient: projected-size gates, hysteresis memories, gesture
     freeze → resolved screen-presentation decisions: Door normal-vs-small fallback,
     Window 1-vs-2 lines, silhouette aids, feature visibility)
  → plan-overlays (transient screen-space state consuming salience output:
     label candidates + sticky map, dimension lanes, snap feedback, intents,
     instrument zone — never document/history)
  → PlanSvg.svelte + chrome (paint only: world→screen for band/jambs;
     fixed-px ink/cues/handles/dimensions; tokens via --editor-plan-*)
```

- **Compiled geometry owns:** samples, sections, solid spans, openings (offset/width/center/tangent/normal/yaw), floor polygons, lengths (incl. arc), validity/diagnostics. Never pixels, dash counts, label positions. It does not expose a finished junction-correct Plan-band polygon contract — P23.13 renders the canonical physical-width projection from centerline/section truth and exposes insufficient-junction seams for P23.15.
- **Render model owns:** stable renderer-neutral source facts (listed above) + which primitives exist. No px math, no gate resolution, no hysteresis memory, no DOM.
- **Overlay/salience owns:** all px thresholds, hysteresis memories, sticky label map, frozen lanes/zones, snap presentation, intents. Keyed by canonical IDs; partitioned by lifetime — view-lifetime (semantic-zoom hysteresis memory, sticky Room-label placement; survives ordinary gestures and small pan/zoom), gesture-lifetime (frozen feature gates/control vocabulary, dimension lane, numeric ownership, instrument-zone bounds; clears on commit/cancel/tool change), bounded feedback lifetime (refusal annotation, ~1.2s or next deliberate action/Escape); geometry changes invalidate only affected derived state. None is serialized or enters document/history snapshots (follow the P23.12 `identityHighWater` precedent — derived marks outside the snapshot).
- **SVG owns:** paint mapping (band stroke from world width; ink/cues/handles/dims in fixed px with `vector-effect:non-scaling-stroke`; symmetric cue via `screenAt(center) ± perp·(cue/2)`).
- **Canonical interaction owns:** selection/history/transactions/validation/planners (`commitLayoutTransaction` five sites + three persistence seams stay the only durable paths; transient paths never `promoteLayoutIdentity` — CURRENT.md traps).
- **Forbidden:** second geometry truth, SVG topology repair/resampling, SVG hit resolver, cue-driven hit/snap/measure, persistent dims/annotations, hinge/swing schema, BIM/material/role semantics, shell restructuring (P23.14), junction-geometry fixes (P23.15).

---

## 3. Ordered slice register

| Slice | Objective | Depends on |
|---|---|---|
| S0 | Presentation-foundation refactor: split band vs ink, remove width-clamp lie, retire `door-threshold`, Window 3→2, token split | — (first; unlocks everything) |
| S1 | Static architectural grammar: canonical-width projection, silhouette-aid paint path, jambs/caps, perpendicular Door cue shapes, Window stroke shapes, partitions, curves via canonical path | S0 |
| S2 | Salience + paper/grid/ruler: regime + per-feature gates + hysteresis + gesture freeze; `#F5F7F8`; grid/ruler/scale gates | S0 (needs S0 fields); parallel-safe with S1 after S0 |
| S3 | Room labels: free-space placer + tiers + sticky + fallback readout | S1 (band/void truth), S2 (regime/gates) |
| S4 | State composition + hit authority + shape-coded controls + acquisition targets | S1 (symbols), S2 (freeze) |
| S5 | Snap/guide presentation: one winner, glyphs, explicit-value suppression | S4 (composition order) |
| S6 | Working dimensions: transient derived set + lanes + overflow | S2 (lanes/freeze), S4 (composition) |
| S7 | Type-to-enter lifecycle (A5) | S6 (dimension anchors), S4 (ownership), S5 (snap suppression) |
| S8 | Preview/refusal per tool + release truth + local attention zone | S1–S7 (reuses all pipelines) |
| S9 | Objects/Scene/empty/dense/icons/theme surrounds | S1–S2 (grammar + tokens); icon swap anytime after S1 |
| S10 | Keyboard/a11y traversal + announcements + final QA gate | S3–S9 (controls/dims/entry/previews + Scene/objects/empty/icons/themes) |

**Ordering rationale.** S0 is the load-bearing refactor: every later slice reads the band/ink split and the render-model source facts, so hiding it inside "polish" would scatter architecture changes. S1 establishes the static primitive grammar (what is drawn), S2 owns the projected-size and hysteresis decisions (when/how much is drawn) — labels (S3) need both. Interaction grammar (S4) needs symbols + freeze before snap (S5), dims (S6), entry (S7) layer on top. Previews/refusals (S8) integrate all pipelines, so it comes after them. Objects/Scene/icons (S9) are largely independent paint/token work parked late to avoid churn. Keyboard/final QA (S10) closes the slice because traversal + announcements can only be pinned once controls, dims, entry, previews **and** the S9 surfaces (Scene, objects, empty/dense, icons, surrounds) exist — S10 depends on S3–S9.

---

## 4. Detailed slice plans

### S0 — Presentation-foundation refactor

- **Objective.** Establish the band-vs-ink split as the single foundation: canonical physical-width
  projection plumbed as renderer-neutral source facts, screen ink separated, `max(7,…)` clamp
  removed as geometry, `door-threshold` retired, Window frame contract 3→2, token split.
- **Why this boundary.** All later slices consume render-model source facts (kind/center/tangent/
  width/thickness/role) plus the band/ink split; doing this inside any visual slice would hide
  architecture churn. No resolved screen decisions (fallback, frame count, silhouette need,
  regime) enter `PlanRenderModel` — those belong to transient salience (S2).
- **Prerequisites.** None (first slice).
- **Files/symbols touched.** `plan-render-model.ts` (`PlanPolylinePrimitive.architecture`, `PlanStyleToken`, `buildPlanRenderModel:545`); `PlanSvg.svelte` (`architecturalStrokeStyle:95`, `openingSymbol:137`, `wallStateClass:103`, `wallPartitionClass:116`, CSS `:250-292`); `tokens.css:162-172` + `plan.css:8-53`; `layout-geometry-types.ts:239-261` (read-only — add nothing persistent).
- **New.** Renderer-neutral source-fact exposure only (opening kind/center/tangent/normal,
  authored width, host thickness, wall role — fill only what is missing; all derived, none
  resolved by px/zoom); tokens `--editor-plan-wall-band`, `--editor-plan-silhouette`, `--editor-plan-partition-body`, `--editor-plan-door-cue` (names descriptive; ownership stays `--editor-plan-*`).
- **Reuse.** Compiler samples/spans/openings; `opening.center.tangent/normal/yaw`; `wallCenterlineSamples` single adapter; canonical transaction/history paths untouched.
- **Replaced.** `Math.max(7,…)` as geometry (becomes paint-time silhouette aid in S1); `.door-threshold` host-parallel span line; third window frame; `room-bg` void punch (becomes paper/transparent punch, S1).
- **Invariants.** No geometry/topology/selection/history change; byte-identical compile output; visitor chunks untouched; P23.12 identity paths untouched.
- **Steps.** 1) Add/verify source-fact fields + unit tests (defaults preserve current visuals; assert no px/regime/hysteresis state in the model). 2) Split tokens. 3) Rewire `openingSymbol` to expose source facts + paint ≤2 window frames from an injected (S2-owned) count (paint still old-style behind flag if needed for bisection). 4) Remove clamp-from-geometry; keep temporary visual floor flagged for S1. 5) `check` + render-model tests green.
- **Tests.** Render-model source-fact passthrough tests; Window 3→2 regression; token-existence tests; compile-output parity (golden) tests.
- **Visual QA.** Side-by-side old/new on small/angled/curved/dense fixtures; no topology change.
- **Stop.** If compile output differs by one byte, stop — foundation must be behavior-preserving.
- **Gate.** Golden/parity + render-model tests pass; reviewer confirms no durable-path change.

### S1 — Static architectural grammar

- **Objective.** Static primitive grammar: canonical-width band projection, jambs/caps, shared
  perpendicular Door cue shape, Window stroke shapes, partition body, canonical-curve path.
  Projected-size and hysteresis decisions (fallback, frame count, silhouette need) belong to
  S2 — S1 builds the grammar and tests it with gate values injected/stubbed.
- **Prerequisites.** S0.
- **Files/symbols.** Same as S0 + `layout-geometry.ts:348-375,909-961` (read), `layout-geometry-openings.ts:188-228`, `plan-overlays.ts:226-335` (affordance source reuse).
- **New.** `doorTypeCueScreenPoints(center, tangent, lengthPx=12)` screen-space helper (single implementation used by rest/preview/selected/zoom); dash `2/2`, 1px secondary ink; symmetric `±6px`; jamb helper stays world-based (`normal·halfWall`).
- **Reuse.** `wallOpeningEdgeWorldPoints` as single affordance source; `CompiledOpening.center` for orientation; canonical curve samples (no SVG resampling; SVG draws flattened polylines as today — exactness work belongs to P23.15).
- **Replaced.** Host-parallel Door cue; thin-wall invisibility (canonical-width projection + 1px silhouette paint path under 2px projected, need resolved in S2); void punch color.
- **Invariants.** Cue never enters hit/snap/measure/validate (assert by test: hit queries ignore cue geometry); no hinge/leaf/arc elements; no join repair (seams stay truthful for P23.15); no px/regime/hysteresis state in `PlanRenderModel` (gate resolution is S2's, injected here).
- **Steps.** 1) Paint canonical-width band projection (world width, no clamp) + silhouette-aid paint path (need signal stubbed until S2). 2) Retire threshold → perpendicular cue via shared helper; both full and displaced shapes built, selection between them stubbed until S2. 3) Window ≤2 shape rule + edge-margin shape rule (count resolution in S2). 4) Partition body token + caps. 5) Dense-ink `<1.5px` single-aid paint path (trigger in S2).
- **Tests.** 8/20/100px Door-vs-Window grammar tests with injected gate values; 3-dash count from painted output (dash-array × length); symmetric protrusion beyond thin wall with unchanged cut/jamb coords; curve-host orientation test (cue ⊥ local tangent). Collapse + hysteresis resolution tests live in S2.
- **Visual QA.** Atlas Far/Normal/Near plates + 8/20/100 strips + theme surrounds; Door protrudes symmetrically on thin walls, fits inside thick walls.
- **Stop.** Any cue participation in hit/snap/measure, or any invented join fill → stop and remove.
- **Gate.** All S1 tests + QA checklist green before salience work.

### S2 — Salience, paper, grid, ruler

- **Objective.** Regime + per-feature gates + hysteresis + gesture freeze; ratified paper; grid/ruler/scale gates.
- **Prerequisites.** S0 (S1 parallel-safe after S0; coordinate to avoid paint conflicts).
- **Files/symbols.** New `plan-salience.svelte.ts` (or overlay-local); `layout-plan-transform.ts` (`worldToPlanScreen:144`, `buildPlanGrid:213`, `buildPlanRulerTicks:55`, `buildSegmentedScaleBar:74`); `PlanCanvasChrome.svelte`; `tokens.css` paper token.
- **New.** `PlanSalience {regime, gates…, hysteresis memories}` derived state; freeze token captured at `beginArchitectureEditGesture` (`LayoutPlanViewport.svelte:591`) and held through `finishArchitectureEditGesture:748`.
- **Reuse.** Existing grid/ruler/scale builders (add gates only); snap step stays independent of grid detail.
- **Replaced.** Warmer paper `#f5f3ee` → `#F5F7F8` (token only; shells unchanged); scattered thresholds → one gate table.
- **Deferred.** The §2 Wall-mass palette (`#343C43` graphite body plus its 1 px profile, replacing today's warm `#d5d1c8`/`#625f59`) is explicitly **not** S2: this slice ratifies paper and grid only. Owner: **S4**, whose step 1 already edits the PlanSvg tokens/classes. Recorded here (review follow-up) so the cool paper cannot leave the warm Wall mass unowned.
- **Invariants.** Thresholds are tuning values, never persisted; no snap-step coupling; no shell chrome redesign.
- **Steps.** 1) Regime + hysteresis + freeze. 2) Per-feature gates incl. the resolved screen decisions: Door normal-vs-small fallback, Window 1-vs-2 count, silhouette-aid need, and all other feature visibility — resolved in transient salience and passed to the presentation layer as overlay/presentation input (never written back into `PlanRenderModel`). 3) Paper token + chrome gates. 4) Ruler 1/2/5 ≥48px + segmented scale.
- **Tests.** Hysteresis traversal (19/25/28/80/88/79/71), gate tests incl. Door fallback + Window count + silhouette resolution, freeze-during-gesture test, token test.
- **Visual QA.** Wheel across thresholds repeatedly; tiny walls remain visible; no flicker.
- **QA notes.** The wheel handler lives on `svg.plan-canvas` itself, not on the `.plan-viewport` wrapper — dispatch wheel events on the canvas or the zoom never runs (learned the hard way during the S2 pass). The chrome half of the pass (grid/ruler gates, paper, no flicker) is done and recorded in the S2 review. The wall-at-zoom half ("tiny walls remain visible") was **not** performed in S2 or S3 — S3's fixture QA exercised the label placer and the band arithmetic, not painted Wall bands — and is carried to **S10**'s final QA gate, where a fixture document with Walls exists. Recorded as open, not as "carried and silently closed".
- **Stop.** Any persisted threshold or snap-step coupling → stop.
- **Gate.** Salience tests + manual zoom pass (chrome half closed; wall half open → S10).

### S3 — Room labels

- **Objective.** Free-space Name/Ref/Area system + sticky + fallback readout.
- **Prerequisites.** S1, S2.
- **Files/symbols.** `plan-overlays.ts:74-144,160-170,1130-1176`; `layout-identity-view.ts:85-150,189` (`roomIdentity`, `identityLabelPair`); `LayoutPlanViewport.svelte:3670-3691` (replace status-only text with placer + readout host).
- **New.** `plan-room-labels.ts`: mask builder, ≤8 candidates, clearance→center ranking, canonical-ID tie-break, sticky map, tier engine, readout component (bounded ≤280px, 12px inset, read-only, temporary). Candidate placement consumes a small `TextMeasure` interface (`measure(text, style) → {w, h}`): production implements it with browser/Canvas/DOM metrics; placement unit tests inject deterministic fixture metrics and never depend on host/browser fonts. Cache invalidation covers font readiness, text change, and relevant scale/layout changes; browser/component visual tests verify real-font behavior.
- **Reuse.** Floor polygons; derived area metrics via the same S3 label path (never renderer math); P23.12 vocabulary (no raw IDs, no pills, max-six-char refs, duplicate pairing).
- **Replaced.** Point-anchor + area/zoom floors + document-order ties.
- **Invariants.** Placement never writes document/history; area display never changes precision/input; no leaders; no forced overlap.
- **Steps.** 1) `TextMeasure` interface + fixture implementation + cache/invalidation policy. 2) Mask + candidates + ranking. 3) Text-box fit (concave-aware) against the interface. 4) Sticky + displacement cap + settle. 5) Tiers + duplicate pairing. 6) Fallback readout + selected-room same-placer.
- **Tests.** Large/medium/small/tiny, concave + obstacle, duplicates, long name, dense stability, fallback bounds, sticky across small pan/zoom — all placement unit tests on deterministic fixture metrics; real-font behavior covered by browser/component visual tests incl. cache-invalidation (font-ready/text-change/scale) cases.
- **Visual QA.** Four face sizes + duplicate pair + suppressed-selected-label fixture, on a fixture document (the empty demo project has no Rooms, and `/museum/editor` mounts no Scene Plan). Done: a standalone plate driving the production placer and `TextMeasure` with real font metrics, covering large/medium/small/tiny, a concave C-face, an authored-object footprint, active text, a 168 px long name, a duplicate pair and the selected readout case. It does **not** cover painted Wall bands; that eyeball moves to S10 (see S2's QA notes).
- **Stop.** Any centroid override for selected rooms or any persisted placement → stop.
- **Gate.** Placement tests + QA fixtures green.

### S4 — State composition, hit authority, controls

- **Objective.** Compositional paint order preserving graphite; owner-aware acquisition; shape-coded controls with real acquisition targets.
- **Prerequisites.** S1, S2.
- **Files/symbols.** `PlanSvg.svelte` classes/CSS; `plan-hit.ts:325` (`resolvePlanHit`); `LayoutPlanViewport.svelte` click/context/keyboard routing (`:1528`, `:3292`); `plan-overlays.ts:1043-1107,226-335`; `layout-interaction.ts:165-190,1157-1221,1520-1604`.
- **New.** Owner/tool eligibility pre-pass (captured → focused-selected-owner → other-selected-owner → tool-eligible → fallback) + within-tier nearest-screen + canonical-ID tie-break; invisible 24px targets (44px coarse); focus ring component; broken-polygon + stop-marker refusal paint.
- **Reuse.** `resolvePlanHit` as canonical fallback; `wallOpeningEdgeWorldPoints` single source; existing selection/history plumbing.
- **Replaced.** Warm Wall fill/profile `#d5d1c8`/`#625f59` → §2 graphite mass `#343C43` with its 1 px exterior profile (migrated here from S2, which ratifies paper + grid only); recoloring selection; hover handle disclosure; endpoint-before-everything fallback supremacy over owner intent (adapt, don't fork). The graphite migration carries the rest of the §2 mark palette with it (label ink `#343C43`, secondary/muted ink `#596570`, room paper = canvas paper), because leaving warm greys under a cool graphite mass would have swapped one mismatch for another.
- **Done.** Control vocabulary extracted to `$lib/layout/plan-control-grammar.ts` (kind, mark shape, acquisition radii, authority verdict) so the canonical resolver reads one definition without importing editor code — the `plan-hit` boundary test enforces `$lib/layout`-only imports, so the vocabulary could not live editor-side. `plan-acquisition.ts` owns the tier engine + focus geometry; `plan-hit.resolvePlanHit` gained `options.controlAuthority`, which reorders exactly two things (a selected-owner curve control above a co-located endpoint, and the endpoint tier standing down for a non-Junction verdict) and otherwise leaves the class fallback untouched.
- **Invariants.** No SVG resolver; single selection; deterministic outcomes; hover never mutates.
- **Steps.** 1) Paint-order + tokens (graphite survives). 2) Eligibility pre-pass + tie-breaks. 3) Shape code + targets + control polygon/centerline on focus-drag. 4) Focus ring + refusal paint.
- **Tests.** Overlap matrix (opening edge vs junction vs curve control vs wall body), owner-priority, target-size, focus-survives-selection, grayscale shape-grammar tests.
- **Visual QA.** Focus selected opening edge at junction; keyboard reaches both; hover/selected/focus/proposal/winner/invalid composed on one target. **Done** for everything painted (see QA notes); "keyboard reaches both" is **not** performed here — control-group keyboard traversal belongs to **S10**, which owns the canvas tab stop, Enter/arrow traversal and announcements. Carried there explicitly rather than claimed.
- **QA notes.** The pass mounted the real `PlanSvg` against a real interaction projection on a temporary dev plate (deleted after). It found two paint defects that the unit suite could not see: (a) the selected/hovered state contour had **no paper separation** — §6's "separated blue perimeter + paper moat" was rendering as a thicker Wall, fixed by painting contour → paper moat → profile → band; (b) the focus ring's 5 px moat at `+4 px` **erased the focused mark's own ink**, so a focused 7 px square read as a ring with nothing in it — the ring is now a page-scaled system computed from the mark's *circumscribed* radius (clearance ≈ 1.45·r + 1, then ink/moat/ink/moat at 2.5 px steps), so the mark stays legible inside its ring. Both fixes are pinned by tests. Confirmed in the live plate: 7 px diamonds at junctions, 7 px squares straddling Opening jambs, a 9 px screen-constant paired slide grip perpendicular to the host, the double ring with paper moat, the octagon stop + × on a known-invalid proposal, and select + hover + focus + refusal composed on one target. Note for the next reader: the handle ink is *themed* (`--editor-plan-handle-stroke: var(--editor-accent-pressed)`), which is brick under the default theme — shape, not colour, is the non-colour identity §6 requires, so a grayscale review must read the marks, not the ink.
- **Review follow-ups (post-commit).** (1) Focus geometry returned one *flattened* reference centerline, but a compiled centerline is split around authored cuts — focusing a control on an opening-host Wall drew a phantom segment straight across the void. It now returns **spans** and the overlay emits one primitive per span; pinned against a door fixture. (2) The control polygon was derived as the centerline's endpoints, which is the true net only for a straight Wall: on a curve it rendered a straight chord over the curve it described, on precisely the Walls where the net matters. The net is now an **input** (the viewport reads junctions for a straight Wall and authored knots for a cubic chain) and an absent net draws no polygon — the helper never derives one. (3) `clearLayoutSelection` now releases focus too: a delete cleared selection but not focus, and because P23.12 recycles canonical IDs a reissued Junction/Opening id could resurrect a ring (plus its owner control net) on an entity never pressed. (4) The §2 separation figures were reconciled numerically against the atlas rather than by intent: the atlas draws hover as a 1 px stroke 4 px from the band edge and selected as a 1.5 px stroke 3 px out, i.e. **both are measured from the band edge**, so the visible paper gap from the 1 px exterior profile is 1 px smaller (2 px hover, 1 px selected). The shipped values land exactly on §2's "1 px neutral contour, 3 px outside body" and "1.5 px blue outer contour, 2 px paper separation", now pinned as arithmetic so S10's grayscale pass reads the same reference.
- **Stop.** Any SVG-side hit truth or second selection → stop.
- **Gate.** Hit/authority + composition tests green.

### S5 — Snap / guide presentation

- **Objective.** One-winner presentation with relation glyphs; explicit-value suppression.
- **Prerequisites.** S4.
- **Files/symbols.** `layout-snap.ts:40-63,216-242,527-650,663-1024`; `plan-overlays.ts:147-157,677-697`; viewport snap call sites incl. stray paths (`layout-interaction.ts:1364`, viewport `:2733`).
- **New.** Glyph map (square / triangle / ×-in-circle / right-angle / bracket / **plus** for the grid fallback — §7's "small cross" is the upright `+`, because the diagonal × is already the intersection glyph's inner stroke *and* the refusal mark) + relation label component; suppression flag from numeric ownership (S7 wiring prepared here, enforced in S7).
- **Reuse.** Winner math untouched; opening-drag anchored pipeline untouched.
- **Replaced.** Multi-guide/candidate-cloud presentation; stray non-winner paths routed through `resolveLayoutSnap` or explicitly documented.
- **Invariants.** No new snap families; snap never writes geometry; release re-derivation preserved (PR #56).
- **Steps.** 1) Glyph/label/guide cap. 2) Route strays — done: the only inlined grid formula (object transform) now goes through `layout-core`'s `snapToGridStep` and is documented as grid-**only** by design (an object transform has no snap family its owner can honour, so routing it through `resolveLayoutSnap` would present a relation nothing can keep); Room-unit and Opening drags already pass `snapEnabled` into their canonical resolvers, and the Wall-chain hover already resolves through `resolveLayoutSnapCandidate`. 3) Explicit-value suppression hook. 4) Source-accent rule — decided, see Deferred.
- **Done.** `plan-snap-grammar.ts` owns the glyph table (shape + relation word + ink), the ratified §7 families, the unratified families *listed* rather than defaulted, and a type-level exhaustiveness check so a new resolver family fails the build until it is decided. `withLayoutSnapFeedback` presents exactly one winner (mark + word), caps guides at one as a presentation guarantee, and carries §7's precedence rule: an explicit value removes the mark and the guide and reports `Exact value` instead. Tokens added: `snap-relation-label`, `snap-glyph-stroke`, `snap-glyph-halo`. `planSnapRelationLabelOffsetPx` chooses the word's side from the caller's viewport.
- **Tests.** Winner + release re-derivation; glyph mapping; shape distinctness; one-guide cap; suppression test (with S7); ink-ownership and label-side placement tests (25 in `p23-13-snap-grammar`).
- **Visual QA.** **Done** — a temporary dev plate (deleted after) mounting the real `PlanSvg` over the real snap projection for every ratified family, the neutral fallback, the explicit-value case, a real guide, the resolver's actual guide, and two canvas corners.
- **QA notes.** The pass found four defects the suite could not see. (a) The grid fallback reused the refusal mark: it painted `crossPath` — spec §6's refusal × — so one stroke meant "grid honoured" and "proposal refused" at once, and grid became indistinguishable from intersection at a glance. §7's "grid = small cross" is the **upright `+`**; the new `plus` shape is deliberate and a test forbids any snap winner using the `cross` path. (b) The open-path glyphs (bracket, right angle) were painted with the *filled* marker token, so their fill had no area and the only visible stroke was the 2 px paper one — a snap winner rendered as a **paper-coloured notch cut out of the Wall band**, i.e. "a snap was honoured" drawn as "a hole was cut here". Ink is now a property of the glyph (`ink: 'fill' | 'stroke'`), the grammar picks the token and the paint layer is told which treatment applies rather than re-deriving it from the shape; P23.2's `snap-marker-grid` (the same treatment hard-coded for one family) is folded into `snap-glyph-stroke`. (c) Those same glyphs had **no paper separation**, so a 1.5 px stroke sat directly on the graphite mass — every other state mark in §6/S4 is separated with paper (the filled markers get it as their own paper stroke, the intersection × as the stroke inside its disc), so they now paint a wider paper halo under the ink. (d) The relation word is anchored to the live pointer, so a winner near an edge drew its word **half outside the canvas** (observed: an `Endpoint` winner in the corner). The side is now chosen from the viewport the caller already has; the extent comes from the grammar's own typography, and the choice is stateless by construction because the winner is recomputed every pointer event. All four are pinned by tests.
- **Deferred / open.** §7's **2 px source accent** is deliberately **not** drawn, and this is a decision rather than an omission: `SnapCandidate` carries a `sourceId` with neither the identity nor the extent needed to paint a source (`grid` has no source entity, a `wall-midpoint` source is a derived point, not a span), so accenting the nearest plausible entity would be exactly the fabrication §7 forbids. Closing it needs canonical source extent first — a resolver change, not a presentation one. Separately, **the guide has no producer at all** — the S5 review asked who owns this, and the answer is sharper than "no extent". `orthogonal-guide` is the only family `guidesForCandidate` ever draws a guide for, and that family is **unreachable**: its sole producer is `layout-core`'s exported `orthogonalGuideCandidates`, which has **zero production callers** (only `p23-2-snap.test.ts`), and `resolveLayoutSnap`'s candidate set is junctions, wall spans, wall-wall intersections, Opening edges, object bounds and grid — never an orthogonal guide. `extension-guide` is worse: it is declared, ranked and named in the §7 table but **never constructed anywhere**. Consequences: (i) no production snap ever carries a guide, so §7's "at most one guide" is satisfiable in presentation but **not satisfiable in fact** by S5 or S8 alone — the ink is proven correct (the plate painted the ratified dashed guide from a real span) but nothing can hand it one; (ii) the `right-angle` glyph and its "Right angle" word are ratified-but-dormant ink, which the grammar module now says out loud. 

**Decision (owner: plan owner, 2026-09-16).** Wiring `orthogonalGuideCandidates` into the resolver is **not** a presentation change: adding a rank-7 candidate family changes what a live gesture snaps to (it would displace the grid fallback and lock an axis against the draft anchor), so it is a behaviour change that must land in a slice with a per-gesture behaviour mandate and release-truth tests — **S8**, which already owns per-tool preview composition and the §7 gesture-row QA, and where a new winner would actually be observed. S8 therefore owns it as an explicit step, with a stated precondition: **wire the family (candidate + real guide extent, so the guide is visible) or retire it**, and update this plan's §7 acceptance row either way. "Recorded with a component owner and no slice" is exactly how the S2 wall eyeball went quiet, so the owner is a slice this time.

**Resolved (S8, 2026-09-16): wired.** The family is produced again — `resolveLayoutSnap` emits it whenever the caller names a draft anchor — and the guide it carries has real extent. See S8's step-3 note for the evidence, including the two facts that made the decision cheaper than this paragraph feared: rank 7 sits *below* every geometry family, so wiring could never displace a join, and the family as written could offer a lock that collapses the leg onto the anchor, which had to be declined before wiring.
- **Stop.** Any invented family or candidate cloud → stop.
- **Gate.** Snap presentation tests + QA pass green.

### S6 — Working dimensions

- **Objective.** Transient dimension set with frozen lanes and overflow policy.
- **Prerequisites.** S2, S4.
- **Files/symbols.** `plan-overlays.ts:37,874-889,977-988`; `layout-geometry` lengths; viewport readout host.
- **New.** `plan-dimensions.ts`: per-context derivation + lane allocator (18px/34px, side frozen at gesture start) + short-span push-out → readout fallback.
- **Reuse.** Canonical lengths incl. `compiledPhysicalWallLength` arc; display formatters from `layout-exact-input.ts`.
- **Replaced.** Ad-hoc edge-length labels + chord fallback for curves (arc + `arc` tag).
- **Invariants.** Derived only; display rounding ≠ precision; passive clearance/area never inputs.
- **Steps.** 1) Straight wall/rect/opening dims + lanes. 2) Curve arc + junction deltas/coords. 3) Overflow + readout. 4) Selected-idle primary measure only. **Handoff from S5:** the label side-choice should stay *one* mechanism — S5 shipped a pure, stateless per-event chooser for the snapped pointer (`planSnapRelationLabelOffsetPx`); S6 adds the frozen-at-start *policy* around a shared `chooseSide(view, extent)` rather than a second chooser with its own arithmetic. Two choosers would drift on exactly the question §7 asks once ("choose the side with more space once at gesture start").
- **Tests.** Lane freeze, overflow, arc-vs-chord, clearance display tests.
- **Visual QA.** **Done** — the live app (draw leg, commit + select, Rect Room candidate, Door insert/slide) plus the temporary dev plate (`__qa-s6-plate.test.ts`, deleted with the slice).
- **QA notes.** The pass found three defects the suite could not see, all in the instrument's *handover* rather than its arithmetic. (a) The witnesses, dimension line and 4 px ticks were pushed as **screen** points into a primitive list the paint layer projects with `worldToPlanScreen`, so all ten strokes per measured face were projected a second time and landed ~`pixelsPerMeter`× off the paper — and since the *text* round-trips through its world anchor, the result read as "the dimension draws text but no witnesses", which is exactly how the plate was misread as correct. The whole instrument is now handed over as world geometry (one `planScreenToWorld` per placed point), and a projection test pins both facts at once: every stroke lands on the paper *and* lane 1 is 18 px from the face it measures. (b) Placement treats its text point as a **centre** — the extent it measures, the short-span push-out reach and the readout fit test all assume it — but `.dimension-label` drew start-anchored, so every value drifted right by half its own width and the arithmetic was a lie about what the user saw. The token is `text-anchor: middle`. (c) §7's "offset from the band by 18 px; reserve a second lane at 34 px" describes one lane *pair*, but a measure with no extent of its own (a leg's angle, a delta, a focused offset) took a hardcoded vertical side, so a leg's angle landed on the far side of the leg from its length: two numbers straddling the geometry they describe. A span-less measure now rides the set's band (`planDimensionBandDirection`), and only a set with no extent at all chooses a vertical side for itself — measured with the same `planSidesWithRoom` every axis-aligned policy uses.
- **QA notes (side chooser).** `planDimensionPreferredSide` measured "more space" as the up/left vs down/right half-planes while its sides are its span's **normal** (`perp(d) · sign`). Those agree for a rightward span and disagree for a downward or leftward one, so a rotated span could take the side with *less* room and be reported to the readout for want of paper. The room is now measured along the lane's own direction (`planRoomAlong`), which is the same measurement the push-out already used. `planSidesWithRoom` stays the shared measurement for the policies whose sides really are screen axes (S5's snap word, a span-less measure with no band).
- **QA note (left open for the plan owner).** On a narrow paper the Rect Room candidate's depth lane can flip **inside** the candidate — §7's "side with more space" is measured in canvas room, and a 50 px strip outside the face loses to the 190 px of canvas beyond the enclosure — so a face reads as the room's own outline even though the derivation marks both faces `outward` from the centroid. Ratified behaviour in `planDimensionPreferredSide`'s doc and tests, so not changed here: the call is whether §7's enclosure row wants the preference to survive unless the preferred side cannot hold the lane *and its text*, or canvas room stays the rule.
- **Stop.** Any persistent annotation state → stop.
- **Gate.** Dimension tests + QA pass.

### S7 — Type-to-enter (A5)

- **Objective.** Full ratified numeric lifecycle through canonical planners.
- **Prerequisites.** S6, S4, S5.
- **Files/symbols.** `LayoutPlanViewport.svelte` (`onKeyDown:3371`, gesture fns `:591-800`, pointer handlers); `layout-exact-input.ts:14-35`; `planExact*` planners; `planCreate/UpdateWallFirstOpening` (`layout-preview-state.svelte.ts:1673-1726`); wall/junction planners.
- **New.** Transient numeric editor component (dimension-attached desktop + mobile fallback layout per atlas): field set per gesture, signedness map (`width/offset` unsigned; angle/delta/coords signed), units (`m`/`cm`, degrees), ownership-transfer flag consumed by `pointerup`/commit paths.
- **Reuse.** Parse/format + canonical operation paths; existing validator; history exactly-once via `commitLayoutTransaction`.
- **Replaced.** Dead `resolveWallChainEndpointAtLength` direction memory — **resolved by wiring it**: the typed Length entry is its first production caller, and `wallChainPendingDirection` (its companion) is now the field's seed for the pending angle. Neither is deleted, and neither had a caller before.
- **Invariants.** No parallel solver; transient paths never promote identity; invalid → editable, no history; blur never commits; IME passthrough (`e.isComposing`); tool change cancels.
- **Steps.** 1) Gesture-triggered entry + freeze + pointer-up consumption. 2) Focused-dimension entry. 3) Tab wrap, Enter-once, Esc-Esc, signedness, units. 4) Snap suppression + same-validator submit. 5) Invalid-retry + mobile fallback.
- **Done (steps 1–4, gesture + explicit-focus reach).** `plan-numeric-entry.ts` holds the whole ratified contract as a pure module (field sets per host, trigger, units/domain parsing, Tab/Enter/Escape/blur/pointer-up lifecycle) with 57 tests in `p23-13-numeric-entry.test.ts`. Wired end to end for §7's row 1 — straight Wall draw Length/Angle: the field opens on the value's own placed primitive, freezes the leg, consumes the eventual pointer-up and the click a chain tool would commit on, submits through the canonical planner, and returns the keyboard to the canvas.
- **Step 2 (focused-dimension entry) is landed, for the hosts whose canonical command exists.** A5's second reach is "numeric editing … from explicitly focused selected primary dimensions", and §7 gives it two ways in: "click on its underlined value is the pointer alternative; keyboard-focused control + Enter is the discoverable alternative". **Both were wired at the time** — the pointer half is retired by D1 (see the end of this bullet) — and *both read the same table*: `planNumericRestingEntryTarget` decides which resting measures carry an editor, the paint layer underlines exactly those (`dimension-label-editable`, a new style token — the placer measures it with the dimension typography, not the Room name's), and the viewport hit-tests exactly those over the ink's own boxes (`planDimensionValueHitAt`, reading the placed primitives back rather than re-deriving a second placement). Clicking a value opens the editor **on** the value it replaces with the text selected, so the first keystroke replaces the number the user was reading; Enter does the same for the selected entity's primary measure, or for the focused control's own: `junction` → X/Z, `opening-edge` → Width, `opening-slide` → Offset. Submission dispatches on the *subject* — one field set is not one command — to `updateWallFirstWallLength`/`Angle` (one transaction, so a typed length+angle pair costs one undo), `updateWallFirstOpening`, `updateWallFirstJunction`. A field opened on a value and submitted unchanged closes silently rather than writing a history entry in which no number changed (the planner's own `no_op` is the same fact). The typed hosts beyond the Wall chain are in too: the Rect Room candidate offers Width/Depth and commits through the same `rectanglePoints` → `onCommit` path a release uses, keeping the direction the user drew so typing a width cannot flip the rectangle across its start corner; a slide/resize drag offers Width/Offset and, per §7's ownership transfer, freezes the proposal, consumes the drag's pointer-up without committing, and finishes through the drag's *own* commit path (the drag already owns the open transaction — that is what makes the transfer a transfer rather than a second route). **Remaining:** §7's *Opening insert* row has no gesture to attach to — a toolbar/menu insert commits on click through `createDefaultWallFirstOpeningIntent` and the viewport holds no transient insert candidate, so the host's field set exists with nothing that could seed it; wiring it means inventing an insert draft, which is a behaviour change with its own mandate (S8 owns per-gesture preview composition). Also still open: `object-transform` and `curve-control` focus targets return null on purpose (no exact command wired here), and a *visual* pass over the coarse-pointer fallback layout (the class exists; no touch device or emulation has seen it). **Superseded (D1, S8, 2026-09-17 — ruled: retire):** the *pointer* door is **gone** — the underlined value and the click that opened a field on it. A5's second reach is now one door, not two: the **keyboard** door (Enter on the selection's own primary measure, or on a focused control), plus gesture typing. §7's sentence "click on its underlined value is the pointer alternative" is corrected by this ruling (see §1.10 and the S8 note, which also records the one table that had to *survive* the deletion because the keyboard door reads it). Everything else in this bullet stands: the focus, the field set, the subject-dispatched submission, the silent no-op close and the one-undo contract are untouched.
- **Tests.** Pointer-up-consumed (commits==0), Enter-once, invalid-refused-no-commit, Esc-restore/Esc-cancel, Tab/Shift+Tab wrap, unsigned-minus rejection, IME, one-or-none history — all in the module suite above. Viewport wiring is diff-verified by the live QA below (the suite is `node`-environment, so no component test may claim it).
- **Review follow-ups (22f8208).** (i) The typed path called the shared continuation helper with `result.endJunctionId!` while the pointed path guarded the same result itself — two callers checking one `success` separately, which is exactly how they drift. The guard now lives in the helper for both, and the assertion is gone. (ii) A typed commit can land mid-press (typing one-handed with the button held): the release that follows belongs to the press that predates the Enter, and its click would have drawn a second, pointer-positioned segment. A button-down fact is now tracked for the press (chain tools never take pointer capture, so `pointerId` cannot answer it) and the trailing click is suppressed with the same mechanism `pendingWallBend` already uses. (iii) That button-down fact can *stick*: a press released off-canvas (or off-window) fires neither `pointerup` nor `pointercancel` on the `<svg>` — chain tools take no capture, so nothing retargets it — leaving the flag true forever and every later typed commit eating its following click. Narrow to reach, persistent once reached, so it is reset in the surface's existing `onpointerleave` ("the pointer is gone") alongside the hover clears. The cost is deliberate: a press that leaves the canvas and returns before release, with a typed Enter inside that window, is no longer suppressed — keyboard typing one-handed mid-drag across the canvas edge is the only way to reach it, and one press already committed.
- **Review follow-ups (step 2, before commit).** (i) *The open seeded display text as if the user had typed it* — the real bug in the landed code. `planNumericEntryOpen` set `typed` from the field's `toFixed(2)` display text and `numericEntryUnchanged` compared numerically at `1e-9`, so clicking a pointer-drawn Wall of 2.675 m, reading `2.68`, and pressing Enter without touching anything committed **2.68**: 5 mm of silent geometry change plus a history entry, on the exact submit the plan promises closes silently, and straight through the front door of S6's "display rounding ≠ precision" invariant. Fixed structurally rather than by widening the tolerance: `typed` now stays **empty** on open, so "unchanged" means "the user typed nothing" and no comparison can round anything. Snap suppression correctly stays off until the first keystroke (a freshly opened field still reads `Grid`, not `Exact value`), and the trigger-seeded gesture paths are unaffected because *that* keystroke really is an explicit value. The test that pinned the old behaviour now pins the rule instead. (ii) *Two hosts had no anchor at all.* `numericEntryMeasureKey` answered `null` for `rectangle` and `junction`, and the fallback was the Wall chain's start/cursor — `null` outside a chain run — which renders the field with no `left`/`top`, i.e. stacked at the container origin instead of on the value it replaces. Rectangles now name their own measures (`rect:width`/`rect:depth`, both derived throughout the gesture since S6), and the fallback covers every subject the component can open (junction → its own point, wall → its span midpoint, opening → its edge midpoint), so no host can reach the placement with nothing to anchor to. (iii) *Unifying the rectangle commit had changed legacy refusal behaviour.* The legacy path kept the draft when `onCommit` returned false; the wall-first path cleared it either way, and the shared helper had silently picked one — discarding a legacy user's sketch where it used to survive for correction. The two are distinguished again: that difference is pre-existing and belongs to the two formats' own commit paths, not to numeric entry. **Nits closed:** `entrySubjectFor` now declines (`null`) rather than defaulting an unknown host to `opening`, so a future host falls through to "no field" instead of quietly editing the wrong thing; and the editable-value style comment says the fill *brightens* as a redundant affordance rather than claiming identical ink.
- **QA notes (live, step-2 review round).** (a) The **Junction host is now exercised end to end**, which the earlier round had listed as unexercised: with the Wall selected (its control polygon reveals the endpoint controls), clicking the corner handle focuses the junction control and Enter opens `junction`/`x` on the junction's own X with the input focused; Tab reaches `z` (`-7.75`); Escape writes nothing (Walls 6 / Junctions 6 unchanged); typing `1.375` + Enter moves the junction canonically, and the reopened field reads `1.38` **with the anchor moved along with the geometry** (`left` 100 → 148 px) — the field's own proof that it is anchored on the live handle rather than on a stale placement. (b) The unchanged-submit fixture fell out of that: the junction now sits at a coordinate that *displays* `1.38` but *is* `1.375`, so an untouched Enter is discriminating. Reopen (field shows `1.38`) → Enter untouched → the field closes and **one undo steps past the typed commit to the junction's pointer-drawn `-1.00`** (anchor back at 100 px). Under the old seeding that single undo would have landed on `1.375`, because the untouched Enter would have written the rounded `1.38`. (c) The rectangle anchor is confirmed the same way: a synthetic Rect Room drag (pointer capture stubbed, as before) opened Width at one anchor and, after Tab, Depth at a **different** one — the two fields sit on their own drawn extents rather than sharing the start-corner fallback, and Depth seeds its live `3.50 m`. Completing the pair (Depth `5`) committed through the release path: **+4 Walls, +1 Room**, field closed. (d) A behaviour this round surfaced but deliberately did **not** settle: with the field *open*, clicking the toolbar's Undo button is an out-of-band geometry change the field never notices — its *anchor* follows the geometry live while its *text* stays what it was opened with, so the field can read `1.38` over a junction undo has put back at `-1.00`. The commit itself stays honest (nothing typed, nothing written), so this is display-only; the principled fix is the one the tool-change rule already uses — cancel the entry on an external history transaction — but that is a behaviour decision, so it is recorded rather than taken. (e) Still unexercised: the Opening resting measure (width, and offset with the width edge focused) and the coarse-pointer fallback layout; the arc exclusion stays pinned by its unit test.
- **QA notes (live, wall-chain host).** (a) A defect the suite could not see: closing the field left focus on `body`, so the *next* digit went nowhere — Escape and a successful Enter now hand the keyboard back to the drafting surface (`focusCanvas`), while blur deliberately does not (focus went somewhere the user chose; stealing it back would fight them). (b) §7's `-` clause reads as two sentences that fix each other: "permitted only for a field whose canonical domain allows negative values" *and* "does not start Length". On the Wall draw the set is `[Length, Angle]`, so `-` opens **Angle** and a set with no signed field (Length/Width/Offset) starts nothing — a test pins both, verified live. A `-` typed *inside* a nonnegative field is text, refused with its own reason. (c) Snap suppression verified live: with a valid value in the field the winner marker is gone and the relation reads `Exact value`; on close the `Grid` winner returns. (d) The field's background is opaque on purpose — a translucent one showed two numbers for one measurement, which is the opposite of "entry replaces the displayed value". (e) The commit is canonical, not grid-snapped: typing `3.37` on a 0.25 m snap grid yields a Wall whose Inspector Length reads **3.37** with Angle −46.3° (the frozen leg's direction), and no snap winner was involved. (f) The viewport is forbidden the world→screen transform by `plan-render-boundary.test.ts` ("owns no world-to-screen rendering transform"), so the field's anchor is computed in `plan-overlays.planNumericEntryAnchorPx` — read back from the very primitive that draws the value — rather than by re-deriving a placement in the component. This is the second time this slice's boundary test has caught a correct-looking change (the first was S6's double projection).
- **Visual QA.** Atlas numeric-lab parity (simulated behaviors, not domain truth).
- **Decision (owner, 2026-09-17; ruled as D4): one Enter submits every typed field.** The review's finding was that Tab re-seeded the newly focused field from its live candidate, so typing a Length, pressing Tab and then Enter committed the angle *with the pointer length* — the typed length was dropped. Nothing in §7 settled it ("Enter submits once" bounds the *count* of commits, not their reach), and the owner chose cumulative: the entry state now carries the text the user *typed* per field, Tab keeps it in both directions, Enter resolves every typed field together into one segment through the one resolver, an invalid typed field refuses the whole submission and focuses the field the reason is about, and the snap stays suppressed while an explicit value waits on another field. A field the user never touched stays absent from the submission, because showing the live candidate is not choosing it. **Live:** typed `2.5` → Tab → `90` on a run starting at J-AEM9 (-2.50, 1.50) produced a segment ending at J-CZ7E (-2.50, 4.00) — exactly 2.50 m at 90.0°. It is authored as **two** Walls (1.25 + 1.25) because the pre-existing junction J-2QUS (-2.50, 2.75) lies on that span: that is canonical noding of the new Wall at an existing junction, not a doubled commit — worth writing down because the Inspector reads "1.25 m" twice and looks like a bug at first glance.
  **Status (D4, 2026-09-17) — ratified, no code change.** Ruled cumulative, in the owner's own words: one Enter submits every typed field, as implemented. Nothing was reopened, so committed S7 history stands untouched; the earlier `(owner, 2026-09-16)` label was withdrawn for want of a ruling and is now restored as `(owner, 2026-09-17)`, this time with the ruling behind it (see the process note below).
- **QA notes (live, step 2 + the typed hosts).** (a) Verified end to end: a selected Wall's resting measure renders underlined (`dimension-label-editable`), clicking it opens `wall-edit`/Length on the value with the input focused and geometry-preserving placement (the field's centre is the value's centre), typing `3.5` + Enter resizes the Wall canonically to `3.50 m`, focus returns to the canvas, and **one undo restores `9.00 m`** — one command, one history entry. Enter on the selection opens the same field with no click; Tab reaches the Angle seeded from the Wall's own canonical angle with the `°` unit; Escape closes and writes nothing. (b) A defect the suite could not see, and the reason this slice keeps a live pass: the Tab handler refreshed a newly focused field from `pendingWallChainCandidates()` — correct while the Wall chain was the only host, wrong for every host added since. A drawn rectangle's Depth field came up **blank**, reporting that nothing was being drawn. Candidates now follow the *subject* (`numericEntryCandidates()`), and the rectangle's Depth seeds its live `3.00 m`. (c) The Rect Room gesture takes pointer capture on pointer-down, which throws for an untrusted pointer id, so no synthetic drag can start it. The QA pass stubbed that one platform call (capture) and left the app's own gesture logic untouched: typing mid-drag opened host `rectangle`/Width with the typed character *replacing* the displayed value, the drag's pointer-up was consumed with nothing committed, and a typed `4` → Tab (`Depth 3.00`) → Enter produced **1 room / 5 walls** through the release path. (d) Not yet exercised live *at that point*: the Opening resting measure, the focused-Junction X/Z path, and a curved Wall's arc exclusion. The Junction path has since been exercised (see the review round below); the Opening measure and the arc exclusion (pinned by a unit test) remain. **Correction (S8, 2026-09-17; ruled by D1 as *retire*):** (a)'s *first* step — the underlined resting measure and the click that opened the field on it — **is removed**, and the live pass below stands as the record of the door that used to exist. The field is still reached by Enter on the selection's measure, by Enter on a focused control, and by typing during a gesture, which is what the rest of (a) and all of (b)–(d) describe; what changed is only how it is reached.
- **Stop.** Any second solver, silent clamp of explicit values (must refuse with reason), or history on invalid → stop.
- **Gate.** Lifecycle tests green; reviewer confirms canonical-path reuse. **Status:** module suite green (57), full suite **4260 passed / 1 skipped**, `svelte-check` 0 errors 0 warnings; step 2's reach, both new typed hosts, the Junction X/Z host and the rectangle's own anchors are all confirmed live, and the step-2 review's three findings are fixed. **Closed by carry (owner ruling 2026-09-17).** P23.13 ships with S7's three rows named rather than solved — §7's Opening-insert row (no gesture to seed it), the undo-with-field-open display question recorded above, and the coarse-pointer visual pass — all three handed to P23.14. See §11's close record for why each is a behaviour or shell decision rather than a Plan one.

### S8 — Preview / refusal per tool + local attention

- **Objective.** True-geometry previews for every gesture; release truth; brief refusal persistence; instrument-zone attention.
- **Prerequisites.** S1–S7.
- **Files/symbols.** Viewport gesture fns; `layout-interaction.ts:203-344,359-652`; `layout-opening-editing.ts:76-102`; `layout-transient-edit.ts:130-259`; overlays intent fns.
- **New.** Per-tool preview composer (band/cap/join from compiler where available, ghost cut + neutral symbol for openings, closure cue gated on canonical face evidence); render-only refusal annotation (1.2s/timeout, Escape-clears); instrument-zone derivation (locus + 64px pad, ≤180px) + suppression/restore engine.
- **Reuse.** Immutable-baseline + `transientArchitectureEdit` + release re-derive pattern extended to the **canonical wall-first** opening drag (fix the known gap: re-resolve release point, don't commit remembered candidate). Legacy room-owned opening drag (`target.kind === 'opening'` → `openingDrag` state, `LayoutPlanViewport.svelte:2376/2777-2815/3083`) is reachable only when a legacy room-owned document is loaded — a smoke-only surface (new projects boot wall-first; Issue #26 retirement is post-P23). S8 first verifies the path's status — **verify dead-or-read-only, then remove-or-gate** — with no P23.13 modernization spend on the legacy side either way. The canonical wall-first path is fully covered, and S1–S9 presentation changes must not regress legacy smoke.
- **Replaced.** Abstract centerline previews; premature closure wash; global dimming (none — local only, no visible spotlight).
- **Invariants.** Invalid never enters history; original geometry visually untouched on refusal; closure never promises a room without canonical evidence.
- **Done (step 3's guide decision).** **Wired, not retired.** `SnapQueryContext` gains an optional `anchor`, and `resolveLayoutSnap` emits `orthogonalGuideCandidates(anchor, point)` (filtered by the same acquisition radius as every other family) when a caller names one — so every caller that does not resolves the candidate set it resolved before, which is the difference between waking a dormant family and changing snapping everywhere. Only the Wall draw passes one (`wallChainSnapAnchor`: the run's own start), on **both** the hover and the commit, because the guide is only honest if the preview and the release agree; a Rect Room deliberately does not, since its corner is axis-aligned by construction and the family would only outrank the geometry snaps a rectangle corner wants. The guide is no longer the zero-length stub it was: it spans the anchor to the locked point, overshot by the candidate's own offset, and **one** line is all a winner can carry. `plan-snap-grammar`'s "ratified-but-dormant" claim is corrected — the mark and its word are now reachable — leaving `extension-guide` as the one family still declared, ranked and unconstructed, which is `layout-core`'s to produce (no predicate there expresses "you are extending this wall's line"), not presentation's to fake.
  Two facts surfaced while implementing, and both changed the work:
  (i) **Rank 7 is below every geometry family, not above it** (`pickSnapWinner` prefers the *lower* number, so junction 0 … object-bounds-center 6 all beat orthogonal-guide 7, which beats only extension-guide 8 and the grid 9). That makes the review's precondition true by construction rather than by test discipline: wiring the family can take an outcome that used to grid-snap, and can never displace a join to real geometry. Pinned as a test — the same probe with and without an anchor must return the identical `wall-span` resolution — and demonstrated live (hovering real geometry roughly on the anchor's axis still reads `On wall` with no guide).
  (ii) **The family as written could offer a lock that collapses the leg onto the anchor.** Dragging straight up from the start makes that projection *be* the anchor; at rank 7 it would beat the grid and commit a zero-length Wall where the grid promised a 0.25 m one. `orthogonalGuideCandidates` now declines a candidate coincident with the anchor (a lock that deletes the leg is not a snap), and a test pins the pointer-above-the-start case to the grid point.
  (iii) **The wired family glided, and snap-on must never glide — it is now quantized ("locked *and* stepped", owner decision, same day).** With Snap on, the locked point was the raw pointer projection (`[point.x, anchor.z]`), so the endpoint slid continuously along the axis instead of stepping: near-axis drawing — most drawing, and where the lock wins most of the time — made the grid unreachable for the whole length of a run, and the draft felt "smooth and free". The toggle ruling settles what to do about it: Snap is global on/off, the guide is one contextual family *inside* snap-on, and toggle-off already owns "smooth" — so every snap-on winner must land on something exact (grid point, geometry point, axis × grid intersection), which is exactly what the family now guarantees. `orthogonalGuideCandidates` quantizes its **free** coordinate to `step` while the held coordinate stays *exactly* the anchor's (that is the lock); `resolveLayoutSnap` threads the one `context.gridStep` it already reads for the grid fallback, so grid and guide cannot disagree about where the lattice is. The guard order is load-bearing — **quantize first, decline second** — because rounding can collapse a short leg onto the anchor, and the coincident decline must see the **final** point or (ii)'s trap reopens; the collinearity decline is the same rule read *before* the stepping (a pointer on the anchor's own line has its projection *be* the anchor, and locking the other axis there would drag the point off the line being drawn). Acquisition is untouched by construction: the radius filter still reads the **raw** perpendicular offset, so a probe is acquired exactly where it was acquired before and a committed coordinate moves by at most half a step — the change can only ever *decline* candidates, never add one, and the only probes that change winner are the ones where the stepping collapse declines a candidate — there the pointer is within half a step of the anchor's own line, so the perpendicular lock is right beside it and its candidate, which used to *be* the anchor and be declined, now steps instead of collapsing. 5 new pins in the same suite: stepping to the nearest subdivision either side (2.6 m → 2.5 m, 2.7 m → 2.75 m), acquisition measured on the raw offset (a 0.395 m offset acquires inside a 0.4 m band although the stepped point is 0.407 m away), the rounding collapse resolving to the nearer stepped lock instead of the raw projection, the collinear probe adding no candidate at all, and a short stepped leg still drawing a visible guide.
  **Evidence (wiring).** 15 tests in the new `p23-13-guide.test.ts`: no anchor → no family (and the grid, not the axis, is the fallback), non-finite anchor ignored, symmetric axis handling, the rank non-regression above, the degenerate-leg decline, exclusion still respected, guide count/extent/orientation, and purity (the release point alone decides). Live: a Wall drawn 4 px off the run's axis read relation **Right angle** with the guide painted as a real line (`50,354 → 170,354`, spanning the anchor to the locked point) and committed **Length 6.00 m / Angle 0.0°** — where the un-locked geometry would have been 1.9°; the same hover 20 px off the axis stayed `Grid` with no guide, and hovering real geometry on the axis stayed `On wall`. Gate: 4275 passed / 1 skipped, `svelte-check` 0/0, `layout-core` `tsc` clean.
  **Evidence (locked and stepped).** 20 tests in `p23-13-guide.test.ts` + the quantized generator pin in `p23-2-snap`. Live (real editor, empty plan, Wall tool, Snap 0.25 m, 20 px/m): hovering 4 px off the run's axis read relation **Right angle** with the guide painted as a real line (`120,420 → 120,220`, both ends exactly on the anchor's held coordinate) — and stepping the pointer up in 6 px moves walked the pending readout **10.00 → 10.25 → 10.50 → 11.00 → 11.25 → 11.50 m** at exactly **-90.0°**, i.e. every winner on the lattice and the endpoint advancing by whole 0.25 m subdivisions (one input crossed two of them), never gliding; releasing at that hover committed a Wall the Inspector reports as **11.50 m**, so preview and release agree on the stepped point. Gate: 4280 passed / 1 skipped, `svelte-check` 0/0, `layout-core` `tsc` clean.
- **Done (step 1 — status check, then `D2` prune).** **Verdict: reachable.** A legacy document still compiles room-scoped Opening spans and `resolvePlanHit` still resolves `{ kind: 'opening' }` from them, so the branch had a live producer. `D2` then ruled the surface gets **no contract**: the drag branch is removed rather than held to one, and no gate was added at load because gating the whole legacy document read-only is **not cheap** — the legacy room/opening surfaces are deliberately still editable and the legacy mutator corpus depends on that. Removed: the `openingDrag` state, its pointer-down begin (the hit is now **select-only**), its move and release branches, and the second projector with it (`applyLegacyOpeningDragPoint`, plus the now-unused `resolveOpeningDragSnap`/`compiledWallLength`/`snapSegmentOffset`/`updateLayoutOpeningFields`/`projectPointToWall` imports). *Reason (D2, the pruning decision):* the room-owned drag was a second mechanism for a question the wall-first gesture already answers, with no canonical command behind it — so it is one mechanism per question, not two, and the values stay editable in the Inspector. Pinned by one inert-smoke source contract in `p23-3-opening-authoring-reachability` (legacy hit still selects, no drag state, no second projector); `changed-but-unpinned behavior is a finding` is satisfied by that pin, since this *is* a behaviour change (a legacy Opening could be dragged; now it cannot) and it has an owner ruling behind it. **Finding (resolved in this pass — the earlier version of this finding overstated it).** The first cut listed two dead exports; one held and one did not. `resolveOpeningDragSnap` is **not** dead: `resolveOpeningDragSnapUseMode` (`LayoutPlanViewport.svelte:2383`) still calls it on the canonical drag/create paths, so nothing happens to it and the record is corrected here. `compiledWallLength` (`plan-hit.ts:468`) **was** dead by this prune — zero production callers, kept alive only by its own `plan-hit.test.ts` pins, since the legacy route it served is exactly the one the prune removed and the canonical route runs through `projectPointToPhysicalWall` instead. Under D2's own logic that is dead code *this* prune created, not a legacy surface deferred to #26: **the prune ruling does not defer what the prune created** — so the function and its two pins are **deleted**. `projectPointToWall`, the sibling room/segment projector the same prune orphaned, sat in the *identical* state — zero production callers, kept alive only by the one `it` that also carried the length pins — and it is **deleted with it, pins and all**, so one prune gets one rule rather than a function-by-function ruling. Both were `(roomId, segmentId)` readers, and with both gone there is no longer a dead reader of that surface: what remains of the surface itself is Issue #26's to retire, and nothing here staffs it. (The `projectPointToPhysicalWall` route is unaffected: it is the live canonical instrument, called by the opening drag at `LayoutPlanViewport.svelte:2416`.)
- **Done (step 2 — canonical release truth, one target).** `applyWallOpeningDragPoint` projects the point onto the host Wall and takes the drag's own raw-vs-snap rule, and **the hover and the release both run the same function** — so the commit re-derives at the pointer-up's own point instead of writing the last preview frame's remembered candidate, exactly as the Wall chain does. A release with no honest projection (off-canvas, off the host) leaves the live candidate standing rather than inventing one. Pinned by a source contract in `p23-3-opening-authoring-reachability` (reason unchanged: component event handlers, no component-render harness).
- **Ruled (D1, 2026-09-17): retire — a value on the drawing is ink, not a target.** The clickable length on an edge and the entry box that opened under the pointer are **deleted**, all-or-nothing in one pass. *Reason (owner):* unnecessary in practice, and fine tuning a specific number is what the Inspector sidebar is for — so the drawing carries numbers to read, not numbers to poke. Deleted: the `.dimension-label-editable` style and its `PlanRenderStyle` token, `planNumericRestingEntryTarget` as a paint-time affordance (every value now paints as plain `dimension-label`), `planDimensionValueHitAt` with the `planDimensionValueHit`/`planDimensionValueBoxPx` box it reads back through, `PLAN_DIMENSION_VALUE_HIT_PAD_PX`/`PLAN_DIMENSION_VALUE_MIN_HIT_PX`, and the viewport's click branch — leaving no style, box, constant or branch that could answer a pointer. The pins that described the door are deleted with it (the three value-target suites); what replaces them is one pin that the *retirement* holds: every `dimension-text` primitive carries `dimension-label`.
  **Finding (why one named table survived the ruling).** The ruling's clause "with mapping-table deletion" named `PLAN_NUMERIC_DIMENSION_TARGETS`, and that table is **not** the pointer door's: `planNumericRestingEntryTarget` is what `beginNumericEntryFromFocus` reads to answer "does this selection's own measure carry an editor, and which one" — the **keyboard** door the same ruling protects. Deleting it would have removed Enter-on-the-measure while claiming the keyboard door untouched, so it stays, and the deletion took the table that *was* pointer-only (the value hit box and its constants) instead. Recorded because "partial deletion is not an option" was the ruling's own rule, and this is the one place it could not be applied literally without contradicting its first sentence; the surviving table is live, not dead — its one remaining consumer is the keyboard path, pinned by the numeric-entry suite.
  **Kept (unchanged by the ruling):** the keyboard door (Enter on the selection's own primary measure, or on a focused control) and typing during a gesture — A5's "explicitly focused selected primary dimensions" — and the whole submission/undo contract. §7's "click on its underlined value is the pointer alternative" is superseded by this ruling; §1.10's target row and the A5 acceptance row carry the correction.
- **Owner decision (2026-09-17, folded into S8): a vertical draft's angle stacks above its length.** *Reason (owner, live QA):* drawing straight up printed the degree and the measurement through each other (`90.0°7.50 m` — one number where two belong), while horizontal drafting was legible. Cause is structural, not a slip: §7's text is horizontal 11 px mono on one 13 px line, and on a vertical band the two lanes (18 px and 34 px) are offsets *sideways*, i.e. 16 px apart along the one axis where the numbers are 30–40 px wide. `planSpanlessLaneOffset` now takes the step **past** the first lane vertically when the band's own direction cannot clear a text line (`|direction.y| × step < lineHeight`), which is exactly the clearance a horizontal band already gets from its lanes; a band whose normal is already vertical is byte-for-byte unchanged. The span-less measure still rides the band (never the leg's own axis) and still keeps the side the freeze chose. Pinned by a new collision test plus the updated band-normal test, which had encoded the colliding behaviour.
- **Ruled (D3, 2026-09-17): quantize — lock *and* snap.** The quantization in fact (iii) stands as landed (`cb07819`): the guide's free coordinate steps to the grid and the held coordinate stays exactly the anchor's, `resolveLayoutSnap` threads the one `context.gridStep`, and the guard order is **quantize first, decline second** so a rounded collapse is still declined as a zero leg. Its pins (5 in `p23-13-guide` plus the rewritten generator pin in `p23-2-snap`) and the live stepping proof (10.00 → 10.25 → 10.50 → 11.00 → 11.25 → 11.50 m at exactly −90.0°, committing 11.50 m) are the ruling's evidence, and nothing is pending: the radius filter still reads the **raw** offset, so rank contention did not move.
- **Process note (for the record, 2026-09-17).** Owner attribution requires a **ruling in-thread** — the D-numbered format used in this plan (`D1` retire, `D2` prune, `D3` quantize, `D4` cumulative submit, and now `D5` extension-guide owner). Plan labels like `(owner, <date>)` are written *only* after that ruling exists, never before: D4's label was withdrawn when no ruling could be found and restored when one was given, and D5 was written into the source *ahead* of its ruling by this pass, caught against this very rule, marked proposed, and only then ruled. **All five are now rulings, not labels — nothing pending.**
- **Steps.** 1) Legacy-drag status check → **done above (reachable → pruned by D2: verify dead-or-read-only, then remove-or-gate)**. 2) Opening release-truth fix (canonical path) → **done above (one target: the canonical `applyWallOpeningDragPoint`; the legacy half was pruned, not specified)**. 3) Per-tool composers (band/cap/join from the compiler where available, ghost cut + neutral symbol for openings, closure cue gated on canonical face evidence) — **done above**, with the guide half wired + quantized to the grid step and the closure row gated on canonical evidence; the opening preview's neutral symbol is recorded open there. 4) Refusal persistence — **done above** (bounded lifetime, Escape/press clears, silent refusals mark nothing). 5) Attention zone + suppression priority + restore — **done above** (zone + settle + the Room-label tier ceiling; §1.12's Scene/object-label rows recorded open with their seams).
- **D-list close-out (2026-09-17).** All four decisions now sit at their ruled end-state, and this pass is their record: **D1** retire (the pointer door is deleted, its pins replaced by the retirement pin, and the tables reconciled — see the finding above), **D2** prune (the legacy room-owned opening drag is gone, the hit is select-only, inert smoke only; the `compiledWallLength` and `projectPointToWall` the prune itself orphaned are both deleted with their pins, while `resolveOpeningDragSnap` stays live through its `…UseMode` sibling — see the step-1 finding), **D3** quantize (landed in `cb07819` with its pins and live stepping evidence), **D4** cumulative (ratified, no code change). Gates at this point: **4277 passed / 1 skipped**, `svelte-check` 0 errors / 0 warnings.
- **Done (step 3 — the per-tool preview row that actually decides: closure).** §7 asks each gesture a different preview question, and every answer but one is already structural: each gesture block paints its own composition and the band/cap/join ink it uses **is** S1's canonical grammar, so restating that as a table would have been a second vocabulary nothing reads — the failure class this slice has already pruned twice — and the opening preview's ghost cut already paints. What was genuinely missing was the closure row. `plan-preview.ts` owns it per tool: `wall-draw` is `canonical-face` (a face must be proved), `partition-draw` is `junction-only` (a partition closes onto a junction and says so, whatever appears), and `resolvePlanClosureCue` returns `none` when the candidate is not closing at all. The **evidence** is `layout-wall-chain-closure.ts`: `wallChainClosureEvidence` plans the closing leg through `planWallSegment` — the same entry the click commits through, with the run's own continuation height — and reads the created Room's boundary polygon back out of that plan via `roomBoundaryPolygon`. No shoelace area, no proximity threshold, no closed-looking polygon: a promised face is the planner's own face or there is no face. The overlay keeps its `closing` condition (so the cue's condition and its evidence can never be answered by two readings of *the candidate is closing*) and paints the canonical polygons as `closure-wash` instead of the `Close at junction` words; without evidence it paints the words exactly as before, which is why every P23.6 pin still passes untouched. The viewport asks for the evidence behind a deliberately **wider** probe gate (4× the snap radius) with a memo keyed on the run's canonical junction identities — closure is an identity outcome (P23.9), so while the pointer sits on the run's own start junction the closing leg is the same leg and the plan runs once per closure rather than once per pointermove. New token: `closure-wash` (no stroke — a wash, not an enclosure outline). **Recorded-open row:** the opening preview's *neutral symbol*. The Door/Window cue shapes are screen-space ink resolved in the render-model/paint vocabulary, and the overlay's primitives are world-space, so the drag preview can paint the ghost cut but **cannot** emit the symbol without a second cue implementation — the honest fix is a preview-opening descriptor the paint layer accepts, not a copy of `doorTypeCueScreenPoints` in the overlay.
- **Done (step 4 — refusal persistence).** The transient contract already restored the frozen baseline on refusal, so the original kept its committed position/ink/opacity by construction; what it lacked was a *lifetime* for the mark itself, which died with the drag that produced it and left only an Inspector status line where the eye is not. `plan-refusal.ts` owns the bounded-feedback lifetime: one annotation at the refused attempt's own locus, expiring on its own after `PLAN_REFUSAL_PERSISTENCE_MS` (1200 ms, §2's bounded-feedback row), cleared immediately by Escape or the next deliberate press. Expiry is a pure function of a clock (`planRefusalAt`) so the timer only tells the view to re-render once it passes. Three rules keep it honest: the locus is the attempt's (a whole-Wall drag marks its own Wall via `architectureEditIntentLocus`, exported from the overlay so the live invalid mark and the persisted one cannot land on two different points); it is render-only (`withPlanRefusalAnnotation` pushes primitives into `drafts`/`labels`, and no hit/snap/measure/validation path can see it); and a refusal with no message marks nothing at all, mirroring `releaseArchitectureEdit`'s own silence on `no_op`. Armed on a rejected architecture-edit release and on both canonical Opening-drag refusals (out-of-fit, and the planner's own refusal), composed **over** the live proposal rather than replacing it. New token: `refusal-reason`.
- **Done (step 5 — instrument zone + suppression).** `plan-attention.ts` derives the zone from the active handle's own locus: §1.12's 64 px pad, capped at 180 px on each axis about the locus, expiring `PLAN_ATTENTION_RESTORE_MS` (150 ms) after the last frame that touched it — so the suppression restores itself and no page-scale dimming ever exists. No border is ever painted: the zone is a decision input, never ink. Two rows are wired — the Room label ceiling and §1.12's `Scene → 10%` — because they are the rows whose consumers exist. S3's placer gained `tierDropZone`, tested against the **accepted anchor** (so a label outside the zone never notices the zone exists) and applied only when it would lower the tier — `full` → `name-reference`, which drops the area line and keeps name + reference together (pair-safe by construction, the same pairing S3 protects for duplicates and for a selected Room). The ceiling is a *preference*, not a truth: if the reduced stack does not fit where the fuller one fits, the fuller one stays, because a label that fits beats a label that is merely quieter. The Scene row is region-scoped the same way, and per footprint rather than per page: `withPlanAttentionSceneInk` answers through `PlanPresentationSource.sceneInkFor`, so the 10% lands on the footprints *inside* the zone while every other footprint keeps S2's regime value — a per-primitive pass, not a page-scale dim, with `min` as the only guard so a regime already quieter than the zone is left alone (this is the per-primitive Scene pass §1.12 needed, and it is why that row is no longer recorded open). **Recorded-open row:** the object-label half ("nonessential object labels"). Plan object labels do not exist until S9 draws them, so there is nothing to suppress today; the zone is that pass's input and its seam is S9's object-label work. S8's steps are therefore complete.
- **QA notes (live, S8 matrix — 2026-09-17).** Real editor, local Session project, 20–28 px/m, Snap 0.25 m and Grid on unless noted; pointer capture stubbed for synthetic pointers (the same single platform-call stub earlier rounds used — no app gesture logic was touched). Rows exercised: (a) **valid draw/commit** — a Wall run reads its pending leg with the two-lane dimension set horizontally and the stacked one vertically (the folded-in owner decision above), with the winner words moving between `Grid`, `Right angle` (the guide family wired above) and `Endpoint`; hovering the run's start junction paints `closure-wash` as the canonical face polygon and nothing else, and the click commits, *ends* the run and creates the Room (1 room / 6 walls / 0 geometry warnings); a Door placed on a Wall commits, selects and paints its full symbol (void + two jambs + `door-cue`), reading `Width 0.90 m` idle, `Offset 1.55 m · clearance 1.55 m` in a body drag and `Width 0.95 m · clearance 0.05 m` in a width-edge drag — the resize row drops the offset and keeps the clearance it is changing. (b) **Release truth** — both drags committed exactly where their last preview frame showed (`Offset 1.05 m`, then `Width 1.00 m`), which is the same function answering hover and release; the committed Opening stayed put *during* both drags and after both refusals. (c) **Refused** — two refusals from the two armed paths: a Junction move the planner rejects (live `architecture-edit-intent invalid` ink during the drag, then the persisted stop/× + the planner's own reason — "Walls 'wall-chain-1.3' and 'wall-chain-1.5' have unsupported proper-crossing" — at the attempt's own locus), and a width edge dragged through the opening's own fixed edge (`opening-drag-preview invalid` with the honest signed readout `Width -0.57 m`, then the persisted "Opening does not fit on this wall"). Both marks expired on their own inside the bounded window (gone at 1.4–1.5 s), both left geometry, counts and warnings byte-identical, and the click following a refused release was consumed by that release's own `suppressNextClick` rather than starting a gesture. (d) **Cancelled** — Escape mid-run clears the scaffolding and writes nothing (counts unchanged), a second Escape is inert, and a chain click that cannot close (two distinct vertices) is rejected with the planner's own message and writes nothing. (e) **Attention plate** — with a Room label painting as name + reference + area, a live Opening drag whose locus was inside the label's zone dropped it to `name-reference` (the area line gone, name + reference moving together) and the fuller stack returned one settle window (150 ms) after the release. (f) **One fit fact the matrix pinned down** (no code change): a pointer beyond a Wall's end is *projected onto the finite Wall*, so an end-overrun drag resolves to a candidate that still fits (the Opening slides flush and commits) instead of an invalid one; the out-of-fit refusal is reachable where that projection clamp cannot rescue the candidate — a width edge pulled through the opposite edge (observed above) or a Wall shorter than the Opening (unit-pinned). Recorded because step 2's "a raw candidate outside fit bounds previews invalid" is true of the path it names and would otherwise read as covering end-overrun drags too. Still unobserved live: the legacy inert-smoke surface (D2, unit-pinned only) and the coarse-pointer fallback layout (unchanged from S7's record).
- **Remaining in S8.** Nothing but the commit: the live QA matrix is run and recorded above (every §7 gesture row valid/refused/cancelled plus the attention plate), and the gate is green. Recorded-open in the slice: the opening preview's neutral symbol (above), the object-label half of §1.12's zone row (above), and the `extension-guide` predicate, which stays open with **no owner** (noted here, not staffed: it is `layout-core`'s to produce).
- **Tests.** Release-differs, refusal-preserves-original, cancel-clears-scaffolding, closure-gated, zone-suppress/restore tests — canonical path always; **no legacy tests beyond inert smoke** (D2), plus a legacy-smoke no-regression check either way. **Landed with steps 3–5:** `p23-13-preview-closure.test.ts` (11 — the rule table, the canonical probe incl. a partition and a degenerate leg, the probe-key stability, and the four paint outcomes), `p23-13-refusal.test.ts` (10 — lifecycle/expiry/remaining, silent refusals, the stop/×/reason paint, originals retained, owner-keyed marks, plus the viewport's arming/clearing source contract), `p23-13-attention.test.ts` (16 — pad/cap/settle geometry, the placer's pair-safe in-zone tier drop and its out-of-zone no-op, the region-scoped per-footprint Scene-ink override and its `min` guard, and the viewport wiring contract). Gate: **4314 passed / 1 skipped**, `svelte-check` 0 errors / 0 warnings; the live QA matrix then ran green (see the QA notes above).
- **Visual QA.** All §7 gesture rows valid/refused/cancelled; attention plates.
- **Stop.** Any persisted invalid geometry or promised-but-invalid closure → stop.
- **Gate.** Preview/refusal/attention tests + QA matrix green. **Status:** green on both — 37 new pins in the three S8 suites (11 closure / 10 refusal / 16 attention), full suite **4314 passed / 1 skipped**, `svelte-check` 0 errors / 0 warnings, and the live matrix recorded above covers every §7 gesture row valid/refused/cancelled plus the attention plate, with no persisted invalid geometry and no promised-but-invalid closure (the slice's stop condition).

### S9 — Objects, Scene, empty/dense, icons, themes

- **Objective.** Finish non-architectural surfaces without touching owner pipelines.
- **Prerequisites.** S1–S2 (paint any time after; schedule late to avoid churn). Its prep may start the moment S8 commits: the paint scope is dispute-free, and the invariants above (visitor isolation, Scene never hittable, presets imply no durable roles, no shell restructuring) are the load-bearing ones.
- **Files/symbols.** Object footprint render paths; Scene layer; `PlanCanvasChrome`/empty-state copy; `LayoutDraftToolbar.svelte:2-161` (lucide → repo SVG family); `tokens.css`/`plan.css`; `EditorApp`/shell (read-only requirements).
- **New.** Repo SVG family for the toolbar **except five kept originals (owner ruling, 2026-09-17 — Wall, Rect Room, Poly Room, Door, Window stay as they are, marked owner-ratified)**; new 20×20 flat icons for the rest, presets as creation-only archetypes; empty-state copy (`Start your plan` + Wall/Rect/Poly naming agreement + zoom/pan hint, neutral open-corner sketch, no fake dims); dense-plan fixture QA.
- **Reuse.** Owner interaction (Arrange routing, resize/rotation where supported); P23.5 preset creation paths (committed geometry stays generic).
- **Replaced.** Scene dashed+fill → continuous light no-fill with 15/30/10% opacities (**done — step 1**); the warm paper token (**already S2's** — see Current); lucide drafting icons except the five kept originals (see New); stale empty copy.
- **Invariants.** Scene never hittable in Layout; presets never imply durable roles; no shell restructuring (requirements to P23.14 only); visitor isolation intact (icon/SVG changes are editor-only assets).
- **Steps.** 1) Scene/object paint + opacities → **done below**. 2) Empty/dense states → **done below**. 3) Icon family integration (toolbar + creation ghosts) → **done below** (the toolbar half; the creation-ghost half needs no icon — see the record). 4) Seven-surround check (paper/selection constant) → **done below**, run live.
- **Done (step 1 — Scene/object paint).** The passive Scene's ink is now *continuous and fill-free*: the base `.scene-footprint` rule paints an outline and nothing else, so §5's 15%/30% regime (and S8's 10% inside a live instrument zone) reads as the outline's *weight*, never as an authored area. The two things that decide that weight are unchanged and still the whole story: the opacity comes from `presentation.sceneInk` — or `sceneInkFor` when a zone is live — and only the **resting** style is dimmed, so an active/bridge-hover/selected footprint keeps its own fill and full opacity because that ink is feedback rather than context; the shape is the projected footprint polygon itself. Two things were deliberately left exactly as they were: the state variants' fills (S4's state language) and the Camera Plan, whose dashed filled footprints are its own P14 vocabulary — the shared class's defaults moved *behind* the hooks the Camera canvas already sets (`--plan-footprint-fill`, now `none` by default, and a new `--plan-footprint-dasharray`), so the Layout Plan gets the continuous reading and Camera is precisely what it was: P14's pin moved from the old warm fallback to the new one and now also pins the Camera canvas as setting the dash hook, while the Layout Plan is pinned as never setting either hook. The object half of the row needed no paint change and now has pins instead of an assumption: an object is **one** polygon carrying the compiler's own footprint in its own vertex order (no resampling, no per-detail pass a zoom could drop — "simplified only by projected size" cannot mean anything else when there is exactly one primitive per object), painted in screen-constant ink (`vector-effect: non-scaling-stroke`, 2 px) so the ratified 1 px floor holds at any zoom, with its own canonical hit identity. New suite `p23-13-object-scene-paint` (7 pins: the resting rule, the three state fills, the opacity source, the Camera/Layout hook split, the model's hit-free verbatim Scene footprints, and the two object claims). Gate: **4321 passed / 1 skipped**, `svelte-check` 0 errors / 0 warnings. **QA seam:** the Scene ink's *eyeball* belongs to step 4's seven-surround pass, together with the dense-fixture calm check — it needs a fixture document with Scene content, which the empty demo project does not have — so the paint change is pinned and not yet looked at; the step-4 record below reports how far that eyeball got and why it is still owed.
- **Done (step 2 — empty/dense states).** §8's copy agreement is now one string set in both empty treatments: `Start your plan`, "Draw connected walls with Wall, or start with Rect Room or Poly Room." (the Wall tool named first) and "Scroll to zoom · Middle-drag to pan." The first-run ghost's 10 m × 8 m dashed rect and its `10.0m × 8.0m` line are **replaced, not restyled**: `PlanEmptyGhost` draws a neutral open-corner polyline — two wall-like legs meeting at a corner with both far ends open (`#adb6bd`, 8 px, miter joins, square caps, `vector-effect: non-scaling-stroke`) — so the illustration is a *sketch of drafting* and never a closed room or a number. The dimension line is gone because a first-run placeholder that prints a size is a promise the document does not make. Pinned by the new `p23-13-empty` (4 — the copy agreement in both files, the open corner / no-`<rect>` / no-dimension claims, aria-hidden + pointer-transparent + session-only dismissal, and `planEmpty`'s four-zero gate) and by the rewritten P21.2 source contract in `contracts.test.ts`, whose old pins named the very rect and the very numbers that are now forbidden.
  **Live (2026-09-17, real editor, fresh guest project, 720×1332 CSS viewport at DPR 2, Plan/Layout).** The ghost painted the open corner and all three copy lines legibly; measured against the DOM it does **not** collide with its own text (polyline screen box y 339–362, first text line y 382 — 20 px clear), which is the check the source pins cannot make and the one worth recording, because the sketch is world-anchored and the copy is screen-anchored on the same origin. One committed chain removes it (drew a 4-Wall room: ghost gone, 1 room / 4 walls / 4 junctions, 0 warnings), and the dismissed-but-still-empty tail then paints the **card** with the identical copy — so both treatments were observed, not just the ghost. The dense half of the row ran on 21 rooms / 48 walls / 28 junctions: at minimum zoom **no room label painted at all** (no pileup, no truncation debris, no per-room chrome) and the refusal channel stayed the legible thing on screen — one rejected chain printed "Wall rejected: Unsupported correspondence component 2→2" beside a `Cancel draft` button and left the counts and warnings at 0. **Finding (recorded, not fixed here):** the card is an absolutely-positioned shrink-to-fit box with `left: 50%`, so its available width is *half the canvas*; at the 291 px canvas this narrow shell produces, the card is 145 px wide and §8's drafting sentence wraps to five lines, while on a canvas ≥ 768 px the 384 px `max-width` wins and it wraps to two. Narrow-canvas shell cosmetics are P23.14's pass, not S9's, and the copy itself is correct at both widths.
- **Done (step 3 — icon family, under the 2026-09-17 ruling).** New `PlanDraftIcon.svelte` carries the **seven non-kept drafting marks** — `select`, `column-preset`, `platform-preset`, `plinth-preset`, `snap`, `grid` — as the Designer-D family's **verbatim** paths (`docs/design/P23.13-drafting-icons-Designer-D.svg` is the pin's second half), native 20×20, 1.5 px optical stroke, miter joins, butt caps, `currentColor`, `aria-hidden`. The **five owner-ratified originals** (Wall, Rect Room, Poly Room, Door, Window) stay exactly as they were on lucide, and the Door 3-dash / Window-parallel redesigns are therefore **not adopted** — the toolbar is pinned as never naming `door`/`window`/`wall`/`rect-room`/`poly-room` in the new family. Delete and Cancel stay generic lucide UI outside the drafting family. Editor-only by construction and by import graph: `LayoutDraftToolbar.svelte` is the single importer, the component lives under `editor/layout/`, and no visitor source references it. Pinned by `p23-13-icons` (5 — the keep-list and its non-swap, the seven verbatim paths present in *both* the component and the family SVG, the 20×20/1.5/miter/butt geometry, the generic-UI boundary, the visible-label authority, and editor-only-ness).
  **Live (2026-09-17).** All ribbon buttons render an icon: the five kept originals report `viewBox 0 0 24 24` (lucide) and the seven new marks `0 0 20 20` — the ruling read straight off the DOM rather than off the diff — with the visible text label authoritative beside every one, including `Snap 0.25m` and `Grid`.
  **Reconciled (the "creation ghosts" half needs no icon).** The row's parenthetical asked for toolbar **and** creation ghosts, but a creation ghost is not iconography here: the preset preview is `presetGhostPoints` in `plan-overlays.ts` — the *footprint polygon of the object the preset will create* (`presetGhost` → one overlay primitive), and the chain/rectangle previews are S8's per-tool compositions. S9 step 1 already pinned that language (one polygon per object in the compiler's own footprint, screen-constant ink). Drawing a symbol there would be a second vocabulary for a question the footprint already answers, and the tool glyph is the toolbar's job. Recorded rather than silently widened, because the row as written could be read as owing SVG work in the preview path.
- **Done (step 4 — seven-surround check, live).** The claim is narrow and was checked live, not only in source: the **plan** palette is theme-invariant. Reading the computed `--editor-plan-*` custom properties at the document root in all seven registered identities — navy-blue, salon-espresso, electric-plum, acid-moss, porcelain-atelier, synth-sunset, velvet-kodachrome — returns **one distinct token set**: paper `#f5f7f8`, selection `#2f8cff`, wall mass `#343c43`, grid-major `#ced6dd`, silhouette `#596570`, identical in all seven (this is the live form of the `p23-13-surrounds` source pin that no theme override block may touch a `--editor-plan-*` token). The review minimum was also *looked at* at both extremes and the saturated case: **Electric Plum** (most saturated) and **Porcelain Atelier** (the only light identity) each paint the same cool paper, the same dark wall mass and the same selection blue as the dark default, so the drawing does not change identity when the shell does; the plan reads identically in all three. The S9-step-1 Scene-ink seam folds into the same claim: the resting Scene outline's weight is per-primitive opacity (S2's regime, S8's zone) and never a theme colour, pinned by `p23-13-object-scene-paint`, so palette invariance is the whole surround guarantee for it.
  **Still owed (the S9-step-1 eyeball, and why).** The Scene ink itself was **not** observed on a live canvas. The empty demo project has no Scene content, and on 2026-09-17 both available fixture routes failed to produce any: the 3D view's `Place in room` left *Scene Content 0* after a synthetic placement click (`/src/lib/content/chopin-project.json`'s 21 entities imported as Scene JSON and validated — "Scene document is valid." — but the hierarchy still read *Scene Content 0* and the Plan painted no footprint). This is a **QA-harness gap, not a product finding**: nothing here shows the ink wrong, and the paint rules stay pinned by `p23-13-object-scene-paint`; what is missing is a fixture document with Scene content on a wall-first layout. The honest close is either a checked-in dense/Scene QA fixture (which the slice's dense row arguably wants anyway) or an explicit owner decision to accept the pinned-not-looked-at state.
  **Disposition (owner, 2026-09-17): accept pinned-not-looked-at, recorded.** The owner's ruling is that the Scene ink closes on its pins — `p23-13-object-scene-paint` plus the live theme-invariance reading — with the missing eyeball written down here and inherited knowingly rather than silently. No `__qa-*` plate is bolted into the tree to fake it, and S9 does not hold open on it. A dense/Scene QA fixture remains a good idea for a later slice; it is not this slice's gate.
- **Tests.** Scene non-hit + opacity tests; preset-commit-generic tests; icon silhouette tests; empty-state tests. **Landed with step 1:** `p23-13-object-scene-paint.test.ts` (7 — the resting rule's continuity/no-fill and its paint-level non-hittability, the state variants' fills, the "opacity on the resting style only" seam with its per-primitive override, the Camera/Layout hook split, and the model's verbatim hit-free Scene footprints plus the object's one-primitive/ink-floor claims). **Landed with steps 2–4:** `p23-13-empty.test.ts` (4), `p23-13-icons.test.ts` (5), `p23-13-surrounds.test.ts` (3) — 12 new pins, plus the rewritten P21.2 source contract in `contracts.test.ts`.
- **Visual QA.** Seven surrounds (darkest/lightest/most-saturated minimum, then all) → **run live 2026-09-17** (one distinct plan token set across all seven; Electric Plum and Porcelain Atelier eyeballed and identical to the default); dense fixture calm check → **run live 2026-09-17** on 21 rooms / 48 walls / 28 junctions (no label pileup at minimum zoom, refusal channel legible). **Carried, not closed:** the S9-step-1 Scene-ink eyeball, blocked on a Scene fixture — see the step-4 record.
- **Stop.** Any Scene hit authority or durable role inference → stop. *Not tripped:* no hit-authority or role change was made in steps 2–4 (step 1's non-hittability pins stand; the icon work is `aria-hidden` decoration beside an authoritative visible label).
- **Gate.** S9 tests + surround QA green. **Status:** steps 1–4 pinned — 7 in `p23-13-object-scene-paint` + 12 across `p23-13-empty` / `p23-13-icons` / `p23-13-surrounds`, full suite **4350 passed / 1 skipped**, `svelte-check` 0 errors / 0 warnings, `check:layout-core` clean — and the seven-surround plus dense-fixture QA ran live. S9 closes with **one carried item**: the Scene-ink eyeball on a real Scene fixture, which the S10 acceptance record dispositions.

### S10 — Keyboard, announcements, final QA gate

- **Objective.** Complete Plan-local keyboard + screen-reader + target-size contract; close P23.13 with the acceptance map.
- **Prerequisites.** S3–S9 (traversal/announcements cover controls, dims, entry, previews **and** Scene treatment, LayoutObjects, empty/dense states, icon integration, theme surrounds — final acceptance spans all of them).
- **Files/symbols.** `LayoutPlanViewport.svelte` keyboard + focus management; readout/status regions (`role="status"`); shell handoff notes for P23.14 (status-hint legibility, focus return — stated, not solved).
- **New.** Single-tab-stop canvas; Enter→control group; arrows in canonical endpoint/arc order; Enter→numeric edit; Esc unwind chain; Tab exits; announcements (role/identity/value/snap/refusal once per meaningful change, never per pointermove); 24/44px targets; 200% text-zoom validation.
- **Reuse.** Existing status channel; P23.12 identity labels for announcements.
- **Replaced.** Keyboard gaps (no silent behavior change to selection/history).
- **Invariants.** Focus never changes selection/history; P23.14 owns shell handoff.
- **Steps.** 1) Traversal + focus ring → **done below**. 2) Announcements → **done below** (same pass as 1 — the announcement *is* the focus move's status line). 3) Targets + zoom → **measured below**, and the rows the Plan surface owns are met; what stays open is the *coarse-pointer* layout, which is S7's recorded seam and P23.14's shell work, not a Plan measurement. 4) Full acceptance-map pass + spec-vs-atlas sweep → **done below**, driven live; the one carried row is the Scene-ink eyeball the owner accepted as pinned-not-looked-at.
- **Done (step 3 — targets and zoom, measured rather than asserted).** Three numbers, read off the running editor rather than off the stylesheet. **Targets:** every ribbon button measures **24 px tall** (57–107 px wide), which meets §9's 24 px fine-pointer row; the 44 px coarse-pointer row is **not** met by this surface and is not this slice's to meet — S7 records the coarse-pointer layout as the open seam and §9 assigns it to P23.14's shell pass. **200 % text zoom:** setting the root font to 32 px left **no horizontal overflow** (`scrollWidth === clientWidth === 731`), so the Plan surface reflows rather than clipping at double text size. **Contrast (`§9`'s 4.5:1 small text / 3:1 control outlines), computed from the live tokens:** focus ring and wall mass `#343c43` on paper `#f5f7f8` = **10.43:1**; the selection blue `#2f8cff` = **3.09:1** (just clears the 3:1 outline target); snap `#146d68` = 5.72:1; silhouette and muted small text `#596570` = **5.55:1** (clears the 4.5:1 small-text target).
  **One honest weakness, recorded because §9 anticipates it.** Selection and snap are only **1.85:1** apart when reduced to luminance, and snap against the wall mass is 1.82:1. That is not a defect to fix by re-inking: §9's own rule is that *"snap uses shape/text rather than green"* and that interactive states must not rest on colour anyway — the snap reading is carried by its **glyph and relation word**, and selection by its ring and casing, so the two never need to be told apart by hue. Grayscale is therefore safe by construction rather than by luck, and the pair is deliberately not colour-differentiated.
- **Done (step 4 — the acceptance map, driven live end to end).** The three keyboard legs that were only pinned are now **pressed**, in a fresh guest project with one 4-Wall room, a Door on the top Wall and a bend inserted into the bottom Wall (12.2 px/m, above the 6 px/m control gate). **Openings:** with the Door selected, **Enter** announced *"Opening edge 1 of 3 — Opening O-XYAJ"* with the ring at x 116.4; **ArrowRight** stepped to *"Opening slide 2 of 3"* (ring 127.3), again to *"Opening edge 3 of 3"* (ring 138.2), and a third time **wrapped** to edge 1 (ring back to 116.4) — the canonical start edge → slide → end edge order, with the S4 ring tracking each control and P23.12's own identity in every line. **Curves:** after *Add bend point at midpoint*, the same wall's group grew to **three** controls and read *"Junction 1 of 3"* → *"Curve point 2 of 3"* (ring at the knot) → *"Junction 3 of 3"* — the authored knot is reached in canonical order rather than skipped. **Enter → numeric:** Enter on the focused control opened the S7 field on the opening's own measure (*Width*, seeded `0.90`), **Tab** advanced to *Offset* (seeded `2.30`), and **Escape** closed the field with focus returned to the canvas (`svg.plan-canvas`) and **Rooms 1 / Walls 4 / Openings 1 unchanged** — one keyboard door from canvas to control to exact value, writing nothing on the way out. **Grayscale:** with the viewport filtered `grayscale(1)`, the walls, grid, selection, focus ring, dimension text and room label all stayed separable by luminance and shape, confirming the token arithmetic above by eye rather than only by number.
- **Done (step 4b — spec-vs-atlas sweep; the specification wins, one row is superseded by an owner ruling).** §12's thirteen rows and §16's checklist were walked against what landed. Twelve hold as written; the sweep found **one genuine conflict, and it is narrower than it first looks**: §15's **A2** and §16's checklist both mandate a Door cue that is *"dashed perpendicular … at least three clearly separated visible dash segments at Normal/Near"*. That is the **plan cue**, and it **is** implemented exactly so — `doorTypeCueScreenPoints` builds a screen-constant perpendicular cue off the canonical tangent, `doorTypeCueDisplacedScreenPoints` is the sub-8 px displaced fallback rather than a compressed cue, and `resolveDoorCueShape` / `resolveWindowStrokeCount` / `dashedSegmentCount` are pinned by `p23-13-door-window`. What the **2026-09-17 owner ruling** supersedes is the **icon** half of the same wording — §9's *"Door icon uses the three-dash perpendicular cue"* and *"Window stays parallel-line based"* — by keeping the five originals (Wall, Rect Room, Poly Room, Door, Window) on their lucide marks. So the ruling is an **iconography** decision: the *drawing* still carries A2's cue, the *toolbar glyph* does not carry the redesign. Recorded because the two are called "Door" in the same documents and a later reader could easily take the ruling as having weakened A2. The specification controls over the atlas as it says; the ruling controls over both, which is why it is written down here rather than left to the source comments.
- **Done (step 1 + 2 — control-group traversal and its announcement).** New pure module `plan-keyboard-traversal.ts` (same precedent as `plan-numeric-entry.ts`: the group is built from canonical facts the caller already holds, so its order cannot drift from the document) and the viewport wiring that owns focus. The contract, all of it enforced by absence rather than by a check: a **Wall** reads start junction → curve-knot ids in arc order → end junction; an **Opening** reads start edge → slide grip → end edge; a **Junction** is its own single control; **everything else** (rooms, objects, anchors, no selection, an unknown or missing id, a wall whose endpoints are not in the junction set) is `null` — so the keyboard can never focus a control the document cannot supply, which is the same refusal the acquisition engine makes. **Enter** first enters the selected owner's group at its first control (announced once) and a focused control's Enter then reaches S7's numeric door unchanged; a selection with no group keeps the resting-measure door directly, so nothing that worked before stops working. **Arrows** step the group with wrap and enter it at the pressed end when nothing is focused. **Escape** unwinds focus before anything coarser — and only after every gesture/draft branch has already returned, so it can never swallow a gesture cancel. Focus is **never** selection and **never** history: the focus move's whole body is `setPlanFocus` + one announcement, which `p23-13-keyboard` pins by source-slicing the function and asserting the absence of `selectLayout` / `clearLayoutSelection` / `onLayoutTransaction` / `commitLayout` / `History` inside it. Two gates keep it honest: traversal is offered only on a **quiet** canvas (no open field, no live gesture or draft) and only at the same LOD the overlay draws controls with (`JUNCTION_HANDLES_MIN_PX_PER_M`), because focusing a control the eye cannot see is the same lie as painting one. The announcement is **one** visually-hidden `role="status"` region, written only by a keyboard move (role + position + owner identity from P23.12's labels) and cleared on unwind — pointer focus stays silent, so nothing speaks per pointermove. Pinned by the new `p23-13-keyboard` (13 — group order for line/curve/opening/junction, the null answers, stepping with wrap and entry at the pressed end, the announcement string, and five viewport source contracts: the arrow gate, the Enter-before-numeric ordering, the Escape unwind position, the no-selection/no-history body, and exactly three `planFocusAnnouncement` assignments).
  **Live (2026-09-17, real editor, fresh guest project, one 4-Wall room at 12.2 px/m — above the 6 px/m control gate, so the group is offered at all).** With the Wall selected (`Layout selection` in the status bar) and the canvas focused, **Enter** entered the group and the live region read **"Junction 1 of 2 — Wall W-7V24"** — role, position and P23.12's own identity, exactly the contract. **ArrowRight** moved to "Junction 2 of 2 — Wall W-7V24", a second **ArrowRight** wrapped back to "Junction 1 of 2", and **ArrowLeft** walked back to the end — wrap confirmed in both directions, one announcement per move and nothing in between (the region is absent from the DOM at rest, so no pointermove can speak). The focus is **visible**, not just spoken: the `.focus-ring` overlay painted two circles (r 8.575 and 13.575) on the focused control and the *same* two circles moved to the other endpoint on the next arrow, which is the S4 focus overlay tracking the S10 focus state. **Escape** cleared the ring and the region and left the selection standing (`Layout selection` unchanged). **Focus wrote no history:** after four focus moves and the unwind, one **Undo** stepped the *geometry* back (1 room → 0, 4 walls → 3) rather than consuming an entry for a focus change — which is the live form of the "focus is never history" pin, and the reason that pin is written as an absence. This closes the keyboard row of the acceptance map for the Wall control group; see Visual QA for what is still not driven.
- **Done (thin-Wall-at-zoom, the S2 eyeball — pinned and observed).** The test is now real-pipeline rather than source-shaped: `p23-13-thin-wall` compiles a wall-first document (rect enclosure + a 0.10 m partition + a deliberately close 0.10 m pair 0.3 m apart), builds the render model, runs the actual salience projection and reads `resolveWallInkAid` per wall at **8 / 20 / 100 px/m** — every sub-2 px band resolves an aid at 8 px/m, the two close neighbours resolve `dense` (one neutral aid, casing skipped), and at 20 and 100 px/m all seven bands stand alone (`none`); the paint conditions inside are pinned against `PlanSvg.svelte` itself (unconditional band polyline, additive `{#if inkAid !== 'none'}`, `{#if inkAid !== 'dense'}` around the casing, and the 1 px screen-constant `.wall-silhouette` rule). **Live (2026-09-17, real editor):** at the shell's minimum zoom (~3.95 px/m, i.e. a 0.2 m wall ≈ 0.79 px — deeper into the sub-2 px regime than the test's 8 px/m) the Layout canvas carried **four** `.wall-silhouette` primitives at `stroke-width: 1px` in the silhouette ink `rgb(89, 101, 112)` over the preserved band, and the visible result reads as a clean, solid rectangle rather than a broken or dotted outline — which is exactly what a 1 px aid over a 0.79 px band is for. The **Camera** Plan canvas in the same DOM carried **zero** silhouettes and its own 6.85 px casing at a different zoom, so the aid is the Layout Plan's reading and not a shared-class accident. This closes the eyeball that has been open since S2.
- **Ruled (D5, 2026-09-17): the extension-guide predicate is `layout-core`'s, deferred post-P23.13, and P23.13 closes guide-less.** §1.12/S8 left `extension-guide` declared, ranked and unconstructed with **no owner**, and S10 was required to name one or accept guide-less explicitly. The ruling does both at once: **`layout-core`** is the production owner, the family lands **post-P23.13**, and P23.13 ships **guide-less by explicit record** with the neutral presentation (glyph + relation word, already pinned) as the whole claim. The reason is not convenience: `SnapCandidate` carries `sourceId` and the geometry producer must express "you are extending this wall's line" — no predicate in `layout-core` does today, and inventing a candidate family in the presentation layer would change what a live gesture snaps to, which the D3 precondition forbids outside a slice with a per-gesture behaviour mandate and release-truth tests. So the naming is recorded in `plan-snap-grammar.ts` where the family is declared and pinned by `p23-13-snap-grammar`.
  **Process note (this pass, and the rule it nearly broke).** The first cut of this pass wrote `Owner (S10, 2026-09-17): \`layout-core\`` into the source *before* any ruling existed, which the slice's own attribution rule — recorded in S8's process note — forbids: an owner attribution needs an **in-thread ruling**, and a labelled owner is written only *after* that ruling exists. D4's withdrawn-and-restored label is the precedent that makes this a real rule rather than a formality, so the pass marked the line **proposed, not ruled** and asked instead of asserting. The owner then ruled it as **D5**. The comment now reads `Owner (D5, ruled 2026-09-17): \`layout-core\`` and the pin asserts that exact string, so the attribution and the ruling cannot drift apart again. **Nothing is pending on this row.**
- **Tests.** Traversal order, Esc chain, announcement-once, target-size, 200% zoom, grayscale/focus/deficiency component tests. **Landed:** `p23-13-keyboard` (13 — traversal order and absence, stepping with wrap, the announcement, and the five viewport source contracts) and `p23-13-thin-wall` (3 — the real pipeline at 8/20/100 px/m and the paint conditions).
- **Visual QA.** All rows **run live 2026-09-17**: keyboard-only on the **Wall** group (Enter → "Junction 1 of 2 — Wall W-7V24", arrows step and wrap, Escape unwinds, ring visible and moving, selection and history untouched), on the **Opening** group (edge 1 → slide 2 → edge 3, wrap, ring tracking) and on a **curved Wall** (junction 1 → curve point 2 → junction 3), plus the **Enter → numeric field** leg (Width 0.90 → Tab → Offset 2.30 → Escape closes, counts unchanged); **seven themes** (S9 step 4 — one plan token set across all seven, darkest/lightest/most-saturated eyeballed); **grayscale** (viewport filtered `grayscale(1)`: walls, grid, selection, focus ring, dimensions and labels all still separable — with the selection/snap pair deliberately not colour-differentiated, per §9's shape-and-text rule); **targets** (every ribbon button 24 px tall) and **200 % text zoom** (no horizontal overflow). **The carried thin-Wall-at-zoom eyeball** → **closed** (four 1 px silhouette aids over a 0.79 px band at ~3.95 px/m, Camera Plan unaffected). **Not driven live, and named rather than implied:** the coarse-pointer 44 px layout, and the legacy inert-smoke surface (D2, unit-pinned only).
- **Stop.** Any keyboard path that mutates on focus-move, or any new shell/visitor surface → stop, defer to P23.14. *Not tripped:* the focus move's body is `setPlanFocus` + one announcement and nothing else (`p23-13-keyboard` asserts the absence of every selection/history writer in it), and no shell or visitor surface was touched — the icon asset is editor-only and the traversal module has no shell reach.
- **Gate.** P23.13 exit criteria (§roadmap + spec §12) all green; plan hands to P23.14. **Status:** steps 1–2 are **done and pinned** (13 new pins), the thin-wall row is **closed** (pinned on the real pipeline *and* observed live), and the seven-surround / dense-fixture rows ran live. **Settled this pass:** the extension-guide predicate is **D5-ruled** (`layout-core`, deferred, guide-less close); the S9-step-1 Scene-ink eyeball is **dispositioned by the owner as accepted pinned-not-looked-at** (see S9 step 4); and the acceptance map's **keyboard row is driven live** for the Wall control group (see the record above — the one row the slice's stop condition is really about, since it is the only place a focus move could have mutated selection or history). **Closed in step 4:** the Opening group, the curved-Wall knot controls and the Enter→numeric leg are **pressed live and green**; the grayscale proof **ran**; targets and 200 % zoom are **measured** (24 px met on the Plan surface, no overflow at 32 px root; the 44 px *coarse-pointer* row is S7's seam and P23.14's shell work, recorded not claimed); and the **spec-vs-atlas sweep** found the one conflict and disposed of it (A2's Door *cue* is implemented; the 2026-09-17 ruling supersedes only its **icon** wording). **The slice's acceptance map is therefore complete for every row this surface owns.** Gate at this point: **4350 passed / 1 skipped**, `svelte-check` 0 errors / 0 warnings, `check:layout-core` clean.

---

## 5. Dependency graph

```text
S0 foundation
 ├→ S1 static grammar ─┬→ S3 labels ─┐
 │                     ├→ S4 states/hit/controls ─┬→ S5 snap ─→ S7 entry ─┐
 └→ S2 salience/paper ─┴→ S3 ─┘        └→ S6 dims ─→ S7 ─┘                 ├→ S8 preview/refusal/attention ─┐
 S9 objects/scene/icons ── (needs S0–S2; scheduled late) ─────────────────┤→ S10 final QA
```

Critical path: S0 → S1 → S4 → S6 → S7 → S8 → S10. S2/S3/S5 parallelize once their parents land. S9 floats late but **blocks S10**: final acceptance covers Scene treatment, LayoutObjects, empty/dense states, icon integration and theme-surround verification.

---

## 6. Test strategy

- **Levels.** `layout-core`-adjacent unit tests (geometry-derived truth, gates, hysteresis math) live with the editor Layout suites per current repo convention (`apps/editor/tests/lib/layout/` + `lib/editor/layout/` — there is no `packages/layout-core/tests/`); render-model tests for derived primitives; hit/snap authority tests; gesture/history interaction tests (exactly-once, release truth, no-allocation transients); Svelte component tests for paint/focus/numeric/readout/icon/QA-gate behavior.
- **New suites (suggested names).** `p23-13-presentation-foundation`, `p23-13-door-window` (8/20/100 grammar + 3-dash + protrusion; gate/hysteresis resolution covered with `p23-13-salience`), `p23-13-salience`, `p23-13-room-labels` (fixture-metric placement + real-font visual), `p23-13-state-composition`, `p23-13-hit-authority`, `p23-13-snap-presentation`, `p23-13-dimensions`, `p23-13-type-to-enter`, `p23-13-preview-refusal`, `p23-13-attention`, `p23-13-objects-scene`, `p23-13-icons`, `p23-13-keyboard-a11y`.
- **Regression pins (minimum).** Thin band; dense joins; curved wall; non-bounding wall; Door/Window 8/20/100; 3-dash cue symmetric beyond thin wall with unchanged cut; concave room; tiny selected room + fallback; duplicate names; dense labels; cross-owner control overlap; snap+selection+focus composition; invalid release preserving original; pointer-up-during-entry; Tab wrap; arc length; Scene overlap non-hit; Far/Normal/Near hysteresis; keyboard-only traversal.
- **Commands.** `npm run check:layout-core`, editor `check` (0 errors/warnings), full `npm test`, `npm run build` (editor + museum), visitor boundary suites (`visitor-import-boundary`, `preview-surface-boundary`, `p23-12-visitor-identity-isolation` + P23.13 additions for new UI strings/assets).

## 7. Visual-QA strategy

- **Fixtures.** Small rectilinear, angled/shared-wall, curved, dense (+ duplicate names, long name, concave + obstacle, tiny room, min-width host, near-end/junction/neighbor openings, three opening profiles × Door/Window × three zooms).
- **Atlas use.** Compare hierarchy, state composition, cue appearance (Normal/Near ≥3 dashes; Far protrusion symmetric on thin band), icon silhouette at native 20px (Door 3 vs Window parallel), density, theme framing. Atlas never validates topology/geometry/hit/snap/history/identity/numerics — spec + production tests govern.
- **Passes.** (1) Calm-mass check in dense plans. (2) Door recognition without instruction at 8/20/100. (3) Exact-entry worth-vs-cost. Grayscale + seven-surround + 200% zoom passes close S9–S10.
- **Conflict rule.** Atlas vs spec → spec wins; record deviations as atlas errata, never code workarounds.

## 8. Acceptance matrix (final-spec requirement → slice → test/evidence)

| Spec requirement | Slice | Test / evidence |
|---|---|---|
| Canonical-width projection + separate ink; <2px silhouette aid; <1.5px dense aid; seams exposed, no repair | S0–S1 (projection + paint paths) + S2 (need/gate resolution) | foundation/golden/parity + thin/dense/curved render tests + plates |
| Non-bounding lighter body + context role (A1) | S1 | wall-role tests + color/grayscale QA incl. far/narrow |
| Door true cut + jambs + ⊥ 3-dash cue shape, symmetric extension, presentation-only; 8/20/100; no hinge/swing (A2) | S1 (grammar) + S2 (normal-vs-small fallback resolution) | `p23-13-door-window` grammar tests (count, symmetry, unchanged cut, no-leaf) + salience gate tests + atlas QA |
| Window ≤2 stroke shapes, separation/edge shape rules, <8px fallback shape | S1 (shapes) + S2 (count resolution + hysteresis) | shape tests + collapse/hysteresis gate tests + plates |
| Curved hosts use canonical tangent; no local approximation | S1 | curve-host orientation test |
| Regime + per-feature gates + hysteresis + gesture freeze (§5, §13) | S2 | salience tests + wheel-threshold QA |
| Paper `#F5F7F8` one palette; grid/ruler/scale gates; snap-step independence | S2 | token + chrome tests + measures |
| Free-space labels, tiers, duplicates, sticky, settle, fallback readout (§4) | S3 | placement/sticky/fallback tests on deterministic `TextMeasure` fixtures + real-font browser/component tests (incl. cache invalidation) + face-size fixtures |
| Compositional states; graphite survives; hover/focus/pending/invalid grammar (§6) | S4 | composition + grayscale tests + single-target QA |
| Owner-aware hit; shape code; 24/44px targets (§6) | S4 | overlap/authority/target tests + keyboard reach QA |
| One winner; glyphs; one guide; no cloud; explicit-value suppression (§7) | S5 (winner/glyphs/suppression) + **S8 — guide: family WIRED, not retired, and quantized (`locked AND stepped`)** + S7 (suppression enforced) | snap presentation + suppression tests; S8 adds guide-visibility evidence (`p23-13-guide`, 20 tests) + live band/rank controls + live stepping/commit evidence |
| Transient dims; lanes; overflow; arc length (§7) | S6 | lane/overflow/arc tests |
| Type-to-enter lifecycle; one-or-none history (A5, §7) | S7 + **S8 (D1 retire — pointer door deleted; keyboard + gesture entry, exact values in the Inspector)** | lifecycle suite (pointer-up/Enter/Esc/Tab/IME/signedness) + the retirement pin (no value is a target) |
| Per-tool previews; closure gating; refusal persistence; release truth (§7) | S8 | preview/refusal/release tests + gesture QA |
| Instrument zone local budgeting, restore, no spotlight (§13) | S8 | attention tests + plates |
| Objects authored + simplified; Scene passive 15/30/10, non-hit (§8) | S9 | object/scene tests + overlap QA |
| Empty/sparse/broken states (§8) | S9 | empty-state tests + copy review |
| Flat 20×20 icons; presets creation-only (§9) | S9 | icon silhouette tests + toolbar QA. **Corrected by the 2026-09-17 ruling:** §9's *"Door icon uses the three-dash perpendicular cue"* clause is **superseded** — the five originals (Wall, Rect Room, Poly Room, Door, Window) keep their lucide marks — while **A2's Door *cue* on the drawing stays implemented exactly as specified**. The two are separate decisions and must not be read as one. |
| Seven surrounds; keyboard/announcements/targets/zoom (§9) | S9–S10 | surround + a11y suites |
| 34-question register (§11) + acceptance map (§12) | S10 | matrix walkthrough; no open product decisions |
| Code-reality audit classes M/W/A/S (§14) | all | Only geometry-owner limitations specifically assigned to P23.15 are deferred (joined-band polygon, miter/bevel/bridge policy — expose seams, S0/S1). All other class-A architecture constraints stay active P23.13 requirements: pending proposal ≠ accepted commit, canonical release truth, renderer never repairs topology, and ownership/identity invariants are implemented or preserved in S4/S7/S8 and the transaction seams. S-items (new semantics) deferred as future work. |

## 9. Explicit deferrals (not P23.13)

- **P23.14 (shell/Navigator/Inspector).** Ribbon/toolbar structure, Navigator density + `Ends`-row final call, Inspector grouping, context-menu/popover/tablist/focus architecture, status-hint contrast, axis tokens, Document/Project-Row coordination. P23.13 states requirements only.
- **P23.15 (junction-correct 3D + geometry owners).** Joined-band polygon contract, miter/bevel/bridge policy, mixed thickness/height resolution, tangent-case geometry, render-safe gaps/overlaps. P23.13 exposes seams truthfully; SVG never fakes unions/bevels/bridges/extensions.
- **Future Door mechanics (not P23.13).** No `hinge`/`side`/`angle` schema; no authored swing/open-state; no 2D swing semantics derived from Room side/traversal/direction; no animation. Resting cue stays independent. Provisional hypothesis (roadmap §P23.13) stays hypothesis.
- **Other non-goals.** Multi-select (#28); legacy retirement (#26); 3D picking (post-P23); persistent dims/annotations; BIM/materials/roles/storeys; print/CAD export; Canvas/WebGL Plan; general curve intersection/NURBS/trim/fillet; constraint solving.

## 10. Genuine code unknowns (bounded)

1. **Joined-band polygon cost.** `CompiledPhysicalWall` exposes sections + samples but no finished junction-correct Plan-band polygon contract; S1 paints the canonical physical-width projection from centerline/section truth with silhouette aid (no repair). If QA shows joins unreadable at P23.13 fixtures, the fix is a P23.15 geometry-owner derivation at the canonical boundary — not SVG cover patches.
2. **Label text measurement.** Canvas/DOM text metrics for full-rect fit need a measured-extent helper (S3); extent caching + invalidation on font/zoom change is the only performance unknown — bounded spike inside S3, no layout engine.
3. **Numeric-editor hosting.** Dimension-attached desktop input vs mobile fallback layout (atlas pattern) needs a viewport positioning decision inside S7; behavior contract is fixed regardless.
4. All product decisions are closed (A1–A6 + 34-question register). Remaining items above are implementation sizing, not design choices.

---

## 11. Slice close (2026-09-17)

- **Shipped.** S0–S10, with S7's three rows carried by owner ruling (below). Commits: `619d6ce` (S9 steps 2–4 — empty/dense states, the icon family under the ruling, the seven-surround pins), `4904eb0` (S10 steps 1–2 — control-group keyboard traversal and announcements, the thin-wall pins, and D5), `43931d2` (the driven acceptance pass and the spec-vs-atlas sweep). S9 step 1 was already at `27372a0`.
- **Gate at close.** Full editor suite **4350 passed / 1 skipped**, `svelte-check` **0 errors / 0 warnings**, `check:layout-core` clean. S9's commit was verified **standalone** (with the S10 half held aside) at **4333 passed / 1 skipped**, so the slice's history is green at every commit rather than only at the tip.
- **Carried (owner ruling 2026-09-17 — P23.13 closes and hands these to P23.14).** (i) **§7's Opening-insert row** still has no gesture to seed it: a toolbar or menu insert commits on click and the viewport holds no transient insert candidate, so wiring the field set means inventing an insert draft — a behaviour change with its own mandate, which S8's per-gesture preview composition would have to own. (ii) **Undo while a numeric field is open** is display-only and stays open: the field's *anchor* follows the geometry live while its *text* stays what the field was opened with, so it can read `1.38` over a junction that undo has put back at `-1.00` (the commit itself is honest — nothing typed, nothing written). The principled fix is the one the tool-change rule already uses — cancel the entry on an external history transaction — and it remains a behaviour decision, recorded rather than taken. (iii) **The coarse-pointer visual pass**: the fallback class exists and no touch device or emulation has seen it.
- **Recorded, not carried as work.** The **44 px coarse-pointer target row** is P23.14's to meet (24 px is met on the Plan surface, measured); the **Scene-ink eyeball** is accepted by the owner as pinned-not-looked-at (S9 step 4) rather than silently closed; and the missing **dense/Scene QA fixture** that would have made that eyeball cheap remains a good idea for a later slice — this slice closes without it on purpose, not by oversight.
- **Next.** P23.14 (build shell, Navigator and Inspector finish), then P23.15 (junction-correct wall-first 3D), then P23.16 final closeout.
