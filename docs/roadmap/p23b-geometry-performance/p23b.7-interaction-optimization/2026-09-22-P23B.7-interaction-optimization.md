# P23B.7 — Interaction optimization

```text
STATUS:   RATIFIED AND AUTHORIZED 2026-09-25 — the reconciled plan of §0, with the owner's two
          clarification rulings OF THE SAME DATE folded into the sections that own them (§0.8:
          recomputed affected candidate sets; the two-axis sample/verdict differential plus the
          retained-requests fallback). Every hard dependency has landed. Under the owner's 2026-09-25
          order ruling this slice runs BEFORE P23B.6 (order only — no slice ID, name, scope or
          identity changed; the SEQUENCE owns the order and this plan does not restate it).
          IMPLEMENTATION AUTHORIZED on the dedicated `P23B.7` branch, in the §6 amended order.
ROLE:     reduce per-gesture cost — drag (pointermove proposal + preflight + overlay), hit-test, snap and
          preview install — without changing gesture semantics, selection or history.
NOT:      a gesture-semantics change · a hit-test/snap behaviour change · a change to what a release
          commits · a change to undo/redo or selection determinism · a Worker · a new interaction mode ·
          a preview that installs geometry · a budget
AUTHORITY: direction → ../p23b.3-synthesis-optimization-direction/2026-09-22-P23B.3-synthesis.md §5
            T-2, T-8; §6.1 M-2; §7.2; §8.3; §8.4 (OR-3/OR-4)
          evidence → ../p23b.1-internal-geometry-pipeline-harvest/2026-09-22-P23B.1-harvest-record.md
            F-P5 · F-P6 · F-P11 · F-P12 · CG-1/CG-1b · PM-1 · UN-1/UN-2 · EQ-7
            (UN-1 WITHDRAWN and UN-2 partly answered — §0.4/§0.2; that record's line anchors are STALE)
          measured → ../p23b-measurement-only-step/2026-09-25-release-containment-record.md (the priced
            commit path + the DEV geometry-identity pin) · its LIVE artifact
            .../p23b-measurement-only-step/2026-09-25-release-containment-capture.json ·
            ../p23b.5-caching-reuse-optimization/2026-09-22-P23B.5-caching-reuse-optimization.md §0.2/§0.4
          invariants → ../2026-09-22-P23B-geometry-performance-stabilization-umbrella.md
          order → ../README.md §SEQUENCE (owner-approved, AMENDED TWICE — the P23B.3a insertion and the
            2026-09-25 order ruling; that block is the ONE authoritative copy, never restated here)
DEPENDS ON: ALL SHIPPED — P23B.3a (topology policy, SHIPPED 2026-09-24; the group-scoped validator this
            slice runs against) · P23B.0-durable (fixtures + baseline + measurement capability, SHIPPED
            2026-09-25) · P23B.4 (the M-1/M-2a primitives this slice drives per gesture, SHIPPED; its
            M-2b verdict reuse is DROPPED, so M-2 here is the first owner — §0.5) · P23B.5 (the shipped
            preflight-only gesture-scoped SAMPLE scope + its reuse-counter ratchet — CONSUME, do not
            re-own; §0.3) · the curved-crossing ruling where verdict reuse is involved
OWNER:    ratifies the slice plan + acceptance criteria; ratifies any budget (none proposed).
```

## 0. Reconciliation against later findings (2026-09-25)

This plan predates P23B.3a's landing, the durable baseline, P23B.4 and P23B.5, and the measurement-only
step. Precedent: P23B.4's and P23B.5's plans were reconciled, then ratified. This section records what
changed; §2's scope, §4's mechanism and §5's acceptance contract are otherwise unchanged.

```text
0.1  STATUS. All hard and "met" dependencies are SHIPPED, including the P23B.3 ratification this plan
     awaited ("not implementation-ready until the owner ratifies P23B.3" is discharged). The only
     remaining gate is the owner's ratification of THIS reconciled plan. The owner's 2026-09-25 order
     ruling places this slice BEFORE P23B.6 (SEQUENCE step 11 — order only; no slice ID, name, scope or
     identity changed, no renumbering). The plan does not restate the order.
0.2  WHAT THE MEASUREMENT STEP SETTLED (evidence, not speculation). S1 is EXECUTED as part of the
     measurement-only step, extended to the wall-authoring release. It priced the release as a
     containment tree and found the decisive per-gesture facts:
       · the transient preflight gate is SMALL next to the commit path — `preflight-topology` p50 11.1-
         11.6 ms curved / 2.2 ms straight per accepted move, while a curved drag release is 341.6 ms of
         which the commit path is 266.6 ms (78%). The old assumption that the preflight is the growing
         per-move term does not survive P23B.4/P23B.5.
       · on EVERY accepted edit the full 40-Wall mesh set is built TWICE, and the second build MISSES
         the identity-keyed cache inside `commit-replace`'s restore (p50 161.7 curved bend / 160.7
         curved drag / 190.7 curved authoring / 135.4 straight), because the commit hands
         `installWallMeshes` a Svelte `$state` PROXY of the compile's geometry, never the object the
         install cached (STATE-SIDE pin).
       · the same restore BETWEEN actions HITS at `restore-mesh-install` p50 0.0 (75 curved / 50
         straight), so the miss is specific to the commit's own identity, not to the restore machinery.
     CONSEQUENCE: S6 (preview-install / reactive work), previously CONDITIONAL, is now the NAMED FIRST
     TARGET. M-2 (the gesture-scoped topology gate) stays IN scope but is no longer the leading term.
0.3  THE SAMPLE STORE IS ALREADY OWNED. P23B.5 shipped ONE bounded gesture-scoped sample owner threaded
     into `preflightWallFirstArchitectureCandidate` only, with a committed reuse-counter ratchet. This
     slice CONSUMES that scope; it must not add a second store, re-key it, or restate P23B.5's
     PREFLIGHT-ONLY disposition. What remains here is the wider per-gesture work: the affected-extent
     rule and reuse of unchanged VERDICTS (M-2 per gesture), plus the now-named commit-path cost.
0.4  ANCHORS RE-DERIVED BY SYMBOL, NOT INHERITED. P23B.1's line anchors are STALE and the precision
     module now lives in `packages/layout-core/src/`. §3 carries the current symbols and lines and §1's
     inline anchors are updated to match. UN-1 ("the install seam is untagged") is WITHDRAWN: the
     planner, `deriveInstallBundle` and the preview-install commit all sit INSIDE the `plan-apply`
     boundary — the seam must be read by symbol, never from P23B.1's anchors.
0.5  M-2b IS CLOSED, NOT DELEGATED. P23B.4 attempted verdict reuse, DROPPED it at its own X-6, and
     recorded the S9 reconciliation-change contract / S10 incremental-compile dependency map as findings
     for P23B.5/P23B.7. This slice's M-2 (per gesture) is therefore the FIRST owner of verdict reuse,
     running against P23B.4's recorded contract rather than a second implementation of it.
0.6  UNCHANGED. §2's scope and non-goals, §4's M-2 mechanism and its alternatives and §5's invariants
     (INV-1…INV-7, OR-3/OR-4/OR-7…OR-10) stand as written, EXCEPT the two §7 clauses amended by §0.7
     below (the S6 reactive regression gate and the DETERMINISTIC scaling clause). The residual limits
     the measurement step recorded (all-curved-40 aborts on the driver's own 6000 ms guard; revision-2/3
     magnitudes run 40-85% above revision 1 and the baseline, so the durable result is the per-edit
     build COUNT and the geometry identity, not the ms) are carried into §7's measurement clause.
0.7  PRE-RATIFICATION GAPS (owner review 2026-09-25) — RESOLVED IN PLACE.
     G-1  S6/R-9 now REQUIRE a `$state`-backed regression test (install → capture → history commit) that
          FAILS before the fix, proves ONE mesh build after it, and re-checks undo/redo content. The
          existing plain-state cache-key test in `layout-transient-preview.test.ts` ("P23B measurement
          step — the restore path and the mesh-cache key") is a SEMANTICS pin, not that oracle: it
          passes today while the app rebuilds twice.                        [§6 S6, §7, §8 R-9]
     G-2  S4 now carries a SAMPLE-REQUEST-CONTINUITY obligation on TWO INDEPENDENT AXES: SAMPLE REUSE
          (the ratchet's quantities; VERDICT MODE HELD FIXED — scope misses come from the
          sampling-ENABLED run, `unscopedPreflightRequests` alone from the sampling-DISABLED run) and
          VERDICT REUSE (OR-3's differential; SAMPLE SCOPE HELD FIXED). The ratchet is RE-DERIVED at the
          S4 revision through the SAME production preflight, never a retained pre-S4 path; an absolute
          invariant in `reuseRatchetInvariantViolations` failing there is a BUG, not a re-record. The
          differential is run as a four-cell matrix, and requests suppressed by verdict reuse are
          accounted explicitly — see §0.8.2.                                              [§6 S4, §7]
     G-3  EXECUTION ORDER amended: S6 and its regression proof run BEFORE S3-S4, and S6's change is
          measured (independently attributable) before the topology work starts. Steps are NOT
          renumbered, and S6 has NO "no-change" exit while its oracle asserts one build.    [§6]
     G-4  DETERMINISTIC clause corrected: initialization MAY scale with document size; the ONLY
          document-size-independent guarantee is ZERO repeated invariant predicate evaluations on moves
          2..k. Affected work is counted separately and bounded by the conservative candidate-pair set,
          which MAY grow with the document.                                  [§7, §9 X-3]
```

### 0.8 Owner ratification and clarification rulings (2026-09-25)

The owner RATIFIED the §0 reconciliation and AUTHORIZED implementation on the dedicated `P23B.7` branch,
with two clarifications. They are stated here as current intent and folded into the sections they govern;
they narrow nothing and add no scope.

```text
0.8.1 RECOMPUTED AFFECTED SETS. The gesture-invariant part is what is computed once; the AFFECTED
      candidate sets and their verdicts are RECOMPUTED AS NEEDED PER MOVE, their size may change from
      move to move and may grow with the document. Per-move work is bounded by THAT MOVE's conservative
      candidate set (broad-phase extent candidates plus the affected Wall's own relationships), never by
      a fixed count and never by a remembered set that a later move has outgrown. The ONLY
      document-size-independent guarantee is zero repeated INVARIANT predicate evaluations after
      initialization — see §7 DETERMINISTIC (i)-(iii), which carries this wording, and §6 S3/S4.
0.8.2 THE SAMPLE-VERDICT MATRIX. Sample reuse and verdict reuse are tested as a FOUR-CELL matrix, one
      axis varied at a time (§6 S4): sample reuse ON/OFF with VERDICT MODE HELD FIXED, run separately
      for verdict OFF and for verdict ON; and verdict OFF/ON with SAMPLING MODE HELD FIXED, run
      separately for sampling ON and sampling OFF. Sampling requests SUPPRESSED BY VERDICT REUSE are
      accounted explicitly in that record, never left implicit. P23B.5's absolute invariants stay intact:
      no invariant is weakened, no hit is fabricated and no request is counted twice to make a run pass.
      RETAINING the sample requests is the APPROVED fallback and is what this slice does — the crossing
      gate's sampling is per-Wall and precedes its pair loop (§3), so skipping invariant PAIRS leaves the
      request set, the ratchet record and every absolute invariant exactly as P23B.5 shipped them, and
      `reuse:record` needs no re-record for this slice.
0.8.3 MEASUREMENT ROUTING AMENDMENT (owner ruling during S4, 2026-09-25). The authorized reply to the
      S6 numbers is a MINIMUM-REPS run, not another full sweep: after S4 the slice measures the ONE
      residual the numbers actually named — `commit-capture`'s 107–149 ms snapshot clone — and S5 runs
      only if that measurement (or the owner's own reproduction) names hit-test/snap. S7 records what
      that run measured and carries §7's CARRIED LIMITS; the full wall-authoring sweep is NOT re-run.
      S4's OR-3/OR-8 correctness evidence is unit-level and unaffected by this routing, and the
      `commit-capture` clone stays measured-beside/S6-adjacent rather than folded into S6's own scope.
```

## 1. Problem, and the evidence that motivates it

```text
1.1  THE PROPOSAL HALF IS ALREADY AFFECTED-SUBSET; THE PREFLIGHT HALF IS WHOLE-DOCUMENT (F-P5/F-P12).
     `transientArchitectureEdit` (`apps/editor/src/lib/editor/layout/layout-transient-edit.ts:131`)
     samples only the edited Wall through `proposeWallFirstArchitectureGeometry`, then runs
     `preflightWallFirstArchitectureCandidate` (`:158`) →
     `validateWallFirstTopology` (`packages/layout-core/src/layout-wall-first-precision.ts:1827`),
     which scans ALL Junction pairs,
     classifies ALL Wall pairs with the chord gate and samples EVERY curved Wall — PER POINTER MOVE.
1.1b P23B.5 S0 EVIDENCE (2026-09-25) — that per-pointermove sampling repetition is measured
     and the identity for reuse is REACHABLE. On the committed curved-40 fixture the preflight
     issues 40 sampling requests per pointermove; 38 are untouched Walls whose centerline OBJECTS
     are shared with the frozen baseline by the shallow splice — proven by cross-call REFERENCE
     equality of the actual `wall.centerline` input — and a bounded gesture-scoped owner would hit 76
     of 120 requests over a three-move drag with complete-result equality (`segmentId`, `length` and
     every sample field). Its S0 recorded the opportunity as PREFLIGHT-ONLY outside P23B.5's
     release-scope reuse, and the P23B.3 dependency map routes "gesture-scoped preflight" here;
     the owner then granted that narrower scope to P23B.5 on 2026-09-25 (plan §0.4) and P23B.5
     SHIPPED it (PR #90 squash-merged; closed stub + anchor `75fbd8a0`, tag `closed/p23b.5`) at the
     SAMPLE-level scope only: one bounded gesture-scoped
     `WallSamplingDerivation` threaded into `preflightWallFirstArchitectureCandidate`, with the
     S1 Wall-id key dimension, the S3 lifetime/reset proofs and a committed reuse-counter ratchet. What remains HERE is the wider
     per-gesture work — the affected-extent rule, reuse of unchanged VERDICTS and the wider
     interaction cost measurements — which this row still supplies evidence for. Do not re-own the
     sample store or add a second one; its safety prerequisites and measurement limits are
     recorded in the P23B.5 plan §0.2/§0.4 and §5 S5.
1.2  THE PREFLIGHT'S INPUT IS A CONSTANT FOR THE WHOLE GESTURE — VERIFIED IN SOURCE.
     The call passes the **frozen pointer-down baseline** ("The frozen pointer-down baseline document —
     never the live preview", `layout-transient-edit.ts:134`), and
     `spliceWallFirstArchitectureCandidate`
     (`packages/layout-core/src/layout-wall-first-precision.ts:1059`) is a **shallow**
     splice: changed records are replaced, everything else is shared by reference. The document itself
     does not change during a drag; only the patch's affected records do.
     ⇒ The work that scans pairs BOTH of whose members are outside the patch is re-deriving a verdict
       that cannot have changed, on every single pointermove.
1.3  THE PREFLIGHT IS ALLOWED TO MISS BUT MUST NEVER BE WRONG. Its own contract states it is "sound by
     construction ... so a failure here is a failure at release too. A miss is merely pending, never
     wrong" (`packages/layout-core/src/layout-wall-first-precision.ts:1096-1102`). Any scoping must preserve exactly that
     asymmetry: it may decline to refute, but it must never produce a `pending` status where the current
     code produces `known-invalid` for a pair the gesture actually affected.
1.4  HIT-TEST AND SNAP ARE CHEAP BUT PER-FRAME (F-P11, M-2 of P23B.3). 200 Plan hit queries measured
     13.8/14.0 ms (E1) and 23.9/24.5 ms (E2) ⇒ ≈0.07–0.12 ms per query; warmed snap ≈0.2/0.4 ms. Neither
     is a leading suspect, and the snap index is already memoized by geometry identity.
1.5  THE INSTALL SEAM IS RE-DERIVED, NOT UNTAGGED (UN-1 WITHDRAWN; UN-2 PARTLY ANSWERED). P23B.1's
     `adapter-plan-apply` → `preview-install` gap is STALE: re-derived by SYMBOL, the planner
     (`planArchitectureEditTarget`), `deriveInstallBundle` and the preview-install commit
     (`commitPreviewBundle`) all sit inside the `plan-apply` boundary since W4. The measurement-only
     step then priced what sits outside it when the boundary is read by symbol: the commit path and the
     between-action reactive restore. P23B.1's line anchors must not be cited.
1.6  THE CONFIRMATION THAT NO GESTURE SEMANTICS MAY CHANGE. Deterministic selection and history are
     product contracts; a drag's cost is not a reason to alter what is committed, when, or how it is
     undone.
```

## 2. Scope, non-goals, dependencies

```text
IN
  · GESTURE-SCOPED preflight: compute the gesture-invariant part of the canonical topology gate ONCE
    per gesture and evaluate only the affected subset per pointermove (M-2 driven per gesture)
  · the affected-subset definition derived from EXTENT (what the patch can move), never from locality
    (synthesis T-2: F-C3 is the counterexample that Room-local reasoning misses)
  · measurement of the per-gesture breakdown across the drag lifecycle on the committed fixtures
  · hit-test / snap / preview-install measurement and, only if measured to matter, work there
  · the per-gesture COMMIT-PATH cost: the duplicate mesh build the release's restore causes (S6, named
    by S1 — §0.2). The fix must be an EQUIVALENCE of work, never a change to what a release commits
OUT
  · changing gesture semantics, the drag threshold, what a release commits, or the status vocabulary
  · changing hit-test priority, snap candidates or selection determinism
  · installing geometry on a pointermove (the transient path installs nothing — that is a design fact)
  · Worker-based interaction (P23B.8's decision) · rendering (P23B.6) · any budget
  · ANY IMPLICIT JOIN, and any new selection UX. Dragging an INDEPENDENT group must not acquire
    connectivity, node, merge or alter the other group (Option E revision; decision record §2.10).
    Where geometry overlaps, AMBIGUOUS PICKING may be IDENTIFIED as a finding — resolving it with a new
    picking/disambiguation interaction is a capability, not part of a performance slice, and is NOT
    created here
  · phantom rejections. A gesture must not be refused merely because it would move one independent
    group across another; if today's shipped gates do refuse that, the refusal is recorded as CURRENT
    BEHAVIOUR (the T2 case) and not silently reinterpreted
DEPENDENCIES (all SHIPPED as of 2026-09-25 — §0.1)
  hard  P23B.4's extent-pruning primitive (OR-3 proves it); this slice must not invent a second scoping
        rule, and must not define its own key grammar. P23B.4's M-2b verdict reuse was DROPPED at its
        X-6, so there is no landed verdict-reuse implementation to build on — M-2 here is the FIRST.
  hard  P23B.0-durable fixtures + measurement capability (SHIPPED). The old "untagged derive/install gap
        mark" is WITHDRAWN — re-derive it by symbol (§0.4/§1.5).
  hard  P23B.5's shipped preflight-only sample scope: CONSUME the one bounded gesture-scoped sample
        owner; add no second store and re-key nothing.
  conditional  the curved-crossing ruling where a reused verdict is NEGATIVE and pre-existing
  met          the Option E ARRANGEMENT (D-11): the policy landed BEFORE the durable baseline, so by the
               time this slice runs the GROUP-SCOPED validator is the shipped one, and the gesture-scoped
               extent rule is re-stated against it — "which claim can this gesture affect" then depends
               on the group model too. If the policy had NOT landed, the document-scoped validator would
               be shipped instead; the slice states which one it ran against
  met          the RECONCILIATION IDENTITY PRECONDITION (D-12, decision record §4.2.2): dragging one
               independent Room across another must not alter either Room's persistent identity. The
               policy slice has already discharged OR-D12-1…6 before this slice runs; this slice consumes
               it and asserts only that its own scoping introduced no identity change of its own
```

## 3. Exact current entry points and ownership boundaries

ANCHORS RE-VERIFIED BY SYMBOL 2026-09-25 (P23B.1's line anchors are STALE — do not cite them).

| Stage | Entry point | Boundary |
|---|---|---|
| gesture begin | `beginArchitectureEditGesture` `LayoutPlanViewport.svelte:955` | captures the immutable baseline + ONE transaction |
| pointer move dispatch | `previewArchitectureEdit` `LayoutPlanViewport.svelte:1090` (mark `pointermove-bend`/`pointermove-rigid` `:3682`) | contains proposal + preflight |
| transient derivation | `transientArchitectureEdit` `layout-transient-edit.ts:131` (marks `proposal-derive:150`, `preflight:158`) | `apps/editor`; owns the baseline reference |
| proposal sampler | `proposeWallFirstArchitectureGeometry` `packages/layout-core/src/layout-wall-first-precision.ts:941` → `sampleProposedChain` `:1136` (private) | `layout-core`; already affected-subset |
| preflight | `preflightWallFirstArchitectureCandidate` `:1105` (mark `preflight-topology:1125`) | `layout-core`; **the mechanism's home** |
| shallow splice | `spliceWallFirstArchitectureCandidate` `:1059` (private) | `layout-core`; the shared-by-reference fact that makes gesture scoping sound |
| topology gate | `validateWallFirstTopology` `:1827` · `detectWallCurveTopologyCrossings` `:2102` | `layout-core`; the ONE crossing authority |
| baseline restore | `restoreTransientArchitectureBaseline` `layout-transient-edit.ts:183` (guarded by `layoutPreviewSnapshotMatchesLive` `layout-preview-state.svelte.ts:549`) | identity-guarded no-op when already baseline |
| release | `releaseArchitectureEdit` `layout-transient-edit.ts:240` (at most ONE planner call) · `commitArchitectureEditGesture` `LayoutPlanViewport.svelte:1155` (mark `gesture-commit:1190`) · `planArchitectureEditTarget` `:915` (wraps the canonical call in `p23bMeasureActivePlanApply` → `plan-apply`) | `apps/editor` |
| install seam | `derivePreviewBundle` `layout-preview-state.svelte.ts:749` · `deriveInstallBundle:811` · `commitPreviewBundle:840` (marks `preview-install:986/1225/1292`) | RE-DERIVED BY SYMBOL: planner, `deriveInstallBundle` and the preview-install commit all sit INSIDE `plan-apply` since W4 — the old `adapter-plan-apply` → `preview-install` gap (UN-1) is WITHDRAWN |
| commit path (S6's target) | `captureLayoutPreviewSnapshot` `layout-preview-state.svelte.ts:2745` (mark `capture`) · `restoreLayoutPreviewSnapshot:2767` (`baseline-restore`; `restore-reactive-write:2779`, `restore-mesh-install:2797`) · `commitLayout` `store/history-controller.svelte.ts:174` (`commit-matches:183`, `commit-replace:192`) · `commitLayoutTransaction` `app/PlanWorkspace.svelte:429` (mark `commit-capture:437`) | the priced commit path: the restore's mesh install MISSES the install's cached identity (STATE-SIDE, §0.2) |
| wall-authoring release | `p23bClick` `LayoutPlanViewport.svelte:4173` (mark `authoring-release:4184`) | the authoring path has NO `plan-apply` boundary — the whole release is one node (S1) |
| identity probe (DEV) | `p2311ObserveMeshIdentity` `layout/p23b-mesh-identity.ts` | DEV-only; the pin's source of truth, no production effect |
| hit-test | `selection-hit` `LayoutPlanViewport.svelte:3220` over the Plan hit path (`apps/editor/src/lib/editor/layout/plan-hit.ts`; budget metric `hit-test`) | unchanged by this slice |
| snap | `resolveLayoutSnap` `packages/layout-core/src/layout-snap.ts:597` · memoized `wallSnapIndex:580` | unchanged; the model for identity-keyed reuse |
| overlays | `layout-interaction.ts` · `plan-overlays.ts` | render-only; must stay render-only |

## 4. Proposed mechanism, and the alternatives

```text
M-2 (per gesture)  GESTURE-SCOPED CANONICAL PREFLIGHT.
     Split the preflight's verdict set into:
       (a) GESTURE-INVARIANT pairs — pairs neither of whose members the patch can move. Their verdict is
           computed ONCE at gesture start (or on the first derivable move) against the frozen baseline
           and reused for every subsequent pointermove;
       (b) GESTURE-AFFECTED pairs — everything whose extent the patch can enter, plus the affected
           Wall(s) themselves. Recomputed per move through the SAME canonical gate and the SAME
           predicate.
     Why this preserves the contract: (a)'s verdicts are derived from inputs that provably did not
     change (the shallow splice replaces only the patch's records), so a reused verdict is the same
     verdict; (b) is always evaluated, so no pair the gesture affected is skipped. The preflight keeps
     its "failure here is a failure at release / a miss is merely pending" asymmetry unchanged.
     ALTERNATIVE (i): a full cached verdict map keyed per pointer move — rejected: it caches what changes.
     ALTERNATIVE (ii): Room-local scoping — REJECTED outright; F-C3 is the counterexample that makes
       locality unsound for exactly this gate.
     ALTERNATIVE (iii): leave the preflight whole-document and only thin the proposal half — still the
       FALLBACK if OR-3 cannot prove equality (synthesis §8.6 AB-2). Its ORIGINAL premise (F-P5: the
       preflight is the growing per-move term) is NARROWED by S1: the per-move preflight is small next to
       the commit path (§0.2/§1.1b), so M-2 is no longer the biggest single lever here and must be
       justified on INCREMENT-PROOF terms (OR-3/OR-8), not on an assumed dominance.
EFFECT-FREE-UNTIL-PROVEN RULE
     Land the mechanism so that an un-scoped call path still behaves exactly as today; the scoping is
     supplied by the caller (the gesture), never silently inside the canonical gate.
PRESENTATION (NOW THE NAMED FIRST TARGET — measured 2026-09-25)
     S1 has run (as the measurement-only step, extended to the wall-authoring release) and NAMED the
     cost this line only anticipated: on every accepted edit the full Wall-mesh set is built twice, the
     second build MISSES the identity-keyed cache inside `commit-replace`'s restore, and the commit path
     is the majority of a drag release (`commit-capture` + `commit-replace` = 266.6 ms of 341.6 ms
     curved — §0.2). So S6 is no longer CONDITIONAL. The fix is per-gesture commit/history work, which is
     exactly why it belongs here and not in P23B.6 (per-frame rendering). The proxy/miss mechanism is
     pinned DEV-only in `p23b-mesh-identity.ts`; the fix must be an EQUIVALENCE of work and must not
     change WHAT is committed.
NOT PROPOSED
     no change to hit-test priority, snap candidates, selection or history; no debouncing of pointermove
     that drops frames of feedback; no moving gesture work off the main thread (P23B.8).
```

## 5. Correctness invariants and equivalence proof obligations

```text
INV-1  IDENTICAL VERDICT AND STATUS. For any gesture, the scoped preflight's returned failure (or
       `undefined`) equals the whole-document preflight's for the same patch — including the code and
       message. A `pending` must never replace a `known-invalid` that today's code produces.
INV-2  No pair the patch can affect is ever skipped (the extent rule is conservative, not tight).
INV-3  The one crossing authority remains `detectWallCurveTopologyCrossings`; scoping changes WHICH
       PAIRS are evaluated, never the predicate.
INV-4  Gesture semantics unchanged: at most one planner call per release; exactly one of commit/cancel;
       exact baseline restored on refusal; no history entry for a refused or sub-threshold release.
INV-4b (Option E revision) A drag never creates, removes or re-targets CONNECTIVITY. No gesture may join
       two independent groups, share a Junction between them, or leave the document with a connectivity
       change the creator did not explicitly request — regardless of how much the groups overlap.
INV-5  Selection/history determinism unchanged; no new persisted or generated state.
INV-6  Hit-test and snap behaviour unchanged unless a measurement names them.
INV-7  Plan/3D/visitor parity; no editor code in `layout-core`; visitor bundle gate green.
OR-3   full vs scoped preflight on: (a) committed E1/E3(C), (b) a patch edited toward an inter-Room
       crossing (F-C3 shape), (c) a baseline that ALREADY carries a crossing, (d) a patch that produces
       self-intersection / duplicate junction points / a zero-length Wall — identical verdict+code+message
       in every case. THE EXISTING FULL PREFLIGHT IS THE ORACLE (P23B.2 §Q3/§E: use exhaustive
       validation as the differential oracle), and the emitted ISSUE ORDER must also match, not only the
       verdict.
OR-4   atomicity of refusal (RT-1 R-b): a refused release leaves document, identity, history and preview
       state unchanged. This is P23B.2 §E's independent requirement verbatim ("Unchanged document,
       selection and history after refused operations").
OR-8   gesture lifecycle: pointerdown → many moves → cancel and → release both end with no residue and
       no stale gesture-scoped state; two consecutive gestures on different Walls do not share a verdict;
       an overlapping/rapid gesture sequence must not let one gesture's scoped state answer for another.
OR-7   parity + bundle gates.
OR-9   (NEW, from P23B.2 §E) CROSS-ROOM IS MANDATORY IN THE CASE SET: the differential set must include
       edits that change a Wall's relationship to a Wall in ANOTHER Room and edits to CONNECTED
       JUNCTIONS, because "Room-local tests alone are insufficient" — the same warning F-C3 makes from
       our own code.
OR-10  (NEW, from the same principle) REJECTED-EDIT RESTORATION: after a refusal the frozen baseline is
       reinstated EXACTLY and no gesture-scoped verdict survives into the next gesture.
```

## 6. Implementation steps (each independently committable and reviewable)

```text
REFERENCE-FIRST RULE (same rule as P23B.4; a step that violates it cannot merge)
    No oracle may be merged that requires code which does not exist yet, and no mechanism may be merged
    without its green oracle in the SAME commit. OR-3 is written as "scoped preflight vs the current
    whole-document one", which cannot be green before the scoped preflight exists. So the reference is
    frozen first (S2) from TODAY'S whole-document behaviour, and the COMPARISON is added by the step that
    introduces the scoped path (S4), in that commit.
EXECUTION ORDER (AMENDED 2026-09-25 — matches the named first target; steps are NOT renumbered)
    S1 (EXECUTED) → S2 → S6 + its regression proof → S6's own measurement → S3 → S4 → S5 (only if
    named) → S7.
    Why: S1 named the commit-path duplication the biggest lever here, so S6 is executed FIRST and
    measured independently BEFORE any topology work begins. S2 stays ahead of S4 because it is a
    zero-risk tests-only freeze and is still S4's oracle. The REFERENCE-FIRST RULE is unaffected: S4
    still may not merge without its comparison against S2's frozen reference in the same commit.
    S6 has NO "no-change" exit while its mandatory oracle still asserts one build. A fix that cannot be
    made without changing what is committed is an ABANDONMENT disposition — mechanism abandoned, the
    failing one-build oracle retained as a DOCUMENTED BLOCKED CASE, reported to the owner for a ruling.
    That disposition is NOT acceptance and does not satisfy X-1.
S1  MEASURE PER GESTURE (no production change) — EXECUTED 2026-09-25
    Ran as the measurement-only step, extended to the wall-authoring release, and CLOSED (PR #91
    squash-merged; closed stub + anchor `1d0fb220`, tag `closed/p23b-measurement`). It reported the
    per-move proposal, the per-move preflight, the derive/install seam, the reactive restore and the
    release chain separately, by containment rather than a sum of pooled distributions, and it added the
    DEV geometry-identity pin the owner extended it for. OUTCOME: the transient preflight is small next
    to the commit path, which is the majority of a release, and the commit's second mesh build MISSES
    the cache. §0.2 carries the numbers. S1 does NOT need re-running; re-measuring is S7.
S2  FREEZE THE REFERENCE — TODAY'S WHOLE-DOCUMENT VERDICTS (tests only)
    Record the current whole-document preflight's verdict, issue code, message and ISSUE ORDER for all
    four OR-3 cases (a)–(d), plus OR-8's lifecycle baseline. Green on landing, because it asserts today's
    code. This frozen set IS the oracle side of the later differential: nothing scoped may be compared
    against another scoped variant.
S3  AFFECTED-EXTENT DERIVATION (runs AFTER S6 and its proof, per the amended order)
    Compute the gesture's affected extent from the patch (which Walls/Junctions the intent can move),
    conservatively, in `layout-core` beside the splice. Unit-test it against pathological intents
    (junction move, wall move, bend insert, knot move/delete).
S4  GESTURE-INVARIANT VERDICT SET AT GESTURE SCOPE (runs AFTER S6 and its proof, per the amended order)
    Compute the invariant part once per gesture; evaluate the affected subset per move. Tests, IN THIS
    COMMIT: OR-3's whole-document comparison against S2's frozen reference (all four cases, verdict +
    code + message + issue order), OR-8, the transient-preview and transient-allocation suites, and the
    gesture suites (`layout-wall-edit-gesture`, `layout-lifecycle-semantics`). The scoped path may not
    merge without that comparison in the same commit.
    SAMPLE-REQUEST-CONTINUITY OBLIGATION (G-2 corrected; §0.8.2). A FOUR-CELL MATRIX, ONE AXIS AT A
    TIME — a single run can attribute a change to neither sample reuse nor verdict reuse:
      AXIS A — SAMPLE REUSE (exactly what P23B.5's ratchet measures), run TWICE: once with VERDICT
      MODE OFF and once with VERDICT MODE ON, the verdict mode HELD FIXED inside each run. The
      sampling-ENABLED run owns `derivations`, `hits` and `preflightMissesPerMove` (the per-move scope
      misses that `sum(preflightMissesPerMove) === derivations` compares); the sampling-DISABLED run owns
      `unscopedPreflightRequests` ALONE. A scope miss may NEVER be sourced from the sampling-disabled
      run — doing so makes `sum(preflightMissesPerMove) === derivations` fail as soon as reuse works.
      AXIS B — VERDICT REUSE (the thing S4 introduces), run TWICE: once with SAMPLING ON and once with
      SAMPLING OFF, the sample scope HELD FIXED inside each run. The correctness oracle is OR-3's
      differential against S2's frozen reference (verdict + code + message + issue order) plus the
      rendered-attempt equality — NEVER the ratchet's counters, which cannot see a verdict change.
    Consequences:
      · both axes are RE-DERIVED at the S4 revision through the SAME production preflight: the
        sampling-disabled reference is never a retained pre-S4 path (which would stop proving
        production behaviour), and it is never asked for a scope miss;
      · SUPPRESSED REQUESTS ARE ACCOUNTED, NOT ASSUMED AWAY. The record states the pre-S4 request set
        against the S4 set per cell and names every request verdict reuse removes. THE APPROVED
        FALLBACK IS WHAT SHIPS: this slice RETAINS the preflight's sample requests (the crossing gate
        samples per Wall, before its pair loop), so verdict reuse removes NO request, the recorded
        ratchet is reproduced byte-for-byte and no invariant changes meaning. If a future revision ever
        does suppress a request, it must PROVE that request redundant (the scope would have served it
        as a hit) and carry the reduction through `reuse:record --reason "…"`;
      · P23B.5's ABSOLUTE invariants (`apps/editor/src/lib/bench/p23b5-reuse-ratchet.ts` —
        `derivations + hits === unscopedPreflightRequests`, `sum(preflightMissesPerMove) === derivations`,
        `coldMisses + refusals === derivations`, `entries === derivations`, the rendered-attempt
        equality) are PRESERVED VERBATIM: a violation is a BUG in S4, never a re-record, and no hit is
        fabricated and no invariant weakened to make a cell pass;
      · with the verdict mode held fixed, Axis A must satisfy EVERY invariant on its own; with the
        sample scope held fixed, Axis B must satisfy the OR-3 differential and the rendered-attempt
        equality.
S5  (CONDITIONAL) HIT-TEST / SNAP WORK — only if S1 names them.
S6  PREVIEW-INSTALL / REACTIVE WORK — THE NAMED FIRST TARGET (S1 named it; executed FIRST in the
    amended order, before S3/S4)
    Fix the duplicate build: the identity the commit hands `installWallMeshes` is a `$state` proxy of the
    compile's geometry, so the restore's rebuild MISSES the identity-keyed cache the install filled. Work
    at the commit/history boundary (the object identity the install and the restore agree on, or the
    snapshot's own geometry) so the restore HITS. Per-gesture commit/history work → belongs here, not
    P23B.6 (per-frame). Must preserve: what a release commits, undo/redo determinism, and the
    capture/restore round-trip contract the existing tests pin. The `commit-capture` snapshot clone
    (107-149 ms per accepted edit) is measured beside it and may be recorded as unaddressed if it is out
    of this step's scope.
    REQUIRED REGRESSION PROOF (G-1) — the oracle is NOT the existing test. `layout-transient-preview.test.ts`
    already pins the cache's identity semantics on PLAIN state and asserts `mesh-prebuild` 0 on the
    commit path WHILE the shipped app rebuilds the full Wall-mesh set twice; read it as a semantics pin
    and never as this fix's proof. S6 must add a REGRESSION TEST whose preview state is created the way
    the app creates it — `$state(...)` (`EditorApp.svelte:248`) — and that:
      (a) FAILS BEFORE the fix: it asserts exactly ONE mesh build per accepted edit and observes two, so
          it is a real oracle rather than a restatement of today's green;
      (b) AFTER the fix proves exactly ONE build on the commit path and ZERO on the between-action
          restore (which already hits);
      (c) verifies undo/redo CONTENT is byte-identical across the committed edit — the fix changes the
          identity/work of the rebuild, never what is committed.
    PINNED COUNTING INTERVAL (oracle design, fixed NOW and not left to implementation): count mesh
    builds AFTER fixture initialization (warm-up outside the count), from BEFORE the accepted edit's
    install THROUGH the history commit. The assertion is TWO builds before the fix and ONE after it. The
    subsequent between-action restore is counted SEPARATELY and must be ZERO — never folded into the
    same interval (it already hits, so folding it in would hide the very regression being fixed).
    A plain-object or `$state.snapshot`-based harness does NOT satisfy (a)-(b): the reactive IDENTITY (a
    proxy vs the raw compile object) is exactly the difference the pin found.
    Then MEASURE this change on its own, BEFORE S3 starts: an INDEPENDENTLY ATTRIBUTABLE capture using
    the EXISTING containment model (`apps/editor/src/lib/bench/p23b-containment.ts`) and identity probes
    (`apps/editor/src/lib/editor/layout/p23b-mesh-identity.ts`) — `mesh-prebuild`/`prebuild-hit`/miss
    COUNT authoritative and ms advisory — recorded as S6's own record, independent of S7's slice-wide
    re-measure. NO topology change may precede this capture.
S7  RE-MEASURE AND RECORD
    Same fixtures, same provenance block; report the per-move and per-release numbers that moved, and
    those that did not.
```

## 7. Acceptance tests, differential tests and measurement

```text
MUST PASS
  OR-3 · OR-4 · OR-7 · OR-8 · OR-9 · OR-10
  gesture/interaction suites: layout-transient-preview · layout-transient-allocation ·
  layout-wall-edit-gesture · layout-lifecycle-semantics · layout-bend-command ·
  layout-interior-anchor-release · plan-keyboard-navigation (its shape-pin caveat applies: drive the
  path live where the test only slices the viewport)
  the S6 REACTIVE regression test: a `$state`-backed preview (install → capture → history commit) that
  fails before the fix, proves ONE mesh build after it, and re-checks undo/redo content; the existing
  plain-state cache-key test is a semantics pin, never this gate
  full applicable suite · `npm run test:arch` (always) · `npm run check` · root build ·
  `npm run test:perf` green
DETERMINISTIC
  SEPARATE INITIALIZATION FROM STEADY STATE (corrected 2026-09-25 — G-4 and §0.8.1; the affected-work
  bound corrected for the candidate-pair reality).
    (i)  INITIALIZATION (building the gesture-invariant set) may be O(document) and happens ONCE per
         gesture — assert only that it never repeats per move;
    (ii) INVARIANT PREDICATE EVALUATIONS on moves 2..k must be ZERO (every invariant result is reused).
         This is THE scaling guarantee and the ONLY clause claimed to be independent of document size;
    (iii) AFFECTED CANDIDATE SETS AND VERDICTS ARE RECOMPUTED AS NEEDED FOR EACH MOVE: the affected set
         is re-derived from THAT move's patch, its size may CHANGE ACROSS MOVES (a later move can enter
         extents the earlier ones did not — the test set covers that), and it is bounded by the
         CONSERVATIVE CANDIDATE-PAIR SET the extent rule admits for that move (broad-phase plus the
         affected Wall's own relationships). That set MAY GROW WITH THE DOCUMENT — one moved Wall can
         have candidate relationships with arbitrarily many Walls in its component — so it is NOT
         claimed document-independent, and the assertion is NOT a fixed count. Two assertions are made:
         the per-move count is bounded by that move's own conservative candidate set, and invariant
         predicate evaluations do not repeat (clause ii).
  Each clause is a RELATIVE, deterministic assertion in the P23.11 style, not a wall-clock floor.
  Sample-request counts are NOT asserted here; they are gated by the P23B.5 ratchet (§6 S4, Axis A).
  S6 BUILD COUNT (G-1): under `$state`-backed preview state, on the PINNED COUNTING INTERVAL (§6 S6),
  an accepted edit performs exactly ONE Wall-mesh build and the separately-counted between-action
  restore performs ZERO; the pre-fix value is TWO.
MEASUREMENT (ADVISORY)
  per-move and per-release wall-clock p50/p95 on the committed fixtures, with provenance and mark
  nesting stated. The 64–314 ms browser long tasks remain NON-ATTRIBUTABLE and must not be cited.
  CARRIED LIMITS (measurement-only step): all-curved-40 aborts on the driver's own 6000 ms action guard
  (a coverage limit, not raised); revision-2/3 millisecond magnitudes run 40-85% above revision 1 and
  the P23B.0 baseline, so the DURABLE result is the per-edit build COUNT and the geometry identity, not
  the ms; every number is single-machine / single-session and advisory; `g3-baseline.json` is neither
  read nor rewritten by this slice (`bench:record` stays its only writer) and no budget metric is added.
```

## 8. Regression and resource-lifetime risks

```text
R-1  A scoped preflight that MISSES a genuine failure changes visible feedback from "known-invalid" to
     "pending" (the drag renders as if fine, the release then rejects). That is a UX/semantics
     regression even though the release verdict is unchanged. OR-3 case (b) is the guard.
R-2  A reused NEGATIVE verdict on a baseline that ALREADY carries a crossing (OR-3c) is where the
     curved-crossing ruling enters — but a ruling does not change the validator (decision record §4.1).
     The test is what the SHIPPED gates do with such a baseline, and reuse is admitted only if
     equivalence is proven against the current FULL validator AND the shipped behaviour yields a stable
     verdict to reuse. A "reject at ingress" policy that has not landed leaves today's admission in
     place, so the reuse must be re-derived, not inherited (synthesis §8.6 AB-3).
R-3  Gesture-scoped state must not outlive the gesture (OR-8). A map keyed on a patch or a Wall that
     survives across gestures can serve a stale verdict for a DIFFERENT baseline.
R-4  The shallow splice shares records by reference with the baseline. Any scoping that assumes
     "unaffected means untouched" must rely on that splice's guarantee, not on the live preview being
     unchanged — the live preview changes on a genuine multi-step gesture.
R-5  Mark nesting: adding marks inside the preflight could double-count `preflight`/`preflight-topology`
     and make the advisory numbers unreadable.
R-6  Debouncing or coalescing pointermove work would change the drag's response even if it reduced cost;
     it is out of scope unless the owner accepts the interaction change explicitly.
R-7  Selection/history: a refused release must remain atomic and produce no history entry (INV-4/OR-4).
R-8  Reactive cost (UN-2) may dominate; if so, record it rather than optimizing a smaller term.
     PARTLY RESOLVED by S1: it does not dominate the release — the commit path does (§0.2).
R-9  (from S1) FIXING THE COMMIT-PATH MISS by forcing identity agreement could change what the cache
     retains, the snapshot's meaning, or the capture/restore round-trip the tests pin. The fix must be an
     EQUIVALENCE proved by the existing `layout-transient-preview` / `layout-lifecycle-semantics` /
     undo-redo suites plus a new pin that the restore HITS the install's identity — never a weakened
     comparison that makes a MISS look like a HIT. The pin is the `$state`-BACKED regression test S6
     requires (§6 S6): a plain-object harness passes today while the app rebuilds twice, so it cannot
     guard this fix.
```

## 9. Exit criteria and what must be recorded before advancing

```text
X-1  All MUST PASS green; commands recorded.
X-2  OR-3/OR-4/OR-8 results recorded, including any case where the scoped and full verdicts differed and
     how the extent rule was corrected (or the mechanism abandoned under synthesis §8.6 AB-2).
X-3  Per-gesture measurements recorded with provenance and mark nesting; the moved and unmoved costs
     stated explicitly. (S1's record exists — the measurement-only step's closed stub. S6's change is
     measured on its OWN before S3 starts; S7 re-measures the slice as a whole, carrying §7's CARRIED
     LIMITS.)
X-4  The rationale for NOT touching hit-test/snap/preview-install recorded, so no later slice
     re-litigates it blindly. PARTLY DONE: S1 named preview-install (S6) as the FIRST target and left
     hit-test/snap unmoved (§1.4); the remaining rationale is stated at S7/closeout.
X-5  Ruling status recorded (it governs reused negative verdicts).
X-6  Slice closed on the same branch with an identifiable commit series; slice-closeout applied.
```

## 10. Explicitly deferred

```text
- moving gesture work off the main thread → P23B.8's decision (entry condition: what remains after this
  slice, P23B.4 and P23B.5; the measurement-only step's per-gesture ranking is the starting point)
- pointermove debouncing / interaction-model changes (a semantics change, not an optimization)
- hit-test/snap re-architecture (both are structurally cheap today)
- rendering-side work → P23B.6 · cache/identity policy → P23B.5
- any ENFORCED interaction budget → owner ratification
```
