# P23B.7 — Interaction optimization (CLOSED)

```text
AUTHORITY: NONE — closed-work stub. The live contracts are owned in code
(`createWallFirstArchitectureVerdictScope` · `wallFirstArchitectureAffectedExtent` ·
`validateWallFirstTopologyPass` in `packages/layout-core/src/layout-wall-first-precision.ts`; the
commit-path mesh identity + the model-free capture in
`apps/editor/src/lib/editor/layout/layout-preview-state.svelte.ts`; the gesture wiring in
`LayoutPlanViewport.svelte` + `layout-transient-edit.ts`) and by the test-enforced proofs routed
from the phase README. P23B.5's reuse ratchet stays LIVE. Full body recoverable via the recovery
line; deliberation lives in Git/PR history.
```

## Delivered

P23B.7 shipped on the dedicated `P23B.7` branch (one PR: #92), after the owner returned it for
correction once (two P2 findings) and then ACCEPTED the corrected head with no remaining blocker:

- **S2 — frozen preflight reference** (tests only): OR-3 (a)–(d), OR-8 and the issue-order row
  pinned with status, code and verbatim message; the case table is shared with S4 through
  `p23b7-preflight-reference-cases.ts`.
- **S6 — commit-path duplicate mesh build fixed**: the identity a restore reads back off the `$state`
  proxy is mapped to the compile's own geometry (`wallMeshIdentities` WeakMap + `wallMeshCacheKey()`),
  so the commit's restore HITS the cache the install filled. Its own independent browser capture ran
  on a clean tree at `d6f65426`, BEFORE any topology change: 200 accepted edit actions → exactly one
  build each, 0 inside `commit-replace` (204 installs → 204 builds; 475 restores → 475 hits / 0 builds).
- **S3 — affected-extent derivation**: `wallFirstArchitectureAffectedExtent` derives one intent's
  conservative candidate set from the same patch the proposal/preflight splice; scoped per move,
  refused (not guessed) when the intent is underivable.
- **S4 — gesture-scoped verdict set**: clean initialization once per target with the canonical
  whole-document pass, then every later move evaluates only its own re-derived extent through the
  same predicates; a non-clean baseline or a target change stays on the canonical pass — the
  approved fallback (no request suppressed, no failure approximated). Differentially equal to the
  whole-document verdict on the frozen table; the four-cell sample/verdict matrix reproduces P23B.5's
  committed ratchet BYTE-FOR-BYTE through the scoped path (no re-record, zero suppressed requests).
- **S7 — targeted attribution + the owner-directed fix**: `commit-capture`'s payload was 99.4 %
  derived `model` (3,412,257 B / 39,106 objects) with no production reader; the review-time bounded
  fix removed the model clone entirely (restore already re-projected from shared geometry), with its
  oracle and a permanent relative-bound guard. Post-fix payload 22,022 B / 643 objects.
- **PR #92 correction round** (`ee0dbecd`): injective tuple target identity (a delimiter collision
  could reuse an initialization and report `pending` where the canonical gate refuses) and
  predicate-site evaluation evidence (`WallFirstTopologyEvaluation`; `stats.candidates` is the
  bound) — each mutation-verified.

S5 was NOT taken — the measurement named the capture clone, not hit-test/snap.

## Rulings (full bodies via recovery)

```text
§0.8.1  RECOMPUTED AFFECTED SETS. The gesture-invariant part is computed once; affected candidate
        sets and their verdicts are recomputed per move, may change size and may grow with the
        document. The only document-size-independent guarantee is zero repeated INVARIANT predicate
        evaluations after initialization (DETERMINISTIC (i)-(iii)).
§0.8.2  THE FOUR-CELL SAMPLE/VERDICT MATRIX, one axis at a time, with suppressed requests accounted
        explicitly; P23B.5's absolute invariants preserved VERBATIM; RETAINING the sample requests is
        the APPROVED fallback — which ships, so the committed ratchet needed no re-record.
§0.8.3  MEASUREMENT ROUTING. One minimum-reps run of the residual S6's numbers named
        (`commit-capture`); no second full wall-authoring sweep; S5 only if that run names
        hit-test/snap.
§0.9    REVIEW-TIME FIX DIRECTION. The bounded model-free capture fix lands on this branch as its own
        revertible commit, with its own oracle and a permanent guard; rescopes nothing.
§0.10   PR #92 CORRECTIONS. (a) A target's identity is an injective tuple of its fields; (b) the
        DETERMINISTIC clause is asserted against OBSERVED predicate evaluations, never candidate
        counts.
```

## Evidence (accepted head `ee0dbecd`)

```text
owner ruling        REVIEWED AND ACCEPTED 2026-09-25 with no blocking findings; routine
                    `slice-closeout` authorized on the same branch.
gates at the head   `npm test` 347 files passed | 1 skipped · 4,913 tests passed | 1 skipped ·
                    `check:layout-core` PASS · `npm run check` (editor + museum) 0 errors /
                    0 warnings · `test:arch` 23 files / 254 tests · `test:perf` 8 passed | 1
                    skipped, 62 passed | 1 skipped (ratchet reproduced; no re-record) · root
                    `npm run build` PASS (adapter-vercel).
reviewer evidence   independently reproduced the lanes at `73a5167c` (full suite 4,912
                    pre-correction) and both mutation styles; accepted the non-blocking
                    dispositions.
closeout            re-ran NOTHING: closeout changes documentation only; the recorded gates above
                    are reused (owner instruction).
```

## Entry points

```text
implementation   packages/layout-core/src/layout-wall-first-precision.ts
                   (verdict scope · affected extent · topology pass · target identity · observer)
                 apps/editor/src/lib/editor/layout/layout-preview-state.svelte.ts
                   (commit-path mesh identity · model-free capture · restore)
                 apps/editor/src/lib/editor/layout/LayoutPlanViewport.svelte
                 apps/editor/src/lib/editor/layout/layout-transient-edit.ts
                   (gesture scope build/release · preflight threading)
validation       apps/editor/tests/lib/layout/p23b7-verdict-scope.test.ts
                 apps/editor/tests/lib/layout/p23b7-verdict-scope-wiring.test.ts
                 apps/editor/tests/lib/layout/p23b7-affected-extent.test.ts
                 apps/editor/tests/lib/layout/p23b7-preflight-reference.test.ts (+ cases helper)
                 apps/editor/tests/lib/bench/p23b7-sample-verdict-matrix.test.ts
                 apps/editor/tests/lib/editor/layout/p23b7-mesh-identity-regression.test.ts
                 apps/editor/tests/lib/bench/p23b7-capture-attribution.test.ts
                 apps/editor/tests/lib/editor/layout/p23b7-snapshot-payload-guard.test.ts
live evidence    2026-09-25-s6-commit-path-identity-capture.json (SHA-256 `95790b00…`, stays LIVE)
```

## Residuals (named, with owners)

```text
- No post-fix BROWSER number for `commit-capture`: 107–149 ms stays S1's/S6's pre-fix evidence;
  a browser re-measure is owner-callable, not owed.
- Bounded heap-retention comparison (repeated edits · history eviction · clearing) — the review's
  named follow-up; no redesign proposed or started.
- Snapshot guard's stated limits: a name-based scan cannot establish universal absence of readers;
  the payload bound detects a re-introduced clone, not arbitrary discarded work.
- Total pointer-move work is NOT document-independent (component derivation, endpoint searches,
  candidate construction and sampling still traverse the document); rendering/adapter/flush →
  P23B.6; curved-wall slowness alone does not violate this slice's contract.
- S5 not taken: hit-test/snap named-but-unmoved, rationale recorded so it is not re-litigated
  blindly.
- P23B.6 and P23B.8 remain unratified; P23B.8 is a conditional decision anyway.
```

## Recovery

```text
git show ee0dbecd:<this path>          # full body as accepted (plan · S4 record · correction record)
git show 2fec2e6f:<path>               # S6 record's last full body
git show 73a5167c:<path>               # S7 records' last full bodies
git show closed/p23b.7:<path>          # any full body, as of the accepted head (tag target)
DEGRADATION (squash): branch commits are not ancestors of `main` post-merge. Recovery runs via the
PR head ref — git fetch origin refs/pull/92/head && git show <A>:<path>. tag closed/p23b.7 (targets
ee0dbecd — the accepted head; local only — not pushed).
```

## CLOSEOUT PRESERVATION — P23B.7 (2026-09-25)

```text
prose compacted:                 6   stubs at their own paths (plan + S4 · S6 · S7 · S7 follow-up ·
                                     correction records)
reference promotions:            0   contracts owned in code + tests; no docs/reference/** doc
                                     matched the slice's mechanisms
renderable evidence archived:    0 files (none existed; the S6 capture JSON is machine-readable
                                     cited evidence and stays LIVE, per the measurement-step convention)
evidence stubs / manifests:      0   (no archive copies needed)
transient artifacts removed:     0   (none existed; no active checkpoint to retire)
historical anchors:              ee0dbecd (accepted head; plan + S4 + correction) · 2fec2e6f (S6) ·
                                 73a5167c (S7) · all ancestors of tag closed/p23b.7 (target ee0dbecd;
                                 local only — not pushed)
new live → archive prose links:  0
manual-owed verification rows:   0   owner accepted with no blockers; closeout reuses the recorded
                                     gates and re-ran nothing (owner instruction)
remaining deferred items:        6   post-fix browser number · heap-retention follow-up · guard
                                     limits · document-dependent pointer-move work (rendering →
                                     P23B.6) · S5 not taken · P23B.6/P23B.8 unratified
```
