# P23B.0-durable — Final Stage A reconciliation and owner ruling

```text
STATUS:   OWNER RATIFIED 2026-09-24 — plan revision `4b32034f` and O-1–O-4 approved.
ROLE:     final Stage A fixture, workload, provenance and environment record; W1–W7 authorized.
NOT:      Stage B execution in this update · a benchmark baseline · a W2 fixture-contract test · an
          optimization · a new gesture or navigation path · a change to the authoritative phase SEQUENCE.
EXECUTION: Stage B has not started at the owner's direction. P23B.4 remains gated until P23B.0's baseline
           is accepted.
CODE BASIS: direct fixture-gate observations and source inspection at PR implementation HEAD
            `e4222a5799b61a14f85332238a9301683dec75f1`; the Stage A corrections are documentation-only.
AUTHORITY: plan → ./2026-09-22-P23B.0-durable-measurement-completion.md
           phase order / execution route → ../README.md §SEQUENCE and §Owner-authorized execution routing
           shipped policy → P23B.3a (PR #82 at `c11938fe`)
```

## 1. Review result and files

F1–F4 remain accepted as corrected:

- **F1:** historical E2 imported and compiled; its pre-existing crossing blocked later unrelated edits. It
  was not rejected at ingestion.
- **F2:** all six size × curvature control-matrix cells are CLASS 5. The `role` field distinguishes the
  four controls from the two timing cells; CLASS 2 is the separate post-policy regression set.
- **F3:** `E1C` remains the constructed 12-Wall fixture. A 40-Wall owner payload cannot replace it.
- **F4:** constructed-fixture verdicts remain EXPECTED until W2 executable assertions test their exact
  bytes. The supplied owner payload's current gate result is separately reported below because Stage A
  directly exercised those gates on its exact bytes.

This update records the owner ruling in the existing durable plan and handoff, the P23B phase status, and
the live work baton. It leaves the owner-supplied `40-walls.json` bytes unchanged and outside this
docs-only PR; W2 fixture work has not started. The phase `SEQUENCE`, including step 10 and the existing
continuation branch/PR arrangement, is unchanged. PR #86's description is updated to match the fixture
classes, owner-case scope and ratified status.

## 2. Owner payload identity and current gate result

The file `../40-walls.json` is the primary real-world responsiveness workload. The owner clarifies that
the measured historical payload had more junctions because its Rooms intersected; the newly supplied
document nevertheless feels nearly as sluggish in a manual whole-editor check at approximately 10 Rooms /
40 curved Walls. Selection, dragging and ordinary editing all contribute to the symptom. That UX result is
owner-reported; Stage A did not time an interaction.

At 61,140 bytes, the supplied file's raw SHA-256 is
`63f15ed8745d08bf5f65ab2c85829d1b9f1df6f36b3a9137147b70d5d09f5e05`; it differs from the archived E2
digest `37a857690ca255e0355f08519a195575d12e53b43ca9bb2f7820b18bb36dc27f`, so its proposed ID is
**`owner-40-curved-v1`**, separate from historical E2. The actual
`serializeWallFirstLayoutDocument` result is byte-for-byte the supplied file and has the same SHA-256. The
new id reflects exact-byte identity; the UX purpose is the same real-world responsiveness case.

The exact supplied bytes have this shape: 10 Rooms, 40 cubic-chain Walls, 40 Junctions, no Openings, and
10 independent four-Wall Room components. Room/Wall/Junction identities and references pass the codec;
curve-knot identifiers are unique within each Wall as required by its schema. No geometry was edited.

**Independently executed at `e4222a57`:** `validateWallFirstLayoutDocument` succeeded;
`validateWallFirstTopology` returned no issue under the shipped post-Option-E policy; and
`compileWallFirstLayoutGeometry` produced 10 Rooms, 40 physical Walls and 40 Junctions with zero issues.
This establishes current CLASS 5 validator/compiler eligibility for the exact payload. It does not establish
a measured response time or replace the W2 assertion that must later pin the exact bytes and verdict.

| Artifact | Proposed role / class | Observed or expected verdict | Baseline use |
|---|---|---|---|
| Historical E2 | CLASS 1 historical evidence | Pass B's archived observation: admitted/imported and compiled, then existing crossings blocked later unrelated edits | Never a new baseline; historical numbers stay outside baseline JSON |
| `owner-40-curved-v1` | CLASS 5 owner-responsiveness workload | **Observed directly at Stage A:** codec accepted, topology gate returned no issue, compile had zero issues | Primary real-world responsiveness input alongside the six cells; owner-ratified, with W2 exact-byte assertions still required |
| Six size × curvature cells | CLASS 5 controlled timing matrix | Expected until W2 assertions exercise each exact constructed fixture | Keep all six as controls/comparisons; do not replace with owner case |
| `N-2` / `N-2i` | CLASS 3 positive / CLASS 4 intrinsic negative | Expected until W2 exact-fixture assertions; shipped policy admits independent-component overlap and rejects intrinsic same-component crossing | Correctness only, never baseline |
| CLASS 2 regression set | CLASS 2 | Existing post-policy behavior remains owned by its current tests | Correctness only, never baseline |

The minimal plan amendment is one additional CLASS 5 owner-case input. “Only six performance fixtures” now
means “the six control-matrix cells remain intact, with one separately identified owner-responsiveness
case added.” “Historical E2 is never a baseline” remains unchanged: this payload has its own id and is not
relabelled E2. No correctness fixture joins either timing group.

## 3. Bounded interaction workload and measurement boundaries

The owner's symptom is whole-editor sluggishness. Bend/knot dragging is one representative edit, not the
symptom definition. Stage B should use the current shipped UI paths and the same `owner-40-curved-v1` bytes
for the primary responsiveness run:

| Path | Bounded representative action | Snap / Grid |
|---|---|---|
| Selection | Select a fixed boundary Wall or Room in the existing Plan viewport | N/A |
| Ordinary drag/edit | Use the existing wall-first Wall/Junction drag path in Plan on a fixed fixture entity | Snap 0.25 m / Grid on where the current path uses them; record the actual setting |
| Bend/knot edit | Use the existing Bend command to drag one fixture Wall's curve knot; capture moves and one accepted release when the edit is valid | Snap 0.25 m / Grid on as the reproducible starting condition |
| Wall authoring | Create a Wall through the existing Plan authoring path in a fixed clear region | Snap 0.25 m / Grid on |
| Plan viewport | Use the current Plan pan and zoom controls | N/A |
| 3D viewport | Use the existing guided PerspectiveCamera navigation path | N/A |

Pin the exact entity and interaction target against the fixture bytes in W2. Reset each edit sample to the
same fixture content; record reset work separately. Do not add a gesture, interaction mode, navigation
system or camera-motion implementation.

For pointer-driven edits, report pointermove/proposal/preflight separately from release. Where applicable,
keep reactive derivation, adapter CPU work, Svelte flush and browser frame boundaries distinct. `tick()`
marks Svelte flush completion; it does not prove GPU upload or a painted frame. A `requestAnimationFrame`
callback marks a frame boundary, not presentation. Claim presented-frame behavior only when a real browser
measurement surface provides that evidence; otherwise report it as unavailable. Selection and viewport
manipulation report only the boundaries that apply to them. Keep internal pipeline cost and end-to-end
interaction responsiveness as separate results.

## 4. Reference environment and O-1…O-4 dispositions

The current development machine is the proposed reference machine. These values were read from the live
Stage A environment, not copied from the historical reports:

| Field | Stage A observation / proposal |
|---|---|
| Device | MacBook Air, Apple M2, 8-core CPU, 16 GB RAM |
| OS | macOS 15.7.2 (build 24G325) |
| GPU | Apple M2 10-core GPU; Metal 3 support |
| Browser | Google Chrome 153.0.8010.54 installed and running |
| Node | v26.7.0 |
| Browser DPR / active renderer | Unknown for the editor workload: no editor tab is open in the inspected browser session |
| Warmup / sampling | No performance samples run in Stage A. Proposed interaction configuration: 5 warmups + 20 measured samples per path; record actual counts. Existing deterministic Node tier keeps its recorder configuration. |
| Session coverage | One named machine, one interactive browser session and one sample configuration across the six matrix cells and owner case |

| Call | Disposition |
|---|---|
| **O-1 — export identity** | **RATIFIED:** preserve the supplied file as new owner input `owner-40-curved-v1`; its raw hash differs from historical E2. Do not label it E2. Keep the six-cell controls and correctness fixtures separately identified. |
| **O-2 — workload / environment** | **RATIFIED:** use the owner case as the primary responsiveness input, retain all six CLASS 5 matrix cells, and use the current machine above as reference. Start relevant authoring paths at Snap 0.25 m / Grid on; record actual settings per path. |
| **O-3 — enforced timing budget** | **RATIFIED:** none. New curved-authoring timing stays advisory; existing deterministic enforced metrics remain unchanged. |
| **O-4 — historical numbers** | **RATIFIED:** keep Pass A/B numbers out of baseline JSON; retain them only as clearly labelled, non-comparable historical context in the slice record. |

## 5. Readiness and remaining work

**Owner ruling recorded:** the owner ratified the amended plan revision `4b32034f`, including O-1–O-4,
the separate CLASS 5 identity and primary responsiveness role for `owner-40-curved-v1`, the bounded
workload, and the proposed reference machine.

**Stage B status:** W1–W7 are authorized and remain unstarted in this update. When work begins, commit the
exact owner payload as a fixture; add W2 assertions for fixture identities and shipped-gate verdicts;
update the existing harness and DEV-gated measurement marks for W3–W5; record actual DPR/renderer/GPU
details and sample configuration in the live editor session; then record the post-policy, pre-optimization
baseline. P23B.4 remains gated until that baseline is accepted.

**Measurement limit:** the Stage A environment has an interactive Chrome installation and actual machine
metadata, but no editor tab or W4 measurement surface was active. Browser DPR, the editor's renderer and
actual presentation/GPU costs remain unknown until W4 runs in the selected browser session. No presented-
frame result is claimed here.

## 6. Owner ruling — **RATIFIED**

The owner ratified P23B.0-durable plan revision `4b32034f` on 2026-09-24, approved O-1–O-4, and authorized
Stage B W1–W7. The supplied document is a valid, separately identified CLASS 5 candidate under the
current shipped codec, topology validator and geometry compiler. It matches the owner-reported real-world
responsiveness symptom and supplements the six-cell matrix. The historical E2 record remains immutable
and no historical number enters the new baseline.

Stage B has not started in this update, as directed. Ratification does not mark a baseline as recorded:
the exact-file W2 assertions and live browser measurement remain prerequisites. P23B.4 stays gated until
P23B.0's baseline is accepted.

## Evidence anchors

```text
Owner payload: ../40-walls.json
Historical observation, unchanged: 2026-09-22-P23B.0-read-only-pass-b-owner-40-curved-walls.md
Current structural codec / serializer: packages/layout-core/src/layout-wall-first-codec.ts
Current topology gate: packages/layout-core/src/layout-wall-first-precision.ts
Current wall-first compiler: packages/layout-core/src/layout-geometry.ts
Wall-first knot identity scope: packages/layout-core/src/layout-wall-first-codec.ts
Current Plan edit paths: apps/editor/src/lib/editor/layout/LayoutPlanViewport.svelte
Current preview actions: apps/editor/src/lib/editor/layout/layout-preview-state.svelte.ts
Camera navigation ownership: packages/camera-core/src/ and apps/editor/src/lib/editor/camera/
```

```text
EXECUTED: raw-byte SHA-256; canonical serializer output/hash; structural codec validation;
          post-Option-E topology validation; wall-first geometry compilation on the exact supplied bytes.
SOURCE-INSPECTED: shipped class rules, supported Plan editing paths, and current editor camera ownership.
OWNER-REPORTED: near-equivalent manual responsiveness slowdown on the supplied 10-Room / 40-curved-Wall case.
NOT RUN: test suite, build, benchmark, or interaction timing; no Stage A fixture was constructed or edited.
```
