# P23B.0 (durable) — W7 budget policy record (closed stub)

**AUTHORITY: NONE.** Standing policy summary below; enforced budgets are code-owned by
`apps/editor/src/lib/bench/bench-types.ts` (`ENFORCED_BUDGET_METRICS`) and checked by
`bench-report.ts`. Advisory interaction metrics are recorded in
`apps/editor/src/lib/bench/baselines/g3-baseline.json` with no targets.

**Status:** RECORDED — no numeric threshold added, changed or removed by this slice.
Preserved unchanged through acceptance and closeout.

**Standing policy (preserved):**

1. **No new enforced threshold.** Seven advisory interaction metrics
   (`interaction-input`, `interaction-release`, `interaction-reactive`,
   `interaction-plan-apply`, `interaction-adapter`, `interaction-svelte-flush`,
   `interaction-browser-frame`) across six bounded editor paths (`selection`,
   `plan-drag-edit`, `bend-knot-edit`, `wall-authoring`, `plan-pan-zoom`,
   `guided-3d-navigation`); all DEV/`__P2311_PERF__`-gated. Later enforcement of any
   curved-authoring wall-clock budget requires a separate owner decision.
2. **No weakening, no silent rebaselining.** `ENFORCED_BUDGET_METRICS` remains exactly
   `compiled-memory · cache-key-code-units · svg-node-count · three-object-estimate ·
   three-material-estimate · three-draw-call-estimate · three-triangle-estimate`.
   Deterministic sampling: 2 warm-up + 5 measured (`chopin`/`small`/`medium`), 1 + 3
   (`large`); 15 metrics per tier.
3. **One writer.** `bench:record` (validated browser-report import) is the only writer of
   `g3-baseline.json`; the default suite never rewrites it (verified by re-hash:
   `5534926e…`, 107,521 bytes). Evidence gates fail closed on inadmissible captures;
   owner-supplied captures that fail them stay owner evidence outside the baseline.
4. **Advisory numbers are reported, not asserted** — with sample counts, warm-up, session,
   environment and the mark-nesting rule (nested P23.11 marks listed separately, never
   summed). Method-v5 semantics: `input`/`release`/`plan-apply`/`adapter`/`reactive`
   synchronous; `svelte-flush`/`browser-frame` deferred from the synchronous input's end;
   `plan-apply` never reported as adapter work; accepted-only distributions.
5. **No product effect.** Marks stay behind the DEV gate; nothing reaches persisted
   schema, visitor bundle or non-DEV build. Preview browser approved as measurement
   environment (no transferability claim); `guided-3d-navigation` sampling stays
   owner-deferred with the deferral text inside the baseline.

**Recovery:** full record at anchor
`d7b9de4e840b8a54a5c72b4772710482f66e33ec`; tag `closed/p23b.0`.
