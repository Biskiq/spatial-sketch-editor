# P23B.0 (durable) — W6 measurement report (closed stub)

**AUTHORITY: NONE.** Acceptance summary and exact recovery are below; the recorded
baseline bytes live at `apps/editor/src/lib/bench/baselines/g3-baseline.json` (method v5,
SHA-256 `5534926e3a2dbceed9e0383da5914f95ff2824d0d90a760ca48f859b56ec5a8d`,
107,521 bytes). Timing stays advisory per the closed W7 stub.

**Status:** RECORDED and ACCEPTED as explicitly partial — owner accepted W6 §10.3
disposition A on 2026-09-25: the missing post-release flush/frame coverage (§0.2) is
deferred, not closed by another capture. Routine slice closeout completed on
`P23B4`.

**Finding (preserved):** the editor is slow on every fixture, including the zero-curvature
straight control (selection input 121.7 ms; drag/edit accepted release 208.4 ms; authoring
accepted release 262.6 ms). Curvature amplifies several observed costs (e.g. authoring
release 1,984.8 ms on the all-curved 40-wall cell) but is not necessary for the problem;
no single root cause is established. Plan update work (pan/zoom flush 22.4 → 343.2 ms
without a planner call) is a justified investigation target, not a proved explanation.
All flush/frame rows are observed press/move/wheel-phase evidence, never end-to-end
settle time.

**Coverage limit (preserved):** the capture predates the S-8 post-release scheduling
correction. Synchronous boundaries (`input`, `release`, `reactive`, `plan-apply`) and the
observed press/move/wheel pairs remain useful; post-release pairs are missing for
selection/pan releases and for drag/edit, bend and authoring clicks. Zero dropped
boundaries does not prove complete coverage. No complete action-settlement claim is
supported. Capture revision `be525f7c` stays distinguished from the corrected
implementation revision.

**Acceptance evidence (W6 §11):** A4/A10 (seven CLASS 5 inputs, one session/shared
viewport), A5 (method v5 + reason), A6/A7 partial under the accepted deferral, A8/A9
(budgets unchanged/green, baseline write-isolated), A12 (U-1…U-7 with closing
observations). Gates at `be525f7c`: `test:perf` 7 files / 53 pass (1 skipped);
`test:arch` 23 files / 254 pass; `npm test` 324 files / 4,752 pass (1 skipped); `check`
0 errors, 0 warnings; both apps build. At the corrected revision: `test:perf` 56 pass
(+3 post-release scheduling regressions); `npm test` 4,755 pass; remainder unchanged.

**Entry points:** recorded baseline
`apps/editor/src/lib/bench/baselines/g3-baseline.json`; ledger
`…/p23b.0-measurement-foundation/fixture-ledger.json`; owner fixture
`docs/roadmap/p23b-geometry-performance/40-walls.json`; driver
`apps/editor/src/routes/dev/perf/p23b/drive.ts`; harness `…/p23b/+page.svelte`,
`+page.server.ts`; focused tests `p23b-interaction-measure.test.ts`,
`p23b-baseline-contract.test.ts`, `p23b-fixture-contract.test.ts`.

**Residuals:** U-2…U-7 open (U-5 flush attribution is the bounded S1b step inside P23B.4,
not authorized here); U-1 was open when P23B.0 closed and was subsequently DECLINED by the
owner on 2026-09-25 — closed, not owed and not a currently owed capture; owner-supplied capture stays owner evidence beside the baseline
(W6 §7); guided-3D capture stays deferred; all interaction numbers stay advisory
single-machine, single-session wall-clock.

**Recovery:** full record at anchor
`d7b9de4e840b8a54a5c72b4772710482f66e33ec`; tag `closed/p23b.0`.

## Closeout preservation — P23B.0 (2026-09-25)

- Prose compacted: 4 stubs at their own paths (plan, W6, W7, Stage A handoff).
- Reference promotions: 0. Measurement findings are slice evidence, not durable
  architecture; the bench contract is code-owned (`apps/editor/src/lib/bench/` + PERF
  lane) and the advisory-timing policy travels with the W7 stub. No roadmap proposal or
  review chronology was promoted.
- Renderable evidence archived: 0 files / 0 bytes; the slice workspace holds no
  PNG/SVG/HTML plates (the recorded baseline JSON stays live as the test-enforced
  baseline, not as archive evidence).
- Evidence stubs/manifests: 0; no renderable evidence moved.
- Transient artifacts removed: 1 (active checkpoint
  `docs/operations/checkpoints/p23b0-stage-b.md` retired after promoting its durable
  content into these stubs; no raw checkpoint archived).
- Historical anchor: `d7b9de4e840b8a54a5c72b4772710482f66e33ec`, tag `closed/p23b.0`.
- New live-to-archive prose links: 0.
- Manual-owed verification rows: 0 (the optional native-Chrome comparison U-1 was never
  owed; disposition A explicitly declines another capture campaign).
- Remaining deferred items: U-2…U-7, the deferred post-release coverage, P23B.4–P23B.8,
  P23B.1 review and the P23B.2 evidence decision — listed above and in the plan stub.
- P2 anchor method and resolution: PR #87 landed as a GitHub squash merge at `d7b9de4e`
  on 2026-09-25 (tree identical to continuation HEAD `935c5ada`), so continuation-branch
  commits are not ancestors of `main`. Recovery runs against the squash anchor and the
  tag above. Post-merge checks on `main` at `d7b9de4e`:
  `git merge-base --is-ancestor d7b9de4e main` and
  `git show d7b9de4e:<path> | head -3` for all four anchored paths return the expected
  ancestry and original document headings.

## Closeout self-check (2026-09-25)

- **Acceptance — PASS.** A1–A12 map to W6 §11 evidence; A6/A7 partial coverage is
  carried by the owner's explicit disposition-A acceptance, not silently dropped. Gate
  numbers at both revisions agree with the record; baseline bytes and enforced budgets
  unchanged.
- **Promotion — PASS.** Zero promotions is correct here: no durable architecture changed
  (measurement-only slice, DEV-gated, no product behaviour change).
- **Preservation — PASS.** All four full prose artifacts recoverable at the anchor;
  `closed/p23b.0` targets it; stubs exist at their own paths with working recovery
  commands; baseline/ledger/owner-payload bytes untouched.
- **Routing — PASS.** Phase README marks P23B.0 shipped and routes the stubs; `current.md`
  advances to P23B.4 reconciliation and preserves baseline hash, advisory policy and
  coverage limits; the checkpoint is retired with no RESUME pointer left.
- **Stable — PASS.** No pending-merge statement; no pinned branch-head SHA invalidated by
  this compaction (the squash anchor is the merged `main` HEAD itself).
- **Report — PASS.** Preservation counts, anchor, tag state, manual-owed count and
  deferred items recorded above.
- **Scope — PASS.** Closeout changes are documentation only; no implementation code or
  tests changed after acceptance.
- **P2 — PASS.** Squash anchor verified on `main` as above; the branch-ancestry
  degradation is recorded, not hidden.
