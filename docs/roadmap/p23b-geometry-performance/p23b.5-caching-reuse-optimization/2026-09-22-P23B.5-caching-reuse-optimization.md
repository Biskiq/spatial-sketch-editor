# P23B.5 — Caching and reuse optimization

```text
STATUS: IMPLEMENTED 2026-09-25, AWAITING INDEPENDENT REVIEW — S1–S6 executed on
        branch `P23B.5`. S0 disposition PREFLIGHT-ONLY (§0.2) was ratified by the
        owner as the slice's scope (§0.4): M-3 lands as a bounded GESTURE-SCOPED
        preflight sample owner, NOT as baseline→candidate (release) reuse. No
        release, validation, history or ownership behavior changes. This status
        is not implementation acceptance: the slice review happens separately.
ROLE:   establish one complete reuse contract; implement M-3 only if S0 proves
        safe, useful cross-chain reachability within this slice's bounds.
DEPENDS ON: P23B.3 RATIFIED (e139a18b); P23B.3a SHIPPED; P23B.0-durable
        SHIPPED with an accepted partial baseline; P23B.4 SHIPPED (#88, #89).
AUTHORITY: P23B.3 synthesis §6.1 M-3, §6.2, §8 and abandonment rules;
        phase README owns sequence/status; WallSamplingDerivation owns the
        landed key grammar. P23B.5 generalizes that grammar in place.
OWNER:  ratified this revised scope and acceptance criteria, and GRANTED the
        preflight-only scope S0 required (2026-09-25) — see §0.4.
```

## 0. Ratification, S0 disposition and owner ruling — 2026-09-25

```text
RATIFICATION (owner, 2026-09-25): this reconciled plan (revision `f26e2319`) is
ACCEPTED and RATIFIED. S0–S3 execution is AUTHORIZED. S4 is authorized ONLY if S0
proves safe, useful reuse inside the ratified baseline→candidate scope and its
prerequisite proofs pass. A CONTRACT-ONLY outcome with M-3 explicitly DROPPED is
accepted if this plan's abandonment conditions (§6) are met. Preflight-only reuse
is NOT assumed authorized: if that is the only viable opportunity, S0 presents
evidence and requests a SEPARATE scope ruling. No broader cloning, validation,
ownership or architectural redesign is authorized. Ratification is a scope/
authority record, NOT implementation acceptance; the full implementation review
happens when the whole slice is ready.
```

### 0.1 S0 execution — evidence base

Executed against current code and the committed fixtures (curved-40
`p23b-40-wall-all-curved-v1`, straight control `p23b-40-wall-straight-v1`) with
bounded test-only observation. Evidence lives in
`apps/editor/tests/lib/layout/p23b5-s0-reachability.test.ts` (9 probes, P1–P9).
No M-3 mechanism was built and no production behaviour changed; the one
production edit is an additive, non-retaining `centerline` reference on the
test-only `WallSamplingCallObservation`, so a test can assert sampler-INPUT
identity rather than infer it from values.

Correction round 1 (review of `4a33c23d`) replaced three weak proofs: (i) the
preflight sharing proof no longer infers identity from an observed mutation (a
deep-clone-per-call preflight would also observe it) — P3 now asserts cross-call
REFERENCE equality of the actual `wall.centerline` input; (ii) P4's
"full-result equality" now digests the COMPLETE `SampledSegment` including
`length` and `segmentId`; (iii) replacement was separated from snapshot restore
(add P9) — the earlier "restore/undo/redo/replacement issues no sampling" claim
was wrong about replacement.

| Probe | Question | Landed fact measured |
|---|---|---|
| P1 | source/parsed/compiled identity | `validateWallFirstLayoutDocument` re-parses **fresh** centerline objects on every call (value-equal, never the source object, never the previous parse); each compile mints a fresh geometry object. An object-identity key therefore cannot carry a hit across validation stages, releases, undo/redo or replacement |
| P2 | release | One accepted curved-40 junction move issues **120 fresh sampling requests** (80 forward + 40 reverse) over only **80 distinct (wall, traversal, endpoints) value keys** — the pre/post stages re-request value-identical inputs, and only the centerline OBJECT identity keeps them apart. A repeated identical release re-issues the same 120 on freshly parsed objects. A rejected release leaves the baseline byte-identical |
| P3 | preflight identity | Cross-call **reference equality** of the actual sampler input: for every sampled Wall, the `wall.centerline` OBJECT observed across two preflights is one single object, and it is the frozen baseline's own object (40 requests/call; 38 untouched curved walls). A deep-clone-per-call preflight would present two distinct equal objects here |
| P4 | prospective owner (SIMULATION) | Replaying those CAPTURED real inputs through the landed `createWallSamplingDerivation` over a 3-pointermove drag: 120 requests → **44 derivations / 76 hits (63%)**, with complete-result equality (`segmentId`, `length` and every sample field) against each observed fresh result. Hits with captured references are themselves cross-call reference repetition, since an identity-keyed store cannot hit for two distinct objects |
| P5 | metadata hazard | Two Walls sharing one centerline object + endpoints receive the **first Wall's `segmentId`**; committed fixtures never construct that alias (one centerline object per Wall). CLOSED by S1: the Wall id is now a key dimension, so the aliased Wall gets its own entry and its own metadata (the probe now pins that closed behaviour; the hazard as measured here is this row) |
| P6 | mutation hazard | The landed grammar has **no in-place mutation refusal** and returns the shared array/record (stale serve); a fresh derivation sees the mutated geometry |
| P7 | restore / undo-redo | Restoring an EXISTING snapshot (the layout-history `capture`/`replace` host pair that undo/redo and cancel use) reuses the same compiled-geometry object and issues **0 sampling requests — N/A, not a 0% hit rate** |
| P8 | control | Straight-40 preflight issues **0 sampling requests** (chord short-circuit); the opportunity is curvature-scaled, not cardinality-scaled |
| P9 | replacement | Replacement is NOT the restore path: `importLayoutPreviewJson` **decodes and re-derives**, so sampling DOES occur (40 requests/import) — but the decode mints **fresh** centerline objects, so an identity-keyed store still cannot carry a hit across a replacement |

### 0.2 S0 exit disposition — **PREFLIGHT-ONLY**

```text
1  RELEASE / baseline→candidate M-3 reuse is NOT REACHABLE.
   The release chain re-parses its candidate into fresh centerline objects (P1)
   and hands each stage its own objects: 120/120 of a curved-40 release's sampling
   requests are fresh derivations, with ZERO shared across the pre/post identity
   boundary or across releases (P2). Those 120 requests cover only 80 distinct
   (wall, traversal, endpoints) VALUE keys — the pre/post stages re-request
   value-identical inputs — so the ONLY thing refusing the hit is the centerline
   OBJECT identity of §3. Cross-stage/cross-release reuse is therefore impossible
   without content interning — explicitly out of scope (§2). Nothing here is a
   measurement gap; it is a key/identity fact.

2  RESTORE / undo / redo (an EXISTING snapshot) is NOT an opportunity: it
   performs no document re-parse and issues no sampling requests at all (P7).
   REPLACEMENT is a different path — it decodes the incoming document and
   derives a new bundle, so sampling DOES occur (P9) — but the decode mints
   fresh centerline objects, so it is the same identity-refresh situation as
   release and still yields no reachable reuse. These two must not be merged.

3  The ONLY reachable cross-chain identity is PREFLIGHT→PREFLIGHT inside one
   frozen-baseline gesture: the shallow splice shares untouched Wall/centerline
   objects (P3) and a gesture-scoped owner would hit ~63% of preflight sampling
   requests on curved-40 (P4). This is a strictly narrower scope than the
   ratified release reuse, and the ratified P23B.3 dependency map already routes
   it to P23B.7 ("gesture-scoped preflight | P23B.7").

4  S4 IS NOT TAKEN. M-3 is neither shipped nor unconditionally dropped: the
   abandonment condition "no useful real cross-chain identity reuse is reachable"
   is not met, because a useful preflight-only one is reachable. Per the owner's
   instruction, preflight-only reuse is left unassumed and routed to a separate
   owner scope ruling.

5  SAFETY PREREQUISITES FOR THAT NARROWER SCOPE (not implemented at S0; BOTH
   CLOSED under the §0.4 ruling — S1 and S3 respectively):
   - complete §3's contract for the full returned artefact — a Wall-id key
     dimension or an explicit refusal guard (P5 shows the current gap is real, if
     not reachable from landed documents);
   - bound input immutability at the proposed lifetime: P6 shows the landed
     grammar serves stale results after an in-place mutation, so a longer-lived
     owner must prove its inputs cannot mutate there or add bounded refusal.

6  Landed M-1 is untouched; S0 changed no production BEHAVIOUR. The single
   production edit S0 made is the additive, non-retaining `centerline`
   reference on the test-only `WallSamplingCallObservation` (§0.1): an extra
   field read only when a test has installed an observer, so the unset
   production default is one undefined check. "No production file changed" was
   imprecise and is corrected here.

REQUIRED OWNER DECISION (one of):
  (a) authorize a bounded GESTURE-SCOPED sample owner (the candidate is the
      frozen-baseline gesture scope: constructed at gesture begin, threaded into
      the preflight, terminated at release/cancel/restore/replacement) as a
      narrower P23B.5 scope; or
  (b) confirm the preflight-only opportunity belongs to P23B.7 (the dependency
      map's existing route) and record M-3 as CONTRACT-ONLY DROPPED in P23B.5.
With (b), §6's contract-only outcome applies and S1/S2 plus the applicable safety
proofs stay; S4 is marked not applicable.

RESOLVED 2026-09-25: the owner chose (a) — see §0.4.
```

### 0.3 Earlier review claims this evidence corrects

```text
C-1  The M-3 framing as a baseline→candidate RELEASE opportunity (P23B.1 EQ-5
     follow-up; P23B.3 §6.1 M-3 "candidate release is the fresh path; sampling is
     the redundant term") is DISPROVEN for release: the release path re-parses its
     candidate, so the shared-object identity EQ-5 observed lives only on the
     transient preflight splice, which re-derives per call today.
C-2  Any expectation that one chain's pre and post stages could share samples via
     the object key is DISPROVEN (P1/P2: distinct parsed objects; 120/120 fresh),
     and P2 makes the reason precise: the truncation is identity, not input
     divergence (80 distinct value keys for 120 requests).
C-3  The plan §1 preflight row ("identity sharing alone produces no cross-call
     hit") is correct for the SHIPPED code (a fresh derivation per call) but must
     not be read as "no cross-call identity exists": P3 shows the identity is
     reachable; only a retained owner is absent.
```

### 0.4 Owner ruling — preflight-only M-3 scope APPROVED (2026-09-25)

```text
RULING: the owner accepted S0's PREFLIGHT-ONLY disposition and approved the
narrower scope, choosing §0.2 option (a). S1–S6 implementation was then ordered
on this branch. This ruling authorizes exactly one thing that the earlier
ratification did not: a bounded gesture-scoped sample owner feeding the
transient preflight. It does NOT authorize release/baseline→candidate reuse,
content interning, a second cache or key grammar, weaker cloning/validation, or
any ownership redesign.

APPROVED OWNER AND LIFETIME (as implemented in S4):
  - one `createWallSamplingDerivation()` per direct-architecture gesture,
    held non-reactively by the Plan viewport;
  - constructed once, `reset()` at pointer-down (so a new gesture cannot inherit
    entries) and at every site that releases the frozen baseline snapshot:
    gesture finish, cancel, and the bypass path that clears the snapshot
    directly;
  - threaded into `preflightWallFirstArchitectureCandidate` ONLY. The proposal
    stage is deliberately unscoped: it derives freshly-built centre lines per
    intent (2 calls per pointermove on curved-40), whose inputs never repeat, so
    a store there would only grow.

WHAT THE SCOPE MAY AND MAY NOT CHANGE: only how many times an identical artefact
is derived. Verdicts (`known-invalid` / `pending`), the rendered attempt, the
frozen baseline, release, history and canonical validation are byte-identical
with and without it (S2/S3/S4 proofs).

EXPLICITLY OUT OF SCOPE (unchanged by this ruling): P23B.7 keeps the wider
gesture-interaction work; P23B.6 keeps mesh/GPU and the wider dependency cone;
release-scope M-3 stays unreachable and unimplemented.
```


### 0.5 Verification record — 2026-09-25, on this branch

```text
Lanes and gates run on the committed implementation (implementation status is not
acceptance; these are the numbers the reviewer re-runs):

  test -w @portfolio/editor -- tests/lib/layout   97 files / 1,527 pass (+ 9 readout = 1,536)
  test:fast                                       299 files / 4,438 pass
  test:full                                       334 pass +1 skipped file /
                                                  4,821 pass +1 skipped test
  test:arch                                        23 files /   254 pass (unconditional)
  test:perf                                         7 pass +1 skipped file /
                                                     56 pass +1 skipped (P23B.0 lane)
  check (editor + museum)                          0 errors, 0 warnings
  check:layout-core (tsc --noEmit)                 pass
  build (editor + museum)                          both succeed
  verify:visitor-bundle                            3 server / 9 client entries (unchanged)
  g3-baseline.json                                 SHA-256 5534926e…, 107,521 bytes (unchanged)
  p23b5-s0-reachability.test.ts                    9/9 pass
  p23b5-preflight-scope.test.ts                   10/10 pass
  p23b5-gesture-scope-wiring.test.ts               7/7 pass (call-site source contract)
  p23b-gesture-sampling-report.test.ts             9/9 pass (DEV live readout)
  P23B.4 suites (shared-derivation, measurements,
  consumer observations, pruning differential)    green

MUTATION CHECK (tests/README rule 4 — the wiring proof must fail when the wiring
is removed; the machinery suite cannot see the removal, which is why the call-site
proof exists). Each mutation was applied alone to `LayoutPlanViewport.svelte` and
the wiring + machinery suites re-run:

  M1  gesture-start `reset()` removed        → start-site assertion FAILS
                                               ("the scope is reset at pointer-down:
                                               expected -1 to be greater than or
                                               equal to 0"); other 4 wiring
                                               assertions + 10 machinery tests green
  M2  finish-path `reset()` removed          → finish assertion FAILS; rest green
  M3  bypass-path `reset()` removed          → bypass assertion FAILS; rest green
  M4  `sampling:` argument removed from the   → threading assertion FAILS; rest green
      transient call

All four were reverted; the committed viewport is unmodified by the check (the
verified working tree carries no mutation).

The same check was run again for the DEV live readout added under S5 (N1–N4), on
the viewport's readout call sites. Each mutation leaves the readout suite green
and fails exactly its own wiring assertion:

  N1  gesture-start `p23bOpenGestureSampling()` removed  → start/watching
                                                            assertion FAILS
  N2  `p23bRecordGestureSampling(...)` removed            → "published after the
                                                            preflight" FAILS
  N3  finish-path `p23bCloseGestureSampling()` removed    → close assertion FAILS
  N4  bypass-path `p23bCloseGestureSampling()` removed    → close assertion FAILS

BROWSER EVIDENCE (2026-09-25, DEV server on the working tree, harness page
`/dev/perf/p23b`, readout switched on): one real bend drag on the hosted
curved-40 owner fixture, driven as synthetic pointer input through the shipped
handlers (the P23B.0 driver's own technique). The live readout, read from the page
mid-drag, reported 4 recorded pointermoves → **160 requests, 43 derivations (40
cold misses + 3 refusals) and 117 hits (73%)**, 43 retained keys, 0 failed derives,
0 cached `undefined`, and the gesture then appeared in the panel's history as
`released`. That is the S5 measurement reproduced by the shipped app rather than
by the suite: the counts differ from the fixture-based S5 numbers above only
because this is the owner layout and a bend drag (one re-keyed Wall per move)
rather than a Junction drag.

NOT covered here: browser release/flush/frame/settlement behaviour (P23B.0's stated
baseline coverage limit stands), any end-to-end interaction gain, deterministic GC,
and the wider mesh/GPU dependency cone (P23B.6).
```

## 1. Verified starting point

Source paths below are relative to the repository root. These are current
implementation facts, not claims of measured cross-chain benefit.

| Surface | Landed behavior / evidence anchor | Consequence |
|---|---|---|
| Sampling | `packages/layout-core/src/layout-wall-centerline.ts`: `createWallSamplingDerivation` owns a strong `Map<centerline, Map<key, SampledSegment \| undefined>>`, counters `derivations`/`hits`; `wallCenterlineSamples` remains the fresh fallback | Sampling is already shared within a chain. It is not globally unmemoized, and the sampling store is not a WeakMap |
| Preflight | `layout-wall-first-precision.ts`: `spliceWallFirstArchitectureCandidate` shares untouched Walls/centerlines; `preflightWallFirstArchitectureCandidate` creates a fresh derivation per call | EQ-5 is true here, but identity sharing alone produces no cross-call hit |
| Release | Same file: `cloneDocument`, `withWallCenterline`, `finalizeWallGeometryCandidate`; `layout-wall-first-codec.ts`: `validateWallFirstLayoutDocument`, `parseWallCenterline` | Normal geometry edits deep-clone centerlines; structural pre/post validation constructs fresh centerlines again. The finalizer owns a fresh derivation per acceptance chain. Equal content is not equal identity |
| M-1 consumers | Finalizer threads its derivation through topology pre/post, face extraction, Opening set and acceptance compile | Preserve accepted M-1. Pre/post can miss because their centerlines differ, while consumers of the same parsed inputs share |
| Sampling metadata | `wallCenterlineSegment` supplies `wall.id`; `layout-geometry-curve.ts`: `SampledSegment.segmentId`, `buildSampledSegment` | Two Wall ids sharing a centerline and endpoint/traversal key can receive the first Wall's metadata. The present key is not an unconditional full-result equivalence proof |
| Snap / mesh reuse | `layout-snap.ts`: `derivedWallSnapIndex` / `wallSnapIndex`; `apps/editor/src/lib/editor/layout/layout-preview-state.svelte.ts`: `derivedWallMeshes` / `resolveWallMeshes` | Keep both compiled-geometry-keyed WeakMaps. A new compiled geometry misses; restoring the same geometry can hit |
| Snapshot / restore | Same editor file: `captureLayoutPreviewSnapshot`, `restoreLayoutPreviewSnapshotUnmeasured` | Capture clones project/model but keeps geometry; restore clones project again and reuses geometry. Source-document identity is NOT the baseline-restore oracle; the container is not a frozen source-identity guarantee |
| Accepted compile | Same editor file: `reusesAcceptedCompile` | Canonical-JSON equality remains the acceptance proof; preserve it |

P23B.4 also shipped M-2a conservative extent pruning; M-2b was DROPPED,
not postponed. Its S9 reconciliation and S10 locality findings do not authorize
incremental compilation here. Browser release/flush/frame/settlement gains
remain unproven; the accepted P23B.0 baseline has that coverage limit.

## 2. Scope and boundaries

Deliver one key/invalidation/lifetime contract plus focused executable proofs.
M-3 is conditional sample reuse, not a promised release optimization. No new
cache is authorized merely by the shallow-splice observation. Keep the existing
chain-scoped derivation if the opportunity does not survive S0.

Preserve P23B.4's accepted threading, independently derived reverse traversal,
extent pruning, and fresh-sampling fallback. Preserve Option E and P23B.3a's
D-12 reconciliation proof: connectivity uses explicit Junction identity;
coordinate coincidence never establishes entity identity. Room reconciliation
is not an extra sampler input when its actual sampling inputs remain unchanged.

Out of scope: weakening cloning or canonical validation; replacing either
WeakMap or the canonical-JSON proof; content-hash interning; generation counters;
per-document caches; derived geometry in documents or editor reactive state;
a second evaluator/key grammar; incremental compile; topology-policy changes;
mesh/adapter caching, disposal or dependency implementation (P23B.6); Worker/WASM
(P23B.8); new budgets. Do not reopen P23B.3a/P23B.0/P23B.4.

## 3. One key and equality contract

The landed grammar is centerline OBJECT identity + traversal + resolved
canonical endpoint coordinates at full precision. Retain all those dimensions;
reverse remains independently derived. Coordinate values are dependencies
within an identity key, never a substitute for identity.

Before widening reuse, complete the SAME `WallSamplingDerivation` contract for
its full returned artefact: require matching Wall id (`segmentId`) as a key
dimension or an explicit refusal guard. Prefer that bounded completion over
splitting out a second metadata-free sample cache. A synthetic alias test must
use two Wall ids with the same centerline object and endpoints and compare the
entire returned `SampledSegment` with fresh results. Canonical parsing currently
separates those objects; this is a demonstrated API hazard, not evidence that
landed normal release operations already return wrong Wall metadata.

Identity implies equality only while ALL sampled input data is unchanged:
centerline kind, knots/points and span controls, resolved endpoints, traversal
and Wall id. The current API/types do not enforce deep immutability. S0 must
trace writers and aliases at the proposed lifetime, prove inputs cannot mutate
there or specify bounded mutation detection with refusal. Returned samples,
points, tangents and normals must remain unmodified by consumers. Tests must
cover input alias mutation and output contamination; do not infer safety from
an unchanged top-level reference or a TypeScript readonly annotation. If this
needs a document-wide mutation/ownership redesign, abandon M-3.

Sampling options are FIXED on this path: `wallCenterlineSamples` calls
`sampleSegment` without options (flatness 0.01, span 0.25, maxDepth 12,
maxSamples 100,000). There is no runtime options dimension to key or vary today.
Pin this call path/default policy; do not expose options to satisfy a test.
Any future configurable policy must extend the same grammar before sharing.

An adjacent Wall or Opening edit is not automatically a sample-cache refusal:
if a shared Junction moves, resolved endpoints change and reuse must refuse;
if every sample input remains identical and the same permitted store is reached,
reuse should hit and equal a fresh derivation. Changed centerline identity may
legitimately miss despite equal values. These sample rules do not establish
mesh equivalence. P23B.6 owns the wider Junction/adjacency/Opening/dimension/
material dependency cone (OR-9b / G15).

## 4. Ownership and lifetime

Current exact owner: the local `sampling` value in
`finalizeWallGeometryCandidate`, or the derivation created by each
`preflightWallFirstArchitectureCandidate` call. The former lasts through the
synchronous acceptance chain; the latter through that preflight. Neither is
returned in the plan/acceptance or retained by editor state. Its strong Maps
become unreachable when the call and consumers release them; this is an
ownership argument, not a deterministic garbage-collection claim.

S0 must name any proposed longer-lived owner, its construction site, caller
threading, allowed scope and every termination/reset path BEFORE S4. A bounded
non-reactive operation/gesture owner is only a candidate, not an approved new
editor cache. Its design must cover success, rejection, cancel, replacement,
undo/redo and disposal, and must not retain document objects through values,
keys, closures or diagnostic collectors. If no narrowly scoped owner can serve
real repeated calls safely, keep the current owner and drop M-3.

The two existing WeakMaps stay module-owned and geometry-keyed. History may
legitimately keep geometry and derived resources reachable; restoring a source
clone neither proves nor invalidates their geometry identity. Test reuse and
stale-result behavior, explicit owner resets and any enumerable store's bounded
entries. Never claim WeakMap population counts, deterministic GC, or that all
artefacts must die on document replacement. Mesh disposal counts and GPU
ownership remain P23B.6's responsibility.

## 5. Execution and decision gates

```text
S0  REACHABILITY / IDENTITY PROOF — before S4, after owner implementation authorization
    Exercise real preflight, release and restore entry points on committed
    fixtures (including curved-40 and the straight control). Trace source,
    parsed centerline and compiled-geometry identities separately, and name
    actual sampling consumers and store owners. Include repeated preflights,
    accepted and rejected release, baseline restore, undo/redo and replacement.
    Reusing a manually retained helper twice is only a unit control.

    Establish whether a bounded owner could yield cross-chain hits without
    changing clones, validation or the accepted chain behavior. Demonstrate
    reachable repeated identities at that proposed boundary and full-result
    equality/metadata/mutation safety. Distinguish within-chain M-1 hits from
    prospective M-3 hits; preflight opportunity is not release evidence.

    Output one concise disposition in this plan: eligible operation + exact
    owner/scope + proof, or M-3 DROPPED + reason. S4 stays blocked until that
    record and S1–S3 proofs exist. Preflight-only benefit requires an explicit
    owner scope ruling; do not silently relabel it baseline→release reuse.
    RECORDED 2026-09-25 (revision f26e2319): disposition PREFLIGHT-ONLY — see §0.
S1  CONTRACT
    Record one artefact → inputs → key/refusal → owner/lifetime → exclusions
    table in the existing appropriate reference contract when proved. Extend
    the landed grammar in place if needed; leave harvest CI-1 historical.
    DONE 2026-09-25: the landed `WallSamplingDerivation` contract comment now
    completes its key grammar with the Wall id (`segmentId` is a traced input of
    the returned artefact — §3's bounded completion, not a second cache), and
    its lifetime/stat fields document the bounded-owner discipline below. No new
    key grammar or store was introduced.
S2  EXISTING BEHAVIOR
    Reuse/extend existing tests, not duplicate P23B.4 acceptance. Pin same-
    geometry snap/mesh reuse, fresh-geometry misses and baseline restore with
    cloned source + identical geometry; preserve canonical-JSON refusal.
S3  LIFETIME / SAFETY
    Prove the chosen owner boundaries and mutation/metadata guards before a
    wider cache lands. Rejected candidates must leave baseline results intact.
    Cycling tests observe explicit retained roots/stores where enumerable,
    not WeakMap population or a forced-GC deadline.
S4  CONDITIONAL M-3
    Only for the S0-approved operation and owner. Use the single derivation
    and grammar; no parallel cache/evaluator. Prove fresh-result equivalence,
    changed-input refusal and reachable unchanged-input hits on real calls.
    If dropped, mark S4 not applicable; S1/S2 and applicable safety proofs stay.
    TAKEN 2026-09-25 under the owner ruling (§0.4), for the preflight operation
    and that owner ONLY. The landed `WallSamplingDerivation` is threaded through
    `preflightWallFirstArchitectureCandidate` (optional parameter; omitted keeps
    P23B.4's per-call scope exactly) and supplied by the Plan viewport for one
    gesture, reset at pointer-down and at every baseline-release site. No
    release/candidate path was given a derivation and no second cache exists.
S5  DIRECT MEASUREMENT
    Per operation and stage record sample requests, fresh derivations/misses,
    within-chain hits, cross-chain hits, refusals by reason and failed derives.
    Define miss (absent entry) separately from refusal (ineligible/changed
    input) and failed derive (undefined); cached undefined is not useful
    geometry reuse. Reconcile request totals without double counting.
    Existing derivation stats + test-only consumer/kernel observers are
    starting points, not complete M-3 instrumentation. Keep counters scoped
    and clear observers; no retained input graphs or production telemetry.
    Separate preflight, release pre/post-validation, compile and restore;
    a restore with no sample requests is N/A, not a 100% sample hit rate.
    Compare against landed P23B.4, using preview-compile/reused and mesh-
    prebuild counts only as secondary evidence. Report zero opportunity if
    M-3 is dropped. Advisory p50/p95 needs identical fixtures and P23B.0
    provenance; do not infer end-to-end gains from kernel/hit counts.
S5  (record) 2026-09-25 — direct measurement on the committed curved-40 fixture,
    executed by `p23b5-preflight-scope.test.ts` (S5 cases), which print as they
    assert. Real transient path, 3-pointermove junction drag: 120 preflight
    requests → 44 fresh derivations and 76 hits (63%); 44 retained entries
    (entries === distinct derived keys), 0 failed derives, 0 cached `undefined`.
    The 44 derivations are attributed, not lumped: **40 cold misses** (the 40
    sampled Walls, first requested on move 1 — this scope had never served them)
    plus **4 changed-input refusals** (the moved Junction's 2 incident Walls,
    whose centreline object and resolved endpoints change on each of moves 2 and
    3). `derivations === coldMisses + refusals` and the store's split is
    cross-checked against an independent attribution computed from the observed
    inputs alone, so a counter cannot agree with itself. The observer sees only
    fresh derives, so hits reconcile against the counters: 120 − 44 = 76 and the
    unscoped path issues the same 120. Per-stage: proposal 2 requests/pointermove
    (unscoped, never repeatable inputs), preflight 40/pointermove. Straight-40
    control: 0 requests and 0 store requests — the opportunity is curvature-
    scaled. The split is also the S0 identity finding restated: on move 1 nothing
    is reused because the scope is empty (cold), and after that nothing is
    refused except Walls whose traced input actually changed.
    Advisory drag timing (identical fixture, P23B.0 non-gating): p50 26.7 →
    8.9 ms, p95 27.3 → 9.2 ms per 6-move drag (repeat runs spread ~9–9.4 ms
    scoped against ~27–29 ms unscoped); reported as a floor on preflight sampling
    cost, NOT an end-to-end frame or interaction gain.

    S5  (readout) 2026-09-25 — the same counters are now watchable LIVE, which is
    what a measurement surface needs before a reviewer trusts it. The Plan
    viewport publishes the scope's counters once per pointermove into
    `p23b-gesture-sampling-report.ts` (DEV only), and the P23B.0 harness page
    renders them: per gesture, requests, hits, reuse %, cold misses, refusals and
    retained keys, updating while the drag is still moving, plus a bounded history
    of released gestures. It is gated by the existing DEV + `__P2311_PERF__`
    switch (no second switch, nothing in a production build beyond one early
    return), mirrors its state on `__P23B_GESTURE_SAMPLING__`, and is deliberately
    NOT part of the capture ledger or the recorded baseline, so the ratified
    P23B.0 contract is untouched. Two honest-boundary rules keep the readout
    truthful rather than flattering: a report total that DROPS means the store was
    reset between records (the open gesture is released as `reset-detected`, never
    averaged with the next one), and turning the switch off mid-gesture labels the
    report `watch-stopped` instead of claiming the gesture ended.

S6  DISPOSITION / HANDOFF
    Record M-3 shipped or dropped, actual supported lifetime, evidence limits
    and the P23B.6 contract. Preserve the negative decisions above without
    adding a review diary or reopening closed work.
    RECORDED 2026-09-25: M-3 SHIPPED at the preflight-only scope (§0.4) —
    `M-3 = SHIPPED (preflight-only)`, release-scope M-3 stays DROPPED/UNREACHABLE.
    Supported lifetime: exactly one direct-architecture gesture on the Plan
    viewport, terminated by `reset()` at pointer-down, gesture finish, cancel and
    the snapshot-clearing bypass path; never reactive state, never module-global,
    never retained across gestures. Evidence limits: no browser frame/flush/
    settlement gain is claimed (P23B.0's baseline coverage limit stands); the p50/
    p95 figures above are advisory preflight-stage timings, not interaction gates;
    no deterministic-GC or WeakMap-population claim is made. P23B.6 handoff: the
    sample store is a strong Map keyed on frozen-baseline centreline objects for
    one gesture (bounded lifetime, not bounded entry count — a gesture that
    touched every Wall would retain one entry per distinct key for that gesture),
    mesh/GPU ownership and the wider dependency cone are unchanged and remain
    P23B.6's. Unchanged enforced gates: `cache-key-code-units`, `compiled-memory`.
```

## 6. Acceptance and abandonment

OR-5: complete-result equality and meaningful refusal, including Wall metadata
and mutation hazards. OR-9: both refusal and real hit directions, conditional
on a reachable S0-approved reuse boundary; unrelated metadata/mesh inputs must
not manufacture refusals. A dropped M-3 has no fabricated cross-chain hit gate.
OR-8/10/11: lifecycle behavior, baseline integrity after rejection and bounded
explicit ownership under repeated edit/undo/redo; no deterministic GC claims.
OR-6: checked-in baseline unchanged. OR-7: Plan/3D/visitor parity and isolation.
OR-9b remains a P23B.6 handoff, not this slice's acceptance test.

For implementation, run the full applicable repository verification owned by
`apps/editor/tests/README.md`: complete test/lane union, unconditional arch,
perf, check and both-app build/visitor gate. Preserve P23.11 equivalence and
accepted-compile assertions, and P23B.4 consumer/pruning proofs. This plan-only
reconciliation needs document/diff checks, not a claim of new runtime acceptance.

Keep enforced `cache-key-code-units` and `compiled-memory` gates unchanged.
Measure additional runtime sample-store/key retention separately: compiled
record size counters do not automatically account for a new sample store.
Never re-record the baseline or relax gates to hide a regression.

Abandon M-3 implementation if no useful real cross-chain identity reuse is
reachable; if it requires broad cloning, canonical-validation or ownership
changes; if identity/mutation/metadata/refusal safety cannot be proved; if the
owner retains documents or has unbounded growth; or if measured overhead defeats
the demonstrated saving. Keep the contract and current chain-scoped M-1 with an
explicit dropped disposition. Do not substitute hashes, interning, a second
cache, weaker validation or revived M-2b to manufacture success.

Exit requires recorded S0 disposition, applicable proofs/verification, direct
stage counts and their limits, one contract for P23B.6, and owner acceptance
before routine slice-closeout. No major-phase closure is implied.

## 7. Remaining owner calls

- DONE 2026-09-25: this reconciled plan was ratified with conditional M-3 and a
  contract-only outcome accepted; that review did not yet authorize
  implementation (see §0).
- DONE 2026-09-25: the preflight-only scope ruling (§0.4) accepted the bounded
  gesture-scoped sample owner, and S1–S6 were implemented under it. Release
  optimization is explicitly NOT claimed for it; the wider gesture scope stays
  routed to P23B.7.
- OPEN — slice review: this implementation now needs the independent review and
  the owner's acceptance; implementation status here is not acceptance, and no
  slice-closeout or merge is implied by it.
- Any broader ownership redesign or new performance budget requires a separate
  owner decision outside this slice. Existing topology rulings are settled;
  P23B.1/P23B.2 evidence-review status does not reopen them.
