# P23B.5 — Caching and reuse optimization

```text
STATUS:   PLANNED, UNRATIFIED. Not implementation-ready until the owner ratifies P23B.3.
ROLE:     make every cache key, reuse guard and invalidation edge honest, documented and demonstrably
          sufficient, and add ONLY the reuse that is provable by identity (synthesis M-3). This slice
          owns the key grammar and the lifetime rule that P23B.4 introduces and P23B.6 applies.
NOT:      a replacement for the two existing geometry-keyed WeakMaps · a replacement for the
          canonical-JSON acceptance-reuse proof · a new cache layer · a per-document cache · derived
          geometry in editor state · an invalidation "solution" without a dependency map · a budget
AUTHORITY: direction → ../p23b.3-synthesis-optimization-direction/2026-09-22-P23B.3-synthesis.md §6.1
            M-3, §6.2 (explicitly NOT admitted), T-5, T-7, §7, §8.2, §8.5, §8.6
          evidence → ../p23b.1-internal-geometry-pipeline-harvest/2026-09-22-P23B.1-harvest-record.md
            F-P3 · F-P7 · F-P8 · F-P9 · CI-1 · §5.1 · EQ-5
          invariants → ../2026-09-22-P23B-geometry-performance-stabilization-umbrella.md
          order → ../README.md §SEQUENCE (unchanged)
DEPENDS ON: P23B.0-durable (fixtures + recorded baseline) · P23B.4 (the key grammar; this slice MUST
            NOT invent a second key)
OWNER:    ratifies the slice plan + acceptance criteria. No budget is proposed.
```

## 1. Problem, and the evidence that motivates it

```text
1.1  THE REUSE PATTERN ALREADY EXISTS DOWNSTREAM AND IS ABSENT UPSTREAM (F-P3).
     Working, geometry-identity-keyed reuse:
       · `derivedWallSnapIndex` / `wallSnapIndex`  `packages/layout-core/src/layout-snap.ts:580`
       · `derivedWallMeshes` / `resolveWallMeshes` `apps/editor/src/lib/editor/layout/layout-preview-state.svelte.ts:650`
     Unmemoized: `wallCenterlineSamples` (`layout-wall-centerline.ts:307`) — no cache key and no owner
     object to hang one on. The reuse boundary sits exactly at "has a compiled geometry object".
1.2  CANDIDATE RELEASE MISSES BOTH CACHES BY CONSTRUCTION (F-P9). Each release compiles a NEW
     `CompiledLayoutGeometry`, so geometry-keyed caches miss for the candidate and hit only on baseline
     restore. That is not a broken cache; it is a cache whose key cannot express "this part is
     unchanged".
1.3  THE UNCHANGED PART IS ALREADY SHARED BY REFERENCE (EQ-5). `spliceWallFirstArchitectureCandidate`
     keeps UNAFFECTED Walls and centerlines shared by reference with the baseline. So an artefact
     derived from an unchanged Wall's centerline CAN legitimately be reused across baseline→candidate
     — if it is keyed on that centerline's identity, and if identity implies equality.
1.4  ACCEPTED-COMPILE REUSE IS ALREADY EFFECTIVE, AND ITS PRICE IS DOCUMENTED (F-P7, F-P8). The
     canonical-JSON equality proof costs one whole-document `JSON.stringify` per install — measured
     ≈0.1/0.2 ms (E2). It is the price of a real guarantee and it is a NON-SUSPECT (T-7).
1.5  THE INVALIDATION EDGES ARE NOT WRITTEN DOWN. CI-1 is a harvest table, not a maintained contract.
     Without a recorded graph, P23B.6 and any later slice will invent incompatible keys — which is
     exactly the failure the synthesised direction (P23B.3 §8.2, "which slice owns each shared
     prerequisite") requires this slice to prevent.
```

## 2. Scope, non-goals, dependencies

```text
IN
  · ONE written key grammar + invalidation graph, executable where possible
  · the identity⇒equality rule for reuse, and the guard obligation that a reuse must be able to REFUSE
  · lifetime policy (what may outlive a chain, what must not) with teardown tests
  · baseline→candidate reuse of per-Wall derived artefacts keyed on centerline identity (M-3), built on
    P23B.4's grammar
  · a measured statement of the acceptance-reuse proof's cost, and a documented decision NOT to weaken it
OUT
  · replacing `derivedWallSnapIndex` / `derivedWallMeshes` (T-7: they work)
  · replacing/weakening `reusesAcceptedCompile`'s canonical-JSON proof (a cheaper identity is only
    admissible if it provably covers every compiler input — not attempted here)
  · per-document caches · derived geometry in editor state · a second sample derivation
  · mesh/adapter caching implementation (P23B.6 applies this slice's rule)
  · an incremental compile cache (P23B.4's deferred item) · Worker/WASM · any budget
  · ANY INFERENCE FROM COORDINATES TO IDENTITY. The key grammar must not treat coincident coordinates
    as shared graph identity, and must not use "same position" as a proxy for "same Junction/Wall"
    (Option E revision; decision record §2.11.2, decision D-9). The model's connectivity authority is
    the explicit Junction id; the key derives from TRACED INPUTS, never from geometry equality
  · solving the mesh-artifact cone here — OR-9b remains a HANDOFF to P23B.6 / G15, not a gate here
DEPENDENCIES
  hard  P23B.4's key grammar must exist and be recorded before this slice generalizes it
  hard  P23B.0-durable's fixtures for the reuse/equivalence tests
```

## 3. Exact current entry points and ownership boundaries

| Artefact | Entry point | Current lifetime / key | This slice |
|---|---|---|---|
| compiled geometry | `resolvePreviewCompile:711` · `buildLayoutPreviewModel` `layout-mesh-factory.ts:62` | reactive field; new object per compile; no key | DOCUMENT only |
| accepted compile reuse | `reusesAcceptedCompile:726` (mark `preview-compile-reused:716` / `preview-compile:723`) | canonical `serializeWallFirstLayoutDocument` string equality | measure; do not weaken |
| snap index | `wallSnapIndex` `layout-snap.ts:580` (mark `snap-wall-index:584`) | `WeakMap` by compiled-geometry object | KEEP as the reference pattern |
| wall meshes + pick index | `resolveWallMeshes:650` · `buildWallMeshesByRoom:570` · `installWallMeshes:664` (mark `mesh-prebuild:658`) | `WeakMap` by compiled-geometry object | KEEP; P23B.6 extends the rule to sub-parts |
| curve samples | `wallCenterlineSamples` `layout-wall-centerline.ts:307` | none | P23B.4 owns the derivation; THIS slice owns its KEY + lifetime policy |
| per-object geometry keys | `cacheKeyOf` `layout-geometry.ts:1459` (`JSON.stringify(parts)`) | stored ON compiled records | bounded by the ENFORCED `cache-key-code-units` budget — measure any addition |
| baseline snapshot | `captureLayoutPreviewSnapshot` · `restoreLayoutPreviewSnapshot:2691` (marks `baseline-restore:2692`, `restore-*:2703-2719`) | frozen container; meshes reused | the reuse ORACLE lives here |
| install seam | `derivePreviewBundle:739` · `deriveInstallBundle:798` · `commitPreviewBundle:827` · `commitLayoutCandidate:882` · `installLayoutPreviewBundle:839` | writes the reactive field set | lifecycle/teardown surface |
| import seam | `importLayoutPreviewJson:953` | whole-document install | document-replacement surface |
| document replacement | the seams pinned by the landed `test(app): derive the document-replacement seams` work | — | use it; do not duplicate it |

## 4. Proposed mechanism, and the alternatives

```text
M-3  IDENTITY-keyed reuse for artefacts derived from UNCHANGED inputs.
     Rule (this slice's contribution, written once and consumed by P23B.6):
       An artefact may be reused across baseline→candidate iff
         (a) its key is the IDENTITY of its inputs (never the document), and
         (b) identity is proven to imply equality by a differential test against a from-scratch derive,
         (c) the reuse guard provably REFUSES when (a) or (b) does not hold, and
         (d) it remains VALID in the candidate's SURROUNDING topology.
     (d) IS THE RESEARCH'S THIRD IDENTITY QUESTION (P23B.2 §Q6, from Pascal's `level-miter-cache`
     `sameMiterInputs` pattern, **SV**), and it is what "prevents naive Room-local or Wall-local
     caching". Its concrete consequence, stated in the research and adopted here: **"An unchanged Wall
     centerline may be reusable for sampling. Its physical boundary mesh may not be reusable if its
     connected Junction, adjacent Wall or hosted Opening changes."**
     ⇒ THE MECHANISM SPLITS, and the split is load-bearing:
        M-3   SAMPLES, centerline-keyed — admissible under (a)–(d), because sampling depends on the
              centerline, its endpoints and traversal, which the key carries.
        M-3m  MESHES, adapter resources — admissible ONLY on a COMPLETE mesh-dependency contract
              (P23B.6's obligation, synthesis §6.5 O-2). A matching Wall id is never sufficient.
     ⇒ WHERE (d)'s CONCRETE CASES APPLY (corrected). The moved connected Junction, the changed adjacent
        Wall and the changed hosted Opening named in (d) are M-3m's cases. They reach M-3 only through
        the RESOLVED ENDPOINTS: if a neighbouring edit moves a shared Junction, this Wall's resolved
        endpoint moves and the sample key moves with it. If it does not, the sample reuse is expected to
        HIT — that is the reuse case this slice exists for, not a safety failure. OR-9 states both
        directions so neither is inferred.
     First application: per-Wall derived samples of UNCHANGED Walls, keyed on
     (centerline identity, traversal, resolved endpoints) — EQ-5's shared-by-reference fact is what makes
     this reachable without a per-document invalidation graph.
     Alternatives considered:
       (i)  per-document memo of the whole derive. REJECTED: the candidate is always a new document, so
            it would never hit where the cost is.
       (ii) content-hash keys over centerline JSON. REJECTED as the primary key: it costs a
            serialization per lookup and adds `cache-key-code-units` pressure (RL-3) for a hit rate no
            better than identity, given EQ-5.
       (iii) a new editor-level cache object. REJECTED: derived geometry in editor state creates
            pressure toward a second geometry truth.
LIFETIME POLICY (RL-1/RL-2).
     · chain-scoped by default; nothing derived retains a DOCUMENT object.
     · `WeakMap` keyed on the input object is the established pattern here (`layout-snap.ts:578`,
       `layout-preview-state.svelte.ts:630`) and is the model to imitate for any object-keyed store.
     · history/undo restore a PREVIOUS document object; identity-keyed reuse is safe exactly while the
       restored object IS the same object (which is why baseline mesh reuse works today). The slice must
       pin that assumption with a test rather than inherit it silently.
WHAT IS DELIBERATELY NOT DONE
     · no change to the accepted-compile proof: a cheaper identity than canonical JSON would have to
       cover every compiler input, and the current proof is not a measured cost. **P23B.2 §Q7 examined
       the alternatives and reached the same conclusion**: field-level identity (Pascal, **SV**) and
       generation-based invalidation (Bonsai, **SV** with tests) are established patterns, but "the
       current JSON check is inexpensive enough that replacement requires a measured reason", a content
       hash "cannot silently replace equality as a correctness proof", and "a revision counter is not
       equivalent to content identity unless every relevant mutation increments it correctly" —
       import, undo/redo, reconciliation, aliasing and candidate creation all count.
     · no generation counter for this phase: adopting one would make correctness depend on mutation
       coverage across import, history and reconciliation, which is a larger change than the measured
       cost it would remove. Recorded as a deliberate non-adoption, not an oversight.
```

## 5. Correctness invariants and equivalence proof obligations

```text
INV-1  Reuse never changes an output: reused artefact ≡ from-scratch derive (geometry, model, issues,
       bounds) on the committed fixtures.
INV-2  Every reuse guard can REFUSE; a guard that cannot refuse is unsafe by construction.
INV-3  No artefact is keyed on a document when its inputs are sub-document (and vice versa).
INV-3b (Option E revision) No artefact is keyed on COORDINATE COINCIDENCE. Two entities that coincide in
       space are the same entity only when they share an explicit identity (a Junction id), never because
       their coordinates match — the same assumption D-9 puts to the owner. This is a KEY constraint, not
       a performance one: a key built on position would silently conflate two independent groups the
       moment the policy permits them to overlap.
INV-4  The two existing geometry-keyed caches keep their current keys and behaviour.
INV-5  No derived geometry is stored in `LayoutDocument`/`SceneDocument` or editor reactive state.
INV-6  Cache lifetime does not retain a replaced document (teardown proven, not assumed).
INV-7  `cache-key-code-units` and `compiled-memory` (ENFORCED) do not regress without an explanation.
       A regression must not be silenced by re-recording the budget.
OR-5   identity⇒equality + refusal, for every reuse this slice adds (the P23.11 pattern in
       `apps/editor/tests/lib/bench/bend-perf.test.ts` is the template: equivalence test first, cost
       test second, and the guard proven to refuse an unproven document).
OR-6   non-mutation of the checked-in baseline, and no baseline write from `npm test`.
OR-7   Plan/3D/visitor parity + visitor bundle gate.
OR-8   LIFECYCLE: after document replacement, undo/redo and baseline restore, (a) no stale artefact is
       served, (b) the caches do not retain the previous document (teardown observable).
OR-9   DEPENDENCY-CONE REFUSAL, SCOPED TO THE ARTEFACT'S OWN INPUTS (CORRECTED, from P23B.2 §Q6 /
       rule (d)). The oracle must follow the artefact's ACTUAL dependency cone. Rule (d) is a
       completeness rule, and the cone of THIS slice's artefact — a per-Wall sampled centerline — is its
       KEY (centerline identity, traversal, resolved endpoints, sampling options), not the document.
       Both directions are binding:
         REFUSAL  a change to ANY declared key input defeats the reuse — a centerline edit, a traversal
                  flip, a resolved-endpoint move, an options change.
         HIT      an edit that leaves those inputs unchanged must still HIT, and the hit must be equal to
                  a from-scratch derivation (INV-1). This IS the slice's reuse case — an adjacent edit
                  that leaves this Wall's centerline and resolved endpoints alone — so a guard that
                  refuses here is a silent performance regression, not safety.
         ENDPOINT COVERAGE  if a neighbouring edit moves a SHARED Junction, the resolved endpoint moves
                  and the key moves with it, so the REFUSAL row covers it — provided the resolved
                  endpoints are genuinely IN the key. That is P23B.4's key-grammar obligation, which this
                  slice CONSUMES rather than re-derives.
OR-9b  MESH-ARTEFACT CONE, NOT ASSERTED HERE (from P23B.2 §Q5). The WIDE negative matrix — moved
       connected Junction, changed adjacent Wall, changed hosted Opening, dimension, material group — is
       the MESH builder's dependency cone: it consumes connected geometry and hosted children that the
       sampler never reads. If M-3m ships, that matrix is evidenced at P23B.6 and gated by G15.
       Asserting it against a per-Wall SAMPLE cache would be a category error — it demands a refusal the
       sample artefact's inputs cannot even observe, and would either fail honestly or push an
       implementer to over-refuse, which is the regression OR-9's HIT row exists to catch.
OR-10  BASELINE INTEGRITY AFTER REFUSAL (NEW, from P23B.2 §Q6/§E): a REJECTED candidate followed by
       restoration must leave baseline resources intact and must not have been sourced from a
       partially-shared candidate artefact.
OR-11  RESOURCE-GROWTH LOOP (NEW, from P23B.2 §Q5/§E): repeated edit/undo/redo cycles must not grow
       retained resources — the observable being the cache/store populations and disposal counts.
```

## 6. Implementation steps (each independently committable and reviewable)

```text
S1  WRITE THE INVALIDATION GRAPH (docs + one executable assertion file)
    One maintained table: artefact → input authority → lifetime → key → what the key does NOT cover →
    who may reuse it. Replace CI-1's harvest-time status with a current contract, and note that CI-1
    remains the harvest's historical record (do not rewrite it).
S2  MAKE THE EXISTING BEHAVIOUR EXECUTABLE
    Pin the two existing WeakMap behaviours and the baseline-restore reuse with explicit tests
    (baseline restore rebuilds 0 meshes; snap index is consulted once per geometry). These tests are the
    regression net for everything that follows.
S3  OR-8 LIFECYCLE/TEARDOWN TEST (and OR-10/OR-11 with it)
    Extend the landed document-replacement seams to assert that no artefact outlives its document, that
    a rejected candidate leaves baseline resources intact, and that edit/undo/redo cycling does not grow
    retained resources. Land BEFORE any new cache so the failure mode is measurable.
S4  M-3 FOR PER-WALL SAMPLES OF UNCHANGED WALLS
    Consume P23B.4's key grammar; reuse across baseline→candidate for untouched Walls only. Tests:
    OR-5 (equivalence + refusal) + OR-9 BOTH DIRECTIONS (refuse on a key-input change; still hit on an
    adjacent change that leaves the key untouched) + INV-3 + the interaction suites that exercise a drag
    (transient preview, layout-curve-read-path, bend-perf). The over-refusal case is a first-class test
    here, not a follow-up: a guard that refuses everything passes every refusal-only oracle.
S5  MEASURE, AND REPORT HONESTLY
    Record `preview-compile`-vs-reused counts, mesh prebuild counts and the advisory per-stage numbers
    across the committed fixtures. State the hit rate at each stage; no proportionality claim.
S6  RECORD THE NEGATIVE DECISIONS
    Write down WHY the accepted-compile proof, the two WeakMaps and the sampling defaults are NOT
    changed (T-7/§6.2), so a later slice cannot quietly "improve" them.
```

## 7. Acceptance tests, differential tests and measurement

```text
MUST PASS
  OR-5 · OR-6 · OR-7 · OR-8 · OR-9 · OR-10 · OR-11
  (OR-9 is TWO-SIDED: the refusal rows AND the hit row are binding. An over-refusing guard fails this
   slice as surely as a stale hit does.)
  NOT IN THIS SLICE'S GATE — OR-9b. It is a HANDOFF, not a test to run here: the mesh-dependency
  negative matrix is evidenced at P23B.6 and gated by the conditional final check G15. Listing it as
  MUST PASS would be self-contradictory, because this slice ships no mesh artefact — nothing here could
  assert it, and the slice would be blocked by an oracle it is not authorized to write.
  the whole applicable suite: `npm test` / lane union · `npm run test:arch` (always, never path-gated)
  · `npm run check` · root build · `npm run test:perf` unchanged green
  the P23.11 bend-perf equivalence tests and the accepted-compile-reuse assertion
DETERMINISTIC
  `cache-key-code-units` · `compiled-memory` — unchanged or explained; never re-recorded to pass.
MEASUREMENT (ADVISORY)
  hit/miss counts per stage (deterministic, assertable) + wall-clock p50/p95 on identical fixtures with
  the P23B.0-durable provenance block. Report the hit rate; do not claim a saving proportional to it.
```

## 8. Regression and resource-lifetime risks

```text
R-1  A key that omits traversal silently mirrors geometry (S-2 of P23B.3). The key grammar must state
     traversal explicitly.
R-2  A key that omits resolved endpoints can serve samples derived from moved endpoints. If P23B.4's
     finding shows reconciliation can move Junction points, endpoints must be in the key.
R-3  Document retention via a strong reference in a long-lived map (synthesis RL-1/RL-2). Mitigation:
     `WeakMap` on the input object, chain-scoped default, and the OR-8 teardown test.
R-4  `cache-key-code-units` is ENFORCED (620,268 code units for Chopin at the recorded baseline; target
     700,000 / fail 1,300,000). Storing key strings per Wall/traversal at authoring density must be
     measured against it. Re-recording a budget to silence a failure is forbidden.
R-5  A reuse that cannot refuse (INV-2/OR-5) is unsafe by construction: it can serve a stale artefact
     after a document change the guard never observed.
R-6  Undo/redo restores previous document objects. Identity-keyed reuse is only correct while the
     restored object is the same object; the slice must pin that with a test, because it is an
     assumption `restoreLayoutPreviewSnapshot` currently relies on implicitly.
R-7  Interaction with P23B.4's threaded context: two derivations (chain-scoped and reused) must not
     disagree. One grammar, one owner.
R-8  Measurement perturbation: hit/miss counters must not themselves be hot-path work (a counter in the
     inner loop changes what it measures).
```

## 9. Exit criteria and what must be recorded before advancing

```text
X-1  All MUST PASS green; commands recorded.
X-2  The invalidation graph is landed as a current contract, and CI-1 is left intact as history.
X-3  OR-5/OR-8 results recorded, including refusals observed (a guard that never refused is unproven).
X-4  Hit rates + advisory timings recorded with provenance; the negative decisions (S6) recorded,
     including the deliberate non-adoption of generation-based invalidation and of any replacement for
     the canonical-JSON acceptance proof.
X-4b OR-9/OR-10/OR-11 evidence recorded: the observed REFUSALS (not only the hits), baseline integrity
     after a rejected candidate, and no retained-resource growth across edit/undo/redo cycles.
X-5  The identity⇒equality rule is written in ONE place for P23B.6 to consume (synthesis §8.2).
X-6  Ruling status recorded (it affects what a duplicate/import reuse may assume).
X-7  Slice closed on the same branch with an identifiable commit series; slice-closeout applied.
```

## 10. Explicitly deferred

```text
- replacing the accepted-compile canonical-JSON proof with a cheaper identity (Q7 — needs P23B.2 and a
  proof that the replacement covers every compiler input)
- any sub-part mesh reuse IMPLEMENTATION → P23B.6 (this slice supplies the rule)
- incremental compilation cache → P23B.4's deferred item
- Worker/WASM → P23B.8
- any ENFORCED curved-authoring budget → owner ratification
```
