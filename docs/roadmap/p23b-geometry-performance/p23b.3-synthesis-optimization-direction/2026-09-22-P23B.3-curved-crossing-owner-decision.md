# P23B.3 — Curved-crossing validity: owner decision record

```text
STATUS:   OPEN — owner decision required. Options A–D remain unranked and unchosen. **Option E is
          recorded as the OWNER-DIRECTED DIRECTION** (§2.10) for revised planning; neither its
          acceptance criteria, its delivery scope nor its delivery slot is ratified by recording it.
ROLE:     the decision record the P23B.3 contract requires, presented SEPARATELY from every performance
          item. This is a correctness / product-policy defect P23B.1 located; it must not be buried in
          an optimization slice, and it must not be resolved by an implementer.
EVIDENCE: P23B.1 harvest record §2 (F-C1…F-C7) and §6 (TO-1, Q1–Q5 answered with anchors) + §8 (PO-1
          options A–D). Source-verified by P23B.3 at 51845b3; symbols re-resolved, not line numbers.
          Option E's contract is grounded in a NEW source verification at `7c3d392a` (§2.11), not in
          the P23B.1 anchors alone — the ownership model E depends on had not been traced before.
NOT:      an implementation · a fix · a gate change · a test change · a reopened P23 decision · a
          performance item · a persisted-format proposal · a UI/command design · a vertical-stacking
          capability.
AUTHORITY: the harvest evidence → ../p23b.1-internal-geometry-pipeline-harvest/2026-09-22-P23B.1-harvest-record.md
          the synthesis that consumes this ruling → ./2026-09-22-P23B.3-synthesis.md (§9.1)
          order / gate placement → ../../README.md §SEQUENCE (unchanged, restated nowhere here)
OWNER:    the Museum Editor product owner. **RULED 2026-09-22 — see §5 and its disposition block:
          D-1 Option E · D-9 confirmed as written · D-10 planning-level only · D-11 ARRANGEMENT 1 ·
          D-12 owned by the policy slice (§4.2).** The record still does NOT authorize implementation:
          the implementation plan and the policy slice's own acceptance criteria remain pending
          ratification. What unblocks the two dependent consumers is the behaviour actually SHIPPED,
          not the ruling text (§4.1).
```

## 1. The defect, stated once

A Room-duplicate / repeat operation and an import can both **admit** geometry that the canonical
topology gate later **rejects**. Concretely: a curved (cubic-chain) Wall copied into a position whose
**Bézier bows cross** another Wall while their **endpoint chords stay disjoint** is admitted, because
the duplicate batch gate classifies Wall pairs with the chord-exact `classifyWallIntersection`
(`packages/layout-core/src/layout-wall-topology.ts:129`) and never calls the sampled crossing gate
`detectWallCurveTopologyCrossings` (`packages/layout-core/src/layout-wall-first-precision.ts:1964`).
The next document-wide sampled gate then rejects — including for edits that have nothing to do with the
crossing — and the rejection is attributed to the *new* edit rather than to the pre-existing geometry.

```text
ONE RULE, FOUR SCOPES (F-C7 / Q5):
  enforced  validateWallFirstTopology (layout-wall-first-precision.ts:1732, gate at :1790)
  enforced  validateChainTopology (layout-wall-chain.ts:982) — Wall-chain / rectangle authoring
  ABSENT    validateWallFirstLayoutDocument (layout-wall-first-codec.ts:164)  → import admits (F-C6)
  ABSENT    validateBatchWallTopology (layout-duplicate.ts:201)               → duplicate admits (F-C1)
  INTRA-ROOM ONLY  the compile (layout-geometry-validation.ts:128/138)        → inter-Room crossing OK
The gate is the same function the duplicate path failed to call, so the two verdicts disagree by
construction, not by a numeric tolerance (F-C4).
```

**Impact is UNKNOWN, mechanism is established.** No repository evidence exists that any live exported
payload actually carries an inter-Room curved crossing (harvest UN-9). Whether the omission at
`layout-duplicate.ts` was a deliberate P23.4 scope choice or an oversight is also **UNKNOWN** (harvest
UN-8) — though the doc comment there claims parity with the canonical bar, which it does not hold.

**How Option E re-reads this defect.** The four-scope table above describes the rule's *enforcement* gap.
Option E (§2.10) changes the rule's *subject*: the gate keeps failing on geometric intersection, but only
between entities in the SAME connected group, so the intended behaviour of a crossing between independent
groups stops being "rejected" and starts being "permitted geometry". The enforcement gap matters either
way — under E, a path that omits the group-scoped gate still cannot distinguish a permitted independent
overlap from a genuine intra-group violation. **The delivered model is in §2.11, traced from source at
`7c3d392a` rather than inherited from these anchors.**

---

## 2. The decision

> **Which validity policy governs curved Walls whose Bézier geometry crosses another Wall, and what
> happens to documents that already contain such a crossing?**

Four bounded options were presented in P23B.1 §8 (PO-1) and are reproduced here with the consequences
each one has on the surfaces this record is required to cover. **No option is recommended. This record
exists so the owner can rule, not so an implementer can infer.**

### Option A — reject at ingress (one rule, every surface)

Every surface — duplicate/repeat, authoring, import — applies the single sampled-curve validity rule, so
an invalid document can never be created **or admitted**.

| Surface | Consequence |
|---|---|
| Authoring / Room duplication | Room duplicate gains `detectWallCurveTopologyCrossings` on its batch path; a chord-disjoint but curve-crossing copy is refused **before** any history entry, identity mint or install. Duplication becomes consistent with Bend/knot/chain authoring. |
| Import of an already-invalid document | **Refused at ingress.** The codec (or the import seam) gains a topology rule it does not have today. An exported payload carrying such a crossing stops importing. |
| Editing / repairing an admitted-invalid document | The state becomes unreachable *through the product*, so no repair path is required for new documents. **But** it remains reachable from payloads already imported before the change and from any hand-edited file: the ruling must say what happens to an already-open document that fails the new gate. |
| Diagnostics, identity, history | Cheap and clean: one verdict, one code, one attribution. No new severity state, no identity semantics, no history semantics. |
| Existing exports | **The real cost.** Compatibility breaks for any payload containing a curved inter-Room crossing. Needs a decision on whether a migration/repair hint is offered, and whether the break is silent or explained. |

*Surfaces touched:* `validateBatchWallTopology` (or its replacement call), the codec or the import path,
any fixture/import-compatibility expectation. *Evidence:* F-C1, F-C3, F-C6.

### Option B — admit with an explicit invalid state

The document may exist and render, is **visibly marked invalid**, and later Wall operations report the
**pre-existing** cause rather than a fresh rejection.

| Surface | Consequence |
|---|---|
| Authoring / Room duplication | Duplication may still admit the crossing — but no longer silently: the result carries an explicit invalid state. Requires a decision on whether duplication is also *warned* up front. |
| Import of an already-invalid document | Import continues to admit; the document is marked invalid on arrival. Needs the vocabulary to exist before import can express it. |
| Editing / repairing an admitted-invalid document | The document is editable in the sense that it loads, but a *repair path* is implied: an invalid document must be repairable or the state is a trap. Whether "repair" means user-managed or assisted is a further call (it shades into Option C). |
| Diagnostics, identity, history | **The expensive option.** Requires a severity/vocabulary decision (`invalid` as a first-class document state vs. an issue list), attribution that names the pre-existing crossing Walls rather than the touched Wall, and a history semantics decision: does the invalid state participate in undo/redo, and does a repair produce a history entry? Identity is unaffected (no new entities), but *entity-level* attribution must survive the document-wide scan. |
| Existing exports | **Best compatibility.** Existing payloads keep importing; the product's honesty improves instead of its strictness. |

*Surfaces touched:* the issue/severity vocabulary, the Plan/3D invalid-rendering path, message
attribution in `validateWallFirstTopology`. *Evidence:* F-C2, F-C5.

### Option C — provide repair (offer an explicit resolution)

When an operation would produce a crossing, the system offers an explicit resolution — separate, nudge
or noding — instead of silently rejecting.

| Surface | Consequence |
|---|---|
| Authoring / Room duplication | Duplication of a crossing-producing Room becomes a **guided interaction** rather than a refusal. |
| Import of an already-invalid document | Still needs an answer; C does not decide ingress. C pairs with A or B. |
| Editing / repairing an admitted-invalid document | This IS the repair path — the option whose purpose is the third surface. |
| Diagnostics, identity, history | Substantial: the planner result vocabulary gains an "offered resolution" outcome; the gesture gains a UI affordance; the resolution itself is an edit and therefore must produce deterministic selection/history behaviour. Choosing "noding" specifically interacts with the landed noding path and with Wall identity (whether a split creates entities). |
| Existing exports | Unaffected by itself (it adds capability, not strictness). |
| **Nature of the change** | **This is CAPABILITY, not cost.** It adds an authoring affordance the product does not have. P23B's umbrella scope is explicitly cost, not capability (`no new architectural capability, view, mode, document...`; "P23B adds no view, no document, no schema"). |

*Surfaces touched:* the planner result vocabulary, the editor gesture UX, history. *Evidence:* F-C5,
F-C3. **Scope flag:** adopting C requires an explicit scope decision — it is not a P23B optimization and
should probably not live in P23B at all.

### Option D — leave as-is, document it

Behaviour is unchanged; the situation is documented.

| Surface | Consequence |
|---|---|
| Authoring / Room duplication | A successful operation can still create geometry a later, unrelated operation rejects. |
| Import of an already-invalid document | Still admitted (F-C6). |
| Editing / repairing an admitted-invalid document | Still blocked by the pre-existing crossing, still misattributed to the new edit. |
| Diagnostics, identity, history | Unchanged — including the misattribution. |
| Existing exports | Fully compatible. |
| Depth | The defect is intentionally kept, and the misleading **code comment** at `layout-duplicate.ts:196-200` (which claims a bar the gate does not hold) should be corrected as part of closing, or the state is actively misleading to the next reader. |
| Interaction with M-2 | **Blocking.** If D is chosen, the synthesis's verdict-reuse half of M-2 is inadmissible (§8.6 AB-3): you cannot cache a negative verdict whose existence the product does not acknowledge. |

*Surfaces touched:* documentation only. *Evidence:* F-C1, F-C5, F-C6.

---

### 2.10 Option E — independent spatial placement; explicit topological connection (OWNER-DIRECTED, DIRECTION ACCEPTED)

**STATUS: the owner ACCEPTED this direction and, on 2026-09-22, RULED its delivery — D-1 = E, D-9
confirmed as written, D-10 planning-level only, D-11 = ARRANGEMENT 1, D-12 owned by the policy slice
(§4.2).** What that does NOT do: authorize an implementation or settle that slice's own acceptance
criteria. The implementation plan remains PROPOSED pending ratification. Two architecture corrections
from the earlier review are folded in — the coincidence rule (§2.11.2) and the incomplete ownership path
(§2.11.3) — and neither introduces a schema change.

The defining principle:

> Spatial overlap is not topological connectivity. Rooms and disconnected Wall groups may occupy
> overlapping coordinates without automatically becoming one architectural structure. Joining them
> requires a deliberate creator action.

**E is not B, and E is not D.** B admits the document and carries a *marked-invalid* state — the overlap
is still an error, merely a tolerated one. D keeps the existing defect and documents it. Under **E the
intersection between INDEPENDENT groups is intentionally permitted geometry**: not an error, not a
defect, and not something a later operation must repair.

What E replaces is narrower and more precise than "the crossing rule": F-C7's *one rule, four scopes*
stays one rule — but its **subject** changes from "all Walls in the document" to "Walls in the same
connected group".

```text
REQUIRED BEHAVIOUR (owner statement)
  · an independent Room can be MOVED across another Room
  · a Room can be DUPLICATED at exactly the same coordinates as its source
  · different independent Room groups may have crossing or coincident Wall geometry
  · the operation does NOT automatically create or share Junctions, split Walls, merge Rooms, create
    portals, or alter either group's identity
  · Wall and Junction ids remain DISTINCT unless an explicit connection operation establishes shared
    topology
  · associated objects and hosted Openings remain with their owning Room/Wall
  · a geometry-alignment or snap suggestion does NOT itself authorize a topological join
  · any two Rooms that need to connect must be deliberately joined by the creator
  · future collision warnings may identify overlapping entities, but stay NON-BLOCKING and must not
    automatically repair, node, join or alter geometry

THE BOUNDARY OF THE POLICY (not negotiable in planning)
  valid WITHIN a connected structure   vs   overlap BETWEEN independent structures
  E relaxes EXACTLY ONE class — geometric intersection between entities that are not in the same
  connected group. Everything intrinsic stays rejected: self-intersecting Walls, malformed Room
  boundaries, illegal relationships inside an intentionally CONNECTED group, zero-length Walls, Room
  boundary closure and role, and the existing intrinsic invariants.

NOT IN SCOPE
  no vertical stacking semantics. Same-position X/Z overlap is in scope for the POLICY definition;
  floor/elevation authoring is a separate product capability and is not opened here.
```

### 2.10.1 Concern matrix (what E directionally decides, and what it defers)

| Concern | Option E direction |
|---|---|
| Independent spatial overlap | **Permitted** geometry, for entities not in the same connected group |
| Automatic connectivity from overlap | **Forbidden** — no junction adoption, no noding, no merge, no portal creation |
| Intrinsic Wall/Room validity | **Preserved** — unchanged rejection set (see the boundary block above) |
| Explicitly connected group topology | **Validated within its applicable connectivity scope** — the group, not the document |
| Deliberate joining | **Explicit creator operation** — interface and invariants defined at planning level, UI/commands/modes deferred |
| Collision warnings | **Deferred and non-blocking** — must not repair, node, join or alter geometry |
| Import compatibility | Once E ships, existing independent overlaps must **not** be rejected solely for their spatial intersection |
| Selection and visual overlap | **Separate acceptance requirements** — no implied mesh union, no visual stacking system, no automatic occlusion or renderer-ordering fix |

---

### 2.11 Verified current behaviour — the topology ownership model (source-verified at `7c3d392a`)

Traced BEFORE designing anything, because Option E's whole question — *can independence be distinguished
from coincidence without a schema change?* — is answerable only from the model. All anchors are symbols,
re-resolved at this HEAD; P23B.1's line numbers are discovery anchors only.

```text
Q1 — HOW ARE CONNECTED WALL/ROOM GROUPS REPRESENTED TODAY?
  There is NO group record and NO persisted relation. Connectivity is DERIVED, and its authority is
  the Junction:
    · layout-wall-first-types.ts — "Junction IDs are the only normal-authoring connectivity authority";
      "Junction ID equality *is* connectivity". LayoutDocumentWallFirst carries junctions · walls ·
      rooms · openings · objects · an optional identity ledger, and NOTHING ELSE: no group, no parent,
      no cluster, no cluster id.
    · connectedRoomIds (layout-room-isolation.ts) — the RIGID-CONNECTIVITY graph. It builds
      junction→rooms from each Room's boundary Walls' endpoints and takes the transitive closure from
      the anchor Room; its own doc says it "subsumes shared Walls, since a shared Wall shares its
      endpoints". Deterministic: anchor first, then members in document order.
    · resolveIsolatedRoomGroupSubgraph (same module) materialises that group as ONE rigidly movable
      unit — the "CONNECTED ROOM GROUP" layout-room-move.ts calls the movable unit. A Room with no
      shared boundary Junction is a group of one, "discovered rather than special-cased".
  ⇒ THE DISCONNECTED-GROUP DISTINCTION OPTION E NEEDS ALREADY EXISTS. It is connectedRoomIds's notion
    of a group. No group id is required, and none should be added.

Q2 — ARE EXPLICIT IDS + ROOM BOUNDARY REFERENCES SUFFICIENT TO DERIVE CONNECTIVITY?
  Yes for Wall/Room connectivity: a Room's boundary: OrientedWallRef[] resolves to Walls, whose
  startJunctionId/endJunctionId give Junction identity.
  Portal relations are NOT a second authority. validateWallFirstPortalRelations (layout-portals.ts)
  requires a door's connectsRoomIds pair to be exactly the two physical Rooms whose reconciled boundary
  references the hosting boundary Wall, and its doc is explicit: "the relation never creates adjacency
  by itself — this set derives purely from Room boundaries". A portal is a DEPENDENT relation validated
  against derived adjacency (fail-closed at Save), never an input to group membership.

Q3 — DOES A SHARED WALL, SHARED JUNCTION OR PORTAL MAKE TWO ROOMS ONE TOPOLOGY GROUP?
    shared Junction  → YES, rigidly
    shared Wall      → YES, by the subsumption above (two Rooms referencing one Wall share its Junctions)
    portal relation  → NO — validated against adjacency, never constitutive of it

Q4 — WHICH VALIDATORS CONFUSE COORDINATE COINCIDENCE WITH SHARED IDENTITY?
  Five, with a precise and consistent confusion: they treat GEOMETRIC INTERSECTION as a topology error
  without ever asking whether the two entities are in the same connected group.
    1. validateWallFirstTopology (layout-wall-first-precision.ts) — THE one canonical gate.
       Document-global pairwise classifyWallIntersection over EVERY Wall pair (WallIntersectionKind =
       none · shared-explicit-junction · endpoint-on-interior · proper-crossing ·
       collinear-endpoint-touch · collinear-overlap · invalid). Every outcome other than `none` /
       `shared-explicit-junction` is a hard failure. Group membership is never consulted. It also
       rejects `duplicate_junction_point` for ANY two Junctions within JUNCTION_COINCIDENCE_EPSILON
       (1e-9) ANYWHERE in the document.
    2. detectWallCurveTopologyCrossings (same file) — the sampled curve authority, equally document-
       global; returns the FIRST crossing in document.walls order.
  3. validateChainTopology (layout-wall-chain.ts) — a SEPARATE function holding the same bar via the
     same two lower-level functions, per its own doc: "every wall pair must relate only through explicit
     shared junctions (or be disjoint)". It is not a call into (1).
    4. validateBatchWallTopology (layout-duplicate.ts) — the same bar, chord-exact, whole-document.
    5. validateLayoutRoomGeometry (layout-geometry-validation.ts) — the compile, which is INTRA-ROOM
       ONLY (validateLayoutDocumentGeometry loops floors → rooms → per-Room validation). A crossing
       between two Rooms is invisible to it. This is F-C2, and it is the OPPOSITE asymmetry from 1–4:
       the compile and the topology gate disagree by construction.

Q5 — WHICH OPERATIONS AUTOMATICALLY ACQUIRE CONNECTIVITY OR PERFORM NODING?
  Exactly ONE authoring path does it, and the claim is narrower than the module names suggest:
    · planWallChain (layout-wall-chain.ts) applies two automatic fixes:
        – ADOPTION — an existing Junction whose point coincidesAsJunction (1e-9) is ADOPTED
          ({ kind: 'adopt', keepJunctionId, duplicateJunctionIds }), so a new Wall drawn to an existing
          group's Junction REUSES its id instead of minting a parallel one;
        – SPLITTING AT CROSSINGS — planWallCrossing(candidate, fix.wallIds, fix.point, …) inserts the
          shared Junction and splits the host Wall. planWallChain is the ONLY production caller of
          planWallCrossing: verified, it is the sole non-test reference.
      Both produce SHARED JUNCTION IDS: automatic connectivity from geometric overlap.
      Its single exception is the curved crossing, which REJECTS rather than auto-noding — "any curve
      crossing that would need automatic curved noding rejects the whole authoring command".
  ⇒ SCOPE THIS CLAIM CAREFULLY (self-correction): 
       the NODER (planWallSplit / planWallSplitAtPoint) is NOT chain-only. It also serves two DELIBERATE,
       POINT-ADDRESSED operations that are not emergent joins:
         – planWallSubdivision (layout-wall-first-precision.ts) — an explicit "add one Vertex by
           subdividing a Wall at a physical meter offset", delegated to the same noder for deterministic
           ids, Opening rebasing and boundary rewrites;
         – layout-migration.ts — replay of legacy node events into splits during migration.
       Neither is overlap-driven and neither may be withdrawn by a policy change. What Option E targets
       is the CHAIN path's adoption and crossing-split, never the noder itself.
  Room duplicate and Room move do NOT node. They validate and reject.

Q6 — WHICH EXISTING CONTRACTS INTENTIONALLY DEPEND ON DOCUMENT-GLOBAL TOPOLOGY VALIDITY?
    · validateWallFirstTopology — the one canonical gate, and its own doc names its scope: "Shared by
      the P23.1 precision planners (via `finalizeCandidate`) and the P23.6a Room-move planner — never
      copied." The CHAIN path does NOT call it: it runs its own validateChainTopology over the same bar
      and the same two lower-level functions. So there are TWO document-global gates with one shared
      predicate layer, not one gate with four callers — a distinction that matters because a policy
      change must re-scope both, and re-scoping only one would leave Room-move and chain authoring
      disagreeing.
    · validateChainTopology — "the P23.9 plan forbids un-noded visual crossings in committed data".
    · validateBatchWallTopology — "every wall pair must relate only through explicit shared junctions".
    · the codec (validateWallFirstLayoutDocument) — explicitly EXCLUDES topology: "P23.8's topology
      validity rules (noding, crossings, enclosure, correspondence) are out of scope here". That is the
      ingress hole F-C6, and the reason duplicate and import admit what the gate later rejects.
    · the compile — depends on topology only INTRA-Room.
  ⇒ FOUR contracts must change scope together for E. The codec's exclusion is NOT one of them: E does
    not require the codec to start validating geometry.

GATE ASYMMETRY, RESTATED AS THE DEFECT'S SHAPE (this is what makes F-C1 and F-C6 possible):
  operation             gates that run                                       curve crossing checked?
  Room duplicate        isolation → codec → validateBatchWallTopology(chord)   NO  → admits (F-C1)
  Room move             codec → validateWallFirstTopology(full) → portals     YES → rejects
  Wall chain authoring  auto-adopt/auto-node → validateChainTopology(full)     YES; rejects the curved case
  import                codec only                                            NO  → admits (F-C6)
  compile               per-Room boundary geometry                            NO  → blind  (F-C2)

Q7 — IS `connectedRoomIds` A SUFFICIENT CONNECTIVITY TEST FOR OPTION E?
  NO. It is ROOM-ONLY: it builds its junction→rooms map by iterating `document.rooms` and each Room's
  `boundary: OrientedWallRef[]`. A Wall that no Room boundary references — a standalone Wall, a
  partition stub, an unenclosed chain — contributes NOTHING to that map, so two such groups, or a Room
  and such a group, cannot be classified by it at all. `connectedRoomIds` answers "which Rooms are
  rigidly joined", which is exactly what Room move needs; it is NOT a general Wall/Junction
  connectivity test. E needs the general test, with the Room grouping DERIVED from it where applicable
  — never an assumption that the Room helper classifies every pair (see §2.11.3 GAP 1).
```

#### 2.11.1 The hard cases, verified against the model

| Hard case | What the code does TODAY | Consequence for Option E |
|---|---|---|
| Exactly coincident independent Junctions | `LayoutJunction` is `{ id, point }` — two records with DIFFERENT ids and EQUAL points are **already representable**, and Junction ID inequality already means "not connected". Two guards forbid the state being *authored*: (1) `validateWallFirstTopology`'s document-wide `duplicate_junction_point` (any two Junctions within 1e-9); (2) every authoring path that resolves a coincident endpoint ADOPTS the existing Junction instead of minting a second (`planWallChain`). | **No representation is missing.** The obstacle is one validation-scope invariant plus one authoring reflex — see §2.11.2. The type, the connectivity rule and the persisted format already express E's distinction. |
| Crossing and collinear overlapping Walls between independent groups | Rejected: `proper-crossing` / `collinear-overlap` / `endpoint-on-interior` via `validateWallFirstTopology`; curve crossings via the sampled gate. | In scope for E. Needs the group-scoped SUBJECT, not new representation. |
| An independent Wall touching another group's endpoint | Chain authoring ADOPTS the coincident Junction ⇒ the two become ONE group. Without adoption the same coordinates are rejected as `duplicate_junction_point`, and a genuine T is rejected as `endpoint-on-interior`. | Today "touching" ⇒ "joining". E requires distinct identity, so this is a real behaviour change — and it is the same 1e-9 question as the first row. |
| Two Rooms with a DELIBERATELY shared Wall | Already representable and already correct: `connectedRoomIds` makes them one rigid group; `resolveIsolatedRoomGroupSubgraph` moves them together (an interior shared Wall travels exactly once); `validateWallFirstPortalRelations` requires a portal pair to match the two Rooms adjacent to the hosting boundary Wall. | **Preserve unchanged.** This IS E's "explicitly connected group", and it already works. It is the containment proof that E's restriction must not break. |
| A disconnected standalone Wall intersecting a Room boundary | Rejected by the document-global gates (rows above); the compile is blind to it (F-C2). | In scope for E as permitted overlap; the compile question is separate and belongs to P23B.9's cross-Room cases (G3). |

#### 2.11.2 The coincidence rule, corrected (decision D-9 — a rule, not a two-way choice)

The 1e-9 rule is documented as "an identity tolerance, not a screen-space acquisition radius and not a
geometry/intersection tolerance. It only absorbs numerical noise that cannot express a separate authored
node" (`layout-junction-identity.ts`). Two of E's stated requirements — "a Room can be duplicated at
exactly the same coordinates as its source" and independent Walls with identical endpoint coordinates —
land squarely on that documented assumption: at the same coordinate there is, by construction, only ONE
node.

**CORRECTED (owner review of `622c1521`).** An earlier revision presented this as a choice between
"(b) group-dependent coincidence" and "(c) purely explicit identity" and asked the owner to pick one.
That framing was wrong on two counts, and both corrections stand:

1. **The two answers address DIFFERENT questions and must coexist.** (b) is about where the *validity*
   invariant applies; (c) is about what determines *connectivity*. Presenting them as exclusive made an
   owner choose between two things that are both required.
2. **No representation is missing.** `LayoutJunction` is already `{ id, point }` with **no uniqueness
   constraint on `point`**, so two records at equal coordinates with different ids are representable
   *today*. What forbids the state being authored is a document-wide validation invariant and an
   authoring reflex — not the schema. Calling this "the one missing representation" was a terminology
   error in this record, and it is corrected here rather than carried.

The resolved rule, now recorded as **D-9** and no longer posed as a two-way choice:

```text
CONNECTIVITY — explicit GRAPH IDENTITY, never coordinates.
  Junction ID equality is connectivity (layout-wall-first-types.ts: "Junction ID equality *is*
  connectivity"). Two Junctions at the same point with different ids are NOT connected, and nothing
  needs to be added to say so. This is (c)'s answer, applied to connectivity.

VALIDITY — the duplicate-node invariant is scoped to the component in which a node is an ACCIDENT.
  ACROSS independent components: distinct Junctions at equal coordinates are PERMITTED. This is E's
  requirement, and it is (b)'s answer, applied to the validity boundary only.
  WITHIN one already connected component: an accidental duplicate node REMAINS INVALID, because there
  it can only describe a malformed graph. E relaxes the BETWEEN case and nothing else.

AUTHORING — an explicitly requested connection may ADOPT a Junction id; ordinary spatial placement
  must NOT.
  planWallChain's adoption is CORRECT for extending a group (operation class 3, §4.0.1) and WRONG for
  independent placement (class 2). The discriminator is the OPERATION's intent, not the geometry.

IDENTITY IS NOT A SCHEMA CHANGE. The persisted format keeps `{ id, point }`. NO group id, cluster
  relation, parent pointer or persisted connectivity record is added, and none may be invented to make
  a plan look complete.
```

The two obstacles this rule must clear — both verified at source, neither a representation gap:

```text
OBSTACLE 1 — a VALIDATION-SCOPE fact, not a type fact.
  validateWallFirstTopology (layout-wall-first-precision.ts) rejects `duplicate_junction_point` for any
  two Junctions within JUNCTION_COINCIDENCE_EPSILON (1e-9) ANYWHERE in the document. The TYPE already
  permits what the INVARIANT forbids, so the change is to SCOPE the invariant to the connected
  component, not to widen the type or the format.

OBSTACLE 2 — the AUTHORING reflex.
  planWallChain ADOPTS a coincident Junction rather than minting a second (its own `{ kind: 'adopt' }`
  fix), and the snap/commit path resolves a coincident endpoint onto the stored node. Ordinary spatial
  placement must stop inheriting this; an explicit Join (D-10) is where adoption belongs.
```

What genuinely REMAINS open, and is all D-9 now asks the owner to confirm:

```text
· Does the 1e-9 tolerance REMAIN an identity tolerance for ACCIDENTAL duplicates within one component
  while two components may sit exactly coincident? This record's proposed answer is YES — that is
  precisely the scoping above; the owner confirms it or amends it.
· WHERE component-scoped duplicate-node validity lives — a scoped form of the existing predicate or a
  distinct gate — is an IMPLEMENTATION choice, DEFERRED to the authorized slice, not an owner decision.
· Does a deliberate Join ADOPT an id or MERGE two nodes, and is either attributable in history? That is
  D-10's contract, not D-9.
```

#### 2.11.3 The ownership path was incomplete — a general connectivity test, and the reconciliation hazard

Option E covers **disconnected standalone Walls and partition stubs**, not only Rooms. Two gaps, both
verified at source, both implementation-proof obligations rather than evidence that E needs new schema:

```text
GAP 1 — THE CONNECTIVITY HELPER IS ROOM-ONLY (see §2.11 Q7).
  connectedRoomIds (layout-room-isolation.ts) builds its junction→rooms map by iterating
  `document.rooms` and each Room's `boundary: OrientedWallRef[]`. A Wall that NO Room boundary
  references — a standalone Wall, a partition stub, an unenclosed chain — contributes NOTHING to that
  map, so two such groups, or a Room and such a group, cannot be classified by it at all.
  ⇒ Option E needs a GENERAL Wall/Junction connectivity test over the Wall graph, with Room grouping
    DERIVED from it where applicable — never an assumption that the Room helper classifies every pair.
    This is the component identity that §2.11.2's scoped validity is defined against.

GAP 2 — FACE-EXTRACTION CORRESPONDENCE IS GEOMETRIC, so spatial overlap alone can reach persistent
  Room identity even after the crossing gates are re-scoped.
  buildCorrespondenceComponents (layout-room-reconciliation.ts) unions a predecessor Room with a
  candidate face when EITHER
    · the predecessor's interior witness lies strictly inside the face (pointStrictlyInsidePolygon), OR
    · the two polygons share interior area (polygonsShareInteriorArea).
  polygonsShareInteriorArea's adjacency guard is TOLERANCE-based (ON_RING_TOLERANCE = 1e-9) and its
  final fallback is explicit: "Identical or nested rings — every boundary probe can lie on the shared
  ring and no edge crosses. Ear-triangle centroids provide real interior evidence."
  ⇒ For EXACTLY-COINCIDENT or FULLY-CONTAINED independent Room polygons the interiors genuinely
    coincide, so the predicate returns TRUE and BOTH faces claim the SAME predecessor. Two
    graph-independent Rooms then collapse into ONE correspondence component, and reconcileRooms either
    treats it as a split/birth or fails with `ambiguous_room_correspondence`. Overlap alone therefore
    CAN merge, retire or ambiguously reassign persistent Room identity today.

NOT A GAP (for the record): the ROUTING of correspondence is already lineage-aware —
  buildCorrespondenceComponents takes predecessor rooms from the BASELINE and faces from the CANDIDATE,
  and reconcileRooms fails closed on any unclaimed or double-claimed face. The defect is the
  UNIONS predicate's input, not the component graph or the allocator.
```

**The proof obligation this creates (owner review, correction 2).** Every path that runs face extraction
+ reconciliation — wall delete, wall role change, dissolve, migration, Room move, chain — must be shown
to keep overlap alone from merging, retiring or ambiguously reassigning Room identity. The proof must
cover at least: **exact-position duplication**, **partial overlap**, and **full containment**, for
graph-independent Rooms. Two further checks belong with it:

```text
· The FACE-EXTRACTION nested-loop diagnostics deserve their own check: extractBoundaryCandidateFaces'
  inner position loop and the face-pair predicates must not itself conflate coincident-but-distinct
  rings.
· The obligation is stated against the GENERAL connectivity test of GAP 1, not against
  connectedRoomIds, so a standalone Wall overlapping a Room boundary is covered by the same proof.
```

---

### 2.12 External precedent for the decision (P23B.2 §D — context, not authority)

P23B.2 researched validity policy specifically for this defect and reached the same four alternatives,
independently and without endorsing any of them. Its precedents are recorded here because they describe
what each option COSTS structurally, which is exactly what a ruling needs:

| Precedent | What it shows | Relevance to our options |
|---|---|---|
| OpenPlan3D `roomDetection.ts` inserts intersection vertices into a DERIVED planar graph, leaving source Walls untouched and source ids attached (**SV**) | normalization can happen during derivation rather than by refusing input | a fifth shape between our B and C — but it means *permitting* intersecting architectural Walls, which is a product position, not a defect fix. Its own validation record notes unresolved collinear overlaps |
| FreeCAD `BRepCheck_Analyzer` with optional, explicitly expensive deeper checks (**SV**) | geometric validity can have LEVELS, not one boolean | supports a policy where cheap structural validity and deep geometric diagnosis are distinct — the distinction our codec-vs-topology gap lacks |
| CGAL exposes BOTH a boolean self-intersection check AND a collection of intersecting primitive pairs (**DOC**/**SV**) | multi-level checking can also be more DIAGNOSTIC, not just more expensive | directly supports retaining the OFFENDING WALL IDENTITIES, which is how a failure is attributed to pre-existing geometry rather than to the current edit |
| IfcOpenShell separates schema validation from geometric validation and documents the limits of the former (**DOC**) | structural/shape validity ≠ geometric validity | the closest external analogue of our exact gap, and a precedent for stating the scope of each check rather than implying one covers the other |

P23B.2's product-policy table is the same A/B/C/D set presented here, and its research conclusion is this
record's position verbatim: **"External precedents support separating structural validation, geometric
diagnostics and repair. They do not select Museum Editor's policy. That decision remains with the owner."**
It adds one implementation constraint for whichever option is chosen, which this record adopts:

```text
"A repair operation must also preserve deterministic entity identity, history and atomicity. Silent
 geometry changes during import or validation could violate those expectations."
```

⇒ Under option C (repair), the repair must be an explicit, attributable edit that produces a
history entry — never a silent geometry change during import or validation.

---

## 3. Consequences that are the same under every option

```text
S-1  The ONE implementation stays the one implementation. All four options reuse
     `detectWallCurveTopologyCrossings` (`layout-wall-first-precision.ts:1964`). None creates a second
     crossing rule, a second compiler or a second validity truth (umbrella invariant).
S-2  No option changes the persisted format or the schema. Every option is a gate/policy decision, not
     a format decision.
S-3  The compile's intra-Room-only scope (F-C2) is unchanged by A and D; B and C may want it revisited
     as part of diagnostics, but that is a separate call and is NOT implied here.
S-4  Plan/3D/visitor parity is unchanged by every option (they consume the same compiled result; the
     crossing state is a validity verdict, not a geometry difference).
S-5  The regression tests RT-1 R-a…R-d are needed under EVERY option — they pin the reproduction, the
     atomicity of a refusal, the pre-existing-crossing case and the import verdict. R-a must not
     presuppose which option wins (its post-fix expectation is deliberately unspecified).
```

---

## 4. Where this ruling blocks implementation (dependency markers)

| Consumer | Dependency | If A | If B | If C | If D |
|---|---|---|---|---|---|
| **P23B.0-dur** (fixture design) | Does the committed fixture set include a valid-then-invalidated pair, and is an "invalid crossing" fixture a *negative* case? | E2-style crossing fixture is a **negative correctness** fixture; a valid fixture must never contain one | same, plus an **invalid admission** fixture showing the marked state | same as A/B | E2-style crossing fixture is only historical evidence |
| **P23B.4** (scoped validation / verdict reuse, M-2) | May an unchanged pair's recorded verdict be reused? | YES — the invariant is total | YES, with the pre-existing verdict surfaced | YES | **NO** — the verdict-reuse half of M-2 is dropped (§8.6 AB-3); extent pruning of newly-invalid pairs may still stand |
| **P23B.7** (interaction) | What does a duplicate gesture report on refusal, and does a refused gesture animate differently? | plain refusal, atomic | refusal + invalid-state presentation | guided resolution offer (capability — likely out of scope) | unchanged behaviour |
| **P23B.9** (gate) | Which import/duplicate verdicts are the oracle? | reject | admit-as-invalid with a stable code | reject-or-offer | admit, unchanged |
| **RT-1 R-a/R-c/R-d** | What may these tests assert? | post-A expected verdict | post-B expected verdict | post-C expected verdict | current behaviour, pinned as accepted |

**Until the ruling, P23B.4 may proceed with M-1 (the shared sampled derivation) only.** M-2's
verdict-reuse half and P23B.0-dur's fixture-polarity decision are both blocked.

**Option E's markers (added — the table above predates it).** E changes the gate's *subject*, so its
dependencies differ from A–D in kind, not degree:

| Consumer | Dependency under Option E |
|---|---|
| **P23B.0-dur** | Fixture polarity follows the SHIPPED gates (§4.1 principle). Under **Path 1(a)** the fixtures pin the group-scoped behaviour; under **Path 2** they pin today's document-global behaviour, where an independent-group crossing is STILL a rejection — and saying otherwise would fail against unmodified code |
| **P23B.4** | M-1 and M-2a proceed either way. M-2a's broad phase must not treat a **permitted independent overlap** as a topology failure, and its superset oracle (OR-9) must be stated against **whichever validator actually ships**. M-2b stays disabled under Path 2 |
| **P23B.5** | The key grammar must not equate coincident coordinates with shared graph identity (§2.11.2 / D-9) |
| **P23B.6** | Independent overlapping groups may produce **separate** meshes. No mesh union, no occlusion fix, no renderer ordering — those are explicitly out of scope |
| **P23B.7** | Independent-group dragging must not introduce implicit joins or unexpected rejections. Ambiguous picking where geometry overlaps may be *identified*; a new selection UX is not created inside a performance slice |
| **P23B.9** | The regression matrix must cover BOTH directions: permitted independent overlap, and preserved rejection within an explicitly connected group |
| **RT-1 R-a…R-d** | Pin behaviour. Expectations follow the **shipped** validator, never the ruling |
| **EVERY reconciliation-running path** (wall delete, role change, dissolve, migration, Room move, chain) | §2.11.3 GAP 2 / **D-12**: prove that overlap ALONE cannot merge, retire or ambiguously reassign persistent Room identity — against the GENERAL connectivity test, covering exact-position duplication, partial overlap and full containment. Until that proof exists, an Option E admit that reaches face extraction + reconciliation has NO verified identity story |

#### 4.0.1 Where current implicit noding conflicts with Option E (§5 of the revision contract)

```text
THE FOUR OPERATION CLASSES THIS MUST NOT CONFLATE
  1. MOVING / DUPLICATING an existing independent Room or group
       today: validated and REJECTED on any intersection (room-move runs the full document-global gate;
              duplicate runs the chord gate and additionally rejects collinear overlap of the clone)
       under E: permitted. This is the class E exists for.
  2. CREATING independent geometry (a new standalone Wall or chain elsewhere in the workspace)
       today: planWallChain ADOPTS a coincident Junction and SPLITS at straight crossings, both of which
              MINT SHARED JUNCTION IDS — i.e. overlap becomes connectivity automatically (§2.11 Q5)
       under E: must NOT auto-join. This is the sharpest conflict, because the joining is not a bug in a
              validator — it is an authoring FIX the chain planner applies on purpose.
  3. EXTENDING an already connected group
       today: adoption and noding are CORRECT here — the new Wall genuinely joins its own group
       under E: unchanged. Noding inside a group must stay, or E would violate its own containment rule.
  4. EXPLICIT Join / Connect (creator-initiated)
       today: does not exist as a distinct operation; its effect is an emergent by-product of class 2
       under E: the ONLY authorized way to establish shared topology. Interface and invariants are
              defined at planning level; NEW UI, modes, commands and interaction design are DEFERRED
              unless the owner expressly expands scope (D-10).

MUST REMAIN UNTOUCHED UNTIL AN AUTHORIZED IMPLEMENTATION CHANGES IT
  collinear overlap within a group · zero-length Walls · Room boundary closure and role · Room boundary
  minimum length · self-intersecting centerlines · duplicate Junction points within a group · the
  isolation and portal gates · every intrinsic invariant. E relaxes ONE class only: geometric
  intersection between entities NOT in the same connected group.
```

---

### 4.1 Delivery slot and authorization path (the ruling needs an implementation owner)

**The gap this section closes.** The table above states what each option *permits*. It does not say who
*implements* it — and as the plans stand, no slice does. P23B.4 excludes defect fixes in its own
non-goals; P23B.7 excludes changed gesture semantics; P23B.9 is verification only; P23B.0-dur is
measurement infrastructure. So under A, B or C the ruling would authorize work with **no delivery slot**,
while still gating fixture polarity (P23B.0-dur W2) and M-2b's admissibility (P23B.4 S8). That is an
inverted dependency — a blocked plan whose unblocking has no owner — and it is a defect in the phase
plan, not in the product.

**Owner of the ruling:** the Museum Editor product owner (unchanged).
**Owner of the work the ruling authorizes:** **UNASSIGNED.** This is a distinct second decision and this
record will not infer it. The owner must name a slot together with the policy.

The decision is a **delivery PATH**, not merely a slot. Two paths are executable, and they have different
critical-path effects inside P23B:

```text
PATH 1 — IN-PHASE IMPLEMENTATION (an expressly authorized topology-policy slice inside P23B)
  PLACEMENT (b) BELOW IS WITHDRAWN by the owner steer at the end of this section (D-11): the policy lands
  BEFORE the durable baseline, so only placement (a) remains. This is ARRANGEMENT 1 below.
  THREE REQUIREMENTS, all of which the owner must supply. Any one missing means Path 1 is NOT available:
    1. AN UMBRELLA SCOPE AMENDMENT. P23B's umbrella is cost, not capability, and a topology-policy
       change is validity + authoring semantics. Without an amendment, Path 1 contradicts the phase's
       own stated boundary.
    2. AN OWNER-APPROVED INSERTION into the phase README's §SEQUENCE. SEQUENCE is the SOLE ordering
       authority; P23B.3 does not amend it and no implementer may. Placement is itself a decision:
         (a) BEFORE P23B.0-dur — fixture polarity is then settled by SHIPPED behaviour (see the
             principle below), so P23B.0-dur commits verdict-bearing fixtures against the FIXED gates
             and never needs relabelling;
         (b) AFTER P23B.0-dur, BEFORE P23B.4 — fixtures land first and the policy slice consumes them;
             the cost is one relabelling of the verdict-bearing fixtures.
    3. A NAMED SLICE with its own plan, acceptance criteria and review checkpoint.
  THE SLICE OWNS at minimum: the group-scoped SUBJECT for validateWallFirstTopology and the sampled
  crossing gate; the chain path's adoption/splitting behaviour (§2.11 Q5); the resolution of D-9 (the
  coincidence representation, §2.11.2); duplicate/import gate parity; and RT-1 R-a…R-d, which pin
  behaviour and are needed under EVERY option (§3 S-5).
  COST: one extra slice, one extra review checkpoint, one phase-scope amendment.
  NOT AVAILABLE for option C without a FURTHER capability amendment: C is repair capability.
  CRITICAL-PATH EFFECT: under (a) P23B.0-dur moves BEHIND the policy slice; under (b) it does not.
  Either way P23B.4–P23B.7 then run against post-policy gates, and the pre/post measurement baseline
  must be established AFTER the policy lands — a pre-policy baseline compared to a post-policy timing
  would present a semantic change as a performance change.

PATH 2 — SEPARATE PRODUCT SLICE (Option E implemented outside P23B)
  The owner rules the direction and routes implementation to a follow-up with its own phase and plan.
  SUPERSEDED IN PART by the owner steer below: under D-11 the follow-up is MERGED FIRST and `p23b` is
  then synchronized, so P23B does NOT continue against the current shipped behaviour (that is the
  MERGE-FIRST variant = ARRANGEMENT 2). The text below describes the pre-steer reading, retained because
  it states what P23B can proceed with WITHOUT the policy — which is still true of every item listed.
  What can proceed WITHOUT waiting:
    · P23B.0-dur — commits fixtures pinning TODAY'S behaviour, labelled `current-behaviour, revisable
      when the topology-policy change lands`, asserting no verdict the shipped gates do not produce;
    · P23B.4 — M-1 (shared sampled derivation), M-2a (extent pruning) and the key grammar all proceed:
      they are orthogonal to the policy. M-2b stays DISABLED;
    · P23B.5 — identity, lifetime and reuse proceed, with the key grammar forbidden from treating
      coincident coordinates as shared graph identity (§2.11.2 / D-9);
    · P23B.6 and P23B.7 — proceed, with no new capability and no visual-stacking or selection-UX work;
    · P23B.8 — unaffected: the Worker/WASM decision is about residual cost, not validity;
    · P23B.9 — asserts TODAY'S verdicts, records the ruling and the delivery target, and marks every
      Option-E-dependent case NOT APPLICABLE with the reason rather than silently omitting it.
  COST: the product keeps the current behaviour through this phase, deliberately and on the record.
  BENEFIT: P23B stays a cost/performance phase — what its umbrella says it is — and no phase scope is
  amended. This is the path of least disturbance to the ratified SEQUENCE.
  SUB-CASE Path 2b — NO CHANGE AT ALL (Option D-like): the same path with no follow-up fix authorized.
  The delivery is the RECORD only (P23B.10 C5), and the verdict-reuse half of M-2 is dropped (AB-3).

NEITHER PATH IS SELECTED HERE. The record presents the consequences and the exact authorization each
requires; choosing is the owner's (D-7).
```

**OWNER STEER (review of `622c1521`) — the parity of the two paths is now settled in ONE direction.**
The owner's stated D-11 direction is to **deliver the topology-policy change BEFORE the durable
pre-optimization baseline**, expressly to avoid finishing P23B's optimizations against the old
global-rejection semantics and only then changing the validity and reconciliation paths. That removes
PATH 1(b) (policy after P23B.0-dur) from consideration and leaves TWO clean arrangements, both
compatible with the one-branch/one-PR workflow:

```text
ARRANGEMENT 1 — IN-PHASE SLICE BEFORE P23B.0-dur (PATH 1(a)).
  An expressly authorized topology-policy slice inserted into §SEQUENCE AHEAD of P23B.0-dur, with an
  umbrella scope amendment, a named slice plan and acceptance criteria. P23B.0-dur then commits its
  verdict-bearing fixtures against the FIXED gates, so nothing is ever relabelled.
  REQUIRES: umbrella amendment + owner-approved §SEQUENCE insertion + a named slice.
  CRITICAL-PATH EFFECT: P23B.0-dur moves BEHIND the policy slice; the baseline is POST-POLICY by
  construction, so no pre/post semantic confound exists.

ARRANGEMENT 2 — SEPARATE PRODUCT-POLICY PR MERGED FIRST, THEN SYNCHRONIZE `p23b`.  [NOT SELECTED —
  retained only so the rejected alternative and its consequences stay on the record]
  Option E is implemented and merged OUTSIDE P23B (its own PR), then `p23b` merges that change in before
  the durable baseline is taken, and P23B benchmarks the NEW behaviour. All P23B work stays in PR #82,
  so the one-PR workflow is preserved and §SEQUENCE is untouched.
  REQUIRES: the policy PR's merge, then one `p23b` synchronization commit.
  CRITICAL-PATH EFFECT: P23B is BLOCKED on that merge; once synchronized the chain is identical to
  Arrangement 1's from P23B.0-dur onward.

EITHER ARRANGEMENT, ONE SHARED CONSEQUENCE: because the policy changes VALIDITY AND AUTHORING
  SEMANTICS, the pre-optimization baseline MUST be taken AFTER it lands. A pre-policy baseline compared
  against post-policy timings would present a semantic change as a performance change (synthesis §7.4).
  UNTIL THEN P23B may run non-mutating discovery and prepare fixture HARNESSES, but must not freeze
  verdict-bearing expectations (P23B.0-durable E-4).
```

**THE PRINCIPLE THAT GOVERNS EVERY PATH: A RULING DOES NOT CHANGE THE VALIDATOR.** Only a landed change
does. An earlier version of this section said that naming D-1 and D-7 together *unblocks* the two
dependent slices; that is true of **Path 1 alone**. Under Path 2 the policy may say "permit" or "reject"
while the tree still does what it does, so each dependent slice is keyed to the **behaviour present in the
tree**, never to the decision text:

```text
FIXTURE POLARITY (P23B.0-dur W2 / E-4) follows the behaviour shipped when the fixture is frozen, and the
  fixture must SAY which behaviour it pins:
    · Path 1(a) — the policy slice lands first, so verdict-bearing fixtures encode the FIXED behaviour.
    · Path 1(b) / Path 2 — the fixtures encode TODAY'S behaviour and are labelled as current-behaviour
      fixtures, revisable when the policy change lands.
    · Path 2b — today's behaviour is the permanent, accepted one.
  WHAT IS FORBIDDEN IN EVERY PATH: a fixture asserting a verdict the shipped gates do not produce. Such a
  fixture fails against unmodified code, so it tests the ruling instead of the product. Under Option E
  this is the trap that matters most: a fixture asserting that an independent-group crossing is PERMITTED
  while the document-global gates still reject it is exactly such a fixture.

M-2b VERDICT REUSE (P23B.4 S8) is NOT unblocked by D-1 + D-7, and it is not unblocked by a "reject"
  ruling at all. It stays DISABLED unless BOTH hold:
    (i)  its equivalence is proven against the CURRENT FULL VALIDATOR, including already-invalid
         baselines (OR-10), never against another scoped variant; and
    (ii) the behaviour of the SHIPPED gates admits a stable verdict to reuse.
  Under Path 2 the shipped behaviour is what it is today, so AB-3's reasoning applies to the SHIPPED state
  rather than the ruled state — with the same outcome: a cached negative verdict would be durable policy
  the product does not enforce. Under Option E specifically, a permitted overlap makes the pair
  classification itself policy-dependent, so a reused verdict must ALSO invalidate when a deliberate
  join changes the pair's connectivity (§8 reconciliation of P23B.4).
  If a fix lands later (Path 1, or a following phase), M-2b's admissibility is RE-EVALUATED against the
  new behaviour. It is never inherited from this ruling.

RT-1 R-a…R-d are unaffected by this distinction: they pin today's behaviour under every path (§3 S-5),
  which is exactly why they do not presuppose the option.
```

**Anti-pattern, named so it is not taken by default:** letting the fix arrive implicitly inside whichever
slice happens to touch the code. P23B.4's M-2a touches the crossing scan; P23B.7 touches the preflight.
Neither is authorized to change admission policy, and a policy change smuggled into a performance slice
is precisely the outcome this record exists to prevent.

### 4.2 The policy slice's acceptance contract (D-11 = ARRANGEMENT 1; D-12 is owned HERE)

**The owner selected ARRANGEMENT 1:** a dedicated, narrowly scoped topology-policy slice placed AHEAD of
P23B.0-durable, inside this branch and PR #82. The umbrella scope amendment and the §SEQUENCE insertion
are therefore owner-authorized; this section specifies the slice's contract but does not itself perform
either mechanical change (§4.2.3).

#### 4.2.1 Scope — narrow, deliberately

```text
IN
  · the group-scoped SUBJECT for validateWallFirstTopology AND the sampled crossing gate
    (detectWallCurveTopologyCrossings);
  · the SEPARATE validateChainTopology gate's re-scoping, or an explicit statement of why it differs —
    re-scoping only one of the two would leave Room-move and chain authoring disagreeing (§2.11 Q6);
  · planWallChain's ADOPTION and crossing-SPLIT behaviour, KEPT for the operation class that must keep
    it (class 3, extending a connected group) and REMOVED from independent placement (class 2);
  · duplicate/import gate parity for the independent-overlap case (the F-C1 / F-C6 ingress hole);
  · scoping `duplicate_junction_point` to the connected component (§2.11.2 OBSTACLE 1);
  · the GENERAL Wall/Junction connectivity test (§2.11.3 GAP 1) and the reconciliation identity proof
    (D-12, §4.2.2) — BOTH PASS BEFORE SLICE ACCEPTANCE, not as later P23B.4 or P23B.9 work;
  · RT-1 R-a…R-d and T1–T13 re-based on the SHIPPED (post-policy) validator.
OUT
  · the deliberate Join/Connect UI, modes, commands or interaction workflow (D-10: PLANNING LEVEL only);
  · collision warnings as a blocking mechanism, and any automatic repair, noding or geometry alteration;
  · any vertical-stacking / floor-elevation authoring (DEF-11);
  · ANY SCHEMA, PERSISTED-FORMAT OR NEW-IDENTIFIER CHANGE — none is needed and none is permitted
    (§2.11.2);
  · any mesh union, occlusion fix, renderer ordering or new selection UX.
```

#### 4.2.2 D-12's DEDICATED oracle — it is NOT OR-3 and NOT OR-9b

```text
WHY A DEDICATED ORACLE. OR-3 is a VALIDATION-SCOPING oracle (scoped verdict ≡ full validator) and OR-9b
is a MESH-DEPENDENCY oracle (a changed input defeats reuse). NEITHER proves that persistent ROOM IDENTITY
survives spatial overlap, so routing D-12 through them would assert the wrong property in the wrong
slice. D-12 therefore gets its own contract, owned by THIS slice.

OR-D12-1  IDENTITY PRESERVATION UNDER EXACT COINCIDENCE
  Two graph-INDEPENDENT Rooms at IDENTICAL coordinates (duplicate at the source position). After EVERY
  path that runs face extraction + reconciliation, both Rooms keep their own ids, names, boundary
  references and associated-object / hosted-Opening ownership. Neither is merged, retired or reassigned.
OR-D12-2  IDENTITY PRESERVATION UNDER PARTIAL OVERLAP
  Move one independent Room so it partly overlaps another. Both ids survive and the correspondence
  components attribute each candidate face to its OWN predecessor. No `ambiguous_room_correspondence`.
OR-D12-3  IDENTITY PRESERVATION UNDER FULL CONTAINMENT
  One independent Room wholly inside another. Both ids survive; containment is NOT read as a
  split / birth / re-parenting event.
OR-D12-4  THE OPERATED GROUP MAY STILL CHANGE — AND ONLY IT
  A legitimate edit to the group being OPERATED ON may have its explicitly authorized lineage effects
  (split, birth, retirement) EXACTLY as it does today. The guarantee is ASYMMETRIC and must be asserted
  that way: SPATIAL OVERLAP ALONE cannot change an UNRELATED group's identity.
OR-D12-5  GENERAL CONNECTIVITY, NOT ROOM-ONLY
  OR-D12-1…4 are evaluated using the GENERAL Wall/Junction connectivity test (§2.11.3 GAP 1), so a
  STANDALONE Wall or a partition stub overlapping a Room boundary is covered by the same oracle.
  `connectedRoomIds` is NOT the oracle's classifier and must not be substituted for it.
OR-D12-6  EVERY RECONCILIATION CALLER
  The four cases are exercised through EVERY path that runs face extraction + reconciliation: wall
  delete · wall role change · dissolve · migration · Room move · chain.
```

#### 4.2.3 SEQUENCE and umbrella

The §SEQUENCE insertion this arrangement requires is **owner-authorized but NOT performed by this pass**:
`SEQUENCE` remains byte-identical in the file, so the ordering authority is unchanged until the staging
commit that creates the slice. That commit must place the policy slice BEFORE `P23B.0-durable`, name it,
and leave every existing step's meaning intact. The umbrella scope amendment is the same authorization.

---

## 5. Exact decision requested

```text
D-1  RULED (2026-09-22): **E — independent spatial placement; explicit topological connection** (§2.10)
            governs curved-crossing validity. Options A–D are retained as historical alternatives only.
D-2  (if A) COMPATIBILITY:  does an already-exported payload carrying a curved inter-Room crossing
            (a) stop importing with an explained refusal, (b) import once with an explicit invalid
            state and require repair, or (c) something else?
D-3  (if B) VOCABULARY + HISTORY:  is "invalid" a first-class document state or an issue-list entry,
            and does a repair participation produce a history entry?
D-4  (if C) SCOPE:  C is capability, not cost. Confirm that it is OUT of P23B (and, if in scope
            somewhere, name the phase) — P23B's umbrella excludes new capability.
D-5  (if D) COMMENT:  is the misleading parity comment at `layout-duplicate.ts:196-200` corrected as
            part of accepting D?
D-6  INDEPENDENT OF D-1..D-5:  are RT-1 R-a…R-d authorized as regression tests (needed under every
            option), and do they land in the P23B.0-dur slice (fixtures) or the slice that fixes the
            gate?
D-7  DELIVERY PATH (required ALONGSIDE D-1 — see §4.1):  which path implements the ruling?
            (a) a defect-fix slice added to §SEQUENCE — and if so, before or after P23B.0-dur;
            (b) held outside P23B and routed to a named follow-up, with P23B pinning TODAY'S behaviour
                (RT-1) and asserting no verdict the shipped gates do not produce;
            (c) Option D — the ruling recorded by P23B.10 C5, no code change.
            AS NARROWED BY D-11 (§4.1): under Option E only (a) and (b) remain, and (a) means a slice
            BEFORE P23B.0-dur. Path 1(b) — policy landing after the durable baseline — is no longer
            admissible, because it optimizes against the old semantics first and changes validity and
            reconciliation afterwards.
            WHAT D-7 IS NOT: it does not by itself unblock fixture polarity or M-2b. Under (b) or (c) the
            validator is unchanged, so both consumers are keyed to the behaviour actually shipped (§4.1).
D-8  If the fix is delivered or held out: is a comment-only correction commit authorized at a NAMED
            point, or does the misleading `layout-duplicate.ts:196-200` comment stay as explicit tech
            debt? P23B.10 C5 records the outcome but is a closeout gate, not a code-change slice, so it
            cannot be the slot for a code edit (§4.1, Paths 1 and 2).
D-9  CONFIRMED (2026-09-22) AS WRITTEN (§2.11.2):  explicit Junction identity governs connectivity;
            coincident INDEPENDENT components are permitted; accidental duplicate nodes WITHIN one
            connected component remain invalid; an explicit join may ADOPT a Junction id while ordinary
            spatial placement must not. Identity stays expressible in the EXISTING persisted format — no
            group id, cluster relation or connectivity record is added. Component-scoped validity's
            exact LOCATION in code (a scoped predicate or a distinct gate) remains an implementation
            choice for the policy slice, not a decision.
D-10 RULED (2026-09-22): Join/Connect stays at PLANNING LEVEL for the policy slice — interface and
            invariants named, with new UI, modes, commands and interaction design DEFERRED. No new UI or
            interaction mode is created in the prerequisite.
D-11 SELECTED (2026-09-22): **ARRANGEMENT 1** — a dedicated, narrowly scoped topology-policy slice
            BEFORE `P23B.0-durable`, inside this branch and PR #82 (§4.2). Its scope, its acceptance
            contract and its dedicated D-12 oracle are specified in §4.2; the umbrella scope amendment
            and the §SEQUENCE insertion are owner-authorized. ARRANGEMENT 2 is NOT selected. PATH 1(b)
            — policy after P23B.0-dur — remains withdrawn.
D-12 OWNED (2026-09-22) BY THE POLICY SLICE: the topology-policy slice owns BOTH the GENERAL
            Wall/Junction connectivity proof (§2.11.3 GAP 1) and reconciliation identity preservation
            (§2.11.3 GAP 2). BOTH PASS BEFORE THAT SLICE'S ACCEPTANCE — never as later P23B.4 or P23B.9
            work. The dedicated oracle is OR-D12-1…6 (§4.2.2); OR-3 and OR-9b are NOT substitutes.
```

**OWNER DISPOSITION (review of `622c1521`): the Option E DIRECTION IS ACCEPTED.** The rulings below are
recorded as the owner's stated direction and are folded into the synthesis and the affected child plans
as the single governing decision, not duplicated across them:

```text
D-1   RULED     Option E — independent spatial placement, explicit topological connection.
D-9   CONFIRMED connectivity is EXPLICIT GRAPH IDENTITY, never coordinates alone; coincident
                INDEPENDENT components are permitted while accidental duplicate nodes WITHIN one
                connected component stay invalid; an explicit join may ADOPT a Junction id, ordinary
                spatial placement may not. NO new representation, group id or schema field.
D-10  RULED     Join/Connect stays at PLANNING LEVEL for the prerequisite; no new UI or interaction
                mode is created in it.
D-11  SELECTED  ARRANGEMENT 1 — a dedicated, narrowly scoped topology-policy slice BEFORE
                P23B.0-durable, inside PR #82. Umbrella amendment + §SEQUENCE insertion owner-authorized
                (§4.2.3). ARRANGEMENT 2 not selected; PATH 1(b) withdrawn.
D-12  OWNED     BY THE POLICY SLICE — the general Wall/Junction connectivity proof AND reconciliation
                identity preservation both PASS BEFORE ITS SLICE ACCEPTANCE. Dedicated oracle
                OR-D12-1…6 (§4.2.2).
```

**WHAT IS STILL PENDING RATIFICATION:** the implementation plan and acceptance criteria — including the
policy slice's own acceptance criteria (§4.2). This record does not authorize implementation, and the
child plans remain PROPOSED. Still open: **D-7** (the comment-free delivery slot is now Arrangement 1;
what remains is the slice's NAME and its staging commit), and **D-8** (the comment-correction slot).
Option E is recorded as **DIRECTION ACCEPTED and its DELIVERY ARRANGEMENT SELECTED**, with
the implementation plan still pending ratification — not as an authorized implementation.

---

## 6. Option E acceptance-test design (planning level — NOT authorized to implement)

Concrete tests for the revision contract's §9, split by whether they can be written **today** or only
**after E ships**. Nothing here is green, and no future-policy test may be presented as passing before
its implementation exists. Each row names the behaviour it pins so the split is checkable.

```text
EXECUTABLE AGAINST CURRENT BEHAVIOUR — these PIN today's shipped verdicts and are the regression net
  for any later policy change. Each asserts a REJECTION today.
  T1  duplicate a curved Room onto ITSELF (delta 0,0): expect a rejection, with the code the chord gate
      produces (collinear overlap of the clone), and NOT a claim about connectivity
  T2  move Room A through Room B: expect a rejection from move's full document-global gate; assert
      document, selection, history and preview are UNCHANGED (atomicity, RT-1 R-b)
  T3  two INDEPENDENT Walls whose endpoint coordinates are identical: today's authoring ADOPTS the
      existing Junction, so the result is a SINGLE Junction id. This is CURRENT-BEHAVIOUR EVIDENCE of
      "touching IS joining", and it FLIPS when E ships (see F3). It is deliberately NOT presented as a
      permanent within-group adoption test — two INDEPENDENT Walls are not a connected group, and
      conflating the two was an error in an earlier revision. T13 is the permanent test
  T4  two independent curved Walls crossing: expect rejection from the sampled crossing authority
  T5  collinear overlap between two independent groups: expect `collinear-overlap` rejection
  T6  two Rooms with a DELIBERATELY shared Wall: expect ACCEPTANCE and a single rigid group —
      connectedRoomIds returns both, resolveIsolatedRoomGroupSubgraph moves them together, and an
      interior shared Wall travels exactly once. THIS TEST MUST PASS BEFORE AND AFTER E
  T7  self-intersection or an invalid boundary INSIDE one group: expect the applicable rejection to
      SURVIVE E — this is the containment proof
  T8  a genuinely invalid operation rejected ATOMICALLY: document, identity, history and preview
      unchanged
  T9  save/import round-trip: identity and relationships preserved exactly
  T10 undo/redo: the exact placement is reproduced, with no stale artefact served
  T11 Plan, 3D and visitor consume equivalent canonical geometry (existing parity suites)
  T12 repeated vs scoped validation: identical outcomes AND identical diagnostic ordering for the SAME
      ordered input (the corrected G2 — order-PRESERVING, not order-invariant)
  T13 INTENTIONAL EXTENSION WITHIN A CONNECTED GROUP — PERMANENT (passes BEFORE and AFTER E, and must
      keep passing afterwards): author a new Wall whose endpoint coincides with a Junction belonging to
      an ALREADY CONNECTED group. Expect ADOPTION — ONE Junction id, the new Wall joined to that group.
      This is operation class 3 (§4.0.1), the case E's containment rule exists to protect, and it differs
      from T3 in the only way that matters: the coincident Junction belongs to the group being EXTENDED

FUTURE OPTION E — these may only be written when E ships, and each MUST fail before it does
  F1  duplicate a curved Room onto itself: SEPARATE ids, no automatic connectivity, both groups
      independently editable
  F2  move Room A through Room B: both remain independently editable; no merge, no noding
  F3  two INDEPENDENT Walls with identical endpoint coordinates: NO implicit shared Junction — distinct
      ids, no connectivity. This is T3 AS IT MUST READ AFTER E, and the pair together is the D-9 rule in
      test form
  F4  two independent curved Walls crossing: no new Junction and no Wall fragments
  F5  collinear overlap between independent groups: permitted
  F6  import of a document carrying an independent-group overlap: admitted once E ships, and NEVER
      rejected solely for its spatial intersection
  F7  a snap or alignment suggestion at an overlapping coordinate does not authorize a join
  F8  deliberate join is the ONLY way to create shared topology, and it is explicit and attributable
```

**Two design constraints these tests encode, so neither is lost:**
1. **T3, T13 and F3 are THREE DISTINCT OPERATIONS, not one.** An earlier revision treated T3 as a
   permanent within-group adoption test; that was wrong, because T3's Walls are INDEPENDENT and
   independence is precisely what E changes. The corrected split:
     · **T3** pins today's adoption between INDEPENDENT Walls — current-behaviour evidence that FLIPS
       when E ships;
     · **F3** is T3's post-E expectation (distinct ids, no connectivity);
     · **T13** is the permanent test for the operation that genuinely must not change — intentionally
       EXTENDING an already connected group by authoring into one of ITS OWN Junctions.
   Only T13 is a permanent regression test, and it passes BEFORE and AFTER E.
2. **Collision warnings stay non-blocking.** `LayoutGeometryIssue.severity` already models
   `'warning' | 'error'` and `hasBlockingLayoutIssues` already ignores `severity !== 'warning'`, so the
   vocabulary for a non-blocking warning EXISTS and is honoured by the blocking predicate. It has **no
   production producer today** (no non-test site emits a warning severity). A future collision warning is
   therefore a *producer* to add at warning severity, not a new mechanism — and it must not repair, node,
   join or alter geometry.
