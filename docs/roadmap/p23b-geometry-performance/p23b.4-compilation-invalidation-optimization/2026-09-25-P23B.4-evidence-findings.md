# P23B.4 — Evidence findings (CLOSED)

```text
AUTHORITY: NONE — closed-work stub. Full body recoverable via the recovery
line; deliberation lives in Git/PR history. S9/S10 findings below are
summarized for routing; P23B.5/P23B.7 inherit them via the anchor, not via a
live dependency on archived prose (no live → archive prose link created).
```

## Delivered

S1b bounded U-5 attribution (supports M-1/M-2a; flush-internal out of slice
→ P23B.6) · S6 before/after record (curved-40 280→120 evals) · S7 pruning
economics (shipped 60→40 candidates, no index escalation) · S8 M-2b DROPPED
· S9 reconciliation contract (OR-11, no post-validation scoping proposed) ·
S10 dependency map (X-5: only per-room floor polygons/room AABBs are locality
candidates; all else document-global) · X-1/X-2/X-4 + anchors + limits ·
F1–F3 correction evidence with narrowed X-3 scope.

## Contract (final, accepted)

Same contract as the plan stub (INV-1…INV-7, OR-1…OR-11, X-1…X-8). No
equality case failed at any step; the conservative key held on first contact;
AB-1/AB-2/AB-10 abandonment never triggered. X-3 met at narrowed
mechanism-level Node scope only.

## Provenance

PR #88, accepted HEAD `4cbcc370` against base `d7b9de4e`. Owner ruling:
ACCEPT IMPLEMENTATION — no remaining blockers. Merge method: squash (repo
policy forbids merge commits; owner chose squash-with-degradation).

## Evidence (owner-verified gates at acceptance)

Full lane 4,786 passed · `test:arch` / `test:perf` green · `check` 0/0 both
apps · both apps build · visitor bundle verified · baseline SHA-256
`5534926e…` unchanged. Exact commands, oracle results, counts and provenance
→ full body via the recovery line.

## Entry points

Same as the plan stub in this directory; measurements repeatable via
`apps/editor/tests/lib/layout/p23b4-measurements.test.ts` (the vitest run
itself: warmup 5, samples 15, no wall-clock threshold asserted).

## Residuals

Same as the plan stub. Additionally: post-release flush/frame pairs (absent
from the accepted baseline) never compared against; no end-to-end settlement
claim exists.

## Recovery

```text
git show 4cbcc370c1cb9dace91da2c0760d06f7258c3e1d:docs/roadmap/p23b-geometry-performance/p23b.4-compilation-invalidation-optimization/2026-09-25-P23B.4-evidence-findings.md
tag closed/p23b.4 (targets this anchor — verified locally)
DEGRADATION (squash): branch commits are not ancestors of `main` post-merge.
Recovery runs via the PR head ref (verified post-merge before reporting complete):
git fetch origin refs/pull/88/head && git show <A>:<path>
tag closed/p23b.4 is local only — not pushed.
```

## CLOSEOUT PRESERVATION — P23B.4 (2026-09-25)

```text
prose compacted:                 2   stubs at their own paths (plan + evidence findings)
reference promotions:            0   key grammar owned in code; S9/S10 findings inherited
                                     by roadmap slices via anchor, not reference
renderable evidence archived:    0 files (none existed; baseline JSON stays live)
evidence stubs / manifests:      0   (no archive copies needed)
transient artifacts removed:     0   (none existed; no active checkpoint to retire)
historical anchor:               branch anchors cb9b83ab (plan) + 4cbcc370 (evidence),
                                     reachable post-merge via `git fetch origin refs/pull/88/head`
                                     · tag closed/p23b.4 (local only — not pushed)
new live → archive prose links:  0
manual-owed verification rows:   0   owner independently passed all lanes at acceptance;
                                     closeout re-run skipped per explicit owner instruction
remaining deferred items:        5   M-2b dropped · flush-internal → P23B.6 · no
                                     browser/settlement claim · U-1 declined · Rust/WASM → P23B.8
```
