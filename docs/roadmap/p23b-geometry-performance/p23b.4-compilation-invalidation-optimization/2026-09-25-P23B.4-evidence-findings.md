# P23B.4 — Evidence findings (S1b, S9, S10)

```text
STATUS:    S1b RECORDED 2026-09-25. S9/S10 pending (appended by their steps).
AUTHORITY: finding, not architecture. Consumed by this slice; P23B.5/P23B.7 inherit S9.
```

## S1b — Bounded U-5 flush attribution (finding only; does not gate M-1/M-2a)

Question: attribute the captured flush to the 38 nested P23.11 marks per path, and record
whether the findings support M-1/M-2a. If the evidence points to mesh/adapter (P23B.6) or
gesture (P23B.7) scope, surface a scope decision; do not silently redirect this slice.

### S1b.1 Method (bounded, in-repo, no browser capture)

- Live mark collection in vitest (node): `__P2311_PERF__` enabled, each exported acceptance
  consumer run 5x per fixture after warmup; `curve-sampling` counts/totals per consumer.
  Timing is single-machine advisory; COUNTS are deterministic.
- Provenance: commit `ca04983a`, node v26.7.0, darwin arm64. Script: throwaway probe
  `p23b4-s1b-probe.test.ts` (counts reproduced below; probe deleted after recording).
- Boundary-level flush numbers are committed evidence: `g3-baseline.json` interaction rows
  (method v5) + closed W6 stub finding.

### S1b.2 Compile-side attribution (measured here)

`curve-sampling` derivations per acceptance round (÷5), by consumer:

| fixture | topology ×1 | faces | openings | compile | round total |
|---|---|---|---|---|---|
| p23b-40-wall-straight-v1 | 0 | 0 | 0 | 120 | 120 |
| p23b-40-wall-all-curved-v1 | 40 | 80 | 0 | 120 | 240 |
| owner-40-curved-v1 | 40 | 80 | 0 | 120 | 240 |

A full acceptance runs topology TWICE (pre+post), so the all-curved 40-wall release
derives the same walls 40 (pre) + 40 (post) + 80 (face, both half-edge directions) +
120 (compile: arc + physical + room-validation) = 280 sampling operations — reproducing
the harvest F-P2 source count as MEASURED counts, not just static analysis. Per curved
wall per release: up to 7 derivations of the same centerline (pre, post, face×2, arc,
physical, validation).

Straight-shared vs curvature-added (same machine, advisory wall-clock, 5 repeats):

| fixture | compile sampling (600 ops) | faces sampling | topology sampling |
|---|---|---|---|
| straight-40 | 14.5 ms | 0 ops (lines skipped) | 0 ops (chord path) |
| all-curved-40 | 77.2 ms | 37.5 ms / 400 ops | 23.5 ms / 200 ops |
| owner-40 | 36.2 ms | 18.0 ms / 400 ops | 11.9 ms / 200 ops |

Curvature-added cost concentrates in `curve-sampling` on curved walls (≈5x per-op vs line
kernel sampling on the compile path); straight walls cost nothing in topology/faces
(chord/short-circuit paths) but still pay kernel sampling in compile (120 ops/round).

### S1b.3 Flush-level attribution (committed baseline evidence)

- Boundary totals per path per fixture ARE captured: straight-40 selection input 121.7 ms,
  drag/edit release 208.4 ms, authoring release 262.6 ms; all-curved-40 authoring release
  1,984.8 ms; pan/zoom flush 22.4 ms (straight) → 343.2 ms (curved) with NO planner call
  (`plan-apply` unavailable). Straight-vs-curved differencing at the boundary-total level
  is sound (control vs timing-target cells).
- The 38-key per-fixture `nestedMarks` bag is session-global: no path/boundary key, no
  timestamp correlation, no nesting ledger; the harness forbids summing. No committed
  artifact maps any unit of flush time to any specific mark.

### S1b.4 Support statement + scope decision

- SUPPORTS M-1: duplication is measured live (280 ops/release on all-curved-40 across
  four consumers; up to 7 derivations per curved wall), not just counted in source. The
  shared-derivation mechanism addresses exactly this distribution.
- SUPPORTS M-2a: the topology gate re-derives all 40 curved walls per pass and scans all
  pairs; the prune target (pair-scan breadth) is confirmed present on every release.
- DOES NOT support any claim about WHICH mechanism inside the Plan reactive render/flush
  path composes the 22.4 ms straight baseline or the +320.8 ms curvature delta. That
  decomposition is not possible from committed data and would require flush-window mark
  correlation that is out of scope here.
- SCOPE DECISION (surfaced, not silent): flush-internal attribution stays OUT of this
  slice. If later evidence shows the release flush dominated by render work, that work
  belongs to P23B.6 (mesh/adapter), not to M-1/M-2a. M-1/M-2a proceed on their own
  evidence (S1 reference + this attribution), regardless of the flush finding.

## S6 — M-1 before/after measurement (X-3; advisory wall-clock, deterministic counts)

Provenance: commit at S5 (`702501fb` tree + S6 preflight threading), node v26.7.0, darwin
arm64, vitest (node, single session). Method: the EXACT finalize consumer sequence
(topology-pre → faces → topology-post → opening-set → compile) run on the committed
fixtures with one chain context ("after") vs default per-call sampling ("before" — the
preserved default path, verbatim today's behaviour). Post runs on a JSON round-tripped
copy: same geometry, different centerline objects (the reconciled-document shape). Counts
are `curve-sampling` kernel evaluations (deterministic); ms is advisory (1 round after
warmup, same session).

| fixture | before kernel evals | after evals (= derivations) | shared hits | ms before → after |
|---|---|---|---|---|
| p23b-40-wall-straight-v1 | 120 | 40 | 80 | 17.2 → 11.2 |
| p23b-40-wall-all-curved-v1 | 280 | 120 | 160 | 48.2 → 34.0 |
| owner-40-curved-v1 | 280 | 120 | 160 | 25.5 → 19.1 |
| bend-3-room-openings | 46 | 12 | 34 | 3.2 → 2.9 |

Attribution limits (honest): kernel-eval counts are exact; wall-clock is advisory and
confounds kernel work with the surrounding compile (straight-40: 3x fewer evals but only
~35% faster, because line-kernel sampling is cheap next to the rest of compile). No
end-to-end settlement claim: the driver is the validation/compile chain in node, not the
browser interaction harness. Marks are nested; containment reported, never a sum.

"Remove the now-dead duplicate derivation" (S6) disposition: there is no discrete
function to delete — the default per-call path is the preserved public contract for
out-of-chain callers (R-4: a partially-migrated tree is never wrong, only slower). What
S6 removes is the duplicate derivation WITHIN the chain: after-counts equal the distinct
key count on every fixture (proven by the committed S6 chain-coverage test), and the
transient preflight joins the chain scope with one fresh derivation per call.
