# P23B.4 — Compilation and invalidation optimization

```text
STATUS:   PLANNED, UNRATIFIED. Not implementation-ready until the owner ratifies P23B.3 and its
          acceptance criteria (SEQUENCE step 8).
ROLE:     eliminate duplicate curve derivation on the release path (synthesis M-1) and scope the
          document-wide validation work to what an edit can affect (synthesis M-2), without changing
          compiled geometry truth.
NOT:      a new compiler · a second geometry truth · a second sampled representation with a different
          key grammar · an incremental sub-graph compile · a sampling-quality change · a defect fix ·
          a Worker · a WASM migration · a cache layer that duplicates the geometry-keyed WeakMaps ·
          a budget
AUTHORITY: direction → ../p23b.3-synthesis-optimization-direction/2026-09-22-P23B.3-synthesis.md §6.1
            M-1/M-2, §7, §8.4 (OR-1…OR-4), §8.5, §8.6
          evidence → ../p23b.1-internal-geometry-pipeline-harvest/2026-09-22-P23B.1-harvest-record.md
            F-P1 · F-P2 · F-P5 · F-P6 · EQ-1…EQ-7 · UN-5 (CLOSED by P23B.3 §3.2 S-1)
          invariants → ../2026-09-22-P23B-geometry-performance-stabilization-umbrella.md
          order → ../README.md §SEQUENCE (unchanged)
DEPENDS ON: P23B.0-durable (committed all-curved fixture + recorded baseline; without it no honest
            before/after exists) · the curved-crossing ruling for the verdict-reuse half (M-2b)
OWNER:    ratifies the slice plan, the acceptance criteria and the ruling dependency; ratifies any
          budget (none proposed here).
```

## 1. Problem, and the evidence that motivates it

```text
1.1  DUPLICATE DERIVATION (M-1). On the all-curved 40-Wall case the SAME Wall's canonical centerline
     is sampled by four active consumers per release — topology-pre 40 + face extraction 80 +
     topology-post 40 + acceptance compile 120 ≈ 280 sampling operations — all through
     `wallCenterlineSamples` (`packages/layout-core/src/layout-wall-centerline.ts:307`) with NO shared
     representation (F-P2). Face extraction pays twice per curved Wall because it samples BOTH half-edge
     directions, including the discarded negative-area outer ring.
1.2  SAMPLING OPTIONS ARE ALREADY EQUIVALENT (S-1, closing harvest UN-5). `wallCenterlineSamples`
     passes NO options to `sampleSegment` (`layout-geometry-curve.ts:384`), and no consumer in the tree
     overrides them. ⇒ The obstacle to sharing is NOT configuration. It is exactly three things:
     traversal (S-2), input endpoints/document identity (EQ-1), and array equality rather than
     near-equality (EQ-3).
1.3  WHOLE-DOCUMENT VALIDATION PER INTERACTION (M-2). The transient preflight is whole-document for a
     single-Wall gesture — all Junction pairs, all Wall pairs, every curved Wall sampled, PER POINTER
     MOVE (F-P5) — and the structural + topology pair runs twice per release with nothing scoped to what
     reconciliation changed (F-P6).
1.4  WHY SCOPING MUST BE EXTENT-BASED, NOT LOCAL. The crossing rule is all-pairs by design and carries
     no notion of the edited Wall. Room-local reasoning is provably insufficient: a Room duplicate that
     translates a curved Wall into a crossing with a Wall OUTSIDE its own Room is admitted today (F-C3).
     A sound scope is therefore "which pairs can have CHANGED relationship", which is an extent/AABB
     argument, plus (conditionally) reuse of verdicts that did not change.
```

## 2. Scope, non-goals, dependencies

```text
IN
  · one canonical sampled-centerline derivation per Wall per release, consumed by topology-pre/post,
    face extraction, the Opening set and the compile (M-1)
  · a documented key grammar for that derivation: canonical centerline identity + traversal + resolved
    endpoints (the grammar P23B.5 then owns and generalizes)
  · extent-derived pair pruning for the crossing gate (M-2a)
  · conditional: reuse of an UNCHANGED pair's verdict across the pre/post pair (M-2b) — blocked on the
    curved-crossing ruling (synthesis §9.1/§8.6 AB-3; decision record §4)
  · equivalence-first tests written BEFORE the mechanism they protect
OUT
  · incremental/sub-graph compilation (synthesis T-4: not justified until a dependency map proves
    locality)
  · any change to `sampleSegment`'s default options (that changes compiled geometry = product change)
  · baseline→candidate artefact reuse (P23B.5) · mesh/adapter work (P23B.6) · gesture work (P23B.7)
  · Worker / WASM (P23B.8) · the curved-crossing POLICY itself (owner ruling)
  · any new cache in editor state · any schema/persisted change · any second geometry path
DEPENDENCIES
  hard   P23B.0-durable  → committed curved fixtures + recorded baseline + the control matrix
  hard   the ruling      → determines whether M-2b is admissible at all and what a duplicate/import
                           verdict must be
  soft   P23B.1 F-P2's ≈280 count is a SOURCE count, not a cost. The slice must not promise a saving
         proportional to it (synthesis M-3: marks are nested and never summed).
```

## 3. Exact current entry points and ownership boundaries

| Concern | Entry point | Ownership boundary |
|---|---|---|
| the sampler seam | `wallCenterlineSamples` `layout-wall-centerline.ts:307` → `sampleSegment` `layout-geometry-curve.ts:384` (mark `curve-sampling`) | `packages/layout-core`; **renderer-free**. The derivation must live here or below, and must not import editor code (`layout-geometry-boundary` walks this) |
| oriented-chain derivation | `wallCenterlineSegment:286` · `traversalEnds:128` · `orientedKnotRefs:177` · `mirrorSpanView:168` | same module; this is where traversal divergence is real (S-2) |
| topology gates | `validateWallFirstTopology` `layout-wall-first-precision.ts:1732` (gate `:1790`) · `detectWallCurveTopologyCrossings` `:1964` (loop `:1978`) · `classifyWallIntersection` `layout-wall-topology.ts:129` | `layout-core`; the ONE crossing authority — must not be duplicated or forked |
| transient preflight | `preflightWallFirstArchitectureCandidate` `layout-wall-first-precision.ts:1098` (mark `preflight-topology:1105`) | pointer-move path only (the harvest corrected the earlier claim that release calls it) |
| acceptance chain | `finalizeWallGeometryCandidate:1508` — `structural-pre:1518` · `topology-pre:1526` · `face-extraction:1537` · `room-reconciliation:1552` · `structural-post:1575` · `topology-post:1579` · `opening-set:1588` · `portal-relations:1598` · `acceptance-compile:1603` | `layout-core`; **this function is where a per-release derivation context belongs** |
| face extraction | `extractBoundaryCandidateFaces` `layout-face-extraction.ts:74` (sample at `:185`, area discard `:215`) | `layout-core`; note it samples BEFORE the discard — a reordering here is a behaviour change and needs its own equivalence argument |
| opening set | `validateWallFirstOpeningSet` `layout-opening-set.ts:104` · `wallFirstWallSpan:73` (sample `:84`) | `layout-core`; INACTIVE on the 0-Opening fixtures — must be covered by tests that do have Openings (`bend-10-wall-openings`) |
| compile | `compileWallFirstLayoutGeometry` `layout-geometry.ts:187` → `compileWallFirstWithPhysicalWalls:320` · `compileLayoutGeometrySource:887`; sampling call sites `:927` (→ `layout-geometry-validation.ts:46`), `:260` (arc length, `'forward'` by design), `:367` (physical Walls) | `layout-core`; `:260` is the canonical-forward arc length used for Opening offset mirroring — its traversal must not change |
| reconciliation | `reconcileRooms` `layout-room-reconciliation.ts:272` | does **not** sample (F-C2). `roomBoundaryPolygon` at `:138` DOES sample but its callers are chain/dissolve/topology-ops — do not conflate |
| intra-Room compile validation | `validatePreparedLayoutRoomGeometry` `layout-geometry-validation.ts:74` (self-intersect `:128`, cross `:138`) | unchanged by this slice |
| measurement | `p2311Measure` `packages/layout-core/src/p2311-perf.ts:4`; marks per stage (harvest CG-1) | DEV + `__P2311_PERF__` gated |

**Ownership boundary that governs the design:** the derivation is a `layout-core` value derived from a
Wall + resolved endpoints + traversal. It is **not** editor state, **not** attached to `LayoutDocument`,
and **not** a module-level global. Its lifetime is the caller's (a per-release/per-acceptance chain
context), which is what keeps RL-1 (document retention) impossible rather than merely unlikely.

### 3.1 External precedent carried in (context, not authority)

From P23B.2 (labels are its own; §F holds the pinned revisions). None of these is a reason to ship —
they tell us what shape the mechanism takes and what its failure modes are:

| This slice's mechanism | Precedent | The research's own limitation, adopted as a constraint here |
|---|---|---|
| M-1 shared sampled derivation | OpenPlan3D shared faceted path retaining source Wall ids (**SV**); Bezier.js `reduce`/`curveintersects`/`pairiteration` (**SV**) | "demonstrates reusable curve-processing structure, not a verified equivalent of Museum Editor's sampling semantics" ⇒ OR-8's case families are mandatory, not optional |
| M-2a candidate-pair pruning | RBush / Flatbush / CGAL box-pair filtering (**SV**) | **"A simple per-Wall AABB scan may have lower maintenance cost … No evidence yet establishes an R-tree as the faster choice for the measured 40-Wall fixture."** ⇒ ship the extent scan; see AB-10 |
| M-2b verdict reuse | FreeCAD touched/`mustRecompute` + `Document::recompute` (**SV**); Pascal dirty propagation (**SV**) | the invalid-baseline precondition (§4.3 of the synthesis) — corroborates the ruling block |
| dependency map for incremental compile | FreeCAD ordered recomputation (**SV**); IfcOpenShell element-granularity filtering (**DOC**) | "element granularity only" ⇒ a per-Wall/per-Room claim must be proven at that granularity, not assumed |
| not removing post-validation | — | "The second pass cannot be removed merely because the first passed" |

## 4. Proposed mechanism, and the alternatives

```text
M-1  ONE DERIVATION PER WALL PER RELEASE, PROVIDED EXPLICITLY.
     Preferred: `finalizeWallGeometryCandidate` (`:1508`) constructs ONE derivation context for the
     candidate document and threads it into the topology gates, face extraction, the Opening set and
     the compile. The context derives a Wall's samples once per (centerline identity, traversal,
     resolved endpoints) and returns THE SAME array object to every consumer.
     Why explicit threading over the alternatives:
       (a) no global/`WeakMap` module state ⇒ no document retention (RL-1) and no lifetime ambiguity;
       (b) the key grammar is visible at the call sites that must document it;
       (c) it is testable without reaching into module internals.
     Alternatives, and why not first:
       (i)  memoize inside `wallCenterlineSamples` with a module `WeakMap` keyed on the centerline
            object. Cheapest to write; rejected as the FIRST move because it silently changes every
            caller including tests and callers outside the acceptance chain, and because a module-level
            memo is exactly the resource-lifetime risk RL-1 names. Retained as a fallback if threading
            proves structurally impossible (see AB-1).
       (ii) a scene/editor-level sample cache. REJECTED — it puts derived geometry in editor state and
            creates pressure toward a second geometry truth, which the umbrella forbids.
M-1 TRAVERSAL HANDLING (S-2).
     The key MUST include traversal. Whether `reverse` may be served by reversing the `forward` array
     instead of re-deriving is an OPEN EQUIVALENCE QUESTION, answered by OR-2, not assumed. Ship the
     conservative form (key includes traversal) first; adopt reverse-on-read only if OR-2 proves array
     equality AND the slice records the proof.
M-2a EXTENT-DERIVED PAIR PRUNING.
     A Wall pair whose swept extents (AABB of the sampled polyline / chord, per the existing geometry)
     cannot overlap after the edit cannot newly cross. Prune those pairs from the sampled crossing scan
     while KEEPING the same predicate and the same reporting. This is a broad phase, not a new rule:
     the P23.11 precedent already shipped a conservative broad phase for the crossing predicates and
     its equivalence tests are the pattern.
     START WITH THE CHEAPEST FORM: a plain conservative extent scan, no new dependency. P23B.2 §Q2
     (SOURCE-VERIFIED precedents, but its own limitation) says an R-tree's economics are unproven at our
     document sizes, so a spatial-index library is NOT the first mechanism and NOT a rescue for a
     disappointed measurement (AB-10). The index is admitted only if the extent scan is measured
     insufficient on the committed fixtures AND the index is measured to beat it.
M-2b VERDICT REUSE ACROSS THE PRE/POST PAIR  — CONDITIONAL, BLOCKED.
     Pre and post run the same two functions on different documents (EQ-1/EQ-6); if a pair's inputs did
     not change and the ruling admits a stable verdict, reuse it (keyed on the pair's inputs). BLOCKED
     because under a "leave as-is / admit-as-invalid" ruling a cached NEGATIVE verdict becomes product
     semantics, not an optimization (synthesis §8.6 AB-3). Do not implement before the ruling.
```

## 5. Correctness invariants and equivalence proof obligations

```text
INV-1  Compiled geometry is BYTE-IDENTICAL before and after this slice, for the same document.
INV-2  One crossing rule: `detectWallCurveTopologyCrossings` remains the only sampled crossing
       authority; pruning changes WHICH PAIRS are evaluated, never the predicate or the verdict.
INV-3  Verdicts and diagnostics are IDENTICAL: same issue code, same message, same crossing Wall id pair.
INV-4  A hit and a recompute are ARRAY-EQUAL, not near-equal (EQ-3).
INV-5  Sampling options are unchanged (S-1); `sampleSegment`'s defaults are untouched.
INV-6  Plan/3D/visitor parity holds; no editor code enters `layout-core`; the visitor bundle gate is green.
INV-7  Rejection stays ATOMIC: a refused operation leaves document, identity, history and preview state
       unchanged (RT-1 R-b, OR-4).
OR-1   shared derivation vs fresh `wallCenterlineSamples`, EVERY curved Wall, BOTH traversals, on the
       committed E1/E3(C) fixtures ⇒ array-equal; the fresh call stays reachable as the control.
OR-2   reverse: independent reverse derivation vs reversed-forward array ⇒ equality, or the key keeps
       traversal. Recorded either way.
OR-3   full vs scoped/pruned validation on: (a) E1, (b) E3(C), (c) a document edited into an inter-Room
       crossing (F-C3 shape), (d) a document that ALREADY carries one ⇒ identical verdict + code +
       message pair. Case (d) is the one that proves pruning did not hide a pre-existing failure.
OR-4   atomicity of a refusal (RT-1 R-b).
OR-7   Plan/3D/visitor parity + the visitor bundle gate.
OR-8   (P23B.2 §Q1, mandatory) DIFFERENTIAL CASE FAMILIES for the shared derivation: straight ·
       quadratic/cubic · reversed · highly curved · near-degenerate · multi-segment, compared on sample
       count, orientation, endpoint inclusion, traversal reversal and numeric tolerance — asserting
       topology verdicts, intersection identities, Room polygons, Opening positions and compiled
       geometry, not just visually similar output.
OR-9   (P23B.2 §Q2/§Q3, mandatory) PRUNED-PAIR SUPERSET + EXHAUSTIVE ORACLE. The candidate pair set the
       broad phase yields must be a superset of every pair the unpruned narrow phase rejects — checked
       over curved crossings, TOUCHING ENDPOINTS, SHARED JUNCTIONS, NEAR-TANGENT CURVES, REVERSED WALLS,
       MULTIPLE ROOMS and DEGENERATE BOXES. Every scoped/pruned verdict is compared against the EXISTING
       FULL validator, never against another scoped variant.
OR-10  (P23B.2 §Q3, mandatory) INVALID-BASELINE CASE. The differential set must include a baseline that
       ALREADY carries an inter-Room crossing, and the result must state whether the pre-existing issue
       is preserved or re-derived. This is the case the ruling turns into policy (AB-3).
OR-11  (P23B.2 §Q4, mandatory) RECONCILIATION CHANGE CONTRACT. Before any post-validation scoping is
       proposed, the slice must record the traced input/output contract of each reconciliation outcome —
       as a FINDING. If it cannot be completed, no scoping of the post pass is proposed (the research's
       position, adopted).
```

## 6. Implementation steps (each independently committable and reviewable)

```text
S1  EQUIVALENCE TESTS FIRST (tests only, no production change)
    Land OR-1 and OR-2 as differential tests against the CURRENT per-consumer behaviour across the
    committed fixtures. They must be green before any mechanism lands, and they must fail loudly if a
    later change makes a shared derivation diverge. This is the slice's oracle; it commits alone.
S2  THREAD THE DERIVATION THROUGH ONE CONSUMER — face extraction (the largest, 80 samples/release)
    Introduce the context type and pass it from `finalizeWallGeometryCandidate` (`:1538` region) into
    `extractBoundaryCandidateFaces`. Default behaviour (no context) = today's per-call sampling, so the
    change is inert for every other caller. Tests: OR-1 for this consumer + the full face-extraction
    and curved-room suites.
S3  EXTEND TO topology-pre and topology-post
    Both gates receive the context. This is where EQ-1 is actually exercised (pre on the candidate,
    post on the reconciled document) — the test must prove the shared derivation is CORRECT for both
    inputs, not merely fast.
S4  EXTEND TO the compile's three sampling call sites (`layout-geometry.ts:927`, `:260`, `:367`)
    `:260` (canonical forward arc length for Opening mirroring) must keep `'forward'`: a change there
    is a behaviour change. Tests: Opening offset/mirroring suites (`layout-openings`, curve read-path).
S5  EXTEND TO the Opening set (`layout-opening-set.ts:84`) and verify with the OPENING-BEARING fixtures
    (`bend-10-wall-openings`, `bend-3-room-openings`) — the all-curved fixtures have 0 Openings and
    cannot exercise this consumer.
S6  REMOVE the now-dead duplicate derivation only when every consumer is covered; measure on the
    committed fixtures; record before/after with provenance. No promise of a proportional saving.
S7  M-2a EXTENT PRUNING for the crossing scan
    Conservative broad phase over Wall pair extents, same predicate, same reporting. Tests: OR-3 (all
    four cases) + the existing sampled-crossing equivalence tests.
S8  (BLOCKED) M-2b VERDICT REUSE — not before the ruling; if admitted, its own commit with its own
    differential test.
S9  RECONCILIATION CHANGE CONTRACT (OR-11, finding only)
    Trace each reconciliation outcome's inputs/outputs and record the contract, or record that it
    cannot be completed. No post-validation scoping is proposed either way in this slice; this step
    exists so the answer is a finding rather than an assumption, and so P23B.5/P23B.7 inherit it.
S10 DEPENDENCY-MAP FINDING for incremental compilation (X-5)
    Record which compile outputs are document-global (junction resolution, finite-thickness, aggregate
    bounds) and which are candidates for sub-graph locality. A FINDING, never an implementation.
```

## 7. Acceptance tests, differential tests and measurement

```text
MUST PASS
  OR-1 · OR-2 · OR-3 · OR-4 · OR-7 (§5) · OR-8 · OR-9 · OR-10 · OR-11 (§5, added from P23B.2)
  the whole applicable suite, not a narrowed subset: `npm test` (or the lane union), `npm run test:arch`
    before the PR (never path-gated), `npm run check`, root build
  PERF lane structural assertions unchanged — in particular the P23.11 equivalence tests and the
    accepted-compile-reuse assertion (`preview-compile` 0 / `preview-compile-reused` 1)
  `layout-geometry-boundary` (renderer-free `$lib/layout` sweep) and `wall-mesh-shell-boundary`
DETERMINISTIC
  INV-1 is asserted as a BYTE/STRUCTURAL comparison on the committed fixtures, not as a timing.
MEASUREMENT (ADVISORY — no enforced budget proposed)
  per-stage marks on the committed E1/E3(C) fixtures with the SAME provenance block as P23B.0-durable's
  baseline, reported as p50/p95 with sample counts. Marks are NESTED; report containment, never a sum.
  The comparison is baseline-vs-this-commit on identical fixtures and identical hardware.
RELATIVE ASSERTIONS ONLY where a wall-clock floor would be dishonest (the P23.11 precedent).
```

## 8. Regression and resource-lifetime risks

```text
R-1  A shared derivation that returns a MUTABLE array lets one consumer corrupt another. Mitigation:
     hand out frozen/read-only views or document the non-mutation contract and assert it in a test.
R-2  Face extraction currently samples BEFORE the `signedArea < 0` discard (`layout-face-extraction.ts:215`).
     Serving it from a shared derivation must not accidentally change the polygon-construction order or
     the discard decision.
R-3  Traversal misuse (serving a `reverse` request from `forward` data) would silently mirror geometry.
     OR-2 is the guard; without it the key must include traversal.
R-4  Threading a context through `layout-core` signatures is an API change with many call sites. Keep
     the parameter OPTIONAL with today's behaviour as the default so no other caller changes behaviour,
     and so a partially-migrated tree is never wrong — only slower.
R-5  Reconciliation between pre and post (EQ-1): if a shared derivation is keyed on document-scoped
     values it can be wrong for the post pass. Key on centerline identity + endpoints + traversal.
R-6  Resource lifetime (synthesis RL-1/RL-2/RL-3): a derivation that outlives the acceptance chain must
     not retain documents; prefer chain-scoped. Cached key material must respect the ENFORCED
     `cache-key-code-units` budget (620,268 code units for Chopin at the recorded baseline;
     target 700,000 / fail 1,300,000). Do not re-record a budget to silence a failure.
R-7  Mark nesting: adding marks inside the derivation could double-count `curve-sampling` and make the
     advisory numbers unreadable. Decide and document one owner for the mark.
R-8  Pre-existing-invalid documents: on a document that ALREADY carries a crossing (case OR-3d),
     pruning must still report it. A pruned scan that skips an already-failing pair is a correctness
     regression, not a speedup.
R-9  INDEX ESCALATION. The tempting failure mode after a disappointed extent scan is to add a spatial-
     index dependency to rescue the idea. AB-10 forbids it without a measured win, and P23B.2 §Q2 says
     the economics are unproven at our sizes. Record the measurement and stop.
R-10 ORACLE SUBSTITUTION. Comparing a pruned variant against another pruned variant proves nothing; the
     oracle is the EXISTING full validator (OR-9) and the existing per-consumer sampling path (OR-1).
```

## 9. Exit criteria and what must be recorded before advancing

```text
X-1  All MUST PASS items green on the committed fixtures; the slice record states the exact commands.
X-2  OR-1/OR-2/OR-3/OR-4/OR-8/OR-9/OR-10 results recorded, including any case where equality FAILED and
     how the key grammar was adjusted (or the mechanism abandoned under §8.6 AB-1/AB-2/AB-10).
X-2b OR-11's reconciliation change contract is recorded (completed, or explicitly recorded as not
     completable) before any post-validation scoping is proposed by a later slice.
X-8  The extent scan's economics are recorded against the current exhaustive scan on the committed
     fixtures. If the scan did not help, that is the RESULT — no index escalation (AB-10).
X-3  Before/after measurements recorded with provenance, marked ADVISORY, with mark nesting stated.
X-4  The KEY GRAMMAR is written down in one place — because P23B.5 must consume it and must NOT invent
     a second one (synthesis §8.2).
X-5  The dependency-map finding for incremental compilation is recorded: which compile outputs are
     document-global (junction resolution, finite-thickness, aggregate bounds) and which are candidates
     for sub-graph locality — as a FINDING, not an implementation.
X-6  The curved-crossing ruling's status is recorded and, if it arrived, M-2b's disposition is stated.
X-7  Slice closed on the same branch with an identifiable commit series; slice-closeout applied.
```

## 10. Explicitly deferred

```text
- incremental / sub-graph compilation (needs the X-5 dependency map, and may legitimately never be
  justified)
- M-2b verdict reuse (blocked on the ruling; may be dropped permanently under one option)
- any change to sampling tolerance/span/depth/cap (a product change, not an optimization)
- baseline→candidate artefact reuse and cache-lifetime policy → P23B.5
- mesh and adapter work → P23B.6 · gesture work → P23B.7 · Worker/WASM → P23B.8
- the curved-crossing policy itself → owner ruling
- any ENFORCED curved-authoring threshold → owner ratification
```
