# P23B.4 — Compilation and invalidation optimization (CLOSED)

```text
AUTHORITY: NONE — closed-work stub. The live contract is owned in code
(WallSamplingDerivation key grammar) and the phase README below. Full body
recoverable via the recovery line; deliberation lives in Git/PR history.
```

## Delivered

M-1: one chain-scoped shared sampled-centerline derivation per distinct key,
threaded from `finalizeWallGeometryCandidate` through face extraction,
topology pre/post, the compile's three sampling sites and the Opening set.
M-2a: plain conservative extent-scan pruning of the crossing scan (no index).
M-2b verdict reuse: DROPPED at X-6 (not postponed). S9 reconciliation-change
contract and S10 incremental-compile dependency map recorded as findings for
P23B.5/P23B.7. F1–F3 correction batch (observed-consumer reference, pruning
differential + superset proof, repeatable measurement) accepted.

## Contract (final, accepted)

INV-1…INV-7, OR-1…OR-11, exit X-1…X-8 as stated in the full body (see
recovery). Key grammar, owned in code at `WallSamplingDerivation`
(`packages/layout-core/src/layout-wall-centerline.ts`): centerline OBJECT
identity + traversal + resolved endpoints at full precision, chain-scoped,
non-mutation contract. Reverse independently derived; reverse-on-read NOT
adopted. P23B.5 must consume this grammar, not invent a second one.

## Provenance

PR #88, accepted HEAD `4cbcc370` against base `d7b9de4e`. Owner ruling:
ACCEPT IMPLEMENTATION — no remaining blockers (F1/F2/F3 resolved, lanes
independently passed). Merge method: squash (repo policy forbids merge
commits; owner chose squash-with-degradation).

## Evidence

Owner-verified gates at acceptance: full lane 4,786 passed; `test:arch` and
`test:perf` lanes green; `check` 0/0 both apps; both apps build; visitor
bundle verified; baseline `g3-baseline.json` SHA-256 `5534926e…` unchanged.
Performance acceptance covers reduced computation (curved-40: 280→120 kernel
evals; narrow-phase candidates 60→40 of the shipped 60, not 780→40) and
advisory Node timings. Browser release, flush/frame and settlement
improvements remain unproven (X-3 narrowed scope). Exact commands and oracle
results → evidence stub at
`2026-09-25-P23B.4-evidence-findings.md` (same directory).

## Entry points

```text
implementation  packages/layout-core/src/layout-wall-centerline.ts (derivation + key)
                packages/layout-core/src/layout-wall-first-precision.ts (chain context, gates)
                packages/layout-core/src/layout-face-extraction.ts
                packages/layout-core/src/layout-geometry.ts (compile sites)
                packages/layout-core/src/layout-opening-set.ts
                packages/layout-core/src/layout-geometry-curve.ts (test-only kernel observers)
                packages/layout-core/src/layout-wall-topology.ts (test-only prune bypass)
                packages/layout-core/src/layout-geometry-validation.ts
validation      apps/editor/tests/lib/layout/p23b4-sampling-reference.test.ts (S1)
                apps/editor/tests/lib/layout/p23b4-shared-derivation.test.ts
                apps/editor/tests/lib/layout/p23b4-extent-pruning.test.ts (S7)
                apps/editor/tests/lib/layout/p23b4-consumer-observations.test.ts (F1)
                apps/editor/tests/lib/layout/p23b4-observed-reference.ts (F1 reference)
                apps/editor/tests/lib/layout/p23b4-observation-collector.ts (F1 harness)
                apps/editor/tests/lib/layout/p23b4-pruning-differential.test.ts (F2)
                apps/editor/tests/lib/layout/p23b4-measurements.test.ts (F3)
                apps/editor/tests/lib/layout/p23b4-wild-fixtures.ts (OR-8 wild families)
regressions     full `npm test` lane · `test:arch` (incl. renderer-free + bundle gates) ·
                `test:perf` (P23.11 equivalence + compile-reuse assertions unchanged)
```

## Residuals (named, with owners)

- M-2b DROPPED — revival must re-prove the full gate from zero (P23B.4 S8 record).
- Flush-internal (Plan render path) attribution open → P23B.6.
- No R-tree/index work (AB-10); no browser/settlement claim (X-3 narrowed).
- U-1 declined; Rust/WASM undecided → P23B.8. P23B.5–P23B.8 unratified.

## Recovery

```text
git show cb9b83abd11a05c38907ade26d56b01bbf3ac147:docs/roadmap/p23b-geometry-performance/p23b.4-compilation-invalidation-optimization/2026-09-22-P23B.4-compilation-invalidation-optimization.md
tag closed/p23b.4 (targets 4cbcc370; this anchor is its ancestor — verified locally)
DEGRADATION (squash): branch commits are not ancestors of `main` post-merge.
Recovery runs via the PR head ref (verified post-merge before reporting complete):
git fetch origin refs/pull/88/head && git show <A>:<path>
tag closed/p23b.4 is local only — not pushed.
```
