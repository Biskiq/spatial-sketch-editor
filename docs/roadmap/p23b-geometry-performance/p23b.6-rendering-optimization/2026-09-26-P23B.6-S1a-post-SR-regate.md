# P23B.6 S1a post-S-R re-gate (CLOSED)

```text
AUTHORITY: NONE — closed-work stub. Full body recoverable via the recovery line; the two LIVE JSON
records named below carry the per-action timings and provenance.
```

## Result (as accepted)

The plan's §8 S3 admission conditions (a)–(c) still pass after S-R, so S3b/S3 were admitted. This
re-gate did NOT decide M-3m's exclusion — that was resolved by the comparator A/B record.

| Condition | Post-S-R evidence | Result |
| --- | --- | --- |
| (a) accepted-edit reachability > 0 | post-S-R capture: 25/25 accepted rigid-drag actions on straight, all-curved and owner; accepted single-Wall move reachable on all four fixtures with 36–37/40 value-equal Walls | Pass |
| (b) prepared-mesh construction attributed on a curved fixture | `mesh-prebuild` p50/p95 19.5/21.2 ms all-curved, 9.2/9.6 ms owner | Pass |
| (c) comparator < 50 % of skipped builds on every 40-Wall fixture | highest accepted-action ratio per fixture: straight 22.2 % DEV / 21.9 % PROD · target-curved 30.7 % / 20.9 % · all-curved 20.0 % / 19.5 % · owner 21.1 % / 21.2 % | Pass |

Run at clean head `22201d84` (node probe, both modes, 5 warm-ups + 15 samples; Apple M2, Node
v26.7.0). No baseline or ratchet was written. Evidence:
[development JSON](./2026-09-26-P23B.6-S1a-post-SR-development.json) ·
[production JSON](./2026-09-26-P23B.6-S1a-post-SR-production.json).

The D-5 requirement to beat the planning probe's JSON-string comparison was measured separately by
the S3 A/B probe (see the comparator reconciliation stub in this directory).

## Recovery

```text
A = 07dfa532   (last commit containing this file's full body)
git show 07dfa532:<this path>   ·   git show closed/p23b.6:<this path>
DEGRADATION (squash): recovery via refs/pull/93/head — git fetch origin refs/pull/93/head.
tag closed/p23b.6 targets 354f9b6f (local only — not pushed).
```
