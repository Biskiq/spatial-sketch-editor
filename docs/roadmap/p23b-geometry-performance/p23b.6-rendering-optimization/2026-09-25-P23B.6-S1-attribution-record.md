# P23B.6 S1 attribution record (CLOSED)

```text
AUTHORITY: NONE — closed-work stub. The ratified plan's S1 taxonomy and the DEV harness are the
live measurement infrastructure; the per-class distributions, counts and provenance stay LIVE in
`2026-09-25-P23B.6-S1-attribution-data.json` (this directory). Full body recoverable via the
recovery line.
```

## Result (as accepted)

S1 attributed the post-P23B.7 residual across the five layers and filled the plan's mechanical
admission table:

- **S-R ADMITTED.** In the curved persistent-viewport (Pan/zoom) class, `plan-render-model` is the
  largest active attributed term (all-curved p50/p95 4.9/6.3 ms; no compile, `mesh-prebuild` or
  adapter mark active). The same-process Node A/B on that fixture is raw 0.314 ms vs warm proxy
  19.47 ms (61.97×) with matching output; DEV cold-proxy multiplier up to 174.21× on all-curved.
- **S3 provisionally ADMITTED**, subject to the mandated post-S-R re-gate. Accepted Wall
  resize/move, bend, Wall authoring and Room creation are reachable (37/40 · 36–37/40 · 38/40 ·
  40/44 value-equal), curved release classes attribute `mesh-prebuild` (all-curved p50 17.3–27.8 ms;
  owner 11.5–14.7 ms), and the D-5 comparator prototype stayed below the 50 % gate on every
  fixture (max 27.0 %).
- Order after this record: **S2 → S-R → S1a re-gate → S3b → S3 → S6.** Whole-Room movement is
  BRIDGE-only and never satisfies accepted-edit admission.
- No §11 abandonment condition and no §2 contradiction fired in S1.

## Provenance and method (abbreviated)

```text
S1a node   one-off probe `apps/editor/tests/lib/bench/p23b6-s1-node-probe.cli.ts` at S1 head
           192d6381; 5 warm-ups + 15 measured samples; DEV under NODE_ENV=development, PROD under
           NODE_ENV=production; raw/proxy pairs produced matching output
S1b browser  clean capture at 6a08b704 (`treeDirty: false`), Apple M2 / 8 logical CPUs / 16 GB,
           macOS 15.7.2, HeadlessChrome 152, DPR 1, 1500×1000 (needed to fit the owner fixture at
           the unchanged 19.2926 px/m scale); Plan view only; 25 accepted actions per editing class
           (5 warm-ups, 20 measured); 52 measured pan/zoom events of 41 planned operations; 120 idle
           rAF intervals per fixture; 6000 ms guard unchanged
excluded   two harness setup failures (label hit target; viewport fit) — retried and excluded;
           rejected attempts never enter an accepted-action distribution
```

## Recovery

```text
A = 2a415371   (last commit containing this file's full body)
git show 2a415371:<this path>   ·   git show closed/p23b.6:<this path>
DEGRADATION (squash): recovery via refs/pull/93/head — git fetch origin refs/pull/93/head.
tag closed/p23b.6 targets 354f9b6f (local only — not pushed).
```
