# P23B.6 S6 final evidence (CLOSED)

```text
AUTHORITY: NONE — closed-work stub (the slice's QA/acceptance record). The live contracts are owned
in code and by the test-enforced proofs routed from the phase README; the accepted gate numbers are
reused here as recorded evidence, not re-produced. The nine JSON measurement records in this
directory stay LIVE as cited evidence. Full body recoverable via the recovery line; deliberation
lives in Git/PR history.
```

## Acceptance record

```text
owner ruling     REVIEWED AND ACCEPTED 2026-09-26, no remaining blocker; routine `slice-closeout`
                 authorized on the same branch.
accepted head    354f9b6f (docs/evidence) over the corrected test head d9ea6dfa and the production
                 code head e11f804b — production source and S1b capture inputs unchanged by the
                 review corrections.
PR               one PR for the slice: #93 — the closeout commit stays inside it; no new PR, no new
                 scope.
merge method     squash (repo policy; branch commits are not ancestors of `main` post-merge, so
                 recovery runs via `refs/pull/93/head`). Merge is an owner action.
closeout         re-ran NOTHING: closeout changes documentation only, so the recorded gates below
                 are reused verbatim.
```

## Recorded gates reused by the closeout

Owner-verified at the review-correction tree that became `354f9b6f` (production code and capture
inputs unchanged from `e11f804b`):

| Gate | Result |
| --- | --- |
| H-5 mounted-scene lifecycle + cleanup mutation | Passed edit, Undo/Redo, branch, view switch, remount and unmount accounting; disabling production Wall cleanup made the test fail |
| H-6 required forced-GC lane | 2 passed, including the strong-owner negative control; required mode fails explicitly when GC is unavailable |
| `npm run test:arch` | 23 files; 254 passed |
| `npm test` | 351 files passed, 2 skipped; 4,981 passed, 4 skipped |
| `npm run test:heavy` | 7 files; 89 passed, including H-1…H-7 retention checks |
| `npm run check` | Editor and museum; 0 errors, 0 warnings |
| `npm run build` | Editor and museum built successfully; existing chunk-size advisory only |
| `npm run verify:visitor-bundle -w @portfolio/museum` | Passed; `/museum` reaches 3 server and 9 client entries |
| `npm run test:perf` | 8 files; 63 passed, 1 skipped; budgets and reuse ratchet reproduced unchanged |

The focused oracle suites the plan's §9 names (OR-5 parity, OR-9 refusals, watertight matrices,
mesh parity, the P23B.7 S6 one-preparation oracle and the snapshot guard) are recorded in the
[S3 guard recheck](./2026-09-26-P23B.6-S3-guard-recheck.md) and covered by the lane results above;
no separate focused row is claimed at the correction head. No baseline or reuse-ratchet write
occurred at any point, and no browser capture was repeated because production code and capture
inputs did not change.

## Interaction-performance scope (as accepted)

The production preparation and reactive-model measurements support those mechanisms, but **overall
interaction improvement is not established**. In the final S1b capture, all-curved Whole-Room
release rose from 73.4 to 493.4 ms p50 and Wall-authoring release rose from 2,147.0 to 3,601.2 ms
p50. Both increases remain unresolved; the diagnosis identifies a planner/correspondence bottleneck
in the measured pre-change path but does not establish the cause of these final-capture increases.
They are owner-routed to P23B.11.

## X-2, retention and routing (as accepted)

- **X-2 S-R:** all three fixtures show an attributable reduction in the Plan reactive-model term
  under the same capture protocol (`plan-render-model` 2.7/3.3 → 0.1/0.2 straight, 4.9/6.3 → 0.2/0.3
  all-curved, 3.2/3.7 → 0.1/0.2 owner-curved). A-2 is met.
- **X-2 S3:** accepted-edit `mesh-prebuild` fell on every accepted class (e.g. all-curved rigid drag
  19.5/21.2 → 11.5/13.6; straight authoring 12.8/13.2 → 7.1/9.6). The integrated Node gate is net
  positive on all four accepted fixtures. Whole-Room movement stays separate bridge-only evidence
  and does not satisfy accepted-edit admission.
- **Retention:** H-1…H-6 green; the advisory H-7 delta is 4,451,892 B heap + 63,328 B external per
  generation straight and 7,090,547 B + 68,352 B all-curved. The P26-inherited contract is recorded
  in the plan stub (§6 H-7): generations strongly held only by live state and history; derived
  artifacts weakly keyed; GPU resources under renderer lifecycle; future view/session trails
  identify generations by canonical ID + generation number and never enter history.
- **§13 routing:** canonical compile cost → owner / P23B.4 family / P23B.8 input · whole-Room
  per-move compile+install → owner / P26 §3.5 · curved Wall-authoring and Room-creation uncovered
  interval → **P23B.11** · snap/hit-test residuals → P23B.7 family · adapter/GPU → P26 P6 · Plan
  template/SVG/affine → P26 P1 · history-storage redesign → separate evidence-gated owner decision ·
  dormant S6 mapping → optional follow-up · any ENFORCED rendering budget → owner ratification.

Full mark-coverage tables, the complete X-2 and routing tables and the H-7 rows are recoverable
via the recovery line; the LIVE JSON records below carry the per-class distributions.

## Live evidence

```text
2026-09-25-P23B.6-S1-attribution-data.json          S1 five-layer attribution, admission table
2026-09-26-P23B.6-S1a-post-SR-development.json      post-S-R node re-gate (DEV)
2026-09-26-P23B.6-S1a-post-SR-production.json       post-S-R node re-gate (PROD)
2026-09-26-P23B.6-S1b-final-head.json               final-head S1b capture + X-2 layer data
2026-09-26-P23B.6-S1b-final-head-coverage.json      final-head interval-coverage derivation
2026-09-26-P23B.6-S3-abandonment-data.json          strict-comparator attempt (historical)
2026-09-26-P23B.6-S3-comparator-reconciliation-data.json  comparator A/B (admission)
2026-09-26-P23B.6-S3-guard-recheck-data.json        final integrated net-benefit + oracle rows
2026-09-26-P23B.6-S3-integrated-probe-data.json     first integrated attempt (historical)
```

## Recovery

```text
A = 354f9b6f   (last commit containing this file's full body; the accepted head)
git show 354f9b6f:<this path>          # full body as accepted
git show closed/p23b.6:<this path>     # same, via the tag target
DEGRADATION (squash): branch commits are not ancestors of `main` post-merge —
git fetch origin refs/pull/93/head && git show <A>:<path>. tag closed/p23b.6 targets 354f9b6f
(local only — not pushed). After the merge, verify each anchor is reachable from `main`
(`git merge-base --is-ancestor <A> main`) before relying on the written recovery lines.
```

## CLOSEOUT PRESERVATION — P23B.6 (2026-09-26)

```text
prose compacted:                 9   stubs at their own paths (plan · S1 attribution · S1a re-gate ·
                                     S3 abandonment · S3 comparator reconciliation · S3 integrated
                                     probe · S3 guard recheck · this S6 final evidence · release-
                                     delay diagnosis)
reference promotions:            0   the landed contracts are owned in code + tests (S-R raw
                                     generation fields and the state-free prepared-mesh module); no
                                     docs/reference/** doc owns this editor-preview mechanism, so
                                     nothing was promoted (no arch-doc rewrite attempted)
renderable evidence archived:    0 files / 0 B   (the nine JSON measurement records are
                                     machine-readable cited evidence with live inbound links and stay
                                     LIVE per the phase convention — P23B.5, P23B.7 and the
                                     measurement-only step)
evidence stubs / manifests:      0   (no archive copies needed)
transient artifacts removed:     1   superseded S6 evidence checkpoint (historical pre-fix capture;
                                     durable findings already promoted into the S3 records and this
                                     stub; full body recoverable at anchor 761b3a22)
historical anchor:               354f9b6f · tag closed/p23b.6 (local only — not pushed);
                                 per-record anchors c726fb1d (plan) · 2a415371 (S1) · 07dfa532
                                 (S1a re-gate) · 114d3c24 (comparator reconciliation) · 761b3a22
                                 (S3 abandonment · guard recheck · integrated probe · retired
                                 checkpoint) · 354f9b6f (this record · diagnosis) — all verified
                                 ancestors of the tag target
new live → archive prose links:  0
manual-owed verification rows:   0   owner accepted with no blockers; closeout reuses the recorded
                                     gates and re-ran nothing
remaining deferred items:        8   overall interaction improvement not established →
                                     P23B.11 · canonical compile cost · whole-Room per-move
                                     compile+install · snap/hit-test · adapter/GPU → P26 P6 · Plan
                                     template/SVG/affine → P26 P1 · history-storage redesign ·
                                     dormant S6 mapping retirement
```
