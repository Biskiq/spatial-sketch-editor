# Historical P23B.6 first integrated S3 M-3m probe (CLOSED — SUPERSEDED)

```text
AUTHORITY: NONE — closed-work stub, HISTORICAL ONLY. This is the first integrated attempt's net
loss under a guard-in-the-hot-loop implementation; it is NOT the current S3 decision. Full body
recoverable via the recovery line; the LIVE JSON record carries the timings and oracle rows.
```

**Historical result:** under that first implementation (structural guard run for both inputs of
every Wall comparison) the integrated preparation reused value-equal meshes but lost on every
accepted single-Wall release: straight −3.579, target-curved −3.799, all-curved −5.198,
owner-curved −2.659 ms p50; bridge rows were also net losses. That result would have met §11 A-1
without the owner's subsequent correction. The comparator-only subprobe was already below the 50 %
gate (18.8–22.6 %) and did not override the integrated result. Work stopped there; the temporary
live-preview wiring and generation-level cache used by the probe were discarded, the S1 comparator
plus structural-guard helper remained as the S3b input-contract implementation, and the full
repository contract and final capture were not run at that point.

The owner directed the guard recheck, which is net positive on all four accepted fixtures and is
the landed decision — see the [guard recheck](./2026-09-26-P23B.6-S3-guard-recheck.md) and the
[S6 final evidence](./2026-09-26-P23B.6-S6-final-evidence.md). Data:
[integrated-probe JSON](./2026-09-26-P23B.6-S3-integrated-probe-data.json) (live).

## Recovery

```text
A = 761b3a22   (last commit containing this file's full body)
git show 761b3a22:<this path>   ·   git show closed/p23b.6:<this path>
DEGRADATION (squash): recovery via refs/pull/93/head — git fetch origin refs/pull/93/head.
tag closed/p23b.6 targets 354f9b6f (local only — not pushed).
```
