# P23B.6 S3 guard recheck (CLOSED)

```text
AUTHORITY: NONE — closed-work stub. The landed guard decision is owned in code
(`apps/editor/src/lib/editor/layout/prepared-wall-meshes.ts`); the LIVE JSON record carries the
p50/p95, refusal counts and diagnostic splits. Full body recoverable via the recovery line.
```

## Result (as accepted)

S3 M-3m stays LANDED. After moving the structural plain-data verdict OFF the per-Wall comparison
path (computed once per generation and stored with its prepared set; production trusts
`layout-core`'s compiler-owned output), the integrated generation preparation is **net positive on
all four accepted single-Wall release fixtures** — straight +8.821, target-curved +8.354, all-curved
+12.997, owner-curved +7.362 ms p50 (full → integrated preparation 12.224→3.403, 11.650→3.296,
17.139→4.142, 9.842→2.480 ms). The earlier guard-in-hot-loop loss is historical and superseded.
Owner authorization: PR #93, 2026-09-26; this recheck carried it out.

Oracles: OR-HIT reused 36/36/37/37 of 40 on accepted actions (146 hits, 14 rebuilds); no-reference
control 0 hits and fails the positive assertion. OR-9 refused all 14 changed-input rows (10
`compiled-wall-changed`, 2 `resolved-ends-changed`, 1 `floor-elevation-changed`, 1
`builder-signature-changed`) plus the non-enumerable future-field control (40/40 refuse); the
unchanged independent-overlap control reused. OR-5 parity, OR-8 lifecycle, the P23B.7 S6
one-preparation oracle, the snapshot guard and H-6 (forced GC, with its strong-Map negative
control) all passed; no mesh-builder file changed and ENFORCED metrics were untouched.

Whole-Room bridge rows are recorded separately and do not satisfy accepted-edit admission. Data:
[guard-recheck JSON](./2026-09-26-P23B.6-S3-guard-recheck-data.json) (live).

## Recovery

```text
A = 761b3a22   (last commit containing this file's full body)
git show 761b3a22:<this path>   ·   git show closed/p23b.6:<this path>
DEGRADATION (squash): recovery via refs/pull/93/head — git fetch origin refs/pull/93/head.
tag closed/p23b.6 targets 354f9b6f (local only — not pushed).
```
