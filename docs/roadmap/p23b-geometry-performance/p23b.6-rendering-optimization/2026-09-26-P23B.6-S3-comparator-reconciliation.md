# P23B.6 S3 comparator reconciliation (CLOSED)

```text
AUTHORITY: NONE — closed-work stub. Full body recoverable via the recovery line; the LIVE JSON
record carries the A/B rows and provenance.
```

## Result (as accepted)

The prior M-3m exclusion is SUPERSEDED: under a D-5-conforming comparator, §11 A-1 does not fire.
The committed S3 A/B probe (one production Node process at clean head `f9230731`, Node v26.7.0,
Apple M2, 5 warm-ups + 15 samples, four 40-Wall fixtures, twenty fresh compiled generations per
action, one prepared reference generation) measured:

| Comparator | Ratio vs skipped builds | Accepted-move net |
| --- | --- | --- |
| `deepEqualOwnData` (early attempt) | 82.1–99.3 % | mixed, ≤ +3.871 ms |
| **S1 `Object.keys` (admitted)** | **15.9–19.1 %** | **+9.792 … +18.139 ms on all four** |
| JSON-string ceiling | 19.9–40.9 % | +7.954 … +15.446 ms on all four |

The S1 comparator beat the JSON-string ceiling on every row, kept every existing refusal row
(including a changed future field), and a one-time structural check confirmed compiled Walls and
resolved ends are plain JSON-like data across the four fixtures. Decision is mechanical; the
recommendation to authorize production integration was given, and the owner then authorized it in
PR #93 on 2026-09-26. No production integration was included in this record. Data:
[comparator A/B JSON](./2026-09-26-P23B.6-S3-comparator-reconciliation-data.json) (live).

## Recovery

```text
A = 114d3c24   (last commit containing this file's full body)
git show 114d3c24:<this path>   ·   git show closed/p23b.6:<this path>
DEGRADATION (squash): recovery via refs/pull/93/head — git fetch origin refs/pull/93/head.
tag closed/p23b.6 targets 354f9b6f (local only — not pushed).
```
