# P23.15 junction-correct wall-first 3D — QA record — closed work (stub)

```text
STATUS:    CLOSED — P23.15 accepted 2026-09-21
AUTHORITY: NONE — historical acceptance evidence. Not an instruction; competes with nothing.
RECOVER:   git show 1f1d2652be52cf545403889530d52a6fb76c30c8:docs/roadmap/p23-layout-depth/p23.15-junction-correct-wall-first-3d/qa/2026-09-21-P23.15-junction-qa-record.md
           · git show closed/p23.15:<same path>   (annotated tag → 1f1d265; pushed to origin)
DURABLE:   docs/reference/architecture.md §Ownership ("3D wall meshes") · §Geometry boundary
```

**What was here.** The full P23.15 acceptance record: the landed
resolve → validate → per-Wall-packaging pipeline, the degree ≥ 3
partition policies (continuation core + branch trim; Y/star sector beams and
keels), per-band and Opening-aware exposure, bounds/cache-key reconciliation,
the measured perf-lane marks and mesh sizes, the A–I architecture cases, and the
§5.1 owner self-review probe that found and fixed the material-ownership defect.
Full body via the anchor above.

## Closeout record (2026-09-21)

```text
ACCEPTED:  owner-accepted 2026-09-21 · branch `p23.15` · PR #72 (plan + implementation + closeout, one branch)
GATES:     npm run test:fast   285 files · 4276 passed
           npm run test:heavy  5 files · 73 passed
           npm run test:perf   4 files · 22 passed · 1 skipped
           npm run check       0 errors, 0 warnings (editor + museum)
           npm run build       editor + museum built (adapter-vercel)
           npm test (full)     318 files (317 passed, 1 skipped) · 4625 tests (4624 passed,
                                 1 skipped) — run 2026-09-22 at `d3252e3`, i.e. after this closeout,
                                 to close the PR's full-suite merge gate with real numbers; the lane
                                 runs above remain the closeout-time evidence. The docs-only commits
                                 after it (the closeout correction pass) cannot invalidate it — no
                                 code changed
           — the lane runs were taken at closeout on branch `p23.15` @ 1f1d265
ROWS:      J1–J9, as the PR #72 acceptance matrix declares them. **No interactive session was run
           at any point in this slice's closeout, so no manual or visual pass is claimed anywhere
           in this record.** J1–J6 are visual-only and were NOT re-run — they need an interactive
           owner session, so they stay manual-owed. J7–J9 were partly exercised automatically:
           J7's opening geometry, J8's editor/museum byte-identity, and J9's keel ownership +
           coverage oracles (§7.5–§7.6 and §3 of the full body, anchor above) are the automated
           halves; their visual halves stay manual-owed with J1–J6. J9 (Y / star sector partition)
           entered the matrix in the second correction pass (`c0783f0`) and is recorded here
           because the plan's own matrix stops at J8. Nothing is inferred from source inspection.
DECISION:  Owner decision 2026-09-22: the J1–J9 visual halves are carried to P23.16 as
           owner-carried work instead of blocking PR #72's merge. The merge gate is therefore
           automated acceptance plus architecture acceptance, and P23.16's whole-product
           integration pass is the verifier for every visual row.
BASIS:     the accepted implementation state is branch `p23.15` @ `1f1d265`; the closeout commit
           only compacts documentation and reconciles `docs/reference/architecture.md`. No code
           changed after the gated state, so the numbers above are the state the slice ships.
RULINGS:   Decision 4 (branch-cut-independent seam ownership) and Decision 13 (broad rebuild is
           acceptable; no incremental-cache rewrite required for correctness) are landed
           behaviour, not open calls. Decision 14's polygon/oval/curve+curve extension point is
           preserved, deliberately unimplemented.
RESIDUALS (carried, NOT accepted as done):
           1. J1–J9 visual 3D rows — owner-carried to P23.16 by the 2026-09-22 decision above; no
              interactive session was run, so the visual half of every row, including J7–J9, is
              still owed. P23.16 is the natural verifier: whole-product integration is its scope.
           2. Incremental Junction invalidation is not implemented: a Wall/Junction edit
              recomputes the affected Junction and invalidates every incident Wall mesh. This is
              an accepted non-goal (Decision 13), not a defect.
           3. P23.14's residuals (TD-2 Inspector `:invalid` announces, manual-owed accessibility
              rows, Inspector role-migration residue, §0.3 open owner calls, the two unverified
              Task 5 behaviour rows) were **not** touched by this slice and keep their owners —
              P23.15 adds no shell surface.
           4. TD-1 (floor-supported placement unreachable) remains owner-deferred to P24 and was
              deliberately not worked around here.
ENTRY POINTS:
  implementation  packages/layout-core/src/layout-junction-resolution.ts   Junction-local solve —
                    continuation core + branch trim, Y/star sector partition, per-band/Opening-aware
                    exposure, resolved-join surfaces and clip distances
                  packages/layout-core/src/layout-junction-clearance.ts    canonical compile acceptance
                    of the resolved geometry (fail closed) + builder defense-in-depth
                  packages/layout-core/src/layout-geometry.ts               CompiledJunction +
                    incident-leg facts (startJunctionId/endJunctionId), resolved bounds/query AABBs
                  apps/editor/src/lib/layout/wall-mesh-builder.ts          per-Wall mesh packaging
                    from resolved ends; byte-identical twin apps/museum/src/lib/layout/wall-mesh-builder.ts
  validation      apps/editor/tests/helpers/wall-mesh-fixtures.ts          coverage + single-Wall
                    ownership oracles (raster method; test oracle only, never a production predicate)
  regressions     apps/editor/tests/lib/layout/junction-partition.test.ts   Y/star keels (J9)
                  apps/editor/tests/lib/layout/junction-matrix.test.ts      90°/X/T/curved matrix
                  apps/editor/tests/lib/layout/layout-junction-resolution.test.ts, layout-junction-topology.test.ts,
                  layout-junction-wall-ends.test.ts, layout-junction-clearance.test.ts
                  apps/editor/tests/lib/editor/layout-mesh-parity.test.ts   editor/museum parity (J8)
TEST MAP:  new/extended suites this slice owns:
           apps/editor/tests/lib/layout/layout-junction-topology.test.ts
           apps/editor/tests/lib/layout/layout-junction-resolution.test.ts
           apps/editor/tests/lib/layout/layout-junction-wall-ends.test.ts
           apps/editor/tests/lib/layout/layout-junction-clearance.test.ts
           apps/editor/tests/lib/layout/junction-matrix.test.ts
           apps/editor/tests/lib/layout/junction-partition.test.ts
           apps/editor/tests/helpers/wall-mesh-fixtures.ts (independent coverage + ownership oracles)
           apps/editor/tests/lib/editor/layout-mesh-parity.test.ts (extended: resolved-end wiring)
MERGE:     PR #72 must land with a **merge commit** (owner-confirmed 2026-09-21) so the anchors
           recorded here and in the sibling stubs stay reachable on `main`. A squash merge would
           invalidate them; the reachable form would then be
           `git fetch origin refs/pull/72/head && git show <A>:<path>`.
TAG:       `closed/p23.15` (annotated) → `1f1d265`, created 2026-09-22 as the P1 insurance for these
           anchors, and **pushed to `origin`** the same day (owner-approved) — so they survive a
           squash/force-push upstream, and PR #72 landing with a merge commit is no longer the only
           thing keeping them reachable.
ANCESTRY:  verified at creation with `git merge-base --is-ancestor`: `c0783f0`, `1f1d265` and
           `204d132` are **all ancestors of the tag target**, so one tag covers all three anchors.
           Ancestry was checked, never assumed — had one fallen outside, a common reachable
           preservation commit or a second tag would have been required instead.
```

```text
CLOSEOUT PRESERVATION — P23.15 (2026-09-21)
prose compacted:                3   stubs at their own paths (plan, this QA record, research brief)
reference promotions:           1   docs/reference/architecture.md — the stale Junction
                                    "records that fact as a warning" claim reconciled to the landed
                                    sector-beam partition (the Ownership row already named the
                                    compiled Junction contract)
renderable evidence archived:   0 files / 0 B   no PNG/SVG/HTML plate, screenshot or fixture
                                    belonged to this slice — it produced no browsable evidence
evidence stubs / manifests:     0   nothing renderable to manifest
transient artifacts removed:    0   no empty states, husks or `__qa-*` plates; no checkpoint existed
historical anchor:              c0783f0 (plan) · 1f1d265 (QA record) · 204d132 (research)
                                tag: closed/p23.15 → 1f1d265 · pushed to origin (2026-09-22), so it
                                covers all three anchors remotely; PR #72 still has to land with a
                                merge commit for `main` itself to reach them
new live → archive prose links: 0   this close creates no dependency on archived prose; the phase
                                    README cites the stubs at their own live paths
manual-owed verification rows: 9   J1–J9, each with its visual half owed (J7–J9's automated halves
                                    are recorded above)
remaining deferred items:       3   J1–J9 visual rows → P23.16 · incremental Junction invalidation
                                    (accepted non-goal, Decision 13) · P23.14's residuals and TD-1
                                    stay with their existing owners
```

Research artefact classification (A/B/C): the sibling brief
[`research/future-proof-room-wall-junction-3d-geometry.md`](../research/future-proof-room-wall-junction-3d-geometry.md)
is **B — decision-support**: alternatives and rejected approaches (CSG, Room-owned ring solids, a
second topology abstraction). Its durable conclusions were promoted into
`docs/reference/architecture.md` §Geometry boundary as the slice landed, so the brief is
anchor-only — no archive copy.
