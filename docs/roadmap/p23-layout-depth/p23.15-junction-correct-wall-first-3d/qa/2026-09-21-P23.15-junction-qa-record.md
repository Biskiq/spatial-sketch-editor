# P23.15 — Junction-correct wall-first 3D · QA record (2026-09-21)

Status: **implementation landed, owner closeout pending.** This record carries the
automated acceptance for Tasks 1–7 and the measurements the plan asked for. Live
3D rows that need an interactive session are marked **manual-owed** — never
claimed from source inspection.

## 1. Landed architecture

```text
canonical Wall graph
        ↓
compiled Wall paths + Junction incidence        (CompiledJunction / CompiledJunctionLeg)
        ↓
Junction-local network resolution               (resolveJunctionGeometry)
        ↓
Wall-attributed resolved end geometry           (CompiledLegJoin + CompiledJunctionResolution)
        ↓
per-Wall mesh packaging                         (buildStandaloneWallMesh)
```

- Per-Wall **ownership/output** remains the unit; per-Wall geometry **solving**
  does not. The resolver inspects every incident leg before any Wall endpoint is
  determined; the builder only triangulates.
- Renderers read `legJoinsByWall(geometry.junctions)` and pass
  `{ start, end }` into the builder. A connected end with no resolution is a
  blocking issue and produces **no mesh** (no square-cap fallback).
- Canonical compile acceptance owns validity: resolve → validate →
  `LayoutGeometryIssue` → only valid compiled geometry reaches consumers. Both
  mesh builders re-run the same pure predicate as defense-in-depth with the same
  codes.
- Production validity is analytic (finite coordinates, positive band spans,
  non-collapsed seam polygons, join completeness), never sampled grid coverage.
  The raster/coverage probe lives only in `tests/helpers/wall-mesh-fixtures.ts`
  as an independent oracle.

## 2. Implementation notes worth recording

- **Degree ≥ 3 pairing.** The resolver pairs collinear antiparallel legs as the
  through Wall first and suppresses their interface; every remaining leg is a
  branch **trimmed flush at the Junction plane**, closed by interpenetration. The
  earlier angular-neighbour pairing mitred the through Wall against a branch,
  which buried a surface and produced a self-intersecting end quad and a real gap
  at the Junction. The independent coverage oracle caught this; the fixture
  matrix now pins it.
- **Continuation is a centerline relationship.** A tangent pair suppresses only
  the common interface; thickness, height and Opening-profile differences still
  emit their exposed step. At degree ≥ 3 the Junction decides whether that step
  is exposed or buried by another leg, because only the Junction has every leg.
- **Seam ownership** is the start leg, else the smaller `wallId` — never an
  angle, so `atan2`'s branch cut cannot move physical ownership.

## 3. Automated acceptance

| Case | Where |
|---|---|
| Compiled Junction topology, outward frames, endpoint Opening bands | `tests/lib/layout/layout-junction-topology.test.ts` |
| Junction-local policy (degree 1–4, continuation, miter, bevel, fold, mixed thickness/height, rotation-invariant ownership) | `tests/lib/layout/layout-junction-resolution.test.ts` |
| Standalone Wall end contract (three branches, fail closed, 45/90/135°, mixed thickness/height steps, split invariance) | `tests/lib/layout/layout-junction-wall-ends.test.ts` |
| Seam codes + parity triad (compiler == editor builder == museum builder) | `tests/lib/layout/layout-junction-clearance.test.ts` |
| Fixture matrix: degree 1/2/3/4/5, angle sweep 30–175°, thickness and height mixes, door at a Junction, A–I architecture cases | `tests/lib/layout/junction-matrix.test.ts` |
| Independent coverage oracle (boxes + per-Wall ray parity) | `tests/helpers/wall-mesh-fixtures.ts` |
| Editor/museum builder byte-identity and live-surface resolved-end wiring | `tests/lib/editor/layout-mesh-parity.test.ts` |

Architecture cases: **A** straight subdivision invariance (shell extent equal, no
wedge, no internal cap), **B** bend after subdivision (deterministic corner),
**C/D** tangent continuation, **E** T/X provenance determinism, **F** one physical
shared Wall, **G** bounds, **H** 0.20 → 0.30 thickness step with no buried cap,
**I** 1.2 → 3.0 height step.

## 4. Measurements

Perf-lane marks (shipped `p2311Measure`, reported not asserted), 24 samples per
tier; `junction-resolution` is per compile, `standalone-wall-build` per Wall:

| Mark | Tier | p50 | p95 | max |
|---|---|---|---|---|
| `junction-resolution` | chopin | 0.007 ms | 0.012 ms | 0.013 ms |
| `junction-resolution` | small | 0.070 ms | 0.089 ms | 1.055 ms |
| `junction-resolution` | medium | 0.039 ms | 0.050 ms | — |
| `standalone-wall-build` | chopin | 0.085 ms | 0.095 ms | 0.119 ms |
| `standalone-wall-build` | small | 0.051 ms | 0.086 ms | 0.113 ms |

Fixture mesh size (canonical Wall meshes only, equal 0.2 m thickness, 3 m height):

| Fixture | Triangles | Vertices |
|---|---|---|
| degree-1 Wall | 128 | 136 |
| 90° corner | 256 | 272 |
| T | 384 | 408 |
| X | 512 | 544 |

`apps/editor/src/lib/bench/baselines/g3-baseline.json` is intentionally **not**
re-recorded: the committed baseline predates this work on different hardware, so
its deltas are not attributable, and re-recording would replace a stable artifact
with one machine's noisy numbers (`treeDirty: true`). Task 7's attributable
measurement is the two new marks above. `plan-bench`'s `wall-mesh-build` metric
now also walks the canonical per-Wall Junction-aware path, so a wall-first
document's wall cost is actually measured.

## 5. Live rows

| Row | Surface | Status |
|---|---|---|
| J1 | Scene · 3D + `/museum` Preview — 90° corner miter | manual-owed (owner session) |
| J2 | Scene · 3D — X Junction, no interior void / no double-painted face | manual-owed |
| J3 | Scene · 3D — T with a thinner stem | manual-owed |
| J4 | Camera · 3D + Preview — mixed thickness step | manual-owed |
| J5 | Scene · 3D — mixed height step | manual-owed |
| J6 | Scene · 3D — curved leg meeting a straight leg | manual-owed |
| J7 | Scene · 3D + visitor route — door at a Junction | automation covers the geometry; visual row manual-owed |
| J8 | `/museum` visitor route — editor/museum identical | automation covers byte-identity; visual row manual-owed |
| A / F | shell invariance · single shared Wall | automation (fast lane) |

Automated rows are green: `test:fast` 4258 tests, `test:heavy` 73 tests,
`test:perf` green, plus `check` clean for editor, museum and `layout-core`.

## 6. Bounds

Every emitted vertex is asserted inside the document, Floor, owner-Wall and
Junction aggregate bounds (`junction-matrix.test.ts` case G), so resolved miters
and bevels cannot silently escape the compiled bounds that
`compileWallFirstJunctions` expands.

## 7. Deferred / not in scope

Incremental Junction invalidation is **not** implemented: a Wall/Junction edit
recomputes the affected Junction and invalidates every incident Wall mesh. Live
3D rows above need an owner session. No `RoomVolume`, no Room-owned Wall ring, no
CSG, no exact circle/ellipse primitive, no `formatVersion` change, no new
topology degree validity, no P24/P26 capability.
