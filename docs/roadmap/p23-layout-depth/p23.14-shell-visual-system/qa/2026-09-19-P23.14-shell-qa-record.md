# P23.14 shell QA record — closed work (stub)

```text
STATUS:    CLOSED — P23.14 accepted 2026-09-21
AUTHORITY: NONE — historical acceptance evidence. Not an instruction; competes with nothing.
RECOVER:   git show ca239563d0d034b12da519338074fce80d2bbcf1:docs/roadmap/p23-layout-depth/p23.14-shell-visual-system/qa/2026-09-19-P23.14-shell-qa-record.md
DURABLE:   docs/reference/design-system/editor-shell-and-visual-system.md §0.2–§0.4
           docs/reference/design-system/editor-shell-ratifications.md (evidence annex)
           docs/operations/tech-debt/README.md (TD-2)
```

**What was here.** The full P23.14 acceptance record: the four Atlas specimens (A Scene/Plan/
Layout, B Scene/3D, C Camera/Plan collapsed drawer, D Camera/3D expanded drawer), the §25.2
stress toggles, findings F1–F10, the tray judgment call, the owner ratifications R1–R3, and the
review pass (ownership by host, exposure by workspace).

## Closeout record (2026-09-21)

```text
ACCEPTED:  owner-accepted; closeout folded into the P23.15 branch, 2026-09-21
GATES:     npm test      312 files (311 pass · 1 skip) · 4560 pass / 1 skip · 34.0s
           npm run check 0 errors, 0 warnings (editor + museum)
           npm run build editor + museum built (adapter-vercel)
           acceptance rows — A–D specimens + §25.2 toggles as recorded in the full body
           (anchor above). Basis: implementation unchanged since the reviewed head
           `f7a31e2` (merged `a479f78`); `main` has since moved only in docs and in the
           test-suite harvest that renamed this slice's suites. No live re-run of the
           in-app review rows was performed at closeout.
RULINGS:   R1–R4 + the View Bar MODE grammar — durable in the shell contract §0.2; not restated.
RESIDUALS (carried, NOT accepted as done):
           1. F4 → TD-2 (Inspector numeric fields announce `:invalid` on legal values):
              docs/operations/tech-debt/README.md
           2. manual-owed a11y rows — screen-reader reading order, physical
              device/OS-preference (reduced-motion, coarse pointer) pass
           3. Inspector role-migration residue (12 px / 12.5 px tiers): shell contract §0.4, §13.1
           4. open owner calls (Tool Tray focus treatment, View Bar pressed fill, tray width
              vs a renamed `TRANSFORM`, squeezed-column View Bar): shell contract §0.3
           5. P23.14 DOM/event-dependent source pins still need a rendered mechanism —
              test-suite harvest T3c follow-up (docs/operations/test-suite-harvest-2026-09-19.md)
           6. Task 5's two carried behaviour rows (opening-insert commits on click; field
              cancels on an external history transaction) are NOT separately re-verified by
              this record — treat as owed if a later slice depends on them
TEST MAP:  this slice's suites were renamed by the test-suite harvest from `p23-14-*` to
           durable names: `shell-a11y-motion`, `shell-contrast-floor`,
           `shell-control-ownership`, `shell-state-language`, `shell-type-roles`,
           `inspector-target`, `camera-drawer`, `inspector-junction-dissolve`
           (apps/editor/tests/lib/editor/app/).
```
