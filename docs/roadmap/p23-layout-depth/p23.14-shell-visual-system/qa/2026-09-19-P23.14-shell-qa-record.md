# P23.14 shell QA record — self-review pass

**Date:** 2026-09-19 · **Branch:** `p23.14` · **Method:** own self-review in the running
app at `http://localhost:5173` (Preview) driven through the live DOM (snapshot +
computed style + synthetic pointer/keyboard input + screenshots).

**What this row covers:** the layout, ownership and interaction claims Task 9 asks a QA
gate to falsify — Atlas specimens A–D plus the §25.2 stress toggles.
**What it does not cover:** device/screen-reader rows and OS-level preferences
(`prefers-reduced-motion`, `pointer: coarse`) are **manual-owed** below; nothing in this
record claims them from source inspection.

## Fixture used

The preview pane renders at **731×675 CSS px** (below the 1440×900 reference). To read the
reference composition, the session applied a **QA-only, non-repo override**
(`.project-editor.page { --editor-side-left: minmax(15rem, 268px); --editor-side-right:
minmax(17.5rem, 300px); }`) plus `zoom: 0.5`. Caveat, stated honestly: media queries still
evaluate against 731 px, so component-level narrow variants were active while the shell
tracks were at reference. Shell-level claims below are unaffected; a true 1440×900 capture
remains manual-owed.

Project state (see the TD‑1 note in the slice README):

- **Layout:** canonical wall-first document, one `Rect Room` drawn in Scene · Plan · Layout
  (`3.75 × 2.50 m`, reference `R-2QUS`).
- **Scene:** pre-seeded **canonical world-local Scene** imported through the Document menu
  (`formatVersion: 1`, no `roomId` anywhere; the menu reports “Scene document is valid.”) —
  first 3 nodes/2 connections, then a 9-node/8-connection set for the camera-count stress
  row. This is the fixture/harness path the slice README requires for populated Camera UI.

## A — Scene / Plan / Layout

| Claim | Result |
| --- | --- |
| Recursive multi-floor Navigator; one expanded `Architecture → Walls` branch | **pass** — Navigator pages `Rooms / Walls / Openings / Junctions` + `PLACED CONTENT`, 10 px indent, section species, selection pin with `R-2QUS` |
| PLATE composition: Spine · Head · View Bar · Tool Tray · Navigator · Inspector · Status Rail | **pass** — measured `56 / 36 / 34 / 44 / 268 / 300 / 24`; `.center` spans `x 324 → 1162` |
| P23.13 Plan Paper + drafting tools in the Paper-attached tray | **pass** — tray groups `SELECT / DRAW / OPENINGS / OBJECTS`, `Cancel` appears while armed |
| Property-first Inspector | **pass** — identity header (`Draft Room 1 · R-2QUS · ROOM`) before `Name / Floor thickness / Ceiling thickness`, derived facts, then `EXACT DIMENSIONS`, `DUPLICATE`, `REMOVE ROOM`, `Technical details` |
| Progressive density (Navigator squeezed) | **fixed in this pass** — see F6 |

## B — Scene / 3D

| Claim | Result |
| --- | --- |
| Same shell geometry, 3D transform instruments, no Plan local-mode control | **pass** — identical spine/head/view bar/tray; tray `SELECT / TRANSFORM / OBJECTS / SPACE / View`; no `Layout | Arrange` |
| Scene 3D must not expose Camera framing authoring | **fail — finding F1** (owner decision, not patched here) |

## C — Camera / Plan, collapsed Drawer

| Claim | Result |
| --- | --- |
| Drawer collapses to a transport/readout strip (no lanes, no scrubber) | **pass** — `Sequence (Full Tour)`, flip/prev/play/next, `00:00.00 / 00:02.50`, POV·Observer, recenter/follow, expand |
| Drawer spans the central column only | **pass** — frame rect `x 324 … 1162` (center track), never over Navigator/Inspector |
| No finite frustum in Camera Plan | **pass** — `0` frustum primitives in the Plan canvas with a camera node selected |
| Ordinals = sequence position, not canonical IDs | **pass** — `01 A · Entry` with flow-order badge `#1`; raw `cam-a` only behind `Technical details` |
| Reason-coded refusal, destructive last | **pass** — `Preview Camera` disabled with reason `Inspect at Sequence boundary`; `Delete camera node` last, destructive-treated |

## D — Camera / 3D, expanded Drawer

| Claim | Result |
| --- | --- |
| Expanded height `288px`, central column only | **pass** — measured `288 × 838` at the center track |
| Exactly five lanes + ruler, one playhead | **pass** — `TIME (+ View Key)` ruler, then `Camera Path`, `Shots`, `FOV`, `Look At`, `Roll`; one playhead line; `Roll 0°` quiet |
| Selected-camera finite frustum in 3D | **pass** — frustum wedge rendered for the selected node |
| 8+ cameras | **pass** — 9 nodes: `Shots 9 cameras`, `Camera Path 8 transitions`, sequence `01…09` with `Start` / `Stops at 09`; five lanes unchanged |

## §25.2 stress toggles

| Toggle | Result |
| --- | --- |
| Navigator 268 (reference) vs 240 (minimum) | **pass after F6** — row metadata painted at 232 px inner, shed at 204 px inner; labels/selection/position always painted |
| Empty state (fresh project) | **pass** — teaching card in the Plan paper, `0 rooms / 0 objects / 0 geometry warnings`, empty Camera `No sequence yet` strip |
| Expanded drawer at reduced height | **pass** — lanes scroll inside the drawer; transport and View Bar never covered |
| Keyboard: tablist arrows, roving tabindex, focus-in-popover | **pass (live)** — `ArrowRight` on the Hierarchy tab moved focus *and* selection to `Assets`, tabindex `0 ↔ -1`; Document menu took focus on open and `Escape` returns it to the ⋮ |
| Reduced motion / coarse pointer 44 px | **manual-owed** — requires an OS preference and a touch device; behaviour is source-pinned by `p23-14-a11y-motion.test.ts` |
| Screen reader | **manual-owed** |

## Findings

- **F1 — Scene workspaces can mount the Camera node editor.** With a camera node retained
  from the Camera domain, Scene · 3D renders `EditorCameraInspector` (position/target/FOV —
  framing authoring) while the Status Rail reads `No selection / 0 selected`. Contract:
  `design-shell-specs.md` §15 (“Scene workspaces may show Camera entries … without exposing
  sequence/timeline/framing authoring”) and §20. **Not patched** — clearing or scoping the
  retained camera selection is a selection-model decision, not shell polish.
- **F2 — Inspector header and body can describe different selections.** With the Assets panel
  focused, the sectionheader still read the camera node identity while the body rendered the
  asset-details panel. Same root as F1 (two selection models reaching one panel). Reported,
  not patched.
- **F3 — locality badges asserted the legacy format (fixed in this pass).**
  `CameraPlanInspector` hard-coded `Camera node · room-local` and printed an unguarded
  `Room` slot; `EditorCameraInspector` and `EditorTransformInspector` likewise. All now
  derive from the selection (`roomId ? 'Room-local' : 'World-local'`), matching P23.0b
  world-local being the canonical Scene format.
- **F4 — numeric fields report `:invalid` while holding legal values.** `min="0.001"` with
  `step="0.01"/"0.05"` puts the browser's step base off-grid (`0.1`, `3.75` both fail), so
  the a11y tree announces valid values as invalid. No stylesheet paints `:invalid`, so this
  is an AT-truth defect, not a visual one. **Not patched** — the fix (align `min` to `step`,
  or `step="any"`) changes arrow-key increments, which §7 territory; owner call.
- **F5 — `POV / Observer` appears twice in Camera 3D** (View Bar and the drawer transport
  strip). Both are coherent surfaces, so this is a deliberate-looking duplication; flagged
  for the owner, not patched.
- **F6 — progressive-density threshold was too high (fixed in this pass).** The container
  measured is the Navigator scroll track (232 px inside the reference 268 px column), so a
  `16rem` (256 px) threshold hid row metadata at the *reference* width. Now `216px`: fires
  only when the column is squeezed below ~252 px, i.e. between the reference and the 240 px
  minimum.

- **F7 — the Tool Tray's engraved tier had drifted above what the rail can paint (fixed in
  this pass).** Review F3 read `design-specs.md` §22's "10 px engraved group labels" as a
  floor and moved both the group label and the tool label from 8 px to 10 px. §11 also fixes
  the rail at 44 px, and the two cannot both hold: measured in the running app, at 10 px/600
  the group names need 46.6–63.8 px and single tool words up to 48.6 px inside the text box
  the rail can offer, so every label broke *inside the word* — `SELEC / T`, `OPENI / NGS`,
  `OBJEC / TS`, `Windo / w`, `Colum / n`, `Platfor / m`. Camera 3D's `Add camera` was worse
  than a bad break: `EditorViewportToolbar`'s own `white-space: nowrap` (a View Bar contract)
  is inherited into the tray, so the label painted 46 px wide in a 39 px box and overran the
  rail into the Paper. **Patched** — see the judgment call below; §22 of
  `design-system/design-specs.md` is reconciled to the landed tier and
  `p23-14-contrast-floor.test.ts` re-pins it.

- **F8 — the rest of the shell's type and button chrome had drifted from the Atlas (fixed in
  this pass).** The owner compared the whole shell, not just the rail: the Atlas's fonts,
  font sizes and button sizing/styling did not match what shipped. Measured cause: §7's flat
  ramp was **mostly dead** (nothing consumed `title`, `panel-title`, `toolbar`, `tree-row` or
  `status`), so components carried their own `rem` values instead — seventeen distinct sizes
  between 9.6 px and 13.6 px, no two surfaces agreeing, which is exactly why an Atlas
  comparison can never converge. Deltas found and closed:

  | surface | Atlas | shipped before |
  | --- | --- | --- |
  | View Bar controls | plain 24 px buttons, 10 px utility / 11 px MODE labels, pressed = edge border + inset bottom rule | 28 px enclosed troughs, 12 px labels, accent-soft fill |
  | Head (Project Row) | 26 px controls, 14 px identity | 12 px controls, 13 px identity |
  | Domain Spine station | 74 px, 11 px label in the domain's casing, 24 px icon | 72 px, 10 px uppercase tracked label |
  | Navigator rows | 29 px rows, 12 px label, 10 px mono meta | 2 rem (`32 px`) rows, 0.74 rem label, 0.62 rem meta |
  | Inspector section headers | 10 px/600/+0.04em | 11 px/600/+0.05em |
  | Timeline ruler | 9 px **mono** | 11 px sans |
  | Status Rail | 10 px, 12 px padding, 20 px gaps | 11 px, 8 px padding, 0.9 rem gaps |
  | Tool Tray | roles + fitted geometry | loose px in rules |

  **Fixed structurally, not value by value (owner ratification R3):** the shell no longer
  holds pinned sizes. The seven-step ladder (9/10/11/12/13/15/20 px) and every role
  (`--editor-type-*`, `--editor-control-*`, icon + fitted-geometry roles) are multiples of two
  knobs — `--editor-type-scale` for type, `--editor-control-scale` for button/group geometry —
  so the next Atlas pass retunes one file instead of fifty components. The pre-R3 generic
  control names survive as **aliases** onto the roles, so a component written against either
  vocabulary moves with the same knob. Verified live: the whole shell scales together
  (status 10 → 11.5 px, stations 11 → 12.65 px, rows 12 → 13.8 px, identity 14 → 16.1 px, rail
  44 → 51 px at 115 %), and the control knob moves only the chrome (station 74 → 89 px, rail
  tools 42 → 50 px). The sweep is pinned against regression by
  `tests/lib/editor/app/p23-14-type-roles.test.ts`, which fails if a swept surface
  reintroduces a pinned `font:`/`font-size:` value — a guard, because the failure mode is
  per-surface and invisible to any single component's tests.

  **Two things found while sweeping, worth naming:** the `font:` **shorthand** was the drift
  the greps missed (three pinned values in `EditorViewportToolbar`'s popover only showed up
  once the guard scanned declarations rather than a `font-size` string), and a **nested CSS
  comment** in a token-file note silently dropped the whole ladder from the cascade — real in
  the browser, invisible to a text scan. Both are now scanned for.

- **F8 residual (deliberately not half-migrated):** the **Inspector family**
  (`EditorInspector.svelte` + the eight panel inspectors) still carries pinned 12 px / 12.5 px
  tiers, because P21.5 Slice 4 pins them across all nine components. The band-by-band mapping
  is recorded in `tokens.css` so the batch stays mechanical.

- **F9 — the View Bar's `MODE` caption was painted inside a control capsule (fixed in this pass,
  owner-reported).** The owner read `MODE` as a third segment sharing the box with
  `Layout | Arrange`. The cause is the leak class §2.2 already records: `LayoutDraftToolbar`'s
  own `.segmented` rule (`padding: 1px; border: 1px; border-radius: 6px;
  background: var(--editor-bg-control)`) keeps applying inside the shell, and the shell scope had
  only cleared the properties it happened to name. Measured live before → after: wrapper
  `#E8E5DD` fill / 1 px border / 6 px radius / 1 px padding → transparent / 0 / 0 / 0; `MODE`
  10 px/600 muted with an 8 px gap (Atlas `.mode small`); the pair on the 11 px MODE tier at
  24 px (Atlas `.mode button`).

  **Three more instances of the same leak were found in the same audit and are fixed:** the
  tool-group divider (`padding-right: 0.32rem` + `border-right`) the Atlas does not have;
  `LayoutDraftToolbar`'s `button { height: 28px }`, which was sizing the View Bar's 24 px tiers
  from the component instead of from the role; and the component's pressed fill (a translucent
  accent wash). Pressed now takes the Atlas's recessive surface step (`--editor-bg-recess`) plus
  the edge border and inset rule, so hover lifts / pressed sinks — the pair the armed tray tool
  already uses (R2) — and the state does not rest on hue. Pinned by
  `contracts.test.ts` § "paints MODE as a muted caption beside two plain buttons".

### Judgment call — the tray's engraved tier (§7 vs §11)

Scope: the P23.14 tray's own type only. No other shell typography moved.

1. **Spec first, as asked.** §7's approximate scale lists `10 px: engraved/group labels`;
   §11 mandates the `44 px` rail. The tray was set to 10 px and does not fit it.
2. **Measured in the app**, canvas-metric at weight 600 against the widest text box the rail
   can give a label (`44 − 2 gutter − 1 rail border − 2 control border = 39 px`):

   | size | group labels that overflow 39 px | tool words that overflow 39 px |
   | --- | --- | --- |
   | 10 px | SELECT, OPENINGS, OBJECTS, TRANSFORM, CAMERA (46.6–63.8 px) | Window, Platform, Sculpture, Connect, Sequence (39.5–48.6 px) |
   | 9 px | OPENINGS, OBJECTS, TRANSFORM, CAMERA (39.1–57.4 px) | Sculpture, Sequence (42–43.7 px) |
   | 8 px | OPENINGS, TRANSFORM (41.7 / 51 px) | Sequence (38.8 px) |
   | 7 px | TRANSFORM only (44.7 px) | none |

   So 10 px is not a stylistic preference the rail can be talked into — it is arithmetically
   impossible above ~8 px for the group vocabulary.
3. **Deliberate call: paint the reference, not the tier.** §7 calls its own hierarchy
   *approximate*; §11 asks only for "small persistent group labels and compact icon-led
   tools"; and both the PLATE reference PNGs and the Atlas rail paint 7 px group / 8 px tool
   labels. The tray now paints those two values through two new tokens
   (`--editor-font-size-tray-group` / `-tool`), so the deviation is named, testable and
   reversible in one place instead of hiding as loose pixels in a rule. §7's 10 px tier still
   owns every other engraved label in the shell.
4. **The geometry was borrowed, not invented.** The rail stays `44 px` (`--editor-tray-width`)
   and buttons stay `42 px` tall; the gutter tightened `3 px → 1 px` and the control now
   stretches to the rail instead of a hard `38 px`, which is what turns 36 px of text box into
   39 px. That 3 px is the difference between `Sequence` fitting and breaking, and it is
   inside the rail's own padding — no §5 composition track moved. `white-space: normal` is
   added in the shell scope only, so the frozen relic's floating toolbars keep their View Bar
   contract.
5. **Result, measured live:** tray `scrollWidth == clientWidth` (43 px) in all four reachable
   tray vocabularies — Scene Plan, Scene 3D, Camera 3D, Camera Plan — with every label on one
   line except the two-word labels, which now wrap at their word boundary (`Rect Room`,
   `Poly Room`, `Add Camera`, `Add camera`).
6. **The one word that cannot fit at 7 px is resolved, not accepted (owner ratification R1):**
   the group label `TRANSFORM` (44.7 px at 7 px — wider than a 44 px rail can ever carry)
   now steps down to a 6 px compact floor via an opt-in `data-group-compact` on that group,
   so it paints on one line instead of breaking. The word is kept, because `TRANSFORM` is the
   group name the spec itself uses and renaming it to fit would have contradicted the spec
   being promoted. Nothing else in the tray touches the `overflow-wrap` guard.

### Owner ratification (2026-09-19)

The owner reviewed the raw rail and ratified two decisions against the drift above — the
engraved tier (**R1**) and the armed-tool treatment (**R2**: a darkened surface only, no amber
border, no inboard edge) — and then, reviewing the whole shell against the Atlas (**F8**),
ratified a third: **R3**, one type scale and one control scale expressed as roles, so the
shell's sizes are retuned by two knobs instead of per surface. The owner also promoted the
P23.14 design + Atlas to the durable design authority for later phases. The full record, including the root cause of the drift (the old
ribbon-era contract was the only written authority for the new 44 px rail's numbers, and the
tray's shared toolbar components carry View Bar rules into the rail), lives in
[`../../../../reference/design-system/editor-shell-ratifications.md`](../../../../reference/design-system/editor-shell-ratifications.md).

**R1–R3 and the View Bar `MODE` grammar are now folded into the durable shell
authority** — [`../../../../reference/design-system/editor-shell-and-visual-system.md`](../../../../reference/design-system/editor-shell-and-visual-system.md) §0.2 (the
ratification record, with the pre-implementation direction alongside each outcome),
§2.8–§2.10 (shell inheritance, roles-not-numbers, constraint-over-number), §7 (closed
ladder, roles, the two scales), §10/§11 (View Bar and Tool Tray) and §18 (the four
distinct surface states). §0.3 of that document keeps the open owner calls unresolved
and §0.4 keeps the implementation debt out of design. The ratifications being durable
does **not** close P23.14: the slice stays owner-review-open.

## Open items carried out of this pass

Not findings — the state a reviewer or a later phase inherits.

**Owner calls still open** (no implementation default was taken): **F1** (Scene workspaces can
mount the Camera node editor), **F2** (Inspector header vs body can describe different
selections), **F4** (numeric fields report `:invalid` while holding legal values), **F5**
(`POV / Observer` duplicated in Camera 3D); the Tool Tray's **keyboard focus ring** (R2 removed
the highlight border, the ring is a different cue — remove it and a replacement keyboard cue is
owed); and the **pressed-surface** treatment the View Bar toggles now use (recessive step + edge
rule) if an accent-tinted fill is preferred there instead.

**Manual-owed rows:** device/screen-reader reading order, `prefers-reduced-motion`, and
coarse-pointer usability have no automated cover.

**Known defect carried (not this slice's):** TD-1 — floor-supported placement is unreachable in
canonical wall-first projects, so a fresh project cannot author its first Camera node. It lives
in [`docs/operations/tech-debt/README.md`](../../../../operations/tech-debt/README.md) and is
recorded above as an acceptance limitation; P23.14 does not work around it.

**Deliberate incompleteness:**

- **Inspector family not yet on roles** (the F8 residual): nine components still pin their
  12 px / 12.5 px tiers because P21.5 Slice 4 pins them across all nine. The band-by-band
  mapping is recorded in `tokens.css`, so it stays mechanical.
- **Other older numeric sections in the reference docs** were not rewritten by R3 (the type
  scale table is now explicitly superseded and §20's tree-row block is updated, but not every
  dimension in `design-shell-specs.md`); those sections are descriptive by the authority note,
  not an implementation target.
- **View Bar in a squeezed center column** (~240 px, far below the 1440 × 900 reference):
  `.contextual-tools` (`flex: 1; min-width: 0`) clips its own children, so the `Layout | Arrange`
  pair is cut before anything else gives. Pre-existing, observed during this pass, deliberately
  not changed — the fix is a progressive-density rule, the way the Navigator already sheds its
  row metadata.

**Standing rule for later phases — the leak class, now with four instances:** a new shell scope
must explicitly clear *every* layout-bearing rule it inherits from the component grammar (the
tray's `nowrap`, the View Bar group divider, the segmented enclosure, the ribbon's 28 px button
height) and pin each one shell-scoped, because component tests cannot see any of these failures.

## Known limitation carried (TD‑1)

Fresh-project `Add Camera` still depends on the legacy Room-floor path; Camera UI QA above
therefore ran over a pre-seeded canonical world-local Scene fixture, exactly as the slice
README requires. The placement-refusal row itself is **manual-owed** (armed-state and
refusal text were not captured — the rail's pointer readout overwrites the message), and
P23.14 does not fix or work around the placement semantics.
