# P23.16 — Whole-product integration verification plan

**Parent:** [P23 — Layout Depth umbrella](../2026-09-07-P23-layout-depth-minimum-build.md)

**Status:** verification plan — **owner-accepted and EXECUTED** (2026-09-22). Results, per-row
verdicts and the gate statement live in the
[QA / gate record](./qa/2026-09-22-P23.16-qa-gate-record.md). One owner-approved implementation
amendment was folded in: TD-3 / issue #35 (`7c8dacb`). Everything else remained verification-only,
as the ``TYPE`` block below says.

**Scope authority:**
[`2026-09-08-P23.16-final-whole-product-integration-closeout.md`](../2026-09-08-P23.16-final-whole-product-integration-closeout.md)
(exit criteria) ·
[`2026-09-14-P23-remaining-roadmap-reconciliation.md`](../2026-09-14-P23-remaining-roadmap-reconciliation.md) §P23.16.

```text
TYPE:      verification-led closeout gate — no implementation, no new capability
OWNS:      mapping the P23 exit criteria to already-accepted evidence · the
           cross-slice seams that evidence does not cross · the checks that close
           them · the phase-closure blockers
DOES NOT:  re-run per-slice QA without a stated justification · change product,
           schema, topology, renderer or shell behaviour · close P23
```

Satisfying these exit criteria makes P23 **CLOSABLE, not closed.** Closing P23 is a
separate owner-invoked `phase-closeout` procedure that owns P-level status, the cycle
transition and the close preflight (cycle is `WAITING`, so its target is computed, never
assumed).

## 0. Method

- **Reuse accepted evidence.** A slice's accepted state is its closeout record +
  per-seam suites, not a re-derivation. P23.14 and P23.15 keep it in their QA stubs
  (full bodies behind the recorded `git show` anchors); earlier slices keep it in their
  landed plans' Status/acceptance sections and their suites.
- **Two evidence classes, never conflated.** Automated lane/command output and live
  interactive rows are recorded as separate columns. A row that was not exercised is
  `owed` — never inferred from source inspection.
- **Re-run only on invalidating change.** Automated lanes are re-run at the gate because
  the gate is the last whole-product statement, not to re-litigate each slice. No
  per-slice acceptance is repeated otherwise.
- **Falsification rule.** A check that falsifies landed behaviour is an implementation
  change: it reopens review and **stops** the gate. A check that falsifies only a
  closeout *document* is repaired under exit criterion E8.

## 1. The representative fixture (F1)

One composed canonical project, used by both the automated and the live lane so
identities can be compared across surfaces:

- **Layout:** canonical wall-first `LayoutDocument` — one drawn Room, Walls at degree
  2/3/4, a curved leg, a Wall-hosted Opening, one `partition` Wardrobe run, authored
  names/identities. It is assembled from the same canonical writers the product uses.
- **Scene:** pre-seeded canonical world-local `SceneDocument` (`formatVersion: 1`, no
  `roomId` anywhere) imported through the Document menu.

The pre-seeded Scene is **required**, not a shortcut: TD-1 (documented below) makes a
fresh canonical project unable to author its first Camera node. The fixture path is the
one P23.14's QA harness already used; P23.16 extends it forward through the loop rather
than inventing a second harness.

## 2. Acceptance contract → evidence map

| # | Exit criterion | Existing accepted evidence | State | P23.16 check |
|---|---|---|---|---|
| E1 | One canonical project completes the whole Build loop and round-trips **exact identities** through Undo/Redo, Save/Load, Preview and Publish | Per-seam: `layout-identity.test.ts` (identity allocation + codec round trip), `history-controller.test.ts` + `layout-undo-branch.test.ts` + `layout-wall-segment-history.test.ts` (history), `layout-codec` / `layout-wall-first-codec` / `project-codec` / `package-exporter` + `package-importer` + `package-roundtrip-smoke` (Save/Load), `preview-coordinator` + `project-flows` (Preview), `publish-surface` + `visitor-cold-runtime` (Publish), `visitor-identity-isolation` (visitor cannot see display identity) | **PARTIAL** — every seam is pinned; **no suite crosses them in one fixture** (S1) | **M1** live crossing on F1 + existing suites re-run in A4 |
| E2 | Plan / 3D / visitor agree | `layout-geometry-parity`, `layout-geometry-golden`, `layout-mesh-parity` (editor↔museum byte identity), `wall-mesh-watertight-matrices`, `project-format-visitor-parity` (Preview/visitor compile parity, cold-load byte identity), `plan-scene-footprint` | **COVERED (automated)** | A4 + **M2–M10** for the visual half |
| E3 | Architecture edits do not move Scene/Camera | `layout-wall-edit-gesture.test.ts` "leaves the Scene document, Scene selection and camera focus untouched"; `scene-roots`, `project-format-policy` (ownership/isolation) | **PARTIAL** — one gesture pinned, not the edit surface (S6) | **M11** live stillness row + a bounded assertion extension if the owner wants it automated (implementation change → review) |
| E4 | Included accessibility flows pass | Automated: `shell-a11y-motion`, `shell-contrast-floor`, `shell-control-ownership`, `shell-type-roles`, `plan-keyboard-*`, `inspector-*`. Live (P23.14 QA §Fixture rows): tablist arrows + roving tabindex + focus-in-popover **pass (live)**; reduced-motion/coarse-pointer rule **pass** for shell species incl. `a[href]` | **PARTIAL** — device/screen-reader and OS-preference rows have **no automated cover** and were never run | A4 + **M15** (screen reader), **M16** (reduced-motion + coarse pointer on a device, and the 1440×900 reference capture) |
| E5 | Internal legacy fixtures only smoke through the retained shared adapter | `layout-migration` + `layout-migration-math` + `layout-wall-first-codec` + `project-compat` + `project-format-visitor-parity` (§legacy composer parity, F0 review); frozen relics `/museum` (Chopin visitor) and `/museum/editor` (legacy room-first editor) still routed | **COVERED (automated)** | A4 + a live relic smoke in **M12** (that live row is waived by owner decision 2026-09-22 — frozen relics, no active maintenance) |
| E6 | Full repository checks/builds and **visitor bundle gates** pass | Recorded closeout gates (P23.14, P23.15) covered editor lanes + editor/museum check + build only; `apps/museum/scripts/verify-visitor-bundle.mjs` (`verify:visitor-bundle`) has **no recorded closeout run**, and no gate list named the API or package workspaces | **PARTIALLY OPEN** — scope is now defined as editor + museum + API + packages: A1–A7 for the editor/museum lanes and the bundle gate, A13 for the workspace typechecks/builds the root scripts do not cover, A14 recorded separately because it needs local infrastructure (S8) | A1–A7, A13, A14 |
| E7 | No prerequisite P23 issue remains open | Reconciliation §Issue disposition (table updated 2026-09-22): `#34 #35 #38 #39 #40 #41` → **delivered**; `#6` → closed and re-disposed post-P23 (`#26`); `#26 #28` → post-P23; `#31 #32 #33 #36 #37 #44` → unrelated/post-P23 | **RESOLVED** — `#34 #38 #39 #40 #41` closed against recorded evidence (`shell-a11y-motion` §#34/#38/#39/#40-41, `shell-contrast-floor`, `context-menu`); `#6` closed and re-disposed post-P23 (`#26`); **`#35` delivered by P23.16** (`7c8dacb`) under the owner's 2026-09-22 amendment and closed against A12 + the live theme sweep. **Zero prerequisite P23 issues remain open** (S5) | A8 + **A12** (required pass) |
| E8 | Closeout docs accurately record explicit post-P23 debt | TD-1 and TD-2 open in `docs/operations/tech-debt/README.md`, with **TD-3 preserved as fixed** (history kept, not erased); shell contract §0.3 (open owner calls) and §0.4 (carried implementation debt); phase README carried rows | **PARTIAL** — S7 is corrected (2026-09-22); `#35`'s delivery is landed in the reconciliation table, the ledger and the tracker; S11 is resolved (one suite kept as a regression guard, one deleted) and the record's §5 corrections are applied | A10–A12 + the S11 resolution + record §5 |

## 3. Unverified cross-slice integration seams

Verification targets, not defects. Each is a place where two accepted slices meet and
neither slice's acceptance crossed the joint.

```text
S1  No single crossing of the whole loop. Identity survives undo/redo at the document
    layer and separately in editor history; Save/Load and Preview/Publish are pinned by
    their own suites. Exit E1 asks for one project crossing all four in sequence, from the
    editor, against the visitor. This is the gate's primary new evidence.
S2  P23.15 J1–J9 (junction-correct 3D, visual halves). Owner-carried to P23.16 by the
    2026-09-22 decision; no interactive session has been run, so all nine rows are owed.
S3  P23.14 post-pass "startup-only empty hint" (Plan `Start your plan` card latched to the
    editor session). Landed after the accepted review pass and recorded there as *not part
    of the acceptance, not owner-reviewed*; it owes a live start-draw → undo and
    Plan → 3D → Plan round trip. Structural half is in the retired `p23-13-empty` suite
    (now `plan-empty-state`).
S4  P23.13's two Task 5 behaviour rows (opening-insert commits on click; a field cancels on
    an external history transaction) were never separately re-verified and are carried as
    owed, not landed.
S5  Issue disposition — RE-DISPOSED by owner ruling (2026-09-22). Six of the seven P23-owned
    issues are closed: #34 by removal of the status-hint group, #38/#39/#40/#41 by the landed
    keyboard/focus work and its `shell-a11y-motion` cover, and #6 to post-P23 legacy-stack
    retirement (#26) because P23.11 adopted the validation class for canonical curved Walls only.
    The seventh, **#35** (canonical axis tokens), was not landed by P23.14 — at plan time the raw
    `#f05252`/`#45c878`/`#3b82f6` literals were still in `EditorNumberField` / `EditorVec3Field`
    and no test referenced `--editor-axis-*` — so it was first **re-disposed as post-P23 debt
    (TD-3)**. **Owner amendment 2026-09-22:** rather than defer it, the owner folded the bounded
    correction into P23.16, so it is now **delivered** (`7c8dacb`), with the gate's **A12** promoted
    from baseline audit to required-pass canonical-token regression check and the visual contract
    covered live by M17. TD-3's historical deferral is preserved in the ledger; nothing about the
    re-disposition is erased, only superseded.
S6  Movement stillness. E3 cannot be exercised with a freshly authored Camera node in a
    canonical project (TD-1), so the row must run on F1's pre-seeded Scene and measure
    stillness, not placement (P23.16 must not fix TD-1).
S7  Closeout-evidence integrity — RESOLVED at plan time (2026-09-22). PR #72 landed by
    **rebase** under an owner decision (rebase rather than squash, keeping the commits
    separate), so the three P23.15 bare-SHA anchors are not ancestors of `main` and the remote
    `p23.15` branch is gone. Recovery is the pushed tag `closed/p23.15` (verified: all three
    bodies resolve, 909 / 322 / 732 lines). The stubs' merge-commit claim was corrected to
    record the ruling, so the gate inherits accurate evidence instead of a stale claim.
S8  The visitor bundle gate (`verify:visitor-bundle`) is an exit criterion but appears in no
    recorded closeout gate list.
S9  Manual accessibility rows: screen-reader reading order, OS reduced-motion, coarse
    pointer on a physical device, and a true 1440×900 reference capture (the P23.14 QA
    session ran at 731×675 with a QA-only override).
S10 TD-1 (floor-supported placement unreachable in canonical projects) is an accepted
    P24 deferral. It is a limitation on the fixture (S6), not gate work.
S11 RESOLVED at execution (2026-09-22): `tmp-confirm-gizmo-pointer-pipeline.test.ts` is kept as a
    permanent regression guard (the only real-TransformControls pointer/session pipeline cover;
    its header now records the ruling) and `tmp-confirm-gizmo-frame-bug.test.ts` was deleted —
    its refuted-finding rationale is preserved in the record and every behaviour it pinned has a
    permanent home. The complete suite moved 4624 → 4619 tests (exactly the five deleted ones).
```

## 4. Targeted checks

### Automated (A — commands; run once at the gate)

```text
A1  npm run test:fast                  inner loop
A2  npm run test:heavy                 dense correctness
A3  npm run test:perf                  timing/budget gates
A4  npm test                           complete suite (lane union; includes arch)
A5  npm run check                      svelte-check, editor + museum
A6  npm run build                      editor + museum
A7  npm run verify:visitor-bundle -w @portfolio/museum    after A6 (reads .svelte-kit/output)
A8  gh issue list --state open         → reconcile against the E7 disposition table
A9  tag recoverability (S7): for each of the three P23.15 anchors — git show closed/p23.15:<path>
    resolves the full recorded body (909 / 322 / 732 lines), and git merge-base --is-ancestor <A>
    closed/p23.15^{commit} is true
A10 grep -rn "RECOVER:" docs/          → every recorded anchor resolves (bodies, not stubs)
A11 live → archive prose links: 0 new; no __qa-* plates; the two tmp-* suites dispositioned
A12 #35 canonical-token regression check (TD-3 — REQUIRED PASS as of the 2026-09-22 amendment):
    EditorNumberField.svelte and EditorVec3Field.svelte reference --editor-axis-x/y/z, carry none
    of the #f05252 / #45c878 / #3b82f6 literals, and a test asserts token usage rather than the
    literals; plus the chip-opacity, neutral-field and theme-parity contract LIVE-verified in
    every shipped theme (M17)
A13 npm run check:api · npm run build:api · npm run check:layout-core ·
    npm run check:project-model · npm run check:camera-core    the workspace checks the root
    check/build do not cover (root check/build = editor + museum only)
A14 npm run test:api                    INFRASTRUCTURE-DEPENDENT: builds and runs the API suites
    against a local Postgres/.env — record as not-run when the environment is absent, NEVER as a
    pass
```

A9 asserts the **positive** half of S7: the three bodies are reachable through the pushed tag and
carry their full text. Ancestry against `main` is expected to be **false** by the owner's rebase
ruling, so it is recorded as the known outcome — A9 must not read as a fresh gate failure.

A12 was written as a **baseline audit** while `#35` stood re-disposed as post-P23 debt (TD-3). It is
now a **required-pass regression check**: the owner amended TD-3 into P23.16 as a bounded
correction, it landed as `7c8dacb`, and A12 verifies the token swap, the absence of the duplicated
literals, and the test asserting token usage. The remainder of the `#35` contract (axis-chip
opacity, unchanged neutral fields, per-theme visual parity) is verified live by **M17** across all
eight shipped themes. The amendment is the only implementation change this gate makes; TD-3's
landing is separate from the automated lanes and does not alter any other recorded baseline.

### Live / manual (M — one interactive session; owner-run or owner-present)

```text
M1   Full-loop crossing on F1: draw → direct edit → curve edit → name → Room/Opening →
     duplicate/preset → 3D → undo/redo → Save → cold Load → Preview → Publish → cold-load the
     published visitor at /p/:publicationId. Assert identities and compile parity at every hop,
     and that the visitor sees no editor-only geometry.
     Route note: a published project is served at /p/:publicationId. `/museum` is the frozen
     Chopin relic and is NOT the publish destination — the relics belong to M12.
M2–M10  P23.15 J1–J9, each against the fixture its row names (90° miter · X void/overlap ·
     T flush stem · mixed thickness step · mixed height step · curved+straight miter · door
     at a junction · editor/visitor identity · Y/star keel partition). The visitor side of each
     row runs at /p/:publicationId on the published fixture — never the frozen `/museum` relic.
M11  Architecture-edit stillness (E3/S6): move a Room and a Wall, delete a Wall, edit a
     curve and an Opening on F1's Scene-carrying document; the Scene document, Scene
     selection and camera state must be unchanged.
M12  Legacy smoke — **WAIVED by owner decision 2026-09-22** (frozen relics, no active
     maintenance; they may drift and no live smoke is owed). E5 rests on its automated evidence.
M13  Empty-state latch (S3): start drawing → undo, then Plan → 3D → Plan; the card must not
     resurrect after drafting, and must still show on a session that never drafted.
M14  Carried behaviour rows (S4): opening-insert commits on click; and, for the undo row, the
     verified behaviour rather than an aspiration — the field's pending text is NOT cancelled by
     an external history transaction while the commit stays honest (nothing typed, nothing
     written). That is P23.13's recorded owner decision, not a regression (record §5.2).
M15  Screen-reader reading order over Scene/Plan, Inspector and Status Rail.
M16  OS reduced-motion + coarse pointer on a device; 1440×900 reference capture (S9).
M17  TD-3 axis-token parity (amendment 2026-09-22): in the live editor, scalar X/Y/Z fields and a
     Vec3 field must render the canonical axis inks and the 15% chip background **identically in
     every shipped theme**, with neutral fields and field chrome still following the theme.
```

Rows M2–M10 are the P23.15 J-rows verbatim, with the visitor side repointed at
`/p/:publicationId`; M11 verifies the E3 stillness contract; M12–M16 are the carried
P23.14/P23.13 rows. Nothing here is claimed until it is run and recorded.

## 5. Phase-closure blockers

```text
B1  S5 — cleared 2026-09-22: seven P23-owned issues are closed (#6 re-disposed to post-P23, #35
    delivered by this gate under the owner amendment), so no prerequisite P23 issue remains open.
    A12 moved from baseline audit to required pass when TD-3 was amended into scope.
B2  S7 — cleared 2026-09-22: the three P23.15 stubs and the QA record now record the rebase
    landing and the tag as the recovery form, so E8's closeout-doc accuracy holds for them.
    What remains under E8 is the transient-artifact classification in S11.
B3  S2 manual 3D rows. All nine J-rows are owed and need one interactive session; no
    automated substitute exists for the visual claim.
B4  S9 manual accessibility + reference-capture rows. Device/AT rows are owed and cannot
    be claimed by proxy.
B5  Any falsified landed behaviour (see §0). An implementation change reopens review and
    stops the gate rather than being absorbed here.
```

Explicitly **not** blockers: TD-1 and TD-2 (accepted deferrals with named owners), the
re-disposed post-P23 issues (`#6` via its `#26` owner), the P23.14 §0.3 open owner calls
(settled as open, not as defects), incremental Junction invalidation (accepted non-goal), and the
older shell dimension/type tables marked descriptive.

## 6. Execution order

```text
owner reviews this plan
  → A1–A14                          automated lane, one pass (A12 = #35's required-pass
                                    canonical-token regression check; A14 = API suites,
                                    recorded as not-run without local infrastructure)
  → M1–M16 + M17                   one interactive pass, rows recorded with browser evidence; rows
                                    the environment could not hold (signed-in publish, AT, physical
                                    device) recorded OWED/BLOCKED with the one owner action named
  → A14 note                        ran against real local Postgres — not a NOT RUN
  → (S7 anchor/claim repair is done at plan time, 2026-09-22 — docs only)
  → gate statement + phase README/current.md update
  → owner close decision             P23 becomes CLOSABLE; phase-closeout is separate
```

Gate output is a closeout record in this workspace holding: criterion-by-criterion
verdicts, the manual/automated split, the preservation report, residuals and the explicit
post-P23 debt list. Phase-closure blockers that survive are recorded as debt, not
silently dropped.

## 7. Non-goals

No new product capability, schema, topology, renderer or shell design · no TD-1 or TD-2
fix · no general legacy migration or issue #26 retirement · no P24/P25 work · no
per-slice QA repetition beyond the checks above · no `phase-closeout` invocation.
