# P23.6d — Canonical Room Lifecycle Completion

**Parent:** [P23 — Layout Depth umbrella](2026-09-07-P23-layout-depth-minimum-build.md)  
**Placement:** after [P23.6c](2026-09-13-P23.6c-canonical-wall-deletion.md), before [P23.6e — Final product-semantics reconciliation](P23.6e-directive.md). **Execution order is dependency-driven, not numeric:** P23.6e carries the 6e number only because `P23.6d` was the directive's historical label; this slice must close the wall-first Room authoring debt **before** the final Hierarchy/UI overhaul consumes those capabilities.  
**Status:** `implementation-ready` (2026-09-13) — planned against branch `p23.6c` (P23.6c review fixes on `main` + P23.6c), not yet implemented.  
**Depends on:** P23.8 reconciliation engine (shipped); [P23.6c](2026-09-13-P23.6c-canonical-wall-deletion.md) (the deletion pipeline, its endpoint-scoped Junction-cleanup rule, and the `'wall-delete'` operation vocabulary this slice's planner follows); [P23.6a](2026-09-12-P23.6a-wall-first-room-unit-move.md) (Room-unit move + connected-group precedent); [P23.6b](2026-09-12-P23.6b-architecture-hierarchy-inspector-reconciliation.md) (the hierarchy/Inspector authority that surfaces these operations).  
**Blocks:** [P23.6e](P23.6e-directive.md) (its Hierarchy/UI overhaul edits Rooms through surfaces that must not expose half-capable Room semantics) and P23.7 closeout (integrated acceptance now covers the complete Room lifecycle).

## Why this slice exists

After P23.6c, wall-first authoring can create Rooms (birth from topology), move them (P23.6a unit move, including the connected-group amendment), duplicate eligible isolated Rooms (P23.4), and delete one Wall with deterministic Room reconciliation — but the **Room itself** is not yet a fully lifecycle-managed entity:

- **Room removal is canonical-only by omission.** `deleteLayoutRoom` (`apps/editor/src/lib/editor/layout/layout-preview-state.svelte.ts`) **rejects wall-first documents outright** (`wallFirstLegacyEditMessage()`), and no `planDeleteRoom` exists in `layout-core`. A wall-first user has **no way to remove a Room as a Room** — the only demolition path is deleting boundary Walls one by one (P23.6c), which is physics, not product intent.
- **Room metadata is legacy-only.** `updateLayoutRoomFields` resolves the Room through `layout.floors` (legacy shape) and is exactly what P23.6b forbade wall-first Room rows from reaching. `LayoutWallFirstRoom` carries `{ id, name, boundary, floorThickness, ceilingThickness }`; `name` (the hierarchy's primary label) and the two thicknesses have **no canonical wall-first edit operation** — P23.6b verified there is none ("wall-first Room rows are interactive but cannot reach the legacy `updateLayoutRoomFields` rename"). The P23.6e Hierarchy design (Room pages, `Assigned Layout Objects`, Room naming) assumes authoritative Room naming exists.
- **Duplicate/move are narrower than the identity model.** `planDuplicateIsolatedRoom` refuses shared-boundary subgraphs by design; P23.6a moves only *isolated* units and its amendment A moves a *connected group* rigidly. Neither is a gap this slice widens into topology editing — the boundaries are pinned below — but their contracts must be restated as part of the lifecycle so P23.6e and P23.7 accept the whole surface coherently.
- **P23.6e and P23.7 both depend on the outcome.** P23.6e's final hierarchy presents Room pages with authoritative names; P23.7's closeout currently lists "wall-first Room rename / Room metadata" as an **open deferral** and "Room delete/topology edits" only as adapter-regression smoke. A UI overhaul cannot be the slice that invents Room removal.

This slice closes exactly that debt through the **one** canonical planner path. It creates no second geometry, ownership, history, or Room-identity authority.

## The invariant (unchanged, load-bearing)

> A Room is persistent semantic identity over a derived enclosed face. Room removal must not be implemented as deleting a `LayoutRoom` record while leaving the same enclosure intact.

Consequences:

- Room identity lives in P23.8 correspondence; every lifecycle operation submits a candidate document to `extractBoundaryCandidateFaces` → `buildCorrespondenceComponents` → `reconcileRooms` → `validateAndCompile` — the same pipeline as `planWallRoleChange`/`planDeleteWall` (`packages/layout-core/src/layout-wall-topology-ops.ts`).
- Any operation that would leave an enclosed face with no Room, or a Room record with no face, is not a valid lifecycle operation — it is a rejection.
- "Remove Room" therefore means **open or dissolve the enclosure** (demolish a boundary Wall through the P23.6c pipeline) — never a bare record splice. See §What "Remove Room" means.

## Current-code inventory (what exists, what doesn't)

| Capability | Wall-first status | Owner |
|---|---|---|
| Room birth | `planFirstEnclosureCreation` / `planPartitionToBoundaryRoomBirth` → `runRoomBirthOperation`: deterministic `Draft Room N` names, P23.8 floor/ceiling defaults | shipped (P23.0 stage 2) |
| Room move | `planWallFirstRoomMove` — rigid isolated-unit X/Z; amendment A moves the connected group | shipped (P23.6a) |
| Room duplicate | `planDuplicateIsolatedRoom` — eligible isolated subgraph + cloned junctions/objects | shipped (P23.4) |
| Room metadata (name, floor/ceiling thickness) | **none** — legacy `updateLayoutRoomFields` rejects wall-first | **this slice** |
| Room removal / demolition | **none** — `deleteLayoutRoom` rejects wall-first documents | **this slice** (narrow; see below) |
| Wall delete (the demolition primitive) | `planDeleteWall` — atomic hosted-Openings, endpoint-scoped Junction cleanup, P23.8 reconciliation | shipped (P23.6c) |
| Selection policy | mutation callers clear the canonical selection (P23.6c precedent: post-delete → `none`) | caller policy |

## What "Remove Room" means (product-level decision)

Three plausible semantics were considered:

1. **Demolish the Room's full boundary** — delete every boundary Wall (P23.6c loop) in one operation. Broad: invents multi-Wall cascade demolition tooling, ambiguous about shared Walls with neighbors, and its one-command sweep is exactly the "bulk topology cleanup" P23.6c lists as a non-goal.
2. **Record-splice removal** — drop the `LayoutRoom` record, keep the enclosure. **Forbidden by the invariant**; stated here only to record the rejection.
3. **Room-forward intent over the existing Wall pipeline (chosen — narrowest coherent behavior).** "Remove Room" is one command whose *candidate* is the demolition of **exactly one boundary Wall that belongs to this Room alone** (an outer boundary Wall with no other Room referencing it). The command:
   - rejects when the Room has **no exclusive** boundary Wall (every Wall shared with a neighbor → user must delete shared Walls explicitly, seeing each reconciliation, or the Room is a single-wall face);
   - rejects when the Room has exactly one boundary Wall;
   - otherwise plans `planDeleteWall`'s candidate for the chosen exclusive Wall, so the enclosure opens, the Room **retires through normal P23.8 reconciliation** (retiredRoomIds, portal clear, object-association clear), and the hosted Openings of the removed Wall go atomically;
   - defaults to the user-confirmed exclusive Wall when several exist (Inspector offers the wall list; the API takes `wallId` explicitly — no geometry guessing).

    In effect, P23.6d's "Remove Room" is a **guard-railed intent over P23.6c**, not new demolition machinery: it selects which Wall's deletion expresses "remove this Room" and refuses ambiguity. The plan records semantics 1 and 2 as **deferred**: full-room multi-Wall demolition stays out of scope (register under Deferred, below) until a product case exists.

> Deferred (registered, unscheduled): a bulk "demolish room" multi-Wall cascade. It would need explicit shared-Wall adjudication, per-Wall portal reconciliation previews and a much larger ambiguity contract; no current surface asks for it.

## New operations (semantic planner boundaries)

All planners live beside `planDeleteWall` in `packages/layout-core/src/layout-wall-topology-ops.ts` (or a sibling module in the same package re-exported by the same `WallFirstOpPlan` vocabulary). All are pure, submit complete candidates through the P23.8 engine and the final `validateAndCompile` gate, and reject with stable codes. None is a direct record mutation.

### 1. `planRoomMetadataUpdate(document, roomId, patch)`

- **Patch scope (v1):** `name` (non-empty, trimmed, unique-enough policy = non-empty is the only hard rule; duplicates in `name` are legal since `id` is identity — mirroring legacy behavior), `floorThickness`, `ceilingThickness` (finite, positive, within the P23.8 0.1 m default tolerance rules — same numeric gate the birth defaults satisfy).
- **Boundary is NOT patchable.** The boundary is correspondence-owned; the operations that change it are Wall ops (P23.1/P23.6c) and Room move. A patch containing `boundary` rejects with `invalid_patch`.
- **No-op patch rejects** (`no_op`) so callers write zero history — same convention as `planWallRoleChange`'s same-role rejection.
- Topology does not change (no face extraction, no reconciliation); the candidate runs the full canonical validation + compile gates. Metadata-only edits cannot invalidate faces, but the gate stays uniform.
- Rejection codes: `unknown_room`, `invalid_patch`, `invalid_value`, `no_op`, plus the gate codes.
- `PrecisionOperation` gains `'room-metadata'` (alongside `'wall-delete'`), riding the same operation-typed document-install path.

### 2. `planRemoveRoom(document, roomId, wallId)`

- Guard-railed intent per the decision above: `roomId` must exist; `wallId` must be a **boundary** Wall in that Room's `boundary` list and must be **exclusive** (no other room's `boundary` references it); the Room must have ≥ 2 boundary Walls (a 1-Wall face has no removable wall that opens rather than destroys the graph coherently — reject `ambiguous_room_removal`).
- The candidate is exactly `planDeleteWall(document, wallId)`'s candidate: hosted Openings of `wallId` go atomically, its endpoints prune only when no surviving Wall references them (P23.6c's endpoint-scoped cleanup — unrelated orphan Junctions elsewhere in the document are untouched), face extraction runs, P23.8 reconciliation retires the Room deterministically, portals of surviving openings remap/clear per the existing contract, document-level `LayoutObject.roomId?` associations **clear** (retirement has no merge survivor — per the P23.8 rule already implemented in `reconcileRooms`).
- Reuses `planDeleteWall` **itself** (call the planner, don't fork the pipeline) so demolition stays one authority. The wrapper contributes only the intent guard.
- Rejection codes: `unknown_room`, `unknown_wall`, `wall_not_in_room_boundary`, `wall_not_exclusive`, `ambiguous_room_removal`, plus every P23.6c/P23.8 downstream code unchanged.
- **Not exposed as a second Delete.** The operation is the canonical Room-removal surface for P23.6e's Hierarchy Room page; Wall rows keep plain `planDeleteWall`.

### 3. Restated (not new): move / duplicate boundaries

- **Move** stays P23.6a's contract: rigid X/Z of the isolated unit (or connected group per amendment A), explicit predecessor→candidate correspondence, zero birth/retirement. This slice adds nothing.
- **Duplicate** stays P23.4's contract: eligible isolated subgraphs only; shared-boundary subgraphs reject. Widening duplicate to shared boundaries is a topology-merge problem and stays out of scope.

### Explicit non-goals

- No record-splice Room deletion, ever (the invariant).
- No bulk multi-Wall demolition (deferred above).
- No boundary-edit-by-metadata, no geometry healing, no spatial inference of "which room did the user mean".
- No multi-select Room operations (Issue #28 deferral unchanged).
- No legacy behavior change: `deleteLayoutRoom`/`updateLayoutRoomFields` keep their current legacy-only semantics untouched.
- No second selection, history, or reconciliation authority.

## Cross-cutting contracts (pinned by the P23.6c precedent)

Every new operation follows the same rules; each is regression-pinned in the test plan.

- **Shared-Wall behavior:** a Wall bounding two Rooms is one physical Wall; any operation that would "delete a room's wall" for both sides is refused (`wall_not_exclusive`). Merging 2→1 remains exclusively the P23.6c Wall-delete outcome.
- **Exclusive vs shared boundary handling:** exclusivity is computed from the baseline document's `rooms[].boundary` references, never from geometry or coordinates.
- **Hosted Openings:** the removed Wall's Openings go atomically (P23.6c rule); surviving-Wall Openings keep their host and remap/clear portals through the P23.8 contract.
- **Junction cleanup:** endpoint-scoped only — the removed Wall's `startJunctionId`/`endJunctionId` are pruned when no surviving Wall references them; unrelated pre-existing orphan Junctions anywhere in the document survive (P23.6c review regression stays green).
- **Adjacent Room consequences:** retirement/split/merge/disappearance outcomes come from `reconcileRooms` as-is; P23.6d never overrides a correspondence result. If the resulting topology is unsupported (many↔many), the operation rejects atomically.
- **`LayoutObject.roomId?` remap/clear:** through `reconcileRooms` — merge maps to the survivor, retirement clears (no survivor to inherit). Transforms never move.
- **Portal semantic remapping:** the existing `remapPortalRelations` contract (split descendants via wall-side adjacency, collapsed tuples clear, unresolved rejects `unresolved_portal_remap`). No new portal semantics.
- **Selection outcome:** fixed policy, same as P23.6c — a successful Room-affecting mutation clears the canonical Layout selection to `none`; never a nearest-survivor, never a dangling `roomId`. Rejection changes nothing.
- **Undo/Redo / one command = one history transaction:** every operation runs through the existing guarded runner + `applyWallFirstDocumentPlan`-style single commit; one successful command = exactly one history entry; rejections write zero entries; Undo/Redo restores exact Room/Wall/Junction/Opening IDs; Redo never re-runs correspondence or allocation.
- **Deterministic IDs/reconciliation:** any Room birth inside these flows allocates through the authoring allocator (`createAuthoringRoomAllocator`) — face-key-seeded deterministic IDs, `Draft Room N` naming, collision-free; nothing timestamp- or random-based.
- **Unsupported/ambiguous topology:** rejects atomically with zero partial mutation; the planner is input-pure (baseline document never mutated).
- **Legacy compatibility:** byte-identical compile output for legacy documents; legacy editors/mutators untouched.

## Surfaces (after implementation — target state)

- **Inspector Room panel (wall-first):** authoritative name + floor/ceiling thickness fields (`planRoomMetadataUpdate`), and a `Remove room` danger action listing the Room's exclusive boundary Walls (`planRemoveRoom`), disabled with a reason when none is eligible.
- **Hierarchy Room row (wall-first):** context menu may expose `Remove room…` under the same `isUnifiedTreeRowInteractive` row-authority contract P23.6c's review pinned (menu obeys the activation gate; inert rows expose nothing).
- **Viewport Delete/Backspace:** unchanged — Rooms are not `Delete`-key targets (Room removal is deliberate and named, not a keystroke accident).

## Acceptance (minimum)

- rename/thickness update on a wall-first Room via the canonical planner; no-op and invalid patches reject with zero history;
- `Remove room` on a Room with an exclusive boundary Wall retires the Room deterministically through P23.8 reconciliation (exact `retiredRoomIds`, object associations cleared, portals cleared/remapped per contract, hosted Openings of the removed Wall gone atomically);
- `Remove room` rejects when every boundary Wall is shared (`wall_not_exclusive`) and for a 1-Wall face (`ambiguous_room_removal`) — zero partial mutation;
- unrelated orphan Junctions survive every Room-affecting operation (endpoint-scoped cleanup regression);
- one command = one history entry; rejection = zero entries; Undo/Redo restores exact IDs; Redo reruns nothing;
- successful Room mutation clears canonical selection to `none`;
- planner input purity (baseline never mutated) and byte-identical legacy compile;
- hierarchy/Inspector surfaces obey the row-authority context-menu contract (Camera domain and Plan Arrange expose nothing);
- existing suites stay green; legacy Room editing behavior unchanged.

## Test plan

New suite (e.g. `p23-6d-room-lifecycle.test.ts`): planner contract for `planRoomMetadataUpdate` (success/no-op/invalid/unknown, input purity, compile identity) and `planRemoveRoom` (exclusive-Wall retirement fixture validated through real face extraction, shared-Wall rejection, 1-Wall rejection, unknown ids, atomicity, Junction-cleanup scope, hosted-Opening cascade, portal clear, object-association clear, one-history-entry + exact Undo/Redo through the guarded runner, post-mutation selection `none`, caller source contracts for the Inspector/Hierarchy gating). P23.6c's suite must keep passing unchanged.

## Implementation record

Not implemented. This plan is the implementation-ready contract; execution is a future task on a fresh branch after P23.6c lands.
